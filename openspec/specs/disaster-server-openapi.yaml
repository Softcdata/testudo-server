components:
    parameters:
        LanguageHeader:
            name: X-Language
            in: header
            required: false
            schema:
                type: string
                enum:
                    - zh-CN
                    - en-US
                default: zh-CN
            description: 客户端显式指定响应消息语言。支持 `zh-CN` 与 `en-US`；非法值降级为 `zh-CN`。当该请求头为空时，服务端读取 `Accept-Language`。
        WebSocketLangQuery:
            name: lang
            in: query
            required: false
            schema:
                type: string
                enum:
                    - zh-CN
                    - en-US
                default: zh-CN
            description: WebSocket 握手阶段指定事件流消息语言。浏览器无法稳定设置自定义 WebSocket 请求头时使用该参数。
    headers:
        ContentLanguage:
            description: 本次响应消息实际使用的语言。服务端按 `X-Language`、`Accept-Language`、`zh-CN` 的顺序解析。
            schema:
                type: string
                enum:
                    - zh-CN
                    - en-US
        VaryLanguage:
            description: 固定包含 `X-Language, Accept-Language`，提示代理和缓存按语言头区分响应。
            schema:
                type: string
    examples:
        ValidationNameRequiredZhCN:
            summary: 中文错误响应示例
            value:
                code: 1000
                message: 名称不能为空
                message_key: validation.name_required
                trace_id: req-zh
        ValidationNameRequiredEnUS:
            summary: 英文错误响应示例
            value:
                code: 1000
                message: name is required
                message_key: validation.name_required
                trace_id: req-en
    schemas:
        CollectionMeta:
            properties:
                filters:
                    type: object
                links:
                    type: object
                pagination:
                    $ref: '#/components/schemas/PaginationMeta'
                resourceType:
                    description: 集合资源类型
                    type: string
                sort:
                    type: object
                summary:
                    additionalProperties:
                        type: integer
                    description: 集合级汇总统计。字段由具体列表接口定义，统计范围为过滤后、分页前的结果集。
                    type: object
                type:
                    example: collection
                    type: string
            type: object
        Envelope:
            properties:
                code:
                    description: 业务码，0 表示成功
                    type: integer
                data:
                    description: 业务数据，具体结构由接口决定
                message:
                    description: 响应消息。带有用户可见消息的错误响应按 `X-Language`、`Accept-Language`、默认 `zh-CN` 的顺序本地化；成功响应当前保持 `OK`。
                    type: string
                message_key:
                    description: 稳定消息键。前端兜底展示、日志关联和自动化测试应依赖该字段，不应依赖具体文案。
                    type: string
                meta:
                    description: 分页、筛选、心跳、错误详情等元信息
                trace_id:
                    description: 请求追踪 ID
                    type: string
            required:
                - code
            type: object
        ErrorEnvelope:
            properties:
                code:
                    description: 业务错误码
                    type: integer
                data:
                    description: 错误响应固定为空
                    nullable: true
                message:
                    description: 本地化错误消息。语言由 `X-Language` 请求头优先决定，`Accept-Language` 次之，默认 `zh-CN`。
                    type: string
                message_key:
                    description: 稳定消息键，例如 `validation.name_required`、`auth.invalid_token`。未迁移的历史错误路径可能暂不返回该字段。
                    type: string
                meta:
                    description: 错误详情
                trace_id:
                    description: 请求追踪 ID
                    type: string
            required:
                - code
                - message
            type: object
        LocalRFC3339Time:
            description: server 对外响应 DTO 中的时间字段统一使用 RFC3339 字符串，并按进程本地时区 time.Local/TZ 序列化；例如容器设置 TZ=Asia/Shanghai 时返回 2026-05-15T11:08:19+08:00。Kubernetes CR/YAML 的 metadata.creationTimestamp 仍由 apiserver 以 UTC Z 保存。
            type: string
            format: date-time
            example: "2026-05-15T11:08:19+08:00"
        LicenseErrorMeta:
            properties:
                currentClusters:
                    description: 创建前未删除 Cluster 数量
                    type: integer
                licenseReason:
                    description: License 自身失败原因，例如 LicenseExpired、LicenseFingerprintMismatch、LicenseEnvironmentInvalid。无 License 时为空。
                    type: string
                maxClusters:
                    description: 当前生效集群额度，-1 表示无限制
                    type: integer
                reason:
                    description: 稳定错误原因。集群额度拒绝时固定为 LicenseLimitExceeded。
                    type: string
                state:
                    description: 当前 License 状态
                    type: string
            type: object
        PlatformLicenseInstallRequest:
            properties:
                license:
                    description: 完整 License JSON 文本，必须是合法 JSON 文档。
                    type: string
            required:
                - license
            type: object
        PlatformLicenseFingerprintRequest:
            properties:
                fingerprint:
                    description: 当前管理面集群指纹，格式为 sha256:<64 位十六进制摘要>，用于签发 License。
                    type: string
                fingerprintVersion:
                    description: 指纹算法版本，当前固定为 k8s-v1。
                    enum:
                        - k8s-v1
                    type: string
                generatedAt:
                    description: 指纹请求对象生成时间，UTC RFC3339。
                    type: string
                namespace:
                    description: server 生效的 License Namespace，默认 disaster-system。
                    type: string
                product:
                    description: License 产品名，固定为 disaster-platform。
                    enum:
                        - disaster-platform
                    type: string
            required:
                - product
                - fingerprintVersion
                - fingerprint
                - namespace
                - generatedAt
            type: object
        PlatformLicenseStatus:
            properties:
                clusterCount:
                    description: 当前未删除 Cluster 数量
                    type: integer
                customer:
                    description: 客户名称
                    type: string
                edition:
                    description: License 版本或权益名称
                    type: string
                expiresAt:
                    description: License 到期时间，UTC RFC3339
                    type: string
                features:
                    description: 已授予的功能列表
                    items:
                        type: string
                    type: array
                fingerprint:
                    description: 当前管理面集群指纹，用于签发新的 License。指纹生成失败时为空字符串。
                    type: string
                fingerprintMatched:
                    description: License 指纹是否匹配当前平台部署
                    type: boolean
                fingerprintRequest:
                    allOf:
                        - $ref: '#/components/schemas/PlatformLicenseFingerprintRequest'
                    description: 可直接交给签发侧使用的指纹请求对象。仅在当前指纹生成成功时返回。
                fingerprintVersion:
                    description: 当前管理面集群指纹算法版本，指纹生成失败时仍返回 k8s-v1。
                    enum:
                        - k8s-v1
                    type: string
                lastCheckedAt:
                    description: 状态检查时间，UTC RFC3339
                    type: string
                licenseId:
                    description: License ID
                    type: string
                maxClusters:
                    description: 当前生效集群额度，免费版为 2，企业无限制为 -1
                    type: integer
                message:
                    description: 状态说明
                    type: string
                reason:
                    description: 稳定状态原因。常见值包括 NoLicense、LicenseInvalid、LicenseExpired、LicenseFingerprintMismatch、LicenseEnvironmentInvalid、LicenseUnsupportedVersion、LicenseUnknownKey、LicenseProductMismatch、LicenseLimitExceeded。
                    type: string
                source:
                    description: 状态来源，statusConfigMap 表示展示缓存，liveEvaluation 表示实时校验
                    enum:
                        - statusConfigMap
                        - liveEvaluation
                    type: string
                state:
                    description: License 状态
                    enum:
                        - Free
                        - Active
                        - Expired
                        - InvalidSignature
                        - FingerprintMismatch
                        - NotYetValid
                        - Malformed
                        - UnsupportedVersion
                        - ProductMismatch
                        - UnknownKey
                        - Unknown
                    type: string
            required:
                - state
                - maxClusters
                - clusterCount
                - fingerprint
                - fingerprintVersion
                - fingerprintMatched
                - source
            type: object
        GenericObject:
            additionalProperties: true
            description: 待逐接口按 server DTO 与 operator 调用链补齐具体字段
            type: object
        BusinessDefaultConfigField:
            description: 业务默认配置字段条目。该结构由 server 内置字段目录和 ConfigMap 中的当前值合成，前端可以直接展示字段含义、当前值、默认值、可编辑性、生效方式和校验边界。
            type: object
            required:
                - key
                - name
                - description
                - value
                - defaultValue
                - dataType
                - editable
                - effectMode
                - groupKey
                - groupName
            properties:
                key:
                    description: 字段路径，固定为 `分组.字段名`，例如 `backupRuntime.pollInterval`。PATCH 支持使用该扁平 key 直接更新。
                    type: string
                name:
                    description: 页面展示名称。
                    type: string
                description:
                    description: 字段含义说明，前端配置页面应直接展示该文本，不需要自行拼接说明。
                    type: string
                value:
                    description: 当前生效的业务默认值。duration 使用带单位字符串，例如 `15s`、`5m`；int 使用 JSON number；bool 使用 JSON boolean。
                    oneOf:
                        - type: string
                        - type: integer
                        - type: boolean
                defaultValue:
                    description: 字段目录中的出厂默认值。未在 ConfigMap 中覆盖时，`value` 等于该值。
                    oneOf:
                        - type: string
                        - type: integer
                        - type: boolean
                dataType:
                    description: 字段值类型，决定前端控件和 PATCH 校验方式。
                    type: string
                    enum:
                        - duration
                        - int
                        - bool
                        - string
                        - enum
                        - object
                editable:
                    description: 是否允许通过 PATCH 接口修改。false 时字段仅展示，提交会返回 400。
                    type: boolean
                effectMode:
                    description: 业务默认值被前端再次写入业务请求后的生效方式。`hot` 表示不需要重启；`restart` 表示依赖组件重启后加载；`startup` 表示只作为新建资源或启动默认值。
                    type: string
                    enum:
                        - hot
                        - restart
                        - startup
                min:
                    description: 最小合法值。duration 字段使用带单位字符串，int 字段使用 number。
                    oneOf:
                        - type: string
                        - type: integer
                max:
                    description: 最大合法值。duration 字段使用带单位字符串，int 字段使用 number。
                    oneOf:
                        - type: string
                        - type: integer
                enumValues:
                    description: enum 字段允许值。非 enum 字段为空数组或省略。
                    type: array
                    items:
                        type: string
                groupKey:
                    description: 字段所属分组 key，例如 `backupRuntime`。
                    type: string
                groupName:
                    description: 字段所属分组展示名称。
                    type: string
            example:
                key: backupRuntime.pollInterval
                name: 备份状态轮询间隔
                description: AppBackup 观察 Velero Backup 状态的默认重排队间隔，过小会增加 controller 负载。
                value: 10s
                defaultValue: 10s
                dataType: duration
                editable: true
                effectMode: hot
                min: 1s
                max: 5m
                enumValues: []
                groupKey: backupRuntime
                groupName: 备份运行时
        BusinessDefaultConfigGroup:
            description: 业务默认配置分组，前端可按分组直接渲染配置页面。
            type: object
            required:
                - key
                - name
                - description
                - fields
            properties:
                key:
                    description: 分组 key，例如 `restoreRuntime`。
                    type: string
                name:
                    description: 分组展示名称。
                    type: string
                description:
                    description: 分组用途说明。
                    type: string
                fields:
                    description: 该分组下的字段列表，字段顺序由 server 字段目录控制。
                    type: array
                    items:
                        $ref: '#/components/schemas/BusinessDefaultConfigField'
        BusinessDefaultConfigSnapshot:
            description: 单例业务默认配置快照。快照由 server 字段目录、ConfigMap `disaster-business-default-config` 中的覆盖值、更新时间和更新人合成。
            type: object
            required:
                - schemaVersion
                - groups
            properties:
                schemaVersion:
                    description: 配置文档 schema 版本，当前为 1。
                    type: integer
                updatedAt:
                    description: 最近一次通过 PATCH 成功更新后的 UTC RFC3339 时间；从未更新时为空。
                    type: string
                    format: date-time
                updatedBy:
                    description: 最近一次更新人。dev 模式未注入用户时为 `system`。
                    type: string
                groups:
                    description: 配置分组列表。
                    type: array
                    items:
                        $ref: '#/components/schemas/BusinessDefaultConfigGroup'
        BusinessDefaultConfigFieldCollection:
            description: 业务默认配置字段集合响应 data。
            type: object
            required:
                - items
            properties:
                items:
                    type: array
                    items:
                        $ref: '#/components/schemas/BusinessDefaultConfigField'
        BusinessDefaultConfigFrontendField:
            description: 前端传参字段条目。该结构保留 `key/value`，并返回字段含义、请求字段层级、CRD spec 字段层级以及字段应写入的业务接口位置，前端可以直接按 `requestPathSegments` 组装请求体。
            type: object
            required:
                - key
                - value
                - requestPathSegments
                - specPathSegments
                - apiUsages
            properties:
                key:
                    description: 前端读取默认值并写入业务请求字段时使用的稳定 key，例如 `operation.timeoutMinutes`。
                    type: string
                value:
                    description: 该 key 的默认值。duration 使用带单位字符串，int 使用 number，bool 使用 boolean，object 使用 JSON object。
                    oneOf:
                        - type: string
                        - type: integer
                        - type: boolean
                        - type: object
                name:
                    description: 字段中文名称。
                    type: string
                description:
                    description: 字段用途说明。
                    type: string
                resourceKind:
                    description: 字段主要作用的资源类型，例如 `DisasterOperation`。
                    type: string
                requestPath:
                    description: 字段在业务请求体中的点分路径，例如 `config.timeoutMinutes`。
                    type: string
                specPath:
                    description: 字段最终对应的 CRD spec 点分路径，例如 `spec.timeoutMinutes`。
                    type: string
                configGroupKey:
                    description: 前端字段分组 key，例如 `operation`。
                    type: string
                configKey:
                    description: 前端业务默认配置 key。
                    type: string
                dataType:
                    $ref: '#/components/schemas/FieldValueType'
                editable:
                    description: 前端是否允许编辑该业务默认值。
                    type: boolean
                serverSupported:
                    description: 当前 server 业务接口是否已经解析并透传该字段。为 false 时只表示目录声明存在，提交到对应接口未必生效。
                    type: boolean
                supportedOperations:
                    description: 对操作类字段生效的操作类型列表。
                    type: array
                    items:
                        type: string
                keySegments:
                    description: key 按点拆分后的层级。
                    type: array
                    items:
                        type: string
                requestPathSegments:
                    description: requestPath 按点拆分后的请求体层级，前端可按该数组组装 JSON。
                    type: array
                    items:
                        type: string
                specPathSegments:
                    description: specPath 按点拆分后的 CRD spec 层级。
                    type: array
                    items:
                        type: string
                apiUsages:
                    description: 字段应写入或触发的业务接口位置。
                    type: array
                    items:
                        $ref: '#/components/schemas/BusinessDefaultConfigFrontendFieldAPIUsage'
                note:
                    description: 补充说明，例如字段当前未由 server 解析。
                    type: string
            example:
                key: operation.timeoutMinutes
                value: 60
                name: 单次操作超时
                resourceKind: DisasterOperation
                requestPath: config.timeoutMinutes
                specPath: spec.timeoutMinutes
                keySegments:
                    - operation
                    - timeoutMinutes
                requestPathSegments:
                    - config
                    - timeoutMinutes
                specPathSegments:
                    - spec
                    - timeoutMinutes
                apiUsages:
                    - method: POST
                      path: /apis/disasterinstances.testudo.softcdata.com/v1/instances/:name/actions
                      requestPath: config.timeoutMinutes
                      requestPathSegments:
                          - config
                          - timeoutMinutes
                      resourceKind: DisasterOperation
                      operation: sync-resource
                      description: 触发实例 sync-resource 操作时使用
        BusinessDefaultConfigFrontendFieldAPIUsage:
            description: 前端传参字段对应的业务接口用法。
            type: object
            required:
                - method
                - path
            properties:
                method:
                    description: HTTP 方法。
                    type: string
                    example: POST
                path:
                    description: 字段应写入或触发的 server API 路径。
                    type: string
                    example: /apis/disasterinstances.testudo.softcdata.com/v1/instances/:name/actions
                requestPath:
                    description: 字段在该接口请求体中的点分路径；动作型无请求字段时为空。
                    type: string
                    example: config.timeoutMinutes
                requestPathSegments:
                    description: requestPath 拆分后的请求体层级。
                    type: array
                    items:
                        type: string
                resourceKind:
                    description: 该接口创建或更新的主要资源类型。
                    type: string
                    example: DisasterOperation
                operation:
                    description: 操作类接口的 operation 值，例如 `sync-resource`、`failover`。
                    type: string
                    example: sync-resource
                description:
                    description: 该字段在此接口下的用途说明。
                    type: string
        BusinessDefaultConfigFrontendFieldCollection:
            description: 前端传参字段目录集合响应 data。
            type: object
            required:
                - items
                - fieldMap
            properties:
                items:
                    type: array
                    items:
                        $ref: '#/components/schemas/BusinessDefaultConfigFrontendField'
                fieldMap:
                    description: 按字段 key 索引的 map，便于前端直接通过 `fieldMap["operation.timeoutMinutes"]` 获取字段层级和接口用途。
                    type: object
                    additionalProperties:
                        $ref: '#/components/schemas/BusinessDefaultConfigFrontendField'
        BusinessDefaultConfigPatchRequest:
            description: |-
                业务默认配置部分更新请求。支持分组结构和扁平 key 两种写法。
                - 分组结构：`{"backupRuntime":{"pollInterval":"30s"}}`
                - 扁平 key：`{"backupRuntime.pollInterval":"30s"}`
                未提交字段保持当前值不变。duration 必须是带单位字符串；int 必须是 JSON number；只读字段、未知字段、类型错误、越界值和跨字段关系错误返回 400。
            type: object
            additionalProperties:
                oneOf:
                    - type: object
                      additionalProperties: true
                    - type: string
                    - type: integer
                    - type: boolean
            example:
                backupRuntime:
                    pollInterval: 30s
                operationRuntime.defaultTimeoutMinutes: 120
        BusinessDefaultConfigFieldErrorMeta:
            description: 业务默认配置字段校验错误详情。
            type: object
            properties:
                field:
                    description: 出错字段路径，例如 `restoreRuntime.retryBackoff`。
                    type: string
                reason:
                    description: 稳定错误原因说明，例如 `duration value is below minimum`、`field is read-only`。
                    type: string
                expected:
                    description: 期望的数据类型。
                    type: string
                min:
                    description: 字段最小值。
                max:
                    description: 字段最大值。
                enumValues:
                    type: array
                    items:
                        type: string
                actualValue:
                    description: 客户端提交的原始值。
        VeleroHookStatus:
            description: Velero Backup/Restore status.hookStatus 摘要。`hooksAttempted` 是已尝试执行的 Hook 数量，`hooksFailed` 是失败数量。
            type: object
            properties:
                hooksAttempted:
                    type: integer
                hooksFailed:
                    type: integer
        VeleroBackupHooks:
            description: |-
                Velero v1 BackupHooks 透传对象，写入 AppBackup.spec.template.hooks 或 DisasterInstance.spec.veleroHooks.dataBackup。

                server 只做平台边界校验，不改写 command argv：includedResources 为空或包含 pods；exec.command 必须非空；onError 仅允许 Fail/Continue；exec.timeout 最大 10m；不支持平台占位符渲染；明显明文敏感参数（如 password=、token=、access_key=、secret=）会被硬拒绝并返回 meta.errorCode=VeleroHookSensitiveParameter 与 meta.fieldPath。
            type: object
            additionalProperties: true
            properties:
                resources:
                    description: Velero backup resource hook specs。每项支持 name、includedNamespaces、excludedNamespaces、includedResources、excludedResources、labelSelector、pre、post 等 Velero 原生字段。
                    type: array
                    items:
                        type: object
                        additionalProperties: true
            example:
                resources:
                    - name: freeze-db
                      includedResources: [pods]
                      labelSelector:
                        matchLabels:
                            app: mysql
                      pre:
                        - exec:
                            container: mysql
                            command: ["sh", "-c", "mysqladmin flush-tables --lock-tables"]
                            onError: Fail
                            timeout: 30s
                      post:
                        - exec:
                            container: mysql
                            command: ["sh", "-c", "mysqladmin unlock-tables"]
                            onError: Continue
                            timeout: 30s
        VeleroRestoreHooks:
            description: |-
                Velero v1 RestoreHooks 透传对象，写入 AppRestore.spec.template.hooks、DisasterInstance.spec.veleroHooks.dataRestore 或 DisasterDrill.spec.veleroHooks.dataRestore。

                server 只做平台边界校验，不改写 command argv：includedResources 为空或包含 pods；exec.command 必须非空；onError 仅允许 Fail/Continue；exec.execTimeout 最大 10m；exec.waitTimeout 最大 30m；init.timeout 最大 30m；不支持平台占位符渲染；明显明文敏感参数会被硬拒绝。敏感值应通过业务容器已有 Secret env、Secret volume、valueFrom 或 initContainer envFrom 传入，不要写进 command/args。
            type: object
            additionalProperties: true
            properties:
                resources:
                    description: Velero restore resource hook specs。每项支持 name、includedNamespaces、excludedNamespaces、includedResources、excludedResources、labelSelector、postHooks 等 Velero 原生字段。
                    type: array
                    items:
                        type: object
                        additionalProperties: true
            example:
                resources:
                    - name: warm-cache
                      includedResources: [pods]
                      postHooks:
                        - exec:
                            container: app
                            command: ["sh", "-c", "/app/bin/warm-cache --source=/etc/app/config"]
                            onError: Continue
                            execTimeout: 2m
                            waitTimeout: 10m
                        - init:
                            timeout: 5m
                            initContainers:
                                - name: restore-init
                                  image: busybox:1.36
                                  command: ["sh", "-c", "cp /secret/token /work/token"]
                                  envFrom:
                                    - secretRef:
                                        name: app-restore-secret
        DisasterVeleroHooks:
            description: 容灾级 Velero Hook 配置。dataBackup 只用于 DataSync 数据备份，dataRestore 用于 DataSync 数据恢复；字段使用 presence/clear 语义，更新时未出现表示保持原值，null 或空对象表示清空对应字段。
            type: object
            properties:
                dataBackup:
                    $ref: '#/components/schemas/VeleroBackupHooks'
                dataRestore:
                    $ref: '#/components/schemas/VeleroRestoreHooks'
        DisasterDrillVeleroHooks:
            description: 演练级 Velero Hook 覆盖。仅支持 dataRestore；请求中出现 dataBackup 会返回 400，因为 Drill 不创建新的数据备份。创建 Drill 时如果显式传入 veleroHooks:{}，server 会写入非 nil 空对象，表示清空演练级 Hook 并阻断实例级 dataRestore Hook 继承。
            type: object
            properties:
                dataRestore:
                    $ref: '#/components/schemas/VeleroRestoreHooks'
        SyncHistoryHookStatusDTO:
            description: 同步历史中的 Hook 执行汇总，来自 DataSync 记录的 AppBackup/AppRestore Velero hookStatus。
            type: object
            properties:
                hooksAttempted:
                    type: integer
                hooksFailed:
                    type: integer
        AppBackupSpecDTO:
            description: AppBackup 对外 spec DTO。除列出字段外还可能回显其他 Velero BackupSpec 兼容字段。
            type: object
            additionalProperties: true
            properties:
                cluster:
                    type: string
                schedule:
                    type: string
                storageLocation:
                    type: string
                includedNamespaces:
                    type: array
                    items:
                        type: string
                hooks:
                    $ref: '#/components/schemas/VeleroBackupHooks'
        AppBackupDTO:
            description: AppBackup 对外响应 DTO。spec.hooks 回显 CRD template.hooks；status.history[].veleroStatus.hookStatus 回显 Velero Backup Hook 执行汇总。
            type: object
            additionalProperties: true
            properties:
                name:
                    type: string
                namespace:
                    type: string
                spec:
                    $ref: '#/components/schemas/AppBackupSpecDTO'
                status:
                    type: object
                    additionalProperties: true
        AppBackupCollectionEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        type: object
                        properties:
                            items:
                                type: array
                                items:
                                    $ref: '#/components/schemas/AppBackupDTO'
                    meta:
                        $ref: '#/components/schemas/CollectionMeta'
        AppBackupEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/AppBackupDTO'
        AppBackupCreateRequest:
            description: 创建 AppBackup 请求体。hooks 原样写入 AppBackup.spec.template.hooks，并由 operator 继续透传给 Velero Backup/Schedule。
            type: object
            additionalProperties: true
            properties:
                name:
                    type: string
                cluster:
                    type: string
                schedule:
                    type: string
                storageLocation:
                    type: string
                hooks:
                    $ref: '#/components/schemas/VeleroBackupHooks'
            required: [name, cluster, schedule]
        AppBackupUpdateRequest:
            description: 更新 AppBackup 请求体。hooks 未出现表示保持原值；传对象表示整体替换；传 null 或空对象表示清空 template.hooks。
            type: object
            additionalProperties: true
            properties:
                name:
                    type: string
                hooks:
                    nullable: true
                    oneOf:
                        - $ref: '#/components/schemas/VeleroBackupHooks'
            required: [name]
        BackupRecordDTO:
            description: AppBackup 历史记录。veleroStatus.hookStatus 来自 Velero Backup status.hookStatus。
            type: object
            additionalProperties: true
            properties:
                name:
                    type: string
                phase:
                    type: string
                veleroStatus:
                    type: object
                    additionalProperties: true
                    properties:
                        hookStatus:
                            $ref: '#/components/schemas/VeleroHookStatus'
        BackupHistoryEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        type: array
                        items:
                            $ref: '#/components/schemas/BackupRecordDTO'
        BackupDownloadResponse:
            description: AppBackup 下载签发响应。`download_url` 指向平台服务端代理下载入口，不是对象存储地址。
            type: object
            properties:
                download_url:
                    description: 平台服务端生成的同源下载地址，包含短期 `downloadToken`。客户端可直接打开该地址完成下载。
                    type: string
                    example: /apis/appbackups.testudo.softcdata.com/v1/appbackups/prod-app/backups/prod-app-20260617103000/download/stream?downloadToken=eyJ2Ijox...
                expires_at:
                    $ref: '#/components/schemas/LocalRFC3339Time'
                mode:
                    description: 下载模式。当前固定为 `proxy`，表示由平台服务端代理对象存储下载。
                    enum:
                        - proxy
                    type: string
                type:
                    description: 下载类型。`resource` 下载 Velero 资源包，`data` 下载持久卷数据归档，`all` 下载资源包与持久卷数据归档。
                    enum:
                        - resource
                        - data
                        - all
                    type: string
                file_name:
                    description: 建议下载文件名。`resource` 为 `<backupName>.tar.gz`，`data` 为 `<backupName>-data.tar`，`all` 为 `<backupName>-all.tar`。
                    type: string
                files:
                    description: 历史兼容字段。代理下载模式下不再返回对象存储文件列表。
                    type: array
                    items:
                        type: object
            required:
                - download_url
                - expires_at
                - mode
                - type
                - file_name
        BackupDownloadEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/BackupDownloadResponse'
        AppRestoreSpecDTO:
            description: AppRestore 对外 spec DTO。spec.hooks 回显 CRD template.hooks。
            type: object
            additionalProperties: true
            properties:
                backupSource:
                    type: string
                cluster:
                    type: string
                backupName:
                    type: string
                restorePVs:
                    description: 是否恢复 PV 数据；为 true 时 server 会自动注入 PVC spec.volumeName 清理规则，使用 add /spec/volumeName 空值 patch 以避免字段不存在时失败。
                    type: boolean
                cleanVolumes:
                    description: 是否清理 PVC 卷绑定；为 true 时 server 会自动注入 PVC spec.volumeName 清理规则，使用 add /spec/volumeName 空值 patch。
                    type: boolean
                hooks:
                    $ref: '#/components/schemas/VeleroRestoreHooks'
        AppRestoreDTO:
            description: AppRestore 对外响应 DTO。spec.hooks 回显 CRD template.hooks；status.phase 透传 operator 顶层恢复状态，可能为 Pending、Initiating、Restoring、Succeeded、Failed、PartiallyFailed、Cancelled、Deleting、Unknown，其中 PartiallyFailed 表示 Velero Restore 已结束但存在错误，属于非成功终态；status.restoreStatus.hookStatus 回显 Velero Restore Hook 执行汇总。
            type: object
            additionalProperties: true
            properties:
                name:
                    type: string
                namespace:
                    type: string
                spec:
                    $ref: '#/components/schemas/AppRestoreSpecDTO'
                status:
                    type: object
                    additionalProperties: true
                    properties:
                        phase:
                            type: string
                            enum: [Pending, Initiating, Restoring, Succeeded, Failed, PartiallyFailed, Cancelled, Deleting, Unknown]
                            description: AppRestore 顶层状态，来自 operator `AppRestore.status.status`。`PartiallyFailed` 直接对应 Velero Restore `PartiallyFailed`，server 不会将其映射为 `Succeeded`。
                        restoreStatus:
                            type: object
                            additionalProperties: true
                            properties:
                                phase:
                                    type: string
                                    description: Velero Restore 原生 phase，例如 Completed、Failed、PartiallyFailed、InProgress 等。
                                hookStatus:
                                    $ref: '#/components/schemas/VeleroHookStatus'
        AppRestoreCollectionEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        type: object
                        properties:
                            items:
                                type: array
                                items:
                                    $ref: '#/components/schemas/AppRestoreDTO'
                    meta:
                        $ref: '#/components/schemas/CollectionMeta'
        AppRestoreEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/AppRestoreDTO'
        AppRestoreCreateRequest:
            description: 创建 AppRestore 请求体。hooks 原样写入 AppRestore.spec.template.hooks，并由 operator 继续透传给 Velero Restore。
            type: object
            additionalProperties: true
            properties:
                name:
                    type: string
                backupSource:
                    type: string
                cluster:
                    type: string
                backupName:
                    type: string
                hooks:
                    $ref: '#/components/schemas/VeleroRestoreHooks'
            required: [name, backupSource, cluster]
        AppRestoreUpdateRequest:
            description: 更新 AppRestore 请求体。hooks 未出现表示保持原值；传对象表示整体替换；传 null 或空对象表示清空 template.hooks。
            type: object
            additionalProperties: true
            properties:
                name:
                    type: string
                restorePVs:
                    description: 字段存在且为 true 时覆盖 spec.template.restorePVs，并标准化 PVC spec.volumeName 清理规则；legacy remove /spec/volumeName 会被替换为 add /spec/volumeName 空值 patch。
                    type: boolean
                    nullable: true
                cleanVolumes:
                    description: 字段存在且为 true 时标准化 PVC spec.volumeName 清理规则；legacy remove /spec/volumeName 会被替换为 add /spec/volumeName 空值 patch。
                    type: boolean
                    nullable: true
                hooks:
                    nullable: true
                    oneOf:
                        - $ref: '#/components/schemas/VeleroRestoreHooks'
        KubernetesNamespace:
            description: Kubernetes Namespace 对象的文档化摘要。实际响应由 Kubernetes `corev1.Namespace` 序列化产生。
            properties:
                apiVersion:
                    description: Kubernetes API 版本，Namespace 通常为 `v1`。
                    type: string
                kind:
                    description: Kubernetes 资源类型，Namespace 对象通常为 `Namespace`。
                    type: string
                metadata:
                    $ref: '#/components/schemas/KubernetesObjectMeta'
                spec:
                    additionalProperties: true
                    description: Namespace spec，当前接口不改写该字段。
                    type: object
                status:
                    $ref: '#/components/schemas/KubernetesNamespaceStatus'
            type: object
        KubernetesNamespaceList:
            description: Kubernetes NamespaceList。该接口当前唯一支持的 `resource` 值为 `namespaces`，返回结构来自 Kubernetes API。
            properties:
                apiVersion:
                    description: Kubernetes API 版本，NamespaceList 通常为 `v1`。
                    type: string
                items:
                    description: Namespace 对象列表。
                    items:
                        $ref: '#/components/schemas/KubernetesNamespace'
                    type: array
                kind:
                    description: Kubernetes 资源类型，NamespaceList 对象通常为 `NamespaceList`。
                    type: string
                metadata:
                    additionalProperties: true
                    description: Kubernetes 列表元信息。
                    type: object
            type: object
        KubernetesNamespaceStatus:
            description: Namespace 状态，来自 Kubernetes API。
            properties:
                conditions:
                    description: Namespace 删除、资源发现、finalizer 等条件列表。
                    items:
                        additionalProperties: true
                        type: object
                    type: array
                phase:
                    description: Namespace 生命周期阶段。
                    enum:
                        - Active
                        - Terminating
                    type: string
            type: object
        KubernetesObjectMeta:
            description: Kubernetes metadata 摘要。
            properties:
                annotations:
                    additionalProperties:
                        type: string
                    description: Kubernetes 注解。
                    type: object
                labels:
                    additionalProperties:
                        type: string
                    description: Kubernetes 标签。
                    type: object
                name:
                    description: 资源名称。
                    type: string
                resourceVersion:
                    description: Kubernetes 资源版本。
                    type: string
                uid:
                    description: Kubernetes UID。
                    type: string
            type: object
        PaginationMeta:
            properties:
                first:
                    type: string
                last:
                    type: string
                limit:
                    type: integer
                next:
                    type: string
                page:
                    type: integer
                partial:
                    type: boolean
                previous:
                    type: string
                total:
                    type: integer
            type: object
        WatchEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - properties:
                    data:
                        $ref: '#/components/schemas/WatchEventDTO'
                  type: object
        WatchEventDTO:
            properties:
                object:
                    description: 资源 DTO
                type:
                    enum:
                        - ADDED
                        - MODIFIED
                        - DELETED
                        - ERROR
                    type: string
            required:
                - type
            type: object
        MessageResponse:
            description: |-
                非统一信封错误响应。部分历史接口直接返回 `{"message": "..."}`。
            properties:
                message:
                    description: 错误消息。
                    type: string
            required:
                - message
            type: object
        MiddlewareAuthError:
            allOf:
                - $ref: '#/components/schemas/ErrorEnvelope'
            description: JWT 中间件返回的认证失败响应，统一使用 `transport.Envelope`。`message` 按语言协议本地化，`message_key` 返回稳定认证错误键。
        PanicErrorResponse:
            allOf:
                - $ref: '#/components/schemas/ErrorEnvelope'
            description: 全局 panic recovery 返回的内部错误响应，统一使用 `transport.Envelope`。`message_key` 固定为 `common.internal_error`。
        DeletionCheckRequest:
            description: 删除前依赖检查请求。
            properties:
                name:
                    description: 要检查的目标资源名称。
                    type: string
                namespace:
                    description: 目标资源所在 namespace。为空时默认 `disaster-system`；Cluster、DisasterConfig 等集群级资源读取目标对象时不使用 namespace。
                    type: string
                resource_kind:
                    description: 目标资源类型。支持 canonical kind 和若干别名，server 会通过 `normalizeKind` 归一化。
                    type: string
            required:
                - resource_kind
                - name
            type: object
        DeletionCheckEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - properties:
                    data:
                        $ref: '#/components/schemas/DeletionCheckResponse'
                  type: object
            description: 删除检查成功响应信封。
        DeletionCheckResponse:
            description: 删除依赖检查结果。
            properties:
                can_delete:
                    description: 是否可以删除。只由 `upstream` 数量决定，`upstream` 为空时为 true。
                    type: boolean
                cleanup_plan:
                    $ref: '#/components/schemas/DeletionCleanupPlan'
                downstream:
                    description: 当前目标依赖的下游资源，来自目标对象上的 `dependency-to-*` 标签解析与 token 反查。
                    items:
                        $ref: '#/components/schemas/DeletionDependencyRef'
                    type: array
                message:
                    description: 处理结果消息，成功时为 `OK`。
                    type: string
                target:
                    $ref: '#/components/schemas/DeletionResourceRef'
                upstream:
                    description: 依赖当前目标的上游资源。该列表非空时 `can_delete=false`。
                    items:
                        $ref: '#/components/schemas/DeletionDependencyRef'
                    type: array
            required:
                - target
                - upstream
                - downstream
                - cleanup_plan
                - can_delete
                - message
            type: object
        DeletionResourceRef:
            description: 被检查目标资源引用。
            properties:
                kind:
                    description: 归一化后的资源 Kind。
                    type: string
                name:
                    description: 资源名称。
                    type: string
                namespace:
                    description: 资源 namespace，集群级资源可能为空。
                    type: string
                uid:
                    description: Kubernetes UID。
                    type: string
            required:
                - kind
                - name
            type: object
        DeletionDependencyRef:
            description: 依赖引用。
            properties:
                kind:
                    description: 依赖资源 Kind。无法通过 token 解析下游目标时可能为空。
                    type: string
                name:
                    description: 依赖资源名称。无法解析时可能为空。
                    type: string
                namespace:
                    description: 依赖资源 namespace。
                    type: string
                relation_code:
                    description: 依赖关系编码，来自 operator 写入的 label value 或 ownerReference。
                    type: string
                token:
                    description: 依赖资源 token，来自 `dependency-token` 或 UID 派生。
                    type: string
                uid:
                    description: 依赖资源 UID。
                    type: string
                unresolved:
                    description: 是否未解析到具体资源。主要用于 downstream token 无法反查目标对象。
                    type: boolean
            type: object
        DeletionCleanupPlan:
            description: 删除目标时预计需要一并清理的资源计划。它只用于提示，不在本接口执行清理。
            properties:
                cascade_cleanup:
                    description: 随目标删除应级联处理的资源。
                    items:
                        $ref: '#/components/schemas/DeletionCleanupRef'
                    type: array
                finalizer_cleanup:
                    description: finalizer 相关清理资源，包含远端 Velero 和 resource modifier 资源。
                    items:
                        $ref: '#/components/schemas/DeletionCleanupRef'
                    type: array
                has_cleanup:
                    description: 是否存在清理计划。
                    type: boolean
            required:
                - has_cleanup
                - finalizer_cleanup
                - cascade_cleanup
            type: object
        DeletionCleanupRef:
            description: cleanup plan 中的一条清理目标。
            properties:
                cluster:
                    description: 远端集群名称。Velero 资源通常带该字段。
                    type: string
                kind:
                    description: 清理资源 Kind，例如 `Schedule`、`Backup`、`Restore`、`ConfigMap`、`DataSync`、`ResourceSync`、`DisasterOperation`、`BackupRestoreStatistics`。
                    type: string
                name:
                    description: 清理资源名称。未解析到具体对象时可能为空。
                    type: string
                namespace:
                    description: 清理资源 namespace。
                    type: string
                relation_code:
                    description: 清理关系编码。
                    type: string
                resolved:
                    description: 是否已经解析到具体资源对象。
                    type: boolean
                selector:
                    additionalProperties:
                        type: string
                    description: 未解析时用于提示前端或排查的 label selector。
                    type: object
                strategy:
                    description: 建议清理策略。
                    enum:
                        - delete
                        - delete_request
                        - owner_reference
                    type: string
                uid:
                    description: 清理资源 UID。
                    type: string
                unresolved_reason:
                    description: 未解析原因，常见于远端集群不可访问、资源标签缺失或匹配数量过多。
                    type: string
            required:
                - kind
                - resolved
            type: object
        LocalObjectReference:
            description: Kubernetes LocalObjectReference 摘要。当前存储接口只使用 name 字段引用同 namespace Secret。
            type: object
            properties:
                name:
                    description: Secret 名称。引用 CA Secret 时必须非空。
                    type: string
            required:
                - name
        StorageRepositoryAddressingStyle:
            description: S3 bucket 地址风格。空值进入 CRD 前会归一化为 PathStyle。
            type: string
            enum:
                - PathStyle
                - VirtualHostedStyle
        StorageRepositoryStatusType:
            description: StorageRepository 当前可用状态，由 operator 最近一次校验写入。
            type: string
            enum:
                - Available
                - Unavailable
        DisasterStorageSpecDTO:
            description: 存储仓库规格 DTO。AccessKey 与 SecretKey 为敏感字段，不会在响应中返回。
            type: object
            properties:
                storageType:
                    description: 存储类型。当前 server 不做枚举校验，operator 按 S3 兼容对象存储链路使用。
                    type: string
                bucket:
                    description: 对象存储 bucket 名称。
                    type: string
                region:
                    description: 对象存储 region。
                    type: string
                endpoint:
                    description: 对象存储访问地址。创建和更新时必须以 http:// 或 https:// 开头。
                    type: string
                addressingStyle:
                    $ref: '#/components/schemas/StorageRepositoryAddressingStyle'
                caConfigured:
                    description: 是否配置了 CA Secret 引用。由 spec.caSecretRef 是否存在且 name 非空计算得到。
                    type: boolean
                caSecretRef:
                    $ref: '#/components/schemas/LocalObjectReference'
                quotaBytes:
                    description: 存储容量上限，单位字节；0 或缺省表示未设置。
                    type: integer
                    format: int64
            required:
                - storageType
                - bucket
                - region
                - endpoint
                - addressingStyle
                - caConfigured
        DisasterStorageStatusDTO:
            description: 存储仓库观测状态 DTO，由 disaster-operator 写回。
            type: object
            properties:
                status:
                    $ref: '#/components/schemas/StorageRepositoryStatusType'
                lastCheckTime:
                    description: operator 最近一次校验存储配置的时间。
                    type: string
                    format: date-time
                reason:
                    description: 当前状态原因。成功时通常为 Available，失败时可能为 ValidationFailed 等。
                    type: string
                message:
                    description: 当前状态说明，包含 S3 校验成功消息或失败详情。
                    type: string
                usedSpaceBytes:
                    description: operator 扫描 bucket 后统计的已使用字节数。
                    type: integer
                    format: int64
                totalBackupCount:
                    description: operator 按 backups/ 前缀统计的物理备份对象数量。
                    type: integer
                    format: int64
            required:
                - status
        DisasterStorageDTO:
            description: StorageRepository 对外响应 DTO。
            type: object
            properties:
                id:
                    description: StorageRepository Kubernetes UID。
                    type: string
                name:
                    description: StorageRepository 名称。
                    type: string
                namespace:
                    description: 所在 namespace，当前由配置的管理命名空间决定（默认 disaster-system）。
                    type: string
                labels:
                    description: 资源 labels。列表过滤会按 labels 的 value 做包含匹配。
                    type: object
                    additionalProperties:
                        type: string
                creation_timestamp:
                    description: Kubernetes 创建时间，server 响应按进程本地时区（time.Local/TZ）序列化为 RFC3339 offset 字符串。
                    type: string
                    format: date-time
                spec:
                    $ref: '#/components/schemas/DisasterStorageSpecDTO'
                status:
                    $ref: '#/components/schemas/DisasterStorageStatusDTO'
            required:
                - id
                - name
                - namespace
                - creation_timestamp
                - spec
                - status
        DisasterStorageCollection:
            description: 存储仓库集合数据。
            type: object
            properties:
                items:
                    description: 当前页 StorageRepository DTO 列表。
                    type: array
                    items:
                        $ref: '#/components/schemas/DisasterStorageDTO'
            required:
                - items
        DisasterStorageCollectionEnvelope:
            description: 存储仓库列表统一响应。
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/DisasterStorageCollection'
                    meta:
                        $ref: '#/components/schemas/CollectionMeta'
        DisasterStorageEnvelope:
            description: 存储仓库详情、创建、更新、局部更新统一响应。
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/DisasterStorageDTO'
        DisasterStorageNameDTO:
            description: 存储仓库轻量名称 DTO。
            type: object
            properties:
                name:
                    description: StorageRepository 名称。
                    type: string
                id:
                    description: StorageRepository Kubernetes UID。
                    type: string
                status:
                    $ref: '#/components/schemas/StorageRepositoryStatusType'
            required:
                - name
                - id
                - status
        DisasterStorageNamesEnvelope:
            description: 存储仓库名称列表统一响应。
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        type: array
                        items:
                            $ref: '#/components/schemas/DisasterStorageNameDTO'
        DisasterStorageDeleteResult:
            description: 删除存储仓库成功后返回的资源名称。
            type: object
            properties:
                name:
                    description: 已提交删除的 StorageRepository 名称。
                    type: string
            required:
                - name
        DisasterStorageDeleteEnvelope:
            description: 删除存储仓库成功响应。
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/DisasterStorageDeleteResult'
        StorageBooleanMeta:
            description: 布尔校验接口附加信息。成功时可能包含 message，失败但 HTTP 仍为 200 时可能包含 error。
            type: object
            additionalProperties:
                type: string
        StorageBooleanEnvelope:
            description: 存储相关校验接口统一响应。data 为 true 表示校验通过，false 表示校验未通过。
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        type: boolean
                    meta:
                        $ref: '#/components/schemas/StorageBooleanMeta'
        StorageRepositoryCreateRequest:
            description: 创建 StorageRepository 请求。
            type: object
            properties:
                name:
                    description: 存储仓库名称。server binding 要求 1 到 50 个字符。
                    type: string
                    minLength: 1
                    maxLength: 50
                storageType:
                    description: 存储类型。必须传入；当前链路按 S3 兼容对象存储使用。
                    type: string
                bucket:
                    description: bucket 名称。server binding 要求 1 到 50 个字符。operator 校验时会 HeadBucket，不存在时尝试 CreateBucket。
                    type: string
                    minLength: 1
                    maxLength: 50
                region:
                    description: 对象存储 region。server binding 要求 1 到 50 个字符。
                    type: string
                    minLength: 1
                    maxLength: 50
                endpoint:
                    description: 对象存储 endpoint，必须以 http:// 或 https:// 开头。
                    type: string
                accessKey:
                    description: 对象存储访问 AccessKey。必传；只写入 CRD spec，不在响应 DTO 中返回。
                    type: string
                secretKey:
                    description: 对象存储访问 SecretKey。必传；只写入 CRD spec，不在响应 DTO 中返回。
                    type: string
                addressingStyle:
                    $ref: '#/components/schemas/StorageRepositoryAddressingStyle'
                caBundle:
                    description: PEM 格式 CA 证书内容。传入后 server 创建或更新 disaster-system/storage-ca-<name> Secret，并将 spec.caSecretRef 指向该 Secret。
                    type: string
                caSecretRef:
                    $ref: '#/components/schemas/LocalObjectReference'
                quotaBytes:
                    description: 容量上限，单位字节。
                    type: integer
                    format: int64
            required:
                - name
                - storageType
                - bucket
                - region
                - endpoint
                - accessKey
                - secretKey
        StorageRepositoryUpdateRequest:
            description: 更新 StorageRepository 请求。path name 当前只用于路由匹配，handler 用 body.name 定位资源。
            type: object
            properties:
                name:
                    description: 要更新的 StorageRepository 名称。必传；当前 handler 实际使用该字段执行 Get。
                    type: string
                storageType:
                    description: 非空时覆盖 spec.storageType。
                    type: string
                bucket:
                    description: 非空时覆盖 spec.bucket。
                    type: string
                region:
                    description: 非空时覆盖 spec.region。
                    type: string
                endpoint:
                    description: 非空时覆盖 spec.endpoint；传入时必须以 http:// 或 https:// 开头。
                    type: string
                accessKey:
                    description: 非空时覆盖 spec.accessKey，响应中不返回。
                    type: string
                secretKey:
                    description: 非空时覆盖 spec.secretKey，响应中不返回。
                    type: string
                addressingStyle:
                    $ref: '#/components/schemas/StorageRepositoryAddressingStyle'
                caBundle:
                    description: 传入后创建或更新 server 托管 CA Secret，并覆盖 spec.caSecretRef。与 caSecretRef、clearCa 互斥。
                    type: string
                caSecretRef:
                    $ref: '#/components/schemas/LocalObjectReference'
                clearCa:
                    description: true 表示清空 spec.caSecretRef；若旧引用为 server 托管 Secret，会尽力删除 storage-ca-<name>。与 caBundle、caSecretRef 互斥。
                    type: boolean
                quotaBytes:
                    description: 传入后覆盖 spec.quotaBytes；0 表示设置为 0。
                    type: integer
                    format: int64
            required:
                - name
        StorageRepositoryPatchRequest:
            description: 局部更新 StorageRepository 请求。只更新非 null 字段；当前 patch 不支持修改 storageType、endpoint、quotaBytes。
            type: object
            properties:
                accessKey:
                    description: 传入后覆盖 spec.accessKey，响应中不返回。
                    type: string
                secretKey:
                    description: 传入后覆盖 spec.secretKey，响应中不返回。
                    type: string
                bucket:
                    description: 传入后覆盖 spec.bucket。
                    type: string
                region:
                    description: 传入后覆盖 spec.region。
                    type: string
                addressingStyle:
                    $ref: '#/components/schemas/StorageRepositoryAddressingStyle'
                caBundle:
                    description: 传入后创建或更新 server 托管 CA Secret，并覆盖 spec.caSecretRef。与 caSecretRef、clearCa 互斥。
                    type: string
                caSecretRef:
                    $ref: '#/components/schemas/LocalObjectReference'
                clearCa:
                    description: true 表示清空 spec.caSecretRef；若旧引用为 server 托管 Secret，会尽力删除 storage-ca-<name>。与 caBundle、caSecretRef 互斥。
                    type: boolean
        ValidateS3ConnectionRequest:
            description: S3 直连校验请求。该请求不创建 StorageRepository，不触发 operator reconcile。
            type: object
            properties:
                endpoint:
                    description: S3 endpoint。当前直连校验不会校验 scheme，但 AWS client 需要可访问地址。
                    type: string
                region:
                    description: S3 region；为空时使用 us-east-1。
                    type: string
                bucket:
                    description: bucket 名称。传入时执行 HeadBucket；为空时执行 ListBuckets 校验凭据。
                    type: string
                accessKey:
                    description: S3 AccessKey。必传。
                    type: string
                secretKey:
                    description: S3 SecretKey。必传。
                    type: string
                storageType:
                    description: 兼容字段。当前 handler 解析但不参与 AWS client 构造。
                    type: string
                addressingStyle:
                    $ref: '#/components/schemas/StorageRepositoryAddressingStyle'
                caBundle:
                    description: PEM 格式 CA 证书内容，用于构造本次校验 HTTP client。与 caSecretRef 互斥。
                    type: string
                caSecretRef:
                    $ref: '#/components/schemas/LocalObjectReference'
            required:
                - endpoint
                - accessKey
                - secretKey
        ValidateStorageConnectivityRequest:
            description: 目标集群 Velero BackupStorageLocation 连通性校验请求。
            type: object
            properties:
                cluster_name:
                    description: 目标 Cluster CR 名称。server 用它获取目标集群 kube client。
                    type: string
                storage_name:
                    description: StorageRepository 名称。用于按 <storage_name>-<cluster_name> 查找或触发创建 BSL。
                    type: string
            required:
                - cluster_name
                - storage_name
        DisasterStorageWatchEventDTO:
            description: StorageRepository watch 事件数据。
            type: object
            properties:
                type:
                    description: Kubernetes watch 事件类型。
                    type: string
                    enum:
                        - ADDED
                        - MODIFIED
                        - DELETED
                        - ERROR
                object:
                    $ref: '#/components/schemas/DisasterStorageDTO'
            required:
                - type
        DisasterStorageWatchEnvelope:
            description: StorageRepository WebSocket 推送帧。连接成功、心跳、关闭、超时和事件推送均使用统一 Envelope。
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        oneOf:
                            - $ref: '#/components/schemas/DisasterStorageWatchEventDTO'
                            - $ref: '#/components/schemas/WatchConnectedData'
                    meta:
                        $ref: '#/components/schemas/WatchStreamMeta'
        KubernetesCondition:
            description: Kubernetes metav1.Condition 摘要，来自 CRD status.conditions。
            type: object
            properties:
                type:
                    description: 条件类型，例如 Available、Progressing、Degraded，实际取值由 operator 写入。
                    type: string
                status:
                    description: 条件状态。
                    type: string
                    enum:
                        - "True"
                        - "False"
                        - Unknown
                observedGeneration:
                    description: condition 对应的资源 generation。
                    type: integer
                    format: int64
                lastTransitionTime:
                    description: 最近一次条件状态切换时间。
                    type: string
                    format: date-time
                reason:
                    description: 条件原因。
                    type: string
                message:
                    description: 条件详细消息。
                    type: string
            required:
                - type
                - status
        DisasterJobPhase:
            description: DisasterJob operator 回写的任务阶段。
            type: string
            enum:
                - Pending
                - Ready
                - Backuping
                - Backuped
                - Restoring
                - Restored
                - Succeed
                - Failed
                - Deleting
        DisasterJobSpecDTO:
            description: DisasterJob 规格 DTO，来自 CRD spec。
            type: object
            properties:
                disasterBackup:
                    description: 关联的 DisasterBackup 名称。operator 读取该备份，再通过备份关联的 DisasterConfig 计算源集群、目标集群和存储仓库。
                    type: string
                syncType:
                    description: 同步方向。建议传 forward 或 reverse；为空时 operator 回写 forward。当前 controller 对非 forward 值都会走反向链路。
                    type: string
                scheduleType:
                    description: 调度类型兼容字段，代码注释为 once 或 cron；当前 DisasterJobReconciler 不按该字段分支。
                    type: string
            required:
                - disasterBackup
        DisasterJobStatusDTO:
            description: DisasterJob 观测状态 DTO，由 disaster-operator 回写。
            type: object
            properties:
                phase:
                    $ref: '#/components/schemas/DisasterJobPhase'
                reason:
                    description: 失败或等待原因。读取 DisasterBackup、DisasterConfig、集群 client、Velero Backup 或 Restore 出错时由 operator 写入。
                    type: string
                startTime:
                    description: 任务开始时间。当前 DTO 透传 CRD status.startTime，operator 未写入时为空。
                    type: string
                    format: date-time
                conditions:
                    description: Kubernetes 条件列表，当前 controller 透传 status.conditions。
                    type: array
                    items:
                        $ref: '#/components/schemas/KubernetesCondition'
            required:
                - phase
        DisasterJobDTO:
            description: DisasterJob 对外响应 DTO。
            type: object
            properties:
                id:
                    description: DisasterJob Kubernetes UID。
                    type: string
                name:
                    description: DisasterJob 名称。
                    type: string
                namespace:
                    description: 所在 namespace，当前接口由配置的管理命名空间决定（默认 disaster-system）。
                    type: string
                labels:
                    description: 资源 labels。operator 会同步 dependency token、spec.disasterBackup 和 label.disasterPolicyName 相关依赖标签；列表动态过滤按 label value 包含匹配。
                    type: object
                    additionalProperties:
                        type: string
                creation_timestamp:
                    description: Kubernetes 创建时间，server 响应按进程本地时区（time.Local/TZ）序列化为 RFC3339 offset 字符串。
                    type: string
                    format: date-time
                spec:
                    $ref: '#/components/schemas/DisasterJobSpecDTO'
                status:
                    $ref: '#/components/schemas/DisasterJobStatusDTO'
            required:
                - id
                - name
                - namespace
                - creation_timestamp
                - spec
                - status
        DisasterJobCollection:
            description: DisasterJob 集合数据。
            type: object
            properties:
                items:
                    description: 当前页 DisasterJob DTO 列表。
                    type: array
                    items:
                        $ref: '#/components/schemas/DisasterJobDTO'
            required:
                - items
        DisasterJobCollectionEnvelope:
            description: DisasterJob 列表统一响应。
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/DisasterJobCollection'
                    meta:
                        $ref: '#/components/schemas/CollectionMeta'
        DisasterJobEnvelope:
            description: DisasterJob 创建和详情统一响应。
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/DisasterJobDTO'
        DisasterJobCreateRequest:
            description: 创建 DisasterJob 请求体。server request struct 没有 binding tag，但 Kubernetes Create 和 operator 链路需要 name 与 disasterBackup 才能正常执行。
            type: object
            properties:
                name:
                    description: DisasterJob 名称，写入 metadata.name。
                    type: string
                disasterBackup:
                    description: 关联的 DisasterBackup 名称，写入 spec.disasterBackup。
                    type: string
                syncType:
                    description: 同步方向。建议传 forward 或 reverse；为空时 operator 回写 forward。当前 controller 对非 forward 值都会按反向同步处理。
                    type: string
                scheduleType:
                    description: 调度类型兼容字段，代码注释为 once 或 cron；当前 controller 不按该字段分支。
                    type: string
            required:
                - name
                - disasterBackup
        DisasterJobDeleteResult:
            description: 删除 DisasterJob 成功后返回的资源名称。
            type: object
            properties:
                name:
                    description: 已提交删除的 DisasterJob 名称。
                    type: string
            required:
                - name
        DisasterJobDeleteEnvelope:
            description: 删除 DisasterJob 成功响应。
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/DisasterJobDeleteResult'
        DisasterJobWatchEventDTO:
            description: DisasterJob watch 事件数据。
            type: object
            properties:
                type:
                    description: Kubernetes watch 事件类型。
                    type: string
                    enum:
                        - ADDED
                        - MODIFIED
                        - DELETED
                        - ERROR
                object:
                    $ref: '#/components/schemas/DisasterJobDTO'
            required:
                - type
        DisasterJobWatchEnvelope:
            description: DisasterJob WebSocket 推送帧。连接成功、心跳、关闭、超时和事件推送均使用统一 Envelope。
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        oneOf:
                            - $ref: '#/components/schemas/DisasterJobWatchEventDTO'
                            - $ref: '#/components/schemas/WatchConnectedData'
                    meta:
                        $ref: '#/components/schemas/WatchStreamMeta'
        KubernetesLabelSelectorRequirement:
            description: Kubernetes LabelSelectorRequirement。
            type: object
            properties:
                key:
                    description: label key。
                    type: string
                operator:
                    description: 选择器操作符。
                    type: string
                    enum:
                        - In
                        - NotIn
                        - Exists
                        - DoesNotExist
                values:
                    description: 操作符为 In 或 NotIn 时使用的 label value 列表。
                    type: array
                    items:
                        type: string
            required:
                - key
                - operator
        KubernetesLabelSelector:
            description: Kubernetes metav1.LabelSelector，用于选择命名空间内需要备份的资源。
            type: object
            properties:
                matchLabels:
                    description: 精确匹配的 label key/value。
                    type: object
                    additionalProperties:
                        type: string
                matchExpressions:
                    description: 标签表达式列表。
                    type: array
                    items:
                        $ref: '#/components/schemas/KubernetesLabelSelectorRequirement'
        DisasterBackupPhase:
            description: DisasterBackup operator 回写的备份配置阶段。
            type: string
            enum:
                - Pending
                - Ready
                - Backuping
                - Backuped
                - Restoring
                - Restored
                - Succeed
                - Failed
                - Deleting
        DisasterBackupResourceGroup:
            description: 源集群命名空间资源扫描结果。每个元素表示一类 Kubernetes 资源及其对象名称列表。
            type: object
            properties:
                apiVersion:
                    description: Kubernetes API version，例如 apps/v1。
                    type: string
                kind:
                    description: Kubernetes Kind，例如 Deployment、Service。
                    type: string
                names:
                    description: 该资源类型下扫描到的对象名称列表。
                    type: array
                    items:
                        type: string
            required:
                - names
        DisasterBackupSpecDTO:
            description: DisasterBackup 规格 DTO，来自 CRD spec。
            type: object
            properties:
                labelSelector:
                    $ref: '#/components/schemas/KubernetesLabelSelector'
                namespace:
                    description: 源集群中要扫描和备份的业务 namespace。server 不强制必传，但 operator 资源扫描和后续 DisasterJob Velero Backup 都依赖该值。
                    type: string
                disasterConfig:
                    description: 关联的 DisasterConfig 名称。operator 通过它读取 sourceCluster、targetCluster 和 storageRepository。
                    type: string
                newNamespace:
                    description: 恢复到目标集群时的新 namespace 兼容字段。当前 DisasterBackupReconciler 不使用该字段，后续 DisasterJob/恢复链路可读取。
                    type: string
                newStorageName:
                    description: 恢复或后续备份链路使用的新存储名称兼容字段。当前 DisasterBackupReconciler 不使用该字段，后续 DisasterJob/恢复链路可读取。
                    type: string
            required:
                - disasterConfig
        DisasterBackupStatusDTO:
            description: DisasterBackup 观测状态 DTO，由 disaster-operator 回写。
            type: object
            properties:
                conditions:
                    description: Kubernetes 条件列表，当前 controller 透传 status.conditions。
                    type: array
                    items:
                        $ref: '#/components/schemas/KubernetesCondition'
                phase:
                    $ref: '#/components/schemas/DisasterBackupPhase'
                resources:
                    description: operator 扫描源集群命名空间后写入的资源清单。key 为 namespace，value 为资源类型分组列表。
                    type: object
                    additionalProperties:
                        type: array
                        items:
                            $ref: '#/components/schemas/DisasterBackupResourceGroup'
                workload:
                    description: workload 资源清单兼容字段。当前 DisasterBackupReconciler 未写入该字段。
                    type: object
                    additionalProperties:
                        type: array
                        items:
                            $ref: '#/components/schemas/DisasterBackupResourceGroup'
                updateTime:
                    description: 状态更新时间。当前 controller 中写入 updateTime 的代码被注释，可能为空或为零值时间。
                    type: string
                    format: date-time
        DisasterBackupDTO:
            description: DisasterBackup 对外响应 DTO。
            type: object
            properties:
                id:
                    description: DisasterBackup Kubernetes UID。
                    type: string
                name:
                    description: DisasterBackup 名称。
                    type: string
                namespace:
                    description: CRD 所在 namespace，当前接口由配置的管理命名空间决定（默认 disaster-system）。
                    type: string
                creationTimestamp:
                    description: Kubernetes 创建时间，server 响应按进程本地时区（time.Local/TZ）序列化为 RFC3339 offset 字符串。注意该字段在当前 DTO 中使用 camelCase。
                    type: string
                    format: date-time
                labels:
                    description: 资源 labels。operator 会同步 dependency token 和 spec.disasterConfig 相关依赖标签；列表动态过滤按 label value 包含匹配。
                    type: object
                    additionalProperties:
                        type: string
                annotations:
                    description: 资源 annotations。写接口会尽力写入 trace id annotation。
                    type: object
                    additionalProperties:
                        type: string
                spec:
                    $ref: '#/components/schemas/DisasterBackupSpecDTO'
                status:
                    $ref: '#/components/schemas/DisasterBackupStatusDTO'
            required:
                - id
                - name
                - namespace
                - creationTimestamp
                - spec
                - status
        DisasterBackupCollection:
            description: DisasterBackup 集合数据。
            type: object
            properties:
                items:
                    description: 当前页 DisasterBackup DTO 列表。
                    type: array
                    items:
                        $ref: '#/components/schemas/DisasterBackupDTO'
            required:
                - items
        DisasterBackupCollectionEnvelope:
            description: DisasterBackup 列表统一响应。
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/DisasterBackupCollection'
                    meta:
                        $ref: '#/components/schemas/CollectionMeta'
        DisasterBackupEnvelope:
            description: DisasterBackup 创建、更新和详情统一响应。
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/DisasterBackupDTO'
        DisasterBackupCreateRequest:
            description: 创建 DisasterBackup 请求体。
            type: object
            properties:
                name:
                    description: DisasterBackup 名称，写入 metadata.name。
                    type: string
                labelSelector:
                    $ref: '#/components/schemas/KubernetesLabelSelector'
                namespace:
                    description: 源集群中要扫描和备份的业务 namespace。业务上应传入；server binding 未强制。
                    type: string
                disasterConfig:
                    description: 关联的 DisasterConfig 名称。binding 要求必传。
                    type: string
                newNamespace:
                    description: 恢复到目标集群时的新 namespace 兼容字段。
                    type: string
                newStorageName:
                    description: 后续恢复或备份链路使用的新存储名称兼容字段。
                    type: string
            required:
                - name
                - disasterConfig
        DisasterBackupUpdateRequest:
            description: 更新 DisasterBackup 请求体。path name 当前只用于路由匹配，handler 用 body.name 定位资源；字符串字段只在非空时覆盖，不能用空字符串清空。
            type: object
            properties:
                name:
                    description: 实际要更新的 DisasterBackup 名称。binding 要求必传，handler 用它执行 Get。
                    type: string
                labelSelector:
                    $ref: '#/components/schemas/KubernetesLabelSelector'
                namespace:
                    description: 非空时覆盖 spec.namespace。
                    type: string
                disasterConfig:
                    description: 非空时覆盖 spec.disasterConfig。
                    type: string
                newNamespace:
                    description: 非空时覆盖 spec.newNamespace。
                    type: string
                newStorageName:
                    description: 非空时覆盖 spec.newStorageName。
                    type: string
            required:
                - name
        DisasterBackupDeleteResult:
            description: 删除 DisasterBackup 成功后返回的资源名称。
            type: object
            properties:
                name:
                    description: 已提交删除的 DisasterBackup 名称。
                    type: string
            required:
                - name
        DisasterBackupDeleteEnvelope:
            description: 删除 DisasterBackup 成功响应。
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/DisasterBackupDeleteResult'
        DisasterBackupWatchEventDTO:
            description: DisasterBackup watch 事件数据。
            type: object
            properties:
                type:
                    description: Kubernetes watch 事件类型。
                    type: string
                    enum:
                        - ADDED
                        - MODIFIED
                        - DELETED
                        - ERROR
                object:
                    $ref: '#/components/schemas/DisasterBackupDTO'
            required:
                - type
        DisasterBackupWatchEnvelope:
            description: DisasterBackup WebSocket 推送帧。连接成功、心跳、关闭、超时和事件推送均使用统一 Envelope。
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        oneOf:
                            - $ref: '#/components/schemas/DisasterBackupWatchEventDTO'
                            - $ref: '#/components/schemas/WatchConnectedData'
                    meta:
                        $ref: '#/components/schemas/WatchStreamMeta'
        DisasterInstanceFsmState:
            description: DisasterInstanceReconciler 写入的状态机状态。
            type: string
            enum: [Pending, Initializing, Protected, Paused, FailingOver, Active, FailingBack, ConfigError, Failed]
        DisasterInstanceCurrentState:
            description: server 通过 CurrentStateFromFSM 从 fsmState 映射出的前端聚合状态。
            type: string
            enum: [Running, Active, Paused, Transitioning, Failed, Unknown]
        DisasterInstanceBulkModifierActionType:
            description: 实例级批量资源修改动作类型。replaceExactValue/removeKey 为提交期静态动作，server 会扫描源集群资源并生成长期 modifierRuleSnapshot；rewriteImage 为运行时动态镜像重写意图，server 只校验并保存 DSL，由 operator 在 ResourceSync/Drill 恢复构建阶段读取源集群真实镜像并动态编译规则。
            type: string
            enum: [replaceExactValue, removeKey, rewriteImage]
        DisasterInstanceImageRewriteUnmatchedPolicy:
            description: rewriteImage 未匹配镜像策略。Keep 保留未匹配镜像，Fail 在运行时编译阶段拒绝本次恢复；空值由 server 归一化为 Keep。
            type: string
            enum: [Keep, Fail]
        DisasterInstanceImageRewriteDigestPolicy:
            description: rewriteImage digest 镜像处理策略。当前仅支持 Preserve，表示保留 tag 或 digest suffix。
            type: string
            enum: [Preserve]
        DisasterInstanceDynamicImageRewrite:
            description: rewriteImage 动作的稳定镜像前缀重写意图。该结构不保存完整镜像 tag/digest；源镜像完整值由 operator 在运行时读取源集群资源 spec 后动态推导。
            type: object
            properties:
                sourcePrefix:
                    description: 源侧镜像前缀，例如 10.11.11.1:5000/。必填，server trim 空白后校验非空。
                    type: string
                targetPrefix:
                    description: 目标侧镜像前缀，例如 registry-test.xxx.xxx.com:30088/dr_images/10_11_11_1_5000/。必填，server trim 空白后校验非空。
                    type: string
                unmatchedPolicy:
                    $ref: '#/components/schemas/DisasterInstanceImageRewriteUnmatchedPolicy'
                digestPolicy:
                    $ref: '#/components/schemas/DisasterInstanceImageRewriteDigestPolicy'
            required: [sourcePrefix, targetPrefix]
        DisasterInstanceBulkModifierAction:
            description: 实例级批量资源修改动作。action=replaceExactValue 时必须填写 sourceValue/targetValue；action=removeKey 时必须填写 key；action=rewriteImage 时必须填写 imageRewrite.sourcePrefix/targetPrefix，且不要求 sourceValue/targetValue。
            type: object
            properties:
                id:
                    description: 动作标识，用于错误诊断和生成稳定规则 ID。
                    type: string
                action:
                    $ref: '#/components/schemas/DisasterInstanceBulkModifierActionType'
                enabled:
                    description: 是否启用动作。未传时 server 归一化为 true。
                    type: boolean
                applyTo:
                    description: 动作应用场景。当前实例级 bulk 动作只支持 resourceSync 和 drill，不支持 dataSync；未传时 server 归一化为 [resourceSync]。
                    type: array
                    items:
                        type: string
                        enum: [resourceSync, drill]
                directionPolicy:
                    description: 方向策略。replaceExactValue/rewriteImage 默认 Auto；removeKey 默认 ForwardOnly。
                    type: string
                    enum: [Auto, ForwardOnly, ReverseOnly]
                sourceValue:
                    description: action=replaceExactValue 时的源侧完整值。
                    type: string
                targetValue:
                    description: action=replaceExactValue 时的目标侧完整值。
                    type: string
                key:
                    description: action=removeKey 时要删除的 map/object key。
                    type: string
                imageRewrite:
                    $ref: '#/components/schemas/DisasterInstanceDynamicImageRewrite'
            required: [action]
        DisasterInstanceRestorePolicy:
            description: 实例级恢复策略。server 支持 resourceSelection、execution、storageClassMapping、ingressClassMapping、bulkModifierActions、bulkModifierActionsText、modifierRules、modifierRulesText、useUnifiedDirectionResolver，并在返回 DTO 中回显提交期静态 bulk action 生成的 modifierRuleSnapshot 和 modifierRuleSnapshotHash。rewriteImage 是运行时动态 action，不生成长期 modifierRuleSnapshot。
            type: object
            properties:
                resourceSelection:
                    description: 恢复资源选择策略，包含 namespace/resource include/exclude、labelSelector、includeClusterResources。
                    type: object
                    additionalProperties: true
                execution:
                    description: 恢复执行策略，包含 existingResourcePolicy、restorePVs、itemOperationTimeout。
                    type: object
                    additionalProperties: true
                storageClassMapping:
                    $ref: '#/components/schemas/DisasterInstanceRestoreClassMappingPolicy'
                ingressClassMapping:
                    $ref: '#/components/schemas/DisasterInstanceRestoreClassMappingPolicy'
                bulkModifierActions:
                    description: 结构化 bulk modifier action 列表，支持 replaceExactValue、removeKey、rewriteImage。replaceExactValue/removeKey 会在提交期扫描源集群生成长期快照；rewriteImage 只保存稳定镜像前缀重写意图，运行时动态编译。
                    type: array
                    items:
                        $ref: '#/components/schemas/DisasterInstanceBulkModifierAction'
                bulkModifierActionsText:
                    description: bulk modifier action JSON 文本入口，可为对象或数组字符串。
                    type: string
                modifierRules:
                    description: |-
                        结构化 restore modifier rule 列表。当前每个 DisasterInstance 最多允许 1000 条规则；超过上限时创建/更新接口返回 `400 code=1000`，message 包含 `ModifierRuleRejected: modifier rule count <n> exceeds limit 1000`。
                    type: array
                    items:
                        type: object
                        additionalProperties: true
                modifierRulesText:
                    description: |-
                        modifierRules 的 JSON 数组文本入口。解析后的结构化规则同样受每实例最多 1000 条规则限制。
                    type: string
                useUnifiedDirectionResolver:
                    description: 是否使用统一方向解析器。
                    type: boolean
                modifierRuleSnapshot:
                    description: replaceExactValue/removeKey 等提交期静态 bulk modifier 展开后的 modifier rule 快照，仅响应 DTO 中返回。rewriteImage 不写入长期快照。
                    type: array
                    items:
                        type: object
                        additionalProperties: true
                modifierRuleSnapshotHash:
                    description: modifierRuleSnapshot 的 hash，仅响应 DTO 中返回。
                    type: string
        DisasterInstanceRestoreClassMapping:
            description: StorageClass 或 IngressClass 映射条目。
            type: object
            properties:
                sourceClass:
                    description: 源集群 class 名称。
                    type: string
                targetClass:
                    description: 目标集群 class 名称。
                    type: string
                namespaces:
                    description: 可选 namespace 作用范围。预检接口当前只检查 targetClass 是否存在。
                    type: array
                    items:
                        type: string
            required: [sourceClass, targetClass]
        DisasterInstanceRestoreClassMappingPolicy:
            description: StorageClass/IngressClass 映射策略。
            type: object
            properties:
                mappings:
                    description: 映射列表。
                    type: array
                    items:
                        $ref: '#/components/schemas/DisasterInstanceRestoreClassMapping'
                unmatchedPolicy:
                    description: 未匹配 class 时的处理策略。预检接口仅接受空、Keep、Fail。
                    type: string
                    enum: [Keep, Fail]
                strictTargetValidation:
                    description: true 时目标 class 缺失会返回 data.valid=false，但 HTTP 仍为 200。
                    type: boolean
        DisasterInstanceSpecDTO:
            description: DisasterInstance 对外 spec DTO。
            type: object
            properties:
                config:
                    description: 关联 DisasterConfig 名称。
                    type: string
                dataSyncPolicy:
                    description: 实例级数据同步策略 override，空时继承 DisasterConfig.spec.dataSyncPolicy。
                    type: string
                resourceSyncPolicy:
                    description: 实例级资源同步策略 override，空时继承 DisasterConfig.spec.resourceSyncPolicy。
                    type: string
                effectiveDataSyncPolicy:
                    description: server 计算后的实际数据同步策略名称。
                    type: string
                effectiveResourceSyncPolicy:
                    description: server 计算后的实际资源同步策略名称。
                    type: string
                dataSyncPolicySource:
                    description: 实际数据同步策略来源。
                    type: string
                    enum: [config, instance]
                resourceSyncPolicySource:
                    description: 实际资源同步策略来源。
                    type: string
                    enum: [config, instance]
                namespaces:
                    description: 要保护的源集群 namespace 列表。
                    type: array
                    items:
                        type: string
                labelSelector:
                    $ref: '#/components/schemas/KubernetesLabelSelector'
                operationTimeoutMinutes:
                    description: 实例级容灾操作默认超时时间，单位分钟；operator 在实例初始化、DataSync、ResourceSync、Failover/Reprotect 等操作中使用，未设置时按 operator 默认值 60 分钟处理。
                    type: integer
                    format: int32
                podRestoreMethod:
                    description: Standby 模式下 Pod 处理方式。
                    type: string
                    enum: [replica, initContainer]
                restorePolicy:
                    $ref: '#/components/schemas/DisasterInstanceRestorePolicy'
                veleroHooks:
                    $ref: '#/components/schemas/DisasterVeleroHooks'
                skipPodReadyCheck:
                    description: failover 默认是否跳过 Pod readyReplicas 校验。
                    type: boolean
                description:
                    description: 实例描述，来自 annotation testudo.softcdata.com/description。
                    type: string
            required: [config]
        DisasterInstanceStatusDTO:
            description: DisasterInstance 观测状态 DTO，由 operator 写入。
            type: object
            properties:
                fsmState:
                    $ref: '#/components/schemas/DisasterInstanceFsmState'
                reason:
                    description: 机器可读错误原因。
                    type: string
                message:
                    description: 人类可读错误信息。
                    type: string
                primaryCluster:
                    description: 当前主集群。
                    type: string
                secondaryCluster:
                    description: 当前备集群。
                    type: string
                lastDataSyncTime:
                    description: 最近一次 DataSync 成功时间。
                    type: string
                    format: date-time
                lastResourceSyncTime:
                    description: 最近一次 ResourceSync 成功时间。
                    type: string
                    format: date-time
                availableOperations:
                    description: 当前状态允许的操作。Protected 通常为 failover/pause/synconce/syncdata/syncresource，Active 通常为 reprotect/undo，Paused 为 resume；Failed 通常为 reset，但由 DataSync/ResourceSync/Initialization 等同步链路失败导致的可恢复 Failed 可额外包含 syncdata 或 syncresource 以支持重试。
                    type: array
                    items:
                        type: string
                dataSyncName:
                    description: operator 创建或维护的 DataSync 名称，通常为 dr-ds-<instance>。
                    type: string
                resourceSyncName:
                    description: operator 创建或维护的 ResourceSync 名称，通常为 dr-rs-<instance>。
                    type: string
        DisasterInstanceSyncSummary:
            description: 列表和 watch 中聚合的 DataSync/ResourceSync 摘要。
            type: object
            properties:
                state:
                    description: 子资源状态，来自 DataSync/ResourceSync status.state。
                    type: string
                reason:
                    description: 子资源失败原因。当前 server 只在 Failed 状态下回显。
                    type: string
                message:
                    description: 子资源失败消息。当前 server 只在 Failed 状态下回显。
                    type: string
                lastSyncTime:
                    description: 子资源最近同步时间。
                    type: string
                    format: date-time
                syncSuccessCount:
                    type: integer
                syncFailureCount:
                    type: integer
                paused:
                    type: boolean
        AutoCancelSummaryDTO:
            description: failover 自动补偿摘要，来自最近 failover DisasterOperation status。
            type: object
            properties:
                triggered:
                    type: boolean
                status:
                    type: string
                    enum: [NotTriggered, Running, Succeeded, Failed]
                reason:
                    type: string
                triggerStep:
                    type: string
                manualInterventionRequired:
                    type: boolean
                triggeredAt:
                    type: string
                    format: date-time
                completionTime:
                    type: string
                    format: date-time
        DisasterInstanceDTO:
            description: DisasterInstance 聚合响应 DTO，来自 CRD metadata/spec/status，并尽力聚合 config、storage、sync summary 和 autoCancel。
            type: object
            properties:
                id:
                    description: Kubernetes UID。
                    type: string
                name:
                    description: 实例名称。
                    type: string
                namespace:
                    description: 实例所在 namespace。默认创建到配置的管理命名空间（默认 disaster-system），也支持请求指定 namespace。
                    type: string
                labels:
                    type: object
                    additionalProperties:
                        type: string
                description:
                    type: string
                creationTimestamp:
                    type: string
                    format: date-time
                spec:
                    $ref: '#/components/schemas/DisasterInstanceSpecDTO'
                status:
                    $ref: '#/components/schemas/DisasterInstanceStatusDTO'
                conditions:
                    type: array
                    items:
                        $ref: '#/components/schemas/KubernetesCondition'
                conditionSummary:
                    description: 当前提取 RoleDrift 条件。
                    type: object
                    properties:
                        roleDrift:
                            $ref: '#/components/schemas/KubernetesCondition'
                configName:
                    description: 关联 DisasterConfig 名称，等同 spec.config。
                    type: string
                config:
                    description: 尽力读取到的 DisasterConfig DTO；读取失败时省略。
                    type: object
                    additionalProperties: true
                storage:
                    description: 详情接口尽力读取到的 StorageRepository DTO；读取失败时省略。
                    type: object
                    additionalProperties: true
                currentState:
                    $ref: '#/components/schemas/DisasterInstanceCurrentState'
                dataSyncStatus:
                    $ref: '#/components/schemas/DisasterInstanceSyncSummary'
                resourceSyncStatus:
                    $ref: '#/components/schemas/DisasterInstanceSyncSummary'
                autoCancel:
                    $ref: '#/components/schemas/AutoCancelSummaryDTO'
            required: [id, name, namespace, creationTimestamp, spec, status, configName, currentState]
        DisasterInstanceCollection:
            type: object
            properties:
                items:
                    type: array
                    items:
                        $ref: '#/components/schemas/DisasterInstanceDTO'
            required: [items]
        DisasterInstanceCollectionEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/DisasterInstanceCollection'
                    meta:
                        $ref: '#/components/schemas/CollectionMeta'
        DisasterInstanceEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/DisasterInstanceDTO'
        DisasterInstanceCreateRequest:
            description: 创建 DisasterInstance 请求体。
            type: object
            properties:
                name:
                    type: string
                namespace:
                    description: CRD 所在 namespace。为空时默认 disaster-system。
                    type: string
                config:
                    description: 关联 DisasterConfig 名称。
                    type: string
                dataSyncPolicy:
                    type: string
                resourceSyncPolicy:
                    type: string
                namespaces:
                    type: array
                    items:
                        type: string
                labelSelector:
                    $ref: '#/components/schemas/KubernetesLabelSelector'
                operationTimeoutMinutes:
                    description: 实例级容灾操作默认超时时间，单位分钟；写入 DisasterInstance.spec.operationTimeoutMinutes，未传时由 operator 默认 60 分钟兜底。
                    type: integer
                    format: int32
                podRestoreMethod:
                    type: string
                    enum: [replica, initContainer]
                restorePolicy:
                    $ref: '#/components/schemas/DisasterInstanceRestorePolicy'
                veleroHooks:
                    $ref: '#/components/schemas/DisasterVeleroHooks'
                skipPodReadyCheck:
                    type: boolean
                description:
                    type: string
            required: [name, config]
        DisasterInstanceUpdateRequest:
            description: 更新 DisasterInstance 请求体。未传或 null 的字段保持原值；空字符串/空数组按字段语义清空。
            type: object
            properties:
                dataSyncPolicy:
                    type: string
                resourceSyncPolicy:
                    type: string
                namespaces:
                    type: array
                    items:
                        type: string
                labelSelector:
                    $ref: '#/components/schemas/KubernetesLabelSelector'
                operationTimeoutMinutes:
                    description: 实例级容灾操作默认超时时间，单位分钟；未传或 null 时保持原值。
                    type: integer
                    format: int32
                podRestoreMethod:
                    type: string
                    enum: [replica, initContainer]
                restorePolicy:
                    $ref: '#/components/schemas/DisasterInstanceRestorePolicy'
                veleroHooks:
                    description: 更新容灾级 Velero Hook。未出现保持原值；传 null 或空对象清空全部；子字段 dataBackup/dataRestore 未出现保持对应子字段，传 null 或空对象清空对应子字段。
                    nullable: true
                    oneOf:
                        - $ref: '#/components/schemas/DisasterVeleroHooks'
                skipPodReadyCheck:
                    type: boolean
                description:
                    type: string
        NameResult:
            type: object
            properties:
                name:
                    type: string
            required: [name]
        DisasterInstanceDeleteEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/NameResult'
        DisasterInstanceActionRequest:
            type: object
            properties:
                operation:
                    description: server 接收 failover、reprotect、undo、pause、resume、synconce、sync-data、sync-resource、cancel；sync-data/sync-resource 会映射为 CRD syncdata/syncresource。
                    type: string
                    enum: [failover, reprotect, undo, pause, resume, synconce, sync-data, sync-resource, cancel]
                config:
                    description: 动作配置，支持 force、skipFinalSync、skipScaleDownSource、timeoutMinutes、skipPodReadyCheck、waitUntilReady。
                    type: object
                    additionalProperties: true
            required: [operation]
        DisasterInstanceActionEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        type: object
                        properties:
                            operationID:
                                type: string
                            status:
                                type: string
                        required: [operationID, status]
        DisasterInstanceGroupsDTO:
            type: object
            properties:
                instance:
                    type: string
                namespace:
                    type: string
                inGroup:
                    type: boolean
                groups:
                    type: array
                    items:
                        type: string
            required: [instance, namespace, inGroup, groups]
        DisasterInstanceGroupsEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/DisasterInstanceGroupsDTO'
        HistoryDTO:
            type: object
            properties:
                time:
                    type: string
                    format: date-time
                type:
                    type: string
                status:
                    type: object
                    properties:
                        state:
                            type: string
                        reason:
                            type: string
                        message:
                            type: string
                autoCancel:
                    $ref: '#/components/schemas/AutoCancelSummaryDTO'
                operationName:
                    type: string
                operationUID:
                    type: string
                hasDetail:
                    type: boolean
                result:
                    type: string
                reason:
                    type: string
                operator:
                    type: string
                note:
                    type: string
        DisasterInstanceHistoryEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        type: array
                        items:
                            $ref: '#/components/schemas/HistoryDTO'
        HistoryStatusDTO:
            type: object
            properties:
                state:
                    description: 状态值，例如 Pending、Running、Completed、Failed、Unknown。
                    type: string
                reason:
                    description: 机器可读原因。
                    type: string
                message:
                    description: 人类可读状态描述。
                    type: string
        StepStatusDTO:
            type: object
            properties:
                name:
                    type: string
                state:
                    type: string
                startTime:
                    type: string
                    format: date-time
                completionTime:
                    type: string
                    format: date-time
                message:
                    type: string
        OperationDetailDTO:
            type: object
            properties:
                name:
                    type: string
                uid:
                    type: string
                namespace:
                    type: string
                ownerKind:
                    type: string
                ownerName:
                    type: string
                operationType:
                    type: string
                state:
                    type: string
                    enum: [Pending, Running, Completed, Failed]
                reason:
                    type: string
                currentStep:
                    type: string
                message:
                    type: string
                steps:
                    type: array
                    items:
                        $ref: '#/components/schemas/StepStatusDTO'
                autoCancel:
                    $ref: '#/components/schemas/AutoCancelSummaryDTO'
                roleStatus:
                    type: object
                    properties:
                        primaryCluster:
                            type: string
                        secondaryCluster:
                            type: string
                groupStatus:
                    type: object
                    additionalProperties: true
                startTime:
                    type: string
                    format: date-time
                completionTime:
                    type: string
                    format: date-time
                creationTimestamp:
                    type: string
                    format: date-time
        OperationDetailEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/OperationDetailDTO'
        ValidateRestoreClassesRequest:
            type: object
            properties:
                targetCluster:
                    type: string
                storageClassMapping:
                    $ref: '#/components/schemas/DisasterInstanceRestoreClassMappingPolicy'
                ingressClassMapping:
                    $ref: '#/components/schemas/DisasterInstanceRestoreClassMappingPolicy'
        RestoreClassCheckDTO:
            type: object
            properties:
                enabled:
                    type: boolean
                strictTargetValidation:
                    type: boolean
                checkedTargets:
                    type: array
                    items:
                        type: string
                missingTargets:
                    type: array
                    items:
                        type: string
        ValidateRestoreClassesDTO:
            type: object
            properties:
                instanceName:
                    type: string
                namespace:
                    type: string
                targetCluster:
                    type: string
                valid:
                    type: boolean
                code:
                    type: string
                message:
                    type: string
                storageClassCheck:
                    $ref: '#/components/schemas/RestoreClassCheckDTO'
                ingressClassCheck:
                    $ref: '#/components/schemas/RestoreClassCheckDTO'
        ValidateRestoreClassesEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/ValidateRestoreClassesDTO'
        LastSyncStatusDTO:
            description: DataSync 或 ResourceSync 的上次同步结果，来自对应子资源 status.history 最新记录。
            type: object
            properties:
                status:
                    description: 上次同步结果状态；空值归一为 Unknown。
                    type: string
                    enum: [Completed, Failed, Unknown]
                startTime:
                    description: 上次同步开始时间。
                    type: string
                    format: date-time
                completionTime:
                    description: 上次同步完成时间。
                    type: string
                    format: date-time
                duration:
                    description: 上次同步耗时。
                    type: string
                backupName:
                    description: 上次同步关联的 AppBackup 名称。
                    type: string
                restoreName:
                    description: 上次同步关联的 AppRestore 名称。
                    type: string
                backupResourceCount:
                    description: 上次同步备份资源数量。
                    type: integer
                restoreResourceCount:
                    description: 上次同步恢复资源数量。
                    type: integer
                backupHookStatus:
                    $ref: '#/components/schemas/SyncHistoryHookStatusDTO'
                restoreHookStatus:
                    $ref: '#/components/schemas/SyncHistoryHookStatusDTO'
            required: [status]
        SubResourceStatusDTO:
            type: object
            properties:
                name:
                    type: string
                status:
                    type: string
                reason:
                    type: string
                message:
                    type: string
                paused:
                    type: boolean
                lastBackupName:
                    type: string
                lastTime:
                    type: string
                    format: date-time
                lastSyncStatus:
                    $ref: '#/components/schemas/LastSyncStatusDTO'
                duration:
                    type: string
                resourceCount:
                    type: integer
                runningTerminalCommands:
                    type: array
                    items:
                        type: string
                backupResourceCount:
                    type: integer
                restoreResourceCount:
                    type: integer
                failureCount:
                    type: integer
                syncSuccessCount:
                    type: integer
                syncFailureCount:
                    type: integer
        SyncStatusDTO:
            type: object
            properties:
                dataSync:
                    $ref: '#/components/schemas/SubResourceStatusDTO'
                resourceSync:
                    $ref: '#/components/schemas/SubResourceStatusDTO'
        SyncStatusEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/SyncStatusDTO'
        SyncHistoryItemDTO:
            type: object
            properties:
                id:
                    description: server 生成的同步历史记录 ID，按来源保持稳定。
                    type: string
                syncType:
                    description: 同步类型。
                    type: string
                    enum: [dataSync, resourceSync, syncOnce]
                source:
                    description: 历史来源。syncRecord 来自 DataSync/ResourceSync status.history；operation 来自 DisasterOperation。
                    type: string
                    enum: [syncRecord, operation]
                status:
                    $ref: '#/components/schemas/HistoryStatusDTO'
                startTime:
                    description: 同步开始时间。
                    type: string
                    format: date-time
                completionTime:
                    description: 同步完成时间。
                    type: string
                    format: date-time
                duration:
                    description: 同步耗时。
                    type: string
                backupName:
                    description: 关联 AppBackup 名称。
                    type: string
                restoreName:
                    description: 关联 AppRestore 名称。
                    type: string
                backupResourceCount:
                    description: 备份资源数量。
                    type: integer
                restoreResourceCount:
                    description: 恢复资源数量。
                    type: integer
                backupHookStatus:
                    $ref: '#/components/schemas/SyncHistoryHookStatusDTO'
                restoreHookStatus:
                    $ref: '#/components/schemas/SyncHistoryHookStatusDTO'
                subResourceName:
                    description: 来源 DataSync 或 ResourceSync 名称。
                    type: string
                operationName:
                    description: 来源 DisasterOperation 名称；source=operation 时返回。
                    type: string
                operationUID:
                    description: 来源 DisasterOperation UID；source=operation 时返回。
                    type: string
                operationType:
                    description: 来源 DisasterOperation operationType；source=operation 时返回。
                    type: string
                    enum: [syncdata, syncresource, synconce]
                hasOperationDetail:
                    description: 是否可调用 /instances/:name/operations/:operationName 查看操作详情。
                    type: boolean
            required: [id, syncType, source, status, hasOperationDetail]
        SyncHistoryCollectionEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        type: object
                        properties:
                            items:
                                type: array
                                items:
                                    $ref: '#/components/schemas/SyncHistoryItemDTO'
                        required: [items]
                    meta:
                        $ref: '#/components/schemas/CollectionMeta'
        ValidateTargetDTO:
            type: object
            properties:
                targetName:
                    type: string
                namespace:
                    type: string
                operation:
                    type: string
                valid:
                    type: boolean
                reason:
                    type: string
                message:
                    type: string
                fsmState:
                    $ref: '#/components/schemas/DisasterInstanceFsmState'
                availableOperations:
                    type: array
                    items:
                        type: string
                inGroup:
                    type: boolean
                groups:
                    type: array
                    items:
                        type: string
        ValidateTargetEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/ValidateTargetDTO'
        DisasterInstanceWatchEventDTO:
            type: object
            properties:
                type:
                    type: string
                    enum: [ADDED, MODIFIED, DELETED, ERROR]
                object:
                    $ref: '#/components/schemas/DisasterInstanceDTO'
            required: [type]
        DisasterInstanceWatchEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        oneOf:
                            - $ref: '#/components/schemas/DisasterInstanceWatchEventDTO'
                            - $ref: '#/components/schemas/WatchConnectedData'
                    meta:
                        $ref: '#/components/schemas/WatchStreamMeta'
        OperationWatchEventDTO:
            type: object
            properties:
                type:
                    type: string
                    enum: [ADDED, MODIFIED, DELETED, ERROR]
                object:
                    $ref: '#/components/schemas/OperationDetailDTO'
            required: [type]
        OperationWatchEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        oneOf:
                            - $ref: '#/components/schemas/OperationWatchEventDTO'
                            - $ref: '#/components/schemas/WatchConnectedData'
                    meta:
                        $ref: '#/components/schemas/WatchStreamMeta'
        DisasterGroupFailPolicy:
            description: 容灾组层级执行失败策略。Continue 表示记录失败后继续后续实例或层级；Stop 表示遇到失败后停止父级组操作。
            type: string
            enum: [Continue, Stop]
        DisasterGroupRetryPolicy:
            description: 容灾操作重试策略。由组动作创建父级 DisasterOperation 时读取 group.spec.policy.retryPolicy，并透传给子操作。
            type: object
            properties:
                maxRetries:
                    description: 最大重试次数。0 表示不重试。
                    type: integer
                    format: int32
                retryIntervalSeconds:
                    description: 重试间隔秒数。operator 默认语义为 5 秒。
                    type: integer
                    format: int32
        DisasterGroupPolicy:
            description: 容灾组执行策略。server 不校验枚举和值域，operator 按实际字段解释。
            type: object
            properties:
                failPolicy:
                    $ref: '#/components/schemas/DisasterGroupFailPolicy'
                timeoutMin:
                    description: 单个 Level 超时时间，单位分钟。当前组操作编排代码没有独立按该字段做 Level 超时控制。
                    type: integer
                parallelism:
                    description: 全局最大并行实例数。当前 handleGroupOperation 按 level 中实例创建子操作，未显式使用该字段限流。
                    type: integer
                retryPolicy:
                    $ref: '#/components/schemas/DisasterGroupRetryPolicy'
        DisasterGroupStatusDTO:
            description: 容灾组对外状态 DTO。total/ready/reason/message/conditions 来自 operator；summary/fsmState/availableOperations 由 server 根据成员实例现场计算。
            type: object
            properties:
                totalInstances:
                    description: 组内实例总数，operator 按 spec.levels 统计。
                    type: integer
                readyInstances:
                    description: operator 统计的 Protected 实例数量。
                    type: integer
                reason:
                    description: 机器可读原因。常见值为 InstanceNotFound、InstanceFailed。
                    type: string
                message:
                    description: 人类可读状态消息，包含缺失实例或失败实例摘要。
                    type: string
                summary:
                    description: server 计算展示摘要，例如 3 Levels, 10 Instances。
                    type: string
                fsmState:
                    description: server 根据成员实例推导的组展示态。
                    type: string
                    enum: [Unknown, Initializing, Protected, PartialProtected, Paused, Active, FailingOver, FailingBack, Degraded]
                availableOperations:
                    description: server 根据组展示态推导的可执行操作。
                    type: array
                    items:
                        type: string
                conditions:
                    description: operator 写入的聚合条件，Error=True 表示组内存在缺失或失败成员。
                    type: array
                    items:
                        $ref: '#/components/schemas/KubernetesCondition'
            required: [totalInstances, readyInstances, summary, fsmState, availableOperations]
        InstanceSummaryDTO:
            description: 容灾组详情和列表中的成员实例摘要。
            type: object
            properties:
                name:
                    description: 成员 DisasterInstance 名称。
                    type: string
                namespaces:
                    description: 成员实例保护的业务 namespace 列表。
                    type: array
                    items:
                        type: string
                fsmState:
                    description: 成员实例状态。读取失败时 server 填 NotFound。
                    type: string
                reason:
                    description: 成员实例机器可读原因，来自 DisasterInstance status.reason；为空时省略。
                    type: string
                message:
                    description: 成员实例状态描述，来自 DisasterInstance status.message；为空时省略。
                    type: string
                primaryCluster:
                    description: 成员实例当前主集群。
                    type: string
                secondaryCluster:
                    description: 成员实例当前备集群。
                    type: string
                storageRepository:
                    description: 成员实例关联 DisasterConfig 中的 storageRepository；配置读取失败时为空。
                    type: string
                level:
                    description: 成员所在层级，1-based。
                    type: integer
            required: [name, fsmState, level]
        DisasterGroupDTO:
            description: 容灾组聚合 DTO，来自 DisasterGroup metadata/spec/status，并由 server 补齐成员摘要和组展示态。
            type: object
            properties:
                id:
                    description: Kubernetes UID。
                    type: string
                name:
                    description: 容灾组名称。
                    type: string
                description:
                    description: 业务描述，来自 annotation testudo.softcdata.com/description。
                    type: string
                creation_timestamp:
                    description: 创建时间，server 按进程本地时区（time.Local/TZ）格式化为 RFC3339 offset 字符串并使用 snake_case 字段名。
                    type: string
                    format: date-time
                levels:
                    description: 组内实例分层。外层数组表示切换层级，内层实例同层并行执行。
                    type: array
                    items:
                        type: array
                        items:
                            type: string
                policy:
                    $ref: '#/components/schemas/DisasterGroupPolicy'
                status:
                    $ref: '#/components/schemas/DisasterGroupStatusDTO'
                instances:
                    description: 成员实例摘要。创建和更新成功响应直接 ConvertToDisasterGroupDTO，通常不包含该字段。
                    type: array
                    items:
                        $ref: '#/components/schemas/InstanceSummaryDTO'
            required: [id, name, creation_timestamp, levels, policy, status]
        DisasterGroupCollection:
            type: object
            properties:
                items:
                    type: array
                    items:
                        $ref: '#/components/schemas/DisasterGroupDTO'
            required: [items]
        DisasterGroupCollectionEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/DisasterGroupCollection'
                    meta:
                        $ref: '#/components/schemas/CollectionMeta'
        DisasterGroupEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/DisasterGroupDTO'
        CreateDisasterGroupRequest:
            description: 创建容灾组请求体。server 使用 json tag 解析，未使用 binding required 校验。
            type: object
            properties:
                name:
                    description: 容灾组名称，写入 metadata.name。当前 server 未显式校验空值。
                    type: string
                description:
                    description: 业务描述，写入 annotation testudo.softcdata.com/description。
                    type: string
                levels:
                    description: 分层实例列表。创建阶段不校验实例是否存在；缺失成员由 operator/status 或后续组操作暴露。
                    type: array
                    items:
                        type: array
                        items:
                            type: string
                policy:
                    $ref: '#/components/schemas/DisasterGroupPolicy'
            required: [name, levels]
        UpdateDisasterGroupRequest:
            description: 更新容灾组请求体。PUT 和 PATCH 共用该结构，并且当前都是部分更新语义。
            type: object
            properties:
                description:
                    description: 业务描述。字段存在时覆盖 annotation；传空字符串会清空描述。
                    type: string
                    nullable: true
                levels:
                    description: 分层实例列表。字段存在时整体替换 spec.levels。
                    type: array
                    items:
                        type: array
                        items:
                            type: string
                policy:
                    allOf:
                        - $ref: '#/components/schemas/DisasterGroupPolicy'
                    description: 组执行策略。字段存在时整体替换 spec.policy，不做子字段 merge。
                    nullable: true
        DisasterGroupDeleteEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/NameResult'
        DisasterGroupActionOperation:
            description: 前端动作名称。sync-data 和 sync-resource 会分别映射为底层 syncdata、syncresource。
            type: string
            enum: [failover, reprotect, pause, resume, synconce, sync-data, sync-resource, undo, cancel]
        DisasterGroupActionRequest:
            description: 触发容灾组动作请求体。server 复用实例动作请求结构。
            type: object
            properties:
                operation:
                    $ref: '#/components/schemas/DisasterGroupActionOperation'
                config:
                    description: 动作配置。支持 force/Force、skipFinalSync/SkipFinalSync、skipScaleDownSource/SkipScaleDownSource、timeoutMinutes、skipPodReadyCheck/SkipPodReadyCheck、waitUntilReady/WaitUntilReady。
                    type: object
                    additionalProperties: true
            required: [operation]
        DisasterGroupActionResult:
            type: object
            properties:
                operationName:
                    description: 父级组 DisasterOperation 名称，格式为 <operation>-<group>-<unix seconds>。
                    type: string
                message:
                    description: 受理消息。
                    type: string
            required: [operationName, message]
        DisasterGroupActionEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/DisasterGroupActionResult'
        DisasterGroupHistoryEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        type: array
                        items:
                            $ref: '#/components/schemas/HistoryDTO'
        GroupMemberStatusDTO:
            description: 容灾组成员展示状态。由 server 根据实例状态和配置状态派生。
            type: object
            properties:
                state:
                    description: 展示状态。可能来自实例 fsmState，也可能被派生为 ConfigError、Failed、NotFound、Unknown。
                    type: string
                reason:
                    description: 失败或异常原因。
                    type: string
                message:
                    description: 失败或异常消息。
                    type: string
            required: [state, reason, message]
        InstancePickerItemDTO:
            description: 实例选择器条目。
            type: object
            properties:
                name:
                    type: string
                namespaces:
                    type: array
                    items:
                        type: string
                description:
                    type: string
                status:
                    $ref: '#/components/schemas/GroupMemberStatusDTO'
                fsmState:
                    description: 兼容字段，保留原始 DisasterInstance.status.fsmState。
                    type: string
            required: [name, status, fsmState]
        InstancePickerCollection:
            type: object
            properties:
                items:
                    type: array
                    items:
                        $ref: '#/components/schemas/InstancePickerItemDTO'
            required: [items]
        InstancePickerCollectionEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/InstancePickerCollection'
                    meta:
                        $ref: '#/components/schemas/CollectionMeta'
        GroupMemberInstanceDTO:
            description: 容灾组已选成员实例 DTO。
            type: object
            properties:
                name:
                    type: string
                description:
                    type: string
                namespaces:
                    type: array
                    items:
                        type: string
                status:
                    $ref: '#/components/schemas/GroupMemberStatusDTO'
                fsmState:
                    description: 兼容字段，保留原始 DisasterInstance.status.fsmState；实例缺失时为 NotFound。
                    type: string
            required: [name, status, fsmState]
        GroupMemberInstanceCollection:
            type: object
            properties:
                items:
                    type: array
                    items:
                        $ref: '#/components/schemas/GroupMemberInstanceDTO'
            required: [items]
        GroupMemberInstanceCollectionEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/GroupMemberInstanceCollection'
                    meta:
                        $ref: '#/components/schemas/CollectionMeta'
        DisasterGroupOperationLevelStatusDTO:
            type: object
            properties:
                index:
                    type: integer
                instances:
                    type: array
                    items:
                        type: string
                state:
                    description: Pending、Running、Completed、Failed、TimedOut 等。
                    type: string
                startTime:
                    type: string
                    format: date-time
                completionTime:
                    type: string
                    format: date-time
                failedInstances:
                    type: array
                    items:
                        type: string
        DisasterGroupOperationGroupStatusDTO:
            type: object
            properties:
                totalLevels:
                    type: integer
                currentLevelIndex:
                    type: integer
                levelStatuses:
                    type: array
                    items:
                        $ref: '#/components/schemas/DisasterGroupOperationLevelStatusDTO'
        DisasterGroupOperationDTO:
            description: 容灾组操作 WebSocket 推送 DTO，由 server ConvertToDisasterOperationDTO 转换。
            type: object
            properties:
                name:
                    type: string
                namespace:
                    type: string
                groupName:
                    type: string
                operationType:
                    type: string
                state:
                    type: string
                    enum: [Pending, Running, Completed, Failed]
                reason:
                    type: string
                currentStep:
                    description: 父级组操作常见为 Level-0、Level-1 等。
                    type: string
                message:
                    description: 进度消息，例如 Level-0 执行中 [inst=Running(PreCheck)]。
                    type: string
                steps:
                    type: array
                    items:
                        $ref: '#/components/schemas/StepStatusDTO'
                autoCancel:
                    $ref: '#/components/schemas/AutoCancelSummaryDTO'
                groupStatus:
                    $ref: '#/components/schemas/DisasterGroupOperationGroupStatusDTO'
                startTime:
                    type: string
                    format: date-time
                completionTime:
                    type: string
                    format: date-time
                creationTimestamp:
                    type: string
                    format: date-time
            required: [name, namespace, operationType, state, creationTimestamp]
        DisasterGroupOperationWatchEventDTO:
            type: object
            properties:
                type:
                    type: string
                    enum: [ADDED, MODIFIED, DELETED, ERROR]
                object:
                    $ref: '#/components/schemas/DisasterGroupOperationDTO'
            required: [type]
        DisasterGroupOperationWatchEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        oneOf:
                            - $ref: '#/components/schemas/DisasterGroupOperationWatchEventDTO'
                            - $ref: '#/components/schemas/WatchConnectedData'
                    meta:
                        $ref: '#/components/schemas/WatchStreamMeta'
        DisasterGroupWatchEventDTO:
            type: object
            properties:
                type:
                    type: string
                    enum: [ADDED, MODIFIED, DELETED, ERROR]
                object:
                    $ref: '#/components/schemas/DisasterGroupDTO'
            required: [type]
        DisasterGroupWatchEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        oneOf:
                            - $ref: '#/components/schemas/DisasterGroupWatchEventDTO'
                            - $ref: '#/components/schemas/WatchConnectedData'
                    meta:
                        $ref: '#/components/schemas/WatchStreamMeta'
        DisasterConfigStatusType:
            description: DisasterConfigReconciler 写入的配置状态。Pending 表示首次初始化中；Ready 表示依赖和下层 Velero 存储配置已应用成功；NotReady 表示 Cluster 未就绪；Error 表示依赖缺失、查询失败或下层应用失败。
            type: string
            enum: [Pending, Ready, NotReady, Error]
        DisasterConfigDataSyncType:
            description: 数据同步类型。CRD 注释中的当前取值包括 fsb、snapshot、none、external；server 只写入字符串，不同步做枚举校验。
            type: string
        DisasterConfigImageRewriteApplyTarget:
            description: 镜像源重写生效场景。resourceSync 用于 ResourceSync 创建 AppRestore；drill 用于演练恢复。
            type: string
            enum: [resourceSync, drill]
        DisasterConfigImageRewriteUnmatchedPolicy:
            description: 镜像未匹配任何映射时的处理策略。Fail 表示恢复准备失败；Keep 表示保留原镜像。
            type: string
            enum: [Fail, Keep]
        DisasterConfigImageSourceMapping:
            description: 镜像源别名映射。server 会校验别名在 sourceCluster/targetCluster 的 Cluster.spec.imageSources 中能按正向或反向成对找到。
            type: object
            properties:
                sourceImageSource:
                    description: 源镜像源别名。不能为空，不能重复。
                    type: string
                targetImageSource:
                    description: 目标镜像源别名。不能为空。
                    type: string
            required: [sourceImageSource, targetImageSource]
        DisasterConfigImageRewrite:
            description: 镜像源重写配置，请求和响应字段一致。创建/更新时 server 会归一化 unmatchedPolicy 默认值，并校验 applyTo、mappings。
            type: object
            properties:
                enabled:
                    description: 是否启用镜像源重写。true 时 mappings 至少要有一条。
                    type: boolean
                applyTo:
                    description: 生效场景列表。只允许 resourceSync 和 drill，重复值会返回 400。
                    type: array
                    items:
                        $ref: '#/components/schemas/DisasterConfigImageRewriteApplyTarget'
                unmatchedPolicy:
                    $ref: '#/components/schemas/DisasterConfigImageRewriteUnmatchedPolicy'
                mappings:
                    description: 镜像源别名映射列表。
                    type: array
                    items:
                        $ref: '#/components/schemas/DisasterConfigImageSourceMapping'
        DisasterConfigSpecDTO:
            description: DisasterConfig spec 对外 DTO。resourcesSyncPolicy 与 resourceSyncPolicy 当前都回显 CRD spec.resourceSyncPolicy。
            type: object
            properties:
                sourceCluster:
                    description: 源 Cluster CR 名称。
                    type: string
                targetCluster:
                    description: 目标 Cluster CR 名称。
                    type: string
                storageRepository:
                    description: StorageRepository 名称。operator 从配置的管理命名空间（默认 disaster-system）读取。
                    type: string
                dataSyncType:
                    $ref: '#/components/schemas/DisasterConfigDataSyncType'
                resourcesSyncPolicy:
                    description: 资源同步策略名称的兼容字段，值与 resourceSyncPolicy 相同。
                    type: string
                resourceSyncPolicy:
                    description: ResourceSync 类型 DisasterPolicy 名称；为空表示不绑定资源同步策略。
                    type: string
                dataSyncPolicy:
                    description: DataSync 类型 DisasterPolicy 名称；为空表示不绑定数据同步策略。
                    type: string
                imageRewrite:
                    $ref: '#/components/schemas/DisasterConfigImageRewrite'
                dataSyncCron:
                    description: dataSyncPolicy 指向的策略存在且未 Disabled 时，server 补充 policy.spec.schedule；读取失败或策略禁用时为空。
                    type: string
                resourceSyncCron:
                    description: resourceSyncPolicy 指向的策略存在且未 Disabled 时，server 补充 policy.spec.schedule；读取失败或策略禁用时为空。
                    type: string
            required: [sourceCluster, targetCluster, storageRepository, dataSyncType, resourcesSyncPolicy, resourceSyncPolicy, dataSyncPolicy, dataSyncCron, resourceSyncCron]
        DisasterConfigStatusDTO:
            description: DisasterConfig 状态 DTO，由 operator 写回。
            type: object
            properties:
                status:
                    $ref: '#/components/schemas/DisasterConfigStatusType'
                reason:
                    description: 机器可读原因。常见值包括 SourceClusterNotFound、TargetClusterNotFound、StorageRepositoryNotFound、ClusterNotReady、QueryDependencyFailed、UpdateConfigFailed、ApplyStorageRepositoryFailed。
                    type: string
                message:
                    description: 人类可读状态说明。
                    type: string
        DisasterConfigDTO:
            description: DisasterConfig 对外 DTO，来自 metadata/spec/status，并由 server 尽力补充策略 cron。
            type: object
            properties:
                id:
                    description: Kubernetes UID。
                    type: string
                name:
                    description: DisasterConfig 名称。
                    type: string
                description:
                    description: 业务描述，来自 annotation testudo.softcdata.com/description。
                    type: string
                labels:
                    description: 资源 labels。列表过滤会按 labels 的 value 做包含匹配。
                    type: object
                    additionalProperties:
                        type: string
                creation_timestamp:
                    description: 创建时间，源自 Kubernetes metadata.creationTimestamp；server 在 DTO 转换时按进程本地时区（time.Local/TZ，例如 Asia/Shanghai）格式化为 RFC3339 offset 字符串，例如 2026-05-15T11:08:19+08:00。
                    type: string
                    format: date-time
                spec:
                    $ref: '#/components/schemas/DisasterConfigSpecDTO'
                status:
                    $ref: '#/components/schemas/DisasterConfigStatusDTO'
            required: [id, name, creation_timestamp, spec, status]
        DisasterConfigCollection:
            description: DisasterConfig 列表集合数据。
            type: object
            properties:
                items:
                    type: array
                    items:
                        $ref: '#/components/schemas/DisasterConfigDTO'
            required: [items]
        DisasterConfigCollectionEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/DisasterConfigCollection'
                    meta:
                        $ref: '#/components/schemas/CollectionMeta'
        DisasterConfigEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/DisasterConfigDTO'
        CreateDisasterConfigRequest:
            description: 创建 DisasterConfig 请求体。server 拒绝顶层 syncPolicy，要求改用 dataSyncPolicy 和 resourcesSyncPolicy/resourceSyncPolicy。
            type: object
            properties:
                name:
                    description: 配置名称。binding required，写入 metadata.name。
                    type: string
                description:
                    description: 配置说明，写入 annotation testudo.softcdata.com/description。
                    type: string
                sourceCluster:
                    description: 源 Cluster 名称。binding required；仅 imageRewrite 映射校验会同步读取 Cluster，其他依赖由 operator 异步校验。
                    type: string
                targetCluster:
                    description: 目标 Cluster 名称。binding required；仅 imageRewrite 映射校验会同步读取 Cluster，其他依赖由 operator 异步校验。
                    type: string
                storageRepository:
                    description: StorageRepository 名称。binding required；operator 后续在 disaster-system namespace 读取并应用。
                    type: string
                dataSyncType:
                    $ref: '#/components/schemas/DisasterConfigDataSyncType'
                resourcesSyncPolicy:
                    description: 资源同步策略名称，兼容复数字段。该字段出现时优先于 resourceSyncPolicy，即使传空字符串也不会回退。
                    type: string
                    nullable: true
                resourceSyncPolicy:
                    description: 资源同步策略名称，单数字段。只有 resourcesSyncPolicy 未出现时才会使用。
                    type: string
                    nullable: true
                dataSyncPolicy:
                    description: 数据同步策略名称。null 或空字符串表示不绑定策略。
                    type: string
                    nullable: true
                imageRewrite:
                    $ref: '#/components/schemas/DisasterConfigImageRewrite'
                imageSources:
                    description: 旧版镜像源映射字段，map[sourceAlias]=targetAlias。只有 imageRewrite 未传且该 map 非空时，server 转换为 enabled=true、applyTo=[resourceSync,drill]、unmatchedPolicy=Fail。
                    type: object
                    additionalProperties:
                        type: string
                syncPolicy:
                    description: 不支持的旧字段。只要请求体顶层包含该字段，server 返回 400。
                    deprecated: true
            required: [name, sourceCluster, targetCluster, storageRepository, dataSyncType]
        UpdateDisasterConfigRequest:
            description: 更新 DisasterConfig 请求体。当前 PUT 是部分合并更新，不是完整替换；资源名称只取 path.name。
            type: object
            properties:
                description:
                    description: 配置说明。字段出现时覆盖 annotation，空字符串可清空。
                    type: string
                    nullable: true
                sourceCluster:
                    description: 源 Cluster 名称。非空字符串才覆盖；空字符串不生效。
                    type: string
                targetCluster:
                    description: 目标 Cluster 名称。非空字符串才覆盖；空字符串不生效。
                    type: string
                storageRepository:
                    description: StorageRepository 名称。非空字符串才覆盖；空字符串不生效。
                    type: string
                dataSyncType:
                    description: 数据同步类型。非空字符串才覆盖；空字符串不生效。
                    type: string
                resourcesSyncPolicy:
                    description: 资源同步策略兼容复数字段。字段出现时优先于 resourceSyncPolicy；可传空字符串清空。
                    type: string
                    nullable: true
                resourceSyncPolicy:
                    description: 资源同步策略单数字段。只有 resourcesSyncPolicy 未出现时才使用；可传空字符串清空。
                    type: string
                    nullable: true
                dataSyncPolicy:
                    description: 数据同步策略。字段出现时覆盖；可传空字符串清空。
                    type: string
                    nullable: true
                imageRewrite:
                    allOf:
                        - $ref: '#/components/schemas/DisasterConfigImageRewrite'
                    description: 镜像源重写配置。不传保持原值；传空对象会保存禁用空配置，当前不能直接清除为 nil。
                    nullable: true
                imageSources:
                    description: 旧版镜像源映射字段。只有 imageRewrite 未传且该 map 非空时转换为 imageRewrite。
                    type: object
                    additionalProperties:
                        type: string
                syncPolicy:
                    description: 不支持的旧字段。只要请求体顶层包含该字段，server 返回 400。
                    deprecated: true
        DisasterConfigNameDTO:
            description: 容灾配置轻量名称 DTO。
            type: object
            properties:
                name:
                    description: DisasterConfig 名称。
                    type: string
                id:
                    description: Kubernetes UID。
                    type: string
                sourceCluster:
                    description: 源 Cluster 名称。
                    type: string
                targetCluster:
                    description: 目标 Cluster 名称。
                    type: string
                status:
                    $ref: '#/components/schemas/DisasterConfigStatusType'
            required: [name, id, sourceCluster, targetCluster]
        DisasterConfigNamesEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        type: array
                        items:
                            $ref: '#/components/schemas/DisasterConfigNameDTO'
        DisasterConfigDeleteEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/NameResult'
        DisasterConfigWatchEventDTO:
            description: DisasterConfig Kubernetes watch 事件帧中的 data 对象。
            type: object
            properties:
                type:
                    description: Kubernetes watch 事件类型。
                    type: string
                    enum: [ADDED, MODIFIED, DELETED, ERROR]
                object:
                    $ref: '#/components/schemas/DisasterConfigDTO'
            required: [type]
        DisasterConfigWatchEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        oneOf:
                            - $ref: '#/components/schemas/DisasterConfigWatchEventDTO'
                            - $ref: '#/components/schemas/WatchConnectedData'
                    meta:
                        $ref: '#/components/schemas/WatchStreamMeta'
        DisasterDrillState:
            description: DisasterDrillReconciler 写入的演练状态。
            type: string
            enum: [Pending, Ready, Executing, Completed, CleaningUp, CleanedUp, Failed]
        DisasterDrillRestoreMode:
            description: 演练恢复模式。当前 operator 在演练链路中始终写入 FullRestore。
            type: string
            enum: [Reuse, FullRestore]
        DisasterDrillStatusDTO:
            description: DisasterDrill 对外状态 DTO，由 operator 写入。
            type: object
            properties:
                state:
                    $ref: '#/components/schemas/DisasterDrillState'
                reason:
                    description: 机器可读失败原因，常见值包括 ValidationFailed、TopologyChanged、OperationNotFound、CleanupFailed、OperationFailed、InternalError、DrillFailed。
                    type: string
                restoreMode:
                    $ref: '#/components/schemas/DisasterDrillRestoreMode'
                operationName:
                    description: 当前关联的 DisasterOperation 名称。确认执行后为 drill operation，清理时切换为 drill-cleanup operation。
                    type: string
                currentStep:
                    description: 当前执行步骤。实例演练常见为 RestoreResource、RestoreData、ScaleUp；组演练为 Level-N。
                    type: string
                message:
                    description: operator 写入的人类可读状态消息。
                    type: string
                startTime:
                    type: string
                    format: date-time
                readyTime:
                    type: string
                    format: date-time
                executionTime:
                    type: string
                    format: date-time
                completionTime:
                    type: string
                    format: date-time
        DrillValidationDTO:
            description: 演练前置校验结果。
            type: object
            properties:
                instanceValid:
                    description: 实例状态是否通过校验。未 skipValidation 时实例必须为 Protected 或 Active。
                    type: boolean
                clusterReachable:
                    description: 目标 Cluster 是否存在且 Ready。skipValidation=true 时非 Ready 不拦截。
                    type: boolean
                backupAvailable:
                    description: operator 当前置为 true；真正备份可用性在 Drill Operation 恢复步骤中校验。
                    type: boolean
                lastDataSyncTime:
                    type: string
                    format: date-time
                lastResourceSyncTime:
                    type: string
                    format: date-time
        DrillStepDTO:
            description: 演练执行步骤状态。
            type: object
            properties:
                name:
                    description: 步骤名称。
                    type: string
                state:
                    description: 步骤状态。
                    type: string
                    enum: [Pending, Running, Completed, Failed, Skipped]
                startTime:
                    type: string
                    format: date-time
                completionTime:
                    type: string
                    format: date-time
                message:
                    type: string
        DrillLevelStatusDTO:
            description: 容灾组演练单个层级状态。
            type: object
            properties:
                index:
                    type: integer
                state:
                    type: string
                instances:
                    type: array
                    items:
                        type: string
        DrillGroupProgressDTO:
            description: 容灾组演练进度，由 DisasterOperation.status.groupStatus 同步到 DisasterDrill.status.groupProgress。
            type: object
            properties:
                totalLevels:
                    type: integer
                currentLevelIndex:
                    type: integer
                levelStatuses:
                    type: array
                    items:
                        $ref: '#/components/schemas/DrillLevelStatusDTO'
        DisasterDrillDTO:
            description: DisasterDrill 聚合响应 DTO，来自 CRD metadata/spec/status。
            type: object
            properties:
                id:
                    description: Kubernetes UID。
                    type: string
                name:
                    description: 演练名称。
                    type: string
                namespace:
                    description: 演练 CR 所在 namespace。创建时默认 disaster-system；目标实例或组跨 namespace 回退命中后使用目标对象真实 namespace。
                    type: string
                instanceName:
                    description: 实例演练目标，与 groupName 二选一。
                    type: string
                groupName:
                    description: 容灾组演练目标，与 instanceName 二选一。
                    type: string
                labels:
                    type: object
                    additionalProperties:
                        type: string
                creationTimestamp:
                    type: string
                    format: date-time
                targetCluster:
                    description: operator 解析后的实际目标集群。组演练未指定统一目标集群时可能为 (Auto)。
                    type: string
                namespaceMapping:
                    description: 源 namespace 到演练 namespace 的映射。
                    type: object
                    additionalProperties:
                        type: string
                skipValidation:
                    type: boolean
                confirmed:
                    description: 是否已确认执行。HTTP confirm 只写该字段，状态由 operator 异步推进。
                    type: boolean
                waitUntilReady:
                    description: 是否等待目标 Pod 就绪；operator 会同步写入 DisasterOperation waitUntilReady，并把 skipPodReadyCheck 设为相反值。
                    type: boolean
                cleanup:
                    description: 是否已触发清理。HTTP cleanup 只写该字段，状态由 operator 异步推进。
                    type: boolean
                restorePolicy:
                    $ref: '#/components/schemas/DisasterInstanceRestorePolicy'
                veleroHooks:
                    $ref: '#/components/schemas/DisasterDrillVeleroHooks'
                status:
                    $ref: '#/components/schemas/DisasterDrillStatusDTO'
                validationResults:
                    $ref: '#/components/schemas/DrillValidationDTO'
                steps:
                    type: array
                    items:
                        $ref: '#/components/schemas/DrillStepDTO'
                groupProgress:
                    $ref: '#/components/schemas/DrillGroupProgressDTO'
            required: [id, name, namespace, creationTimestamp, skipValidation, confirmed, waitUntilReady, cleanup, status]
        DisasterDrillCollection:
            type: object
            properties:
                items:
                    type: array
                    items:
                        $ref: '#/components/schemas/DisasterDrillDTO'
            required: [items]
        DisasterDrillCollectionEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/DisasterDrillCollection'
                    meta:
                        $ref: '#/components/schemas/CollectionMeta'
        DisasterDrillEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/DisasterDrillDTO'
        CreateDisasterDrillRequest:
            description: 创建容灾演练请求体。
            type: object
            properties:
                instanceName:
                    description: 实例演练目标。与 groupName 必须且只能传一个。
                    type: string
                groupName:
                    description: 容灾组演练目标。与 instanceName 必须且只能传一个。
                    type: string
                name:
                    description: 自定义演练名称。为空时按 drill-<目标名截断>-<MMDDHHMM> 生成；非空长度不能超过 63。
                    type: string
                targetCluster:
                    description: 演练目标集群。实例演练为空时使用 instance.status.secondaryCluster；组演练为空时每个实例使用自己的 secondaryCluster。
                    type: string
                namespaceMapping:
                    description: 源 namespace 到演练 namespace 的映射。为空时复用目标集群原 namespace，cleanup 会走缩容逻辑；非空时 cleanup 会删除映射后的目标 namespace。
                    type: object
                    additionalProperties:
                        type: string
                skipValidation:
                    description: 是否跳过前置阻断校验。true 时目标 Cluster 非 Ready 不阻断，但仍会记录状态。
                    type: boolean
                namespace:
                    description: 创建演练时优先查询目标对象的 namespace。为空默认 disaster-system；NotFound 后会全 namespace 回退并切换为目标对象真实 namespace。
                    type: string
                waitUntilReady:
                    description: 扩容步骤是否等待目标 Pod ready。
                    type: boolean
                restorePolicy:
                    $ref: '#/components/schemas/DisasterInstanceRestorePolicy'
                veleroHooks:
                    $ref: '#/components/schemas/DisasterDrillVeleroHooks'
        DisasterDrillDeleteResult:
            type: object
            properties:
                name:
                    type: string
                deleted:
                    description: 仅 Kubernetes Delete 成功时为 true；目标不存在或自动解析失败的幂等成功响应不带该字段。
                    type: boolean
            required: [name]
        DisasterDrillDeleteEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/DisasterDrillDeleteResult'
        ProtectedNamespacesDTO:
            description: 演练前查询实例或容灾组保护的 namespace。
            type: object
            properties:
                type:
                    description: 查询目标类型。
                    type: string
                    enum: [Instance, Group]
                instanceName:
                    type: string
                groupName:
                    type: string
                namespaces:
                    description: 聚合后去空、去重、排序的受保护 namespace 列表。
                    type: array
                    items:
                        type: string
                missingInstances:
                    description: 仅组模式返回。组内实例 NotFound 不失败，而是记录到该数组。
                    type: array
                    items:
                        type: string
            required: [type, namespaces]
        ProtectedNamespacesEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/ProtectedNamespacesDTO'
        DisasterDrillWatchEventDTO:
            type: object
            properties:
                type:
                    type: string
                    enum: [ADDED, MODIFIED, DELETED, ERROR]
                object:
                    $ref: '#/components/schemas/DisasterDrillDTO'
            required: [type]
        DisasterDrillWatchEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        oneOf:
                            - $ref: '#/components/schemas/DisasterDrillWatchEventDTO'
                            - $ref: '#/components/schemas/WatchConnectedData'
                    meta:
                        $ref: '#/components/schemas/WatchStreamMeta'
        DisasterPolicyType:
            description: server 对外暴露的策略类型。底层 CRD 仍使用 AutoBackup、DataSync、ResourceSync；server 在 DTO 中把 DataSync/ResourceSync 归一显示为 SyncPolicy。
            type: string
            enum: [AutoBackup, SyncPolicy]
        DisasterPolicyCreateOrUpdateType:
            description: 创建或更新时允许传入的策略类型。SyncPolicy 是推荐值；DataSync、ResourceSync 是历史兼容值。
            type: string
            enum: [AutoBackup, SyncPolicy, DataSync, ResourceSync]
        DisasterPolicyState:
            description: 策略启停状态。Enabled 表示下游 controller 应用调度；Disabled 表示下游调度应暂停或清空。
            type: string
            enum: [Enabled, Disabled]
        DisasterPolicyPhase:
            description: operator 写入的策略生命周期状态。当前成功和校验失败都会保持 Active；Deleting 是历史删除保护阶段，当前删除保护逻辑已临时旁路。
            type: string
            enum: [Active, Deleting]
        DisasterPolicyDuration:
            description: Kubernetes metav1.Duration 字符串，例如 24h、720h、1h30m。仅 AutoBackup 策略的 ttl 使用。
            type: string
            example: 720h
        DisasterPolicySpecDTO:
            description: DisasterPolicy 对外 spec DTO。来自 CRD spec，经 server 做 SyncPolicy 类型归一化。
            type: object
            properties:
                type:
                    $ref: '#/components/schemas/DisasterPolicyType'
                schedule:
                    description: cron 调度表达式。server 创建和更新时只写入 CRD；operator 使用 robfig/cron ParseStandard 做标准五段 cron 校验，非法时写 status.reason=InvalidSchedule。
                    type: string
                    example: '0 2 * * *'
                startTime:
                    description: 策略开始生效时间，写入 CRD spec.startTime。当前 DisasterPolicyReconciler 不使用该字段驱动调度。
                    type: string
                    format: date-time
                    nullable: true
                ttl:
                    allOf:
                        - $ref: '#/components/schemas/DisasterPolicyDuration'
                    description: AutoBackup 备份保留时间。仅 type=AutoBackup 有效；SyncPolicy/DataSync/ResourceSync 不允许携带。
                    nullable: true
                description:
                    description: 策略说明。创建时最大长度 200；更新时空字符串不会清空原值。
                    type: string
                state:
                    $ref: '#/components/schemas/DisasterPolicyState'
            required: [type, schedule, state]
        DisasterPolicyStatusDTO:
            description: DisasterPolicy 对外 status DTO。由 DisasterPolicyReconciler 写入，server 原样转换。
            type: object
            properties:
                phase:
                    $ref: '#/components/schemas/DisasterPolicyPhase'
                lastExecutionTime:
                    description: 最近一次策略执行时间。当前策略 controller 不直接执行备份或同步，该字段通常为空。
                    type: string
                    format: date-time
                    nullable: true
                nextExecutionTime:
                    description: 下一次策略执行时间。当前策略 controller 不计算下一次执行时间，该字段通常为空。
                    type: string
                    format: date-time
                    nullable: true
                reason:
                    description: 机器可读状态原因。常见值为 InvalidSchedule、InvalidTTL；正常时为空。
                    type: string
                message:
                    description: 人类可读状态消息。cron 或 TTL 校验失败时写入具体错误。
                    type: string
        DisasterPolicyDTO:
            description: DisasterPolicy 聚合响应 DTO，来自 CRD metadata/spec/status，并由 server 对同步类策略做外部类型归一化。
            type: object
            properties:
                id:
                    description: Kubernetes UID。
                    type: string
                name:
                    description: 策略名称。
                    type: string
                namespace:
                    description: 策略所在 namespace。当前 server 创建、读取、更新、删除均使用配置的管理命名空间（默认 disaster-system）。
                    type: string
                labels:
                    description: 策略标签。operator 同步 disaster-policy-type/name/state 和 dependency 标签；server 在 DTO 中会把同步类策略的 disaster-policy-type 归一为 SyncPolicy。
                    type: object
                    additionalProperties:
                        type: string
                creation_timestamp:
                    description: 创建时间，server 按进程本地时区（time.Local/TZ）格式化为 RFC3339 offset 字符串。注意该字段按 server DTO 为 snake_case，不是 creationTimestamp。
                    type: string
                    format: date-time
                spec:
                    $ref: '#/components/schemas/DisasterPolicySpecDTO'
                status:
                    $ref: '#/components/schemas/DisasterPolicyStatusDTO'
            required: [id, name, namespace, creation_timestamp, spec, status]
        DisasterPolicyCollection:
            description: DisasterPolicy 列表集合数据，由 transport.BuildCollectionResponse 生成。
            type: object
            properties:
                items:
                    type: array
                    items:
                        $ref: '#/components/schemas/DisasterPolicyDTO'
            required: [items]
        DisasterPolicyCollectionEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/DisasterPolicyCollection'
                    meta:
                        $ref: '#/components/schemas/CollectionMeta'
        DisasterPolicyEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/DisasterPolicyDTO'
        CreateDisasterPolicyRequest:
            description: 创建 DisasterPolicy 请求体。server 创建到配置的管理命名空间（默认 disaster-system），并把请求字段转换为 CRD spec。
            type: object
            properties:
                name:
                    description: 策略名称。必传，长度 1 到 50。
                    type: string
                    minLength: 1
                    maxLength: 50
                type:
                    $ref: '#/components/schemas/DisasterPolicyCreateOrUpdateType'
                schedule:
                    description: cron 调度表达式。必传；server 不同步校验 cron，operator 后续校验并通过 status.reason/status.message 回写 InvalidSchedule。
                    type: string
                startTime:
                    description: 策略开始生效时间。可选，写入 CRD spec.startTime；当前 operator 不使用该字段驱动调度。
                    type: string
                    format: date-time
                    nullable: true
                ttl:
                    allOf:
                        - $ref: '#/components/schemas/DisasterPolicyDuration'
                    description: AutoBackup 备份保留时间。可选；仅 type=AutoBackup 支持，且必须大于 0。
                    nullable: true
                description:
                    description: 策略说明。可选，最大长度 200。
                    type: string
                    maxLength: 200
                state:
                    $ref: '#/components/schemas/DisasterPolicyState'
            required: [name, type, schedule, state]
        UpdateDisasterPolicyRequest:
            description: 更新 DisasterPolicy 请求体。该接口是部分合并更新，不是全量替换。
            type: object
            properties:
                name:
                    description: 策略名称。必传，必须与 path.name 完全一致。
                    type: string
                type:
                    $ref: '#/components/schemas/DisasterPolicyCreateOrUpdateType'
                schedule:
                    description: cron 调度表达式。可选；非空字符串才覆盖原值，空字符串不能清空。
                    type: string
                startTime:
                    description: 策略开始生效时间。可选；非 null 才覆盖原值。
                    type: string
                    format: date-time
                    nullable: true
                ttl:
                    allOf:
                        - $ref: '#/components/schemas/DisasterPolicyDuration'
                    description: AutoBackup 备份保留时间。可选；仅最终 type=AutoBackup 支持，且必须大于 0。
                    nullable: true
                clearTTL:
                    description: 是否清空 ttl。true 时优先于 ttl，server 会把 CRD spec.ttl 置空。
                    type: boolean
                description:
                    description: 策略说明。可选；非空字符串才覆盖原值，空字符串不能清空。
                    type: string
                state:
                    allOf:
                        - $ref: '#/components/schemas/DisasterPolicyState'
                    description: 策略启停状态。可选；传入后覆盖原值。
                    nullable: true
            required: [name]
        DisasterPolicyNameDTO:
            description: 策略下拉选择项 DTO。由 /policies/names 返回，不带分页 meta。
            type: object
            properties:
                name:
                    description: 策略名称。
                    type: string
                id:
                    description: Kubernetes UID。
                    type: string
                type:
                    $ref: '#/components/schemas/DisasterPolicyType'
                schedule:
                    description: 策略调度表达式。
                    type: string
                ttl:
                    allOf:
                        - $ref: '#/components/schemas/DisasterPolicyDuration'
                    description: AutoBackup 策略的备份保留时间；非 AutoBackup 通常为空。
                    nullable: true
            required: [name, id]
        DisasterPolicyNamesEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        type: array
                        items:
                            $ref: '#/components/schemas/DisasterPolicyNameDTO'
        DisasterPolicyDeleteEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - type: object
                  properties:
                    data:
                        $ref: '#/components/schemas/NameResult'
        TaskEventCollection:
            description: 任务事件集合数据。由 `transport.BuildCollectionResponse` 生成。
            properties:
                items:
                    description: 聚合后的任务事件列表。
                    items:
                        $ref: '#/components/schemas/TaskEvent'
                    type: array
            required:
                - items
            type: object
        TaskEventCollectionEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - properties:
                    data:
                        $ref: '#/components/schemas/TaskEventCollection'
                    meta:
                        $ref: '#/components/schemas/CollectionMeta'
                  type: object
            description: 指定资源历史事件查询的统一响应信封。
        TaskEvent:
            description: 由 Kubernetes Event 聚合得到的任务事件 DTO。
            properties:
                cluster:
                    description: 事件载荷中的集群名称，operator 未写入时为空字符串。
                    type: string
                code:
                    description: 事件进度节点编码，来自 operator 载荷 `code`；operator 载荷中的 `errorCode` 当前未映射到该字段。
                    type: string
                duration:
                    description: 任务耗时。结束事件来自 operator 载荷 `duration`；进行中事件可能由 server 按开始时间计算。
                    type: string
                endTime:
                    description: 任务结束时间。只有聚合到 `ExecutionFinished` 事件时返回。
                    format: date-time
                    type: string
                extraMessage:
                    description: 最新事件的人类可读消息，当前与 `message` 保持一致，空值时省略。
                    type: string
                id:
                    description: Kubernetes involvedObject UID。
                    type: string
                message:
                    description: 最新事件的人类可读消息，来自 operator 事件 JSON 载荷 `message`。
                    type: string
                namespace:
                    description: 事件关联对象的 namespace，来自 Kubernetes involvedObject。
                    type: string
                reason:
                    description: 最新 Kubernetes Event reason。
                    enum:
                        - ExecutionStarted
                        - ExecutionProgress
                        - ExecutionFinished
                    type: string
                startTime:
                    description: 任务开始时间。来自 `ExecutionStarted` 事件；只有结束事件时 server 可能按 duration 反推。
                    format: date-time
                    type: string
                status:
                    description: 聚合后的任务状态，来自 operator 事件 JSON 载荷 `status`。
                    enum:
                        - InProgress
                        - Success
                        - Failed
                        - Canceled
                        - Unknown
                    type: string
                taskName:
                    description: 任务名称。优先来自 operator 事件 JSON 载荷 `task`，为空时回退到 involvedObject.name。
                    type: string
                taskType:
                    description: 事件关联对象的 Kubernetes Kind。
                    type: string
                time:
                    description: 最新事件时间，来自 Kubernetes Event `firstTimestamp`。
                    format: date-time
                    type: string
                timeline:
                    description: 同一任务聚合后的事件时间线。
                    items:
                        $ref: '#/components/schemas/TaskEventTimelineNode'
                    type: array
                traceId:
                    description: 调用链追踪 ID，来自 operator 事件 JSON 载荷 `traceId`。
                    type: string
                triggeredBy:
                    description: 触发人，来自 operator 事件 JSON 载荷 `user`。
                    type: string
            type: object
        TaskEventTimelineNode:
            description: 单个 Kubernetes Event 转换后的时间线节点。
            properties:
                code:
                    description: 进度节点编码，来自 operator 载荷 `code`。
                    type: string
                message:
                    description: 节点消息，来自 operator 载荷 `message`。
                    type: string
                reason:
                    description: Kubernetes Event reason。
                    enum:
                        - ExecutionStarted
                        - ExecutionProgress
                        - ExecutionFinished
                    type: string
                status:
                    description: 节点状态，来自 operator 载荷 `status`。
                    enum:
                        - InProgress
                        - Success
                        - Failed
                        - Canceled
                        - Unknown
                    type: string
                time:
                    description: 节点事件时间，来自 Kubernetes Event `firstTimestamp`。
                    format: date-time
                    type: string
            type: object
        TaskEventWatchEnvelope:
            allOf:
                - $ref: '#/components/schemas/Envelope'
                - properties:
                    data:
                        oneOf:
                            - $ref: '#/components/schemas/TaskEventWatchEventDTO'
                            - $ref: '#/components/schemas/WatchConnectedData'
                    meta:
                        $ref: '#/components/schemas/WatchStreamMeta'
                  type: object
            description: 任务事件 WebSocket 推送帧。连接成功、心跳、关闭、超时和事件推送都使用统一 Envelope。
        TaskEventWatchEventDTO:
            description: Kubernetes watch 事件转换后的任务事件推送数据。
            properties:
                object:
                    $ref: '#/components/schemas/TaskEvent'
                type:
                    description: Kubernetes watch 事件类型。
                    enum:
                        - ADDED
                        - MODIFIED
                        - DELETED
                        - ERROR
                    type: string
            required:
                - type
            type: object
        WatchConnectedData:
            description: WebSocket 连接成功后首帧 data。
            properties:
                status:
                    description: 连接状态。
                    enum:
                        - connected
                    type: string
            required:
                - status
            type: object
        WatchStreamMeta:
            description: WebSocket 推送帧元信息。
            properties:
                reason:
                    description: 关闭或超时原因。
                    type: string
                type:
                    description: 帧类型。
                    enum:
                        - connected
                        - heartbeat
                        - closed
                        - timeout
                    type: string
            type: object
    securitySchemes:
        bearerAuth:
            bearerFormat: JWT
            scheme: bearer
            type: http
info:
    description: |-
        disaster-server REST 与 WebSocket 接口契约。HTTP 接口支持 `X-Language` 请求头控制响应消息语言，并兼容 `Accept-Language`；WebSocket 握手支持 `lang` 查询参数。服务端在响应头返回 `Content-Language`，并设置 `Vary: X-Language, Accept-Language`。统一错误信封返回本地化 `message` 与稳定 `message_key`，成功响应 `message` 当前保持 `OK`。

        该文件由 server 路由清单与 RunAPI 清单生成骨架，逐接口 schema 需要按 operator 调用链继续补齐。server 对外响应 DTO 中的 Kubernetes/Velero/operator 时间字段统一按进程本地时区 time.Local/TZ 序列化为 RFC3339 offset 字符串；例如容器设置 TZ=Asia/Shanghai 时返回 2026-05-15T11:08:19+08:00。Kubernetes CR/YAML 原始字段仍由 apiserver 以 UTC Z 保存。
    title: Disaster Server API
    version: 0.1.0
openapi: 3.0.3
paths:
    /api/v1/deletion/check:
        post:
            description: |-
                ## 1. 接口用来干什么

                删除前检查目标资源是否还被其他资源依赖，并返回建议清理计划。该接口只做只读判断，不执行删除，也不阻止后续删除请求；`can_delete` 只由 `upstream` 是否为空决定。

                ## 2. 控制哪些资源

                直接操作资源：
                - disaster-operator CRD：按 `resource_kind/name/namespace` 读取目标对象，再扫描支持的 CRD 列表。
                - Kubernetes metadata：读取目标对象和候选对象的 labels、ownerReferences、UID。
                - AppBackup/AppRestore 场景读取远端 Velero `Schedule`、`Backup`、`Restore` 和 resource modifier `ConfigMap`，用于构造 cleanup plan。

                下层影响资源：
                - 无。该接口不创建、不更新、不删除任何资源，不触发 operator reconcile。

                资源作用范围：
                - 目标资源读取链路：`BindJSON` -> `normalizeKind` -> `getObjectByKind`。
                - 上游依赖链路：目标 `dependency-token` -> `dependency-to-<token>` label selector -> 扫描支持的 CRD -> 合并 ownerReferences。
                - 下游依赖链路：解析目标对象自身 `dependency-to-*` labels -> 按 token 反查目标资源。
                - cleanup plan 链路：按目标 kind 解析 finalizer cleanup 与 cascade cleanup；远端集群读取失败通常进入 `resolved=false/unresolved_reason`，不会使整个接口失败。
                - operator 侧基础能力来自 `disaster-operator/pkg/metadata/dependency_labels.go`、`dependency_query.go` 以及各 controller 的 dependency label sync。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `resource_kind` | string | 是 | `Cluster`、`StorageRepository`、`DisasterPolicy`、`DisasterConfig`、`DisasterInstance`、`DisasterGroup`、`AppBackup`、`AppRestore`、`DisasterDrill`、`DisasterBackup`、`DisasterOperation`、`DataSync`、`ResourceSync`、`DisasterJob` 及别名 | 目标资源类型 | 决定 `getObjectByKind` 读取哪个 CRD，并决定 cleanup plan 规则 | 会忽略大小写、斜杠前缀和非字母数字字符；不支持时返回 `400 code=1000`。 |
                | `name` | string | 是 | 资源名称 | 目标资源名称 | 读取目标对象 | 去除首尾空格后不能为空。 |
                | `namespace` | string | 否 | Kubernetes namespace | 目标资源 namespace | 读取 namespaced CRD 和扫描依赖资源 | 默认 `disaster-system`；Cluster、DisasterConfig 等集群级目标读取目标对象时不使用 namespace，但依赖扫描仍使用该 namespace。 |

                ## 4. 返回的详细说明

                成功响应使用统一 `Envelope`，`data` 为 `DeletionCheckResponse`。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | 成功固定为 `0`。 |
                | `data.target.kind` | string | 归一化 Kind | 目标资源类型 | server `normalizeKind` | 返回 canonical Kind。 |
                | `data.target.name` | string | 资源名称 | 目标资源名称 | Kubernetes metadata | 目标对象实际名称。 |
                | `data.target.namespace` | string | namespace、空字符串 | 目标 namespace | Kubernetes metadata | 集群级资源可能为空。 |
                | `data.target.uid` | string | Kubernetes UID | 目标 UID | Kubernetes metadata | 用于派生 dependency token。 |
                | `data.upstream[]` | array | 依赖引用列表 | 依赖目标的上游资源 | dependency labels、ownerReferences | 非空时 `can_delete=false`。被 cleanup plan 覆盖的子资源会从 upstream 中排除。 |
                | `data.downstream[]` | array | 依赖引用列表 | 目标依赖的下游资源 | 目标对象 `dependency-to-*` labels | token 无法反查时返回 `unresolved=true`。 |
                | `data.cleanup_plan.has_cleanup` | boolean | `true`、`false` | 是否存在清理计划 | server | finalizer 或 cascade 任一非空则为 true。 |
                | `data.cleanup_plan.finalizer_cleanup[]` | array | 清理引用列表 | finalizer 相关清理 | server、远端 Velero | AppBackup/AppRestore 场景可能包含 Velero 和 ConfigMap 资源。 |
                | `data.cleanup_plan.cascade_cleanup[]` | array | 清理引用列表 | 级联清理 | ownerReferences、spec 关系 | DisasterInstance、DisasterGroup、DisasterDrill 等场景可能包含子任务。 |
                | `data.cleanup_plan.*[].strategy` | string | `delete`、`delete_request`、`owner_reference` | 清理策略 | server | 用于前端提示，不代表本接口会执行清理。 |
                | `data.cleanup_plan.*[].resolved` | boolean | `true`、`false` | 是否解析到对象 | server | 远端失败、标签缺失或匹配过多时为 false。 |
                | `data.cleanup_plan.*[].unresolved_reason` | string | 错误或提示文本 | 未解析原因 | server | cleanup plan 的失败通常体现在这里，不使接口失败。 |
                | `data.can_delete` | boolean | `true`、`false` | 是否可删除 | server | 仅由 `len(upstream)==0` 决定。 |
                | `data.message` | string | `OK` | 结果消息 | server | 成功固定为 `OK`。 |
                | `trace_id` | string | trace id | 请求追踪 ID | trace middleware | dev 和非 dev 都会由中间件注入。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 请求体或参数非法 | JSON 绑定失败、`resource_kind` 不支持、`name` 为空。 |
                | 401 | `2001` | 认证失败 | 非 dev 环境下请求未携带有效 JWT，或 JWT 过期、签名无效。响应由 JWT 中间件返回统一信封，包含本地化 `message` 与稳定 `message_key`。 |
                | 404 | `3004` | 目标资源不存在 | `getObjectByKind` 返回 Kubernetes NotFound。 |
                | 500 | `5000` | 目标读取或 token 推导失败 | 目标 Get 返回非 NotFound 错误，或目标对象无法推导 dependency token。 |

                cleanup plan 中远端集群不可访问、远端 Velero List 失败、资源未匹配、匹配过多等情况通常返回在 `resolved=false` 与 `unresolved_reason` 中，不会使整个接口失败。
            operationId: post_api_v1_deletion_check
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/DeletionCheckRequest'
                description: 删除检查请求体。
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DeletionCheckEnvelope'
                    description: 成功返回依赖检查结果和 cleanup plan。
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求体或参数非法。
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 目标资源不存在。
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 目标读取失败或 dependency token 推导失败。
            security:
                - bearerAuth: []
            summary: 删除检查 POST /api/v1/deletion/check
            tags:
                - 删除检查
            x-controlled-resources:
                direct:
                    - disaster-operator CRDs
                    - Kubernetes metadata labels and ownerReferences
                    - Remote Velero resources for cleanup plan
                downstream:
                    - 无
                scope: 指定 namespace 与目标资源相关的依赖关系和清理计划
            x-operator-chain:
                controller: disaster-operator controllers sync dependency labels and cleanup labels; this endpoint only reads them
                crd: Cluster, StorageRepository, DisasterPolicy, DisasterConfig, DisasterInstance, DisasterGroup, AppBackup, AppRestore, DisasterDrill, DisasterBackup, DisasterOperation, DataSync, ResourceSync, DisasterJob
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/metadata/dependency_labels.go
                    - /home/chenxi/YS/disaster-operator/pkg/metadata/dependency_query.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/**/*
                serverFile: internal/apis/deletion_check/v1/router.go
                serverHandler: h.check
            x-runapi-target-id: example-runapi-target-id
    /api/v1/business-default-config:
        get:
            description: |-
                ## 1. 接口用来干什么

                返回业务默认配置单例快照，供前端配置页面一次性展示所有当前变量值、默认值、字段含义、分组、生效方式、可编辑状态和校验边界。
                该接口只读取 server 侧业务默认配置，不直接修改 operator runtime config；前端在创建备份、恢复、容灾操作和实例时读取这些默认值，再按业务表单写入对应请求体。

                ## 2. 控制哪些资源

                直接读取资源：
                - ConfigMap：`disaster-system/disaster-business-default-config`，data key 为 `config`。
                - server 内置字段目录：定义字段 key、中文名称、含义、默认值、类型、范围、分组和生效方式。

                下层影响资源：
                - 无。GET 只读，不创建、不更新、不删除 Kubernetes 资源，也不触发 operator reconcile。

                资源作用范围：
                - ConfigMap 不存在时返回字段目录默认值，`updatedAt` 为空，`updatedBy=system`。
                - ConfigMap 存在时，`values` 中已覆盖字段作为 `value`，未覆盖字段仍使用 `defaultValue`。
                - ConfigMap 中未知字段不会出现在响应中；已知字段如果类型、范围或跨字段关系非法，接口返回 500，避免把绕过接口写入的非法值展示为有效配置。

                ## 3. 入参的详细说明

                该接口不使用 query 参数和请求体。非 dev 环境需要有效 JWT。

                ## 4. 返回的详细说明

                成功响应使用统一 `Envelope`，`data` 为 `BusinessDefaultConfigSnapshot`。

                | 字段 | 类型 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|
                | `data.schemaVersion` | integer | 配置文档版本 | server | 当前固定为 `1`。 |
                | `data.updatedAt` | string | 最近更新时间 | ConfigMap 文档 | PATCH 成功后写入 UTC RFC3339；从未更新时为空。 |
                | `data.updatedBy` | string | 最近更新人 | JWT 上下文 | dev 模式未注入用户时为 `system`。 |
                | `data.groups[].key` | string | 分组 key | server 字段目录 | 例如 `backupRuntime`、`restoreRuntime`。 |
                | `data.groups[].name` | string | 分组名称 | server 字段目录 | 前端可直接展示。 |
                | `data.groups[].description` | string | 分组说明 | server 字段目录 | 前端可直接展示。 |
                | `data.groups[].fields[].key` | string | 字段路径 | server 字段目录 | PATCH 扁平提交时使用同一 key。 |
                | `data.groups[].fields[].description` | string | 字段含义 | server 字段目录 | 前端配置页面直接展示。 |
                | `data.groups[].fields[].value` | string/integer/boolean | 当前值 | ConfigMap 覆盖值或默认值 | duration 使用带单位字符串，int 使用 number。 |
                | `data.groups[].fields[].defaultValue` | string/integer/boolean | 出厂默认值 | server 字段目录 | 未覆盖时 `value` 等于该值。 |
                | `data.groups[].fields[].dataType` | string | 值类型 | server 字段目录 | `duration`、`int`、`bool`、`string`、`enum`。 |
                | `data.groups[].fields[].editable` | boolean | 是否可编辑 | server 字段目录 | false 字段提交 PATCH 会返回 400。 |
                | `data.groups[].fields[].effectMode` | string | 生效方式 | server 字段目录 | `hot`、`restart`、`startup`。 |
                | `data.groups[].fields[].min/max` | string/integer | 校验边界 | server 字段目录 | duration 使用带单位字符串，int 使用 number。 |
                | `data.groups[].fields[].enumValues` | array | 枚举值 | server 字段目录 | 非 enum 字段为空数组或省略。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 401 | `2001` | 认证失败 | 非 dev 环境下未携带有效 JWT。 |
                | 500 | `5000` | 配置读取失败 | Kubernetes ConfigMap 读取失败、ConfigMap JSON 损坏、绕过接口写入已知字段非法值。 |
            operationId: get_api_v1_business_default_config
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                allOf:
                                    - $ref: '#/components/schemas/Envelope'
                                    - type: object
                                      properties:
                                          data:
                                              $ref: '#/components/schemas/BusinessDefaultConfigSnapshot'
                    description: 返回当前业务默认配置快照
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 业务默认配置 GET /api/v1/business-default-config
            tags:
                - 业务默认配置
            x-controlled-resources:
                direct:
                    - ConfigMap disaster-business-default-config
                    - server 业务默认配置字段目录
                downstream:
                    - 无
                scope: disaster-system namespace 单例配置
            x-operator-chain:
                controller: 无直接 controller 调用，字段语义与 disaster-operator OperatorRuntimeConfig 保持一致
                crd: corev1.ConfigMap
                serverFile: internal/apis/business_default_config/v1/router.go
                serverHandler: h.getSnapshot
            x-runapi-target-id: 1cc44c0c5fc01001
        patch:
            description: |-
                ## 1. 接口用来干什么

                部分更新业务默认配置单例。前端提交变化字段，server 合并当前快照、完成强类型校验、写入 ConfigMap，并返回更新后的完整快照。

                ## 2. 控制哪些资源

                直接更新资源：
                - ConfigMap：`disaster-system/disaster-business-default-config`，data key 为 `config`。

                下层影响资源：
                - 不直接写 operator runtime config，不触发 operator reconcile。
                - 更新后的默认值会被前端后续读取，并由前端写入备份、恢复、容灾操作、实例等业务请求体。

                资源作用范围：
                - 未提交字段保持当前值不变。
                - 支持分组结构和扁平 key；两种写法可在同一请求中同时出现。
                - 写入前校验未知字段、只读字段、类型、范围、枚举和跨字段关系。
                - 当前跨字段关系：`instanceRuntime.transitionWatchdogTimeout` 必须大于等于 `instanceRuntime.minTransitionWatchdogTimeout`。

                ## 3. 入参的详细说明

                请求体为 JSON object。

                | 写法 | 示例 | 说明 |
                |---|---|---|
                | 分组结构 | `{"backupRuntime":{"pollInterval":"30s"}}` | 推荐前端表单按分组提交。 |
                | 扁平 key | `{"backupRuntime.pollInterval":"30s"}` | 便于调试工具和脚本直接提交。 |

                字段值规则：
                - duration 字段必须是带单位字符串，例如 `30s`、`5m`、`2h`。
                - int 字段必须是 JSON number，例如 `120`，不能传字符串 `"120"`。
                - 只读字段 `editable=false` 不允许修改。
                - 未知字段不允许写入。

                ## 4. 返回的详细说明

                成功响应使用统一 `Envelope`，`data` 为更新后的 `BusinessDefaultConfigSnapshot`。
                `updatedAt` 会刷新为本次写入时间，`updatedBy` 来自 JWT 中的 `userName`；dev 模式未注入用户时写入 `system`。

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 请求体非法 | 空 body、非 JSON object、空 object、分组值不是 object。 |
                | 400 | `1000` | 未知字段 | 字段路径不在业务默认配置字段目录内。 |
                | 400 | `1000` | 只读字段 | 提交 `editable=false` 字段。 |
                | 400 | `1000` | 类型错误 | duration 未传字符串、int 未传 number、duration 不能解析。 |
                | 400 | `1000` | 越界 | 值小于 `min` 或大于 `max`。 |
                | 400 | `1000` | 跨字段关系错误 | `transitionWatchdogTimeout` 小于 `minTransitionWatchdogTimeout`。 |
                | 401 | `2001` | 认证失败 | 非 dev 环境下未携带有效 JWT。 |
                | 403 | `2003` | 权限不足 | JWT 用户不是 `admin`。dev 模式无用户上下文时保持兼容允许写入。 |
                | 409 | `3009` | 资源冲突 | ConfigMap 更新遇到 Kubernetes resourceVersion 冲突且重试后仍失败。 |
                | 500 | `5000` | 服务端错误 | ConfigMap 读写失败、JSON 编码失败、存储文档损坏。 |

                字段校验失败时，`meta` 为 `BusinessDefaultConfigFieldErrorMeta`，包含 `field`、`reason`、`expected`、`min`、`max`、`actualValue`。
            operationId: patch_api_v1_business_default_config
            parameters: []
            requestBody:
                required: true
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/BusinessDefaultConfigPatchRequest'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                allOf:
                                    - $ref: '#/components/schemas/Envelope'
                                    - type: object
                                      properties:
                                          data:
                                              $ref: '#/components/schemas/BusinessDefaultConfigSnapshot'
                    description: 更新成功并返回最新快照
                "400":
                    content:
                        application/json:
                            schema:
                                allOf:
                                    - $ref: '#/components/schemas/ErrorEnvelope'
                                    - type: object
                                      properties:
                                          meta:
                                              $ref: '#/components/schemas/BusinessDefaultConfigFieldErrorMeta'
                    description: 请求体或字段值非法
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 业务默认配置 PATCH /api/v1/business-default-config
            tags:
                - 业务默认配置
            x-controlled-resources:
                direct:
                    - ConfigMap disaster-business-default-config
                    - server 业务默认配置字段目录
                downstream:
                    - 后续由前端写入业务请求体
                scope: disaster-system namespace 单例配置
            x-operator-chain:
                controller: 无直接 controller 调用，字段语义与 disaster-operator OperatorRuntimeConfig 保持一致
                crd: corev1.ConfigMap
                serverFile: internal/apis/business_default_config/v1/router.go
                serverHandler: h.patchConfig
            x-runapi-target-id: 1cc44c0cb1801001
    /api/v1/business-default-config/fields:
        get:
            description: |-
                ## 1. 接口用来干什么

                按字段维度返回业务默认配置集合，供前端配置页面做搜索、筛选、排序和分页。列表项与快照接口使用同一份字段目录和同一份当前值来源。

                ## 2. 控制哪些资源

                直接读取资源：
                - ConfigMap：`disaster-system/disaster-business-default-config`。
                - server 内置字段目录。

                下层影响资源：
                - 无。该接口只读。

                ## 3. 入参的详细说明

                | 参数 | 类型 | 是否必传 | 可传入值 | 中文含义 | 约束与默认值 |
                |---|---|---|---|---|---|
                | `keyword` | string | 否 | 任意字符串 | 搜索关键字 | 对 `key`、`name`、`description`、`value`、`groupKey`、`groupName` 做大小写不敏感包含匹配。 |
                | `q` | string | 否 | 任意字符串 | 搜索关键字兼容别名 | 当 `keyword` 为空时生效。 |
                | `groupKey` | string | 否 | `backupRuntime`、`restoreRuntime`、`operationRuntime`、`instanceRuntime`、`syncRuntime`、`storageRuntime`、`clusterRuntime` | 分组精确筛选 | 不传时不过滤分组。 |
                | `editable` | boolean | 否 | `true`、`false` | 可编辑性筛选 | 非 boolean 文本返回 400。 |
                | `effectMode` | string | 否 | `hot`、`restart`、`startup` | 生效方式筛选 | 精确匹配。 |
                | `page` | integer | 否 | 大于等于 1 | 页码 | 默认 1。 |
                | `limit` | integer | 否 | `-1` 或正整数 | 每页数量 | 默认 10；`-1` 返回全部。 |
                | `sort` | string | 否 | `key`、`name`、`groupKey`、`groupName`、`dataType`、`effectMode`、`editable`、`value` | 排序字段 | 不传时按 key 升序。 |
                | `order` | string | 否 | `asc`、`desc` | 排序方向 | `sort` 生效时使用；默认按升序处理。 |

                ## 4. 返回的详细说明

                成功响应使用集合信封，`data.items[]` 为 `BusinessDefaultConfigField`，`meta.pagination` 为分页信息，`meta.links.self` 保留本次搜索和筛选参数。

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 查询参数非法 | `editable` 不是 `true` 或 `false`。 |
                | 401 | `2001` | 认证失败 | 非 dev 环境下未携带有效 JWT。 |
                | 500 | `5000` | 配置读取失败 | Kubernetes ConfigMap 读取失败、ConfigMap JSON 损坏、绕过接口写入已知字段非法值。 |
            operationId: get_api_v1_business_default_config_fields
            parameters:
                - name: keyword
                  in: query
                  required: false
                  schema:
                      type: string
                  description: 搜索关键字，对字段 key、名称、说明、当前值、分组 key、分组名称执行包含匹配。
                - name: q
                  in: query
                  required: false
                  schema:
                      type: string
                  description: 搜索关键字兼容别名，仅当 `keyword` 为空时生效。
                - name: groupKey
                  in: query
                  required: false
                  schema:
                      type: string
                  description: 分组精确筛选。
                - name: editable
                  in: query
                  required: false
                  schema:
                      type: boolean
                  description: 按是否可编辑筛选。
                - name: effectMode
                  in: query
                  required: false
                  schema:
                      type: string
                      enum:
                          - hot
                          - restart
                          - startup
                  description: 按生效方式筛选。
                - name: page
                  in: query
                  required: false
                  schema:
                      type: integer
                      default: 1
                  description: 页码，默认 1。
                - name: limit
                  in: query
                  required: false
                  schema:
                      type: integer
                      default: 10
                  description: 每页数量，默认 10，传 -1 返回全部。
                - name: sort
                  in: query
                  required: false
                  schema:
                      type: string
                  description: 排序字段。
                - name: order
                  in: query
                  required: false
                  schema:
                      type: string
                      enum:
                          - asc
                          - desc
                  description: 排序方向。
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                allOf:
                                    - $ref: '#/components/schemas/Envelope'
                                    - type: object
                                      properties:
                                          data:
                                              $ref: '#/components/schemas/BusinessDefaultConfigFieldCollection'
                                          meta:
                                              $ref: '#/components/schemas/CollectionMeta'
                    description: 返回字段集合
                "400":
                    content:
                        application/json:
                            schema:
                                allOf:
                                    - $ref: '#/components/schemas/ErrorEnvelope'
                                    - type: object
                                      properties:
                                          meta:
                                              $ref: '#/components/schemas/BusinessDefaultConfigFieldErrorMeta'
                    description: 查询参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 业务默认配置 GET /api/v1/business-default-config/fields
            tags:
                - 业务默认配置
            x-controlled-resources:
                direct:
                    - ConfigMap disaster-business-default-config
                    - server 业务默认配置字段目录
                downstream:
                    - 无
                scope: disaster-system namespace 单例配置
            x-operator-chain:
                controller: 无直接 controller 调用，字段语义与 disaster-operator OperatorRuntimeConfig 保持一致
                crd: corev1.ConfigMap
                serverFile: internal/apis/business_default_config/v1/router.go
                serverHandler: h.listFields
            x-runapi-target-id: 1cc44c0c96401001
    /api/v1/business-default-config/frontend-fields:
        get:
            description: |-
                ## 1. 接口用来干什么

                返回“前端传参字段目录”，供前端读取需要动态传给业务接口的默认值、请求字段层级和接口用途。
                该接口与 `/business-default-config/fields` 不同：`fields` 返回参数管理页面的运行时参数字段；`frontend-fields` 返回业务请求字段，包括 `key/value`、`requestPathSegments`、`specPathSegments` 和 `apiUsages`。
                前端可以使用 `data.fieldMap` 按字段 key 直接获取字段层级和应调用的业务接口。

                ## 2. 控制哪些资源

                直接读取资源：
                - server 内置前端传参 key/value 目录。

                下层影响资源：
                - 无。该接口只读，不读取或修改 ConfigMap，不创建、不更新、不删除 Kubernetes 资源，不触发 operator reconcile。

                ## 3. 入参的详细说明

                | 参数 | 类型 | 是否必传 | 可传入值 | 中文含义 | 约束与默认值 |
                |---|---|---|---|---|---|
                | `keyword` | string | 否 | 任意字符串 | 搜索关键字 | 对 `key` 与 `value` 做大小写不敏感包含匹配。 |
                | `q` | string | 否 | 任意字符串 | 搜索关键字兼容别名 | 当 `keyword` 为空时生效。 |
                | `page` | integer | 否 | 大于等于 1 | 页码 | 默认 1。 |
                | `limit` | integer | 否 | `-1` 或正整数 | 每页数量 | 默认 10；`-1` 返回全部。 |
                | `sort` | string | 否 | `key`、`value` | 排序字段 | 不传时按 key 升序。 |
                | `order` | string | 否 | `asc`、`desc` | 排序方向 | `sort` 生效时使用；默认按升序处理。 |

                ## 4. 返回的详细说明

                成功响应使用集合信封，`data.items[]` 为 `BusinessDefaultConfigFrontendField`，`data.fieldMap` 为按 key 索引的字段 map，`meta.pagination` 为分页信息，`meta.links.self` 保留本次搜索参数。
                查询全部字段时传 `limit=-1`。

                | 字段 | 类型 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|
                | `key` | string | 目录字段 key | server 字段目录 | 例如 `operation.timeoutMinutes`。 |
                | `value` | string/integer/boolean/object | 默认值 | server 字段目录 | 前端写入业务请求字段的建议默认值。 |
                | `requestPath` | string | 请求体字段路径 | server 字段目录 | 例如 `config.timeoutMinutes`。 |
                | `requestPathSegments` | string[] | 请求体字段层级 | server 字段目录 | 前端可据此组装嵌套 JSON。 |
                | `specPath` | string | CRD spec 字段路径 | server 字段目录 | 例如 `spec.timeoutMinutes`。 |
                | `specPathSegments` | string[] | CRD spec 字段层级 | server 字段目录 | 用于展示最终写入位置。 |
                | `apiUsages[]` | array | 使用该字段的接口列表 | server 字段目录 | 包含 method、path、requestPath、operation 和说明。 |
                | `fieldMap` | object | 字段 key 到字段详情的 map | server 字段目录 | 仅包含当前分页结果中的字段。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 401 | `2001` | 认证失败 | 非 dev 环境下未携带有效 JWT。 |
                | 500 | `5000` | 服务端内部错误 | server 内部异常。 |
            operationId: get_api_v1_business_default_config_frontend_fields
            parameters:
                - name: keyword
                  in: query
                  required: false
                  schema:
                      type: string
                  description: 搜索关键字，对前端传参字段 key 和 value 执行包含匹配。
                - name: q
                  in: query
                  required: false
                  schema:
                      type: string
                  description: 搜索关键字兼容别名，仅当 `keyword` 为空时生效。
                - name: page
                  in: query
                  required: false
                  schema:
                      type: integer
                      default: 1
                  description: 页码，默认 1。
                - name: limit
                  in: query
                  required: false
                  schema:
                      type: integer
                      default: 10
                  description: 每页数量，默认 10，传 -1 返回全部。
                - name: sort
                  in: query
                  required: false
                  schema:
                      type: string
                      enum:
                          - key
                          - value
                  description: 排序字段，支持 `key` 或 `value`。
                - name: order
                  in: query
                  required: false
                  schema:
                      type: string
                      enum:
                          - asc
                          - desc
                  description: 排序方向。
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                allOf:
                                    - $ref: '#/components/schemas/Envelope'
                                    - type: object
                                      properties:
                                          data:
                                              $ref: '#/components/schemas/BusinessDefaultConfigFrontendFieldCollection'
                                          meta:
                                              $ref: '#/components/schemas/CollectionMeta'
                    description: 返回前端传参字段集合
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 业务默认配置 GET /api/v1/business-default-config/frontend-fields
            tags:
                - 业务默认配置
            x-controlled-resources:
                direct:
                    - server 前端传参 key/value 目录
                downstream:
                    - 前端后续写入 AppBackup/AppRestore/DisasterInstance/DisasterOperation/DisasterDrill 请求体
                scope: 只读目录，无 Kubernetes 资源写入
            x-operator-chain:
                controller: 无直接 controller 调用；字段最终由前端通过业务接口写入 CRD spec 后被 operator 消费
                crd: AppBackup, AppRestore, DisasterInstance, DisasterOperation, DisasterDrill
                serverFile: internal/apis/business_default_config/v1/router.go
                serverHandler: h.listFrontendSpecFields
            x-runapi-target-id: 1cc4b3ea76401001
    /api/v1/system-settings:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.listSettings`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - SystemSettings ConfigMap/Secret 及资产

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。
            operationId: get_api_v1_system_settings
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 系统设置 GET /api/v1/system-settings
            tags:
                - 系统设置
            x-controlled-resources:
                direct:
                    - SystemSettings ConfigMap/Secret 及资产
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/system_settings/v1/router.go
                serverHandler: h.listSettings
            x-runapi-target-id: example-runapi-target-id
        post:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.createSetting`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - SystemSettings ConfigMap/Secret 及资产

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。
            operationId: post_api_v1_system_settings
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 请求体字段待逐接口按 server request struct 与 operator 调用链补齐。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 系统设置 POST /api/v1/system-settings
            tags:
                - 系统设置
            x-controlled-resources:
                direct:
                    - SystemSettings ConfigMap/Secret 及资产
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/system_settings/v1/router.go
                serverHandler: h.createSetting
            x-runapi-target-id: example-runapi-target-id
    /api/v1/system-settings/{config_key}:
        delete:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.deleteSetting`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - SystemSettings ConfigMap/Secret 及资产

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。
            operationId: delete_api_v1_system_settings_config_key
            parameters:
                - description: 路径参数 `config_key`，中文含义待逐接口调用链确认。
                  in: path
                  name: config_key
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 系统设置 DELETE /api/v1/system-settings/:config_key
            tags:
                - 系统设置
            x-controlled-resources:
                direct:
                    - SystemSettings ConfigMap/Secret 及资产
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/system_settings/v1/router.go
                serverHandler: h.deleteSetting
            x-runapi-target-id: example-runapi-target-id
        put:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.updateSetting`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - SystemSettings ConfigMap/Secret 及资产

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。
            operationId: put_api_v1_system_settings_config_key
            parameters:
                - description: 路径参数 `config_key`，中文含义待逐接口调用链确认。
                  in: path
                  name: config_key
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 请求体字段待逐接口按 server request struct 与 operator 调用链补齐。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 系统设置 PUT /api/v1/system-settings/:config_key
            tags:
                - 系统设置
            x-controlled-resources:
                direct:
                    - SystemSettings ConfigMap/Secret 及资产
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/system_settings/v1/router.go
                serverHandler: h.updateSetting
            x-runapi-target-id: example-runapi-target-id
    /api/v1/system-settings/assets/{config_key}:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.getAsset`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - SystemSettings ConfigMap/Secret 及资产

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。
            operationId: get_api_v1_system_settings_assets_config_key
            parameters:
                - description: 路径参数 `config_key`，中文含义待逐接口调用链确认。
                  in: path
                  name: config_key
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 系统设置 GET /api/v1/system-settings/assets/:config_key
            tags:
                - 系统设置
            x-controlled-resources:
                direct:
                    - SystemSettings ConfigMap/Secret 及资产
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/system_settings/v1/router.go
                serverHandler: h.getAsset
            x-runapi-target-id: example-runapi-target-id
        post:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.uploadAsset`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - SystemSettings ConfigMap/Secret 及资产

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。
            operationId: post_api_v1_system_settings_assets_config_key
            parameters:
                - description: 路径参数 `config_key`，中文含义待逐接口调用链确认。
                  in: path
                  name: config_key
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 请求体字段待逐接口按 server request struct 与 operator 调用链补齐。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 系统设置 POST /api/v1/system-settings/assets/:config_key
            tags:
                - 系统设置
            x-controlled-resources:
                direct:
                    - SystemSettings ConfigMap/Secret 及资产
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/system_settings/v1/router.go
                serverHandler: h.uploadAsset
            x-runapi-target-id: example-runapi-target-id
    /api/v1/system-settings/public:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.listPublicSettings`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - SystemSettings ConfigMap/Secret 及资产

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。
            operationId: get_api_v1_system_settings_public
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security: []
            summary: 系统设置 GET /api/v1/system-settings/public
            tags:
                - 系统设置
            x-controlled-resources:
                direct:
                    - SystemSettings ConfigMap/Secret 及资产
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/system_settings/v1/router.go
                serverHandler: h.listPublicSettings
            x-runapi-target-id: example-runapi-target-id
    /api/v1/users:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.listUsers`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - disaster-server 用户 Secret

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。
            operationId: get_api_v1_users
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 用户管理 GET /api/v1/users
            tags:
                - 用户管理
            x-controlled-resources:
                direct:
                    - disaster-server 用户 Secret
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/user/v1/router.go
                serverHandler: h.listUsers
            x-runapi-target-id: example-runapi-target-id
        post:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.createUser`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - disaster-server 用户 Secret

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。
            operationId: post_api_v1_users
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 请求体字段待逐接口按 server request struct 与 operator 调用链补齐。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 用户管理 POST /api/v1/users
            tags:
                - 用户管理
            x-controlled-resources:
                direct:
                    - disaster-server 用户 Secret
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/user/v1/router.go
                serverHandler: h.createUser
            x-runapi-target-id: example-runapi-target-id
    /api/v1/users/{username}:
        delete:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.deleteUser`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - disaster-server 用户 Secret

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。
            operationId: delete_api_v1_users_username
            parameters:
                - description: 路径参数 `username`，中文含义待逐接口调用链确认。
                  in: path
                  name: username
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 用户管理 DELETE /api/v1/users/:username
            tags:
                - 用户管理
            x-controlled-resources:
                direct:
                    - disaster-server 用户 Secret
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/user/v1/router.go
                serverHandler: h.deleteUser
            x-runapi-target-id: example-runapi-target-id
    /api/v1/users/{username}/password:
        patch:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.patchUserPassword`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - disaster-server 用户 Secret

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。
            operationId: patch_api_v1_users_username_password
            parameters:
                - description: 路径参数 `username`，中文含义待逐接口调用链确认。
                  in: path
                  name: username
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 请求体字段待逐接口按 server request struct 与 operator 调用链补齐。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 用户管理 PATCH /api/v1/users/:username/password
            tags:
                - 用户管理
            x-controlled-resources:
                direct:
                    - disaster-server 用户 Secret
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/user/v1/router.go
                serverHandler: h.patchUserPassword
            x-runapi-target-id: example-runapi-target-id
    /api/v1/users/{username}/status:
        patch:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.patchUserStatus`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - disaster-server 用户 Secret

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。
            operationId: patch_api_v1_users_username_status
            parameters:
                - description: 路径参数 `username`，中文含义待逐接口调用链确认。
                  in: path
                  name: username
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 请求体字段待逐接口按 server request struct 与 operator 调用链补齐。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 用户管理 PATCH /api/v1/users/:username/status
            tags:
                - 用户管理
            x-controlled-resources:
                direct:
                    - disaster-server 用户 Secret
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/user/v1/router.go
                serverHandler: h.patchUserStatus
            x-runapi-target-id: example-runapi-target-id
    /apis/appbackups.testudo.softcdata.com/v1/appbackups:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.appBackups`。返回 AppBackup 列表，`data.items[].spec.hooks` 回显 CRD template.hooks；备份历史详情中的 `veleroStatus.hookStatus` 用于展示 Hook attempted/failed。

                ## 2. 控制哪些资源

                直接操作资源：
                - AppBackup、Velero Backup/Schedule

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: get_apis_appbackups_testudo_softcdata_com_v1_appbackups
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/AppBackupCollectionEnvelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 应用备份 GET /apis/appbackups.testudo.softcdata.com/v1/appbackups
            tags:
                - 应用备份
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - AppBackup、Velero Backup/Schedule
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/app_backup/v1/router.go
                serverHandler: c.appBackups
            x-runapi-target-id: example-runapi-target-id
        post:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.createAppBackup`。创建 AppBackup 时可传 `hooks`，server 校验后原样写入 `AppBackup.spec.template.hooks`，operator 后续透传到 Velero Backup/Schedule。

                ## 2. 控制哪些资源

                直接操作资源：
                - AppBackup、Velero Backup/Schedule

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: post_apis_appbackups_testudo_softcdata_com_v1_appbackups
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/AppBackupCreateRequest'
                description: 创建 AppBackup 请求体；`hooks` 会写入 `spec.template.hooks` 并透传给 Velero Backup/Schedule。
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/AppBackupEnvelope'
                    description: 创建成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 应用备份 POST /apis/appbackups.testudo.softcdata.com/v1/appbackups
            tags:
                - 应用备份
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - AppBackup、Velero Backup/Schedule
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/app_backup/v1/router.go
                serverHandler: c.createAppBackup
            x-runapi-target-id: example-runapi-target-id
    /apis/appbackups.testudo.softcdata.com/v1/appbackups/{name}:
        delete:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.deleteAppBackup`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - AppBackup、Velero Backup/Schedule

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: delete_apis_appbackups_testudo_softcdata_com_v1_appbackups_name
            parameters:
                - description: 路径参数 `name`，中文含义待逐接口调用链确认。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 应用备份 DELETE /apis/appbackups.testudo.softcdata.com/v1/appbackups/:name
            tags:
                - 应用备份
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - AppBackup、Velero Backup/Schedule
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/app_backup/v1/router.go
                serverHandler: c.deleteAppBackup
            x-runapi-target-id: example-runapi-target-id
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.appBackup`。查询单个 AppBackup 详情，`data.spec.hooks` 回显当前 template.hooks，详情状态和历史可携带 Velero `hookStatus`。

                ## 2. 控制哪些资源

                直接操作资源：
                - AppBackup、Velero Backup/Schedule

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: get_apis_appbackups_testudo_softcdata_com_v1_appbackups_name
            parameters:
                - description: 路径参数 `name`，中文含义待逐接口调用链确认。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/AppBackupEnvelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 应用备份 GET /apis/appbackups.testudo.softcdata.com/v1/appbackups/:name
            tags:
                - 应用备份
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - AppBackup、Velero Backup/Schedule
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/app_backup/v1/router.go
                serverHandler: c.appBackup
            x-runapi-target-id: example-runapi-target-id
        put:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.updateAppBackup`。更新 AppBackup 时 `hooks` 未出现保持原值，传对象整体替换，传 null 或空对象清空 template.hooks。

                ## 2. 控制哪些资源

                直接操作资源：
                - AppBackup、Velero Backup/Schedule

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: put_apis_appbackups_testudo_softcdata_com_v1_appbackups_name
            parameters:
                - description: 路径参数 `name`，中文含义待逐接口调用链确认。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/AppBackupUpdateRequest'
                description: 更新 AppBackup 请求体；`hooks` 未出现保持原值，传对象整体替换，传 null 或空对象清空。
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/AppBackupEnvelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 应用备份 PUT /apis/appbackups.testudo.softcdata.com/v1/appbackups/:name
            tags:
                - 应用备份
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - AppBackup、Velero Backup/Schedule
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/app_backup/v1/router.go
                serverHandler: c.updateAppBackup
            x-runapi-target-id: example-runapi-target-id
    /apis/appbackups.testudo.softcdata.com/v1/appbackups/{name}/actions/{type}:
        post:
            description: |-
                ## 1. 接口用来干什么

                对指定 `AppBackup` 执行动作。`backup/retry/cancel/delete` 写入 `AppBackup.spec.action`，由 appbackup operator 异步处理；`pause/resume` 直接写入 `AppBackup.spec.paused`，并记录 `testudo.softcdata.com/app-backup-manual-paused` annotation，表示用户手动暂停或恢复意图。

                `pause/resume` 的最终效果仍由 operator 收敛到 Velero `Schedule.spec.paused`。当 AppBackup 绑定 AutoBackup 策略时，operator 会把“用户手动暂停意图”和“策略 Disabled 状态”合成为有效暂停状态：手动暂停或策略禁用任一为真，Velero Schedule 都会暂停；策略重新 Enabled 后，仅保留用户手动暂停。

                ## 2. 控制哪些资源

                直接读写资源：
                - `AppBackup` CRD：读写管理集群配置的管理命名空间（默认 `disaster-system`）下的 `{name}`。
                - CRD annotations：写入 trace/user annotation；`pause/resume` 额外写入 `testudo.softcdata.com/app-backup-manual-paused=true/false`。

                下层影响资源：
                - `backup`：operator 确保 BSL 后创建 Velero `Backup`。
                - `retry`：operator 删除目标或最新 Velero `Backup` 后按相同名称重新创建。
                - `cancel`：operator 删除 New/InProgress/空 phase 的 Velero `Backup`，并把历史标记为 `Canceled`。
                - `delete`：operator 创建 Velero `DeleteBackupRequest` 并清理 AppBackup history。
                - `pause/resume`：operator 按 AppBackup 手动暂停意图和 AutoBackup 策略状态更新 Velero `Schedule.spec.paused`。

                资源作用范围：
                - HTTP 层操作配置的管理命名空间（默认 `disaster-system`） 下的 AppBackup。
                - Velero Backup/Schedule 位于 AppBackup `spec.cluster` 指向的业务集群，不一定是 server 当前管理集群。

                ## 3. 入参的详细说明

                | 字段 | 位置 | 类型 | 是否必传 | 可传入值 | 说明 |
                |---|---|---|---|---|---|
                | `name` | path | string | 是 | 已存在 AppBackup 名称 | 指定要操作的应用备份。 |
                | `type` | path | string | 是 | `backup`、`retry`、`cancel`、`delete`、`pause`、`resume` | 动作类型；大小写会被转成小写后匹配。 |
                | `targetBackup` | body | string | 条件必传 | Velero Backup 名称 | `delete` 在 operator 侧必须有目标；`retry/cancel` 可选，空值表示最新备份；`backup/pause/resume` 忽略该字段。 |

                请求体可为空；如果携带 body，必须是合法 JSON。

                ## 4. 返回的详细说明

                `backup/retry/cancel/delete` 成功返回 `data.type`、`data.target_backup`、`data.request_at`，表示动作已写入 CRD，后续执行结果通过详情、历史、事件或 watch 观察。

                `pause/resume` 成功返回 `data.paused`，来源为写入后的 `AppBackup.spec.paused`：`pause` 为 `true`，`resume` 为 `false`。该值代表用户本次手动意图；如果绑定的 AutoBackup 策略处于 Disabled，operator 后续仍会把有效 paused 收敛为 `true`。

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 触发条件 |
                |---|---|---|
                | `400` | `1000` | path `name` 为空、body 非空但 JSON 非法、或 `type` 不在支持列表。 |
                | `401` | `2001` | 未通过 JWT 鉴权。 |
                | `404` | `3004` | `disaster-system` 下不存在指定 AppBackup。 |
                | `409` | `3009` | `RetryOnConflict` 后仍发生 Kubernetes Update 冲突。 |
                | `500` | `5000` | Kubernetes Get/Update 返回未分类错误。 |
            operationId: post_apis_appbackups_testudo_softcdata_com_v1_appbackups_name_actions_type
            parameters:
                - description: AppBackup 名称，在配置的管理命名空间（默认 `disaster-system`） 中查找。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: 动作类型，支持 `backup`、`retry`、`cancel`、`delete`、`pause`、`resume`。
                  in: path
                  name: type
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 可选 JSON body。`targetBackup` 用于 `retry/cancel/delete` 指定目标 Velero Backup；`backup/pause/resume` 可不传 body。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 应用备份 POST /apis/appbackups.testudo.softcdata.com/v1/appbackups/:name/actions/:type
            tags:
                - 应用备份
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - AppBackup
                downstream:
                    - Velero Backup
                    - Velero Schedule
                    - DeleteBackupRequest
                scope: server 更新管理集群配置的管理命名空间（默认 `disaster-system`）下的 AppBackup；Velero 下层资源位于 AppBackup `spec.cluster` 指向的业务集群。
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/app_backup/v1/router.go
                serverHandler: c.executeAction
            x-runapi-target-id: example-runapi-target-id
    /apis/appbackups.testudo.softcdata.com/v1/appbackups/{name}/backups/{backupName}/download:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.downloadBackup`，用于让已登录用户获取 AppBackup 下指定 Velero 备份的平台代理下载地址。客户端不直接访问对象存储地址，而是通过平台服务端生成的下载地址进入下载流程。

                ## 2. 控制哪些资源

                直接操作资源：
                - AppBackup、StorageRepository、Velero Backup/Schedule

                下层影响资源：
                - 对象存储中的 `<cluster>/backups/<backupName>/<backupName>.tar.gz`
                - 对象存储中的 `<cluster>/kopia/<namespace>/`
                - 对象存储中的 `<cluster>/restic/<namespace>/`

                资源作用范围：
                - 管理集群中的 AppBackup 与 StorageRepository
                - AppBackup `spec.cluster`
                - AppBackup `spec.template.storageLocation`
                - AppBackup `status.history`

                ## 3. 入参的详细说明

                路径参数：
                - `name`：AppBackup 名称，必传。
                - `backupName`：Velero 备份名称，必传，必须存在于该 AppBackup 的 `status.history` 中。

                查询参数：
                - `type`：下载类型，可选，默认 `resource`。`resource` 返回资源包代理地址，`data` 返回持久卷数据代理地址，`all` 返回完整归档代理地址。

                ## 4. 返回的详细说明

                返回字段使用统一信封。
                - `data.download_url`：平台服务端生成的同源下载地址，客户端应直接打开该地址完成下载，不得将其视为对象存储地址。
                - `data.expires_at`：下载地址过期时间，当前默认 1 小时。
                - `data.mode`：当前固定为 `proxy`。
                - `data.type`：返回的下载类型，取值为 `resource`、`data`、`all`。
                - `data.file_name`：建议下载文件名。
                - `data.files`：当前不作为该接口的主要返回结构；完整归档由下载流地址处理，不再返回对象存储文件清单。

                ## 5. 可能返回的错误

                - `400`：路径参数缺失、`type` 非法、AppBackup 缺少 `spec.cluster`、`spec.template.storageLocation` 为空。
                - `401`：认证失败。
                - `403`：下载票据失效、签名不合法、票据与当前资源不匹配。
                - `404`：AppBackup 不存在、指定历史备份不存在、StorageRepository 不存在、对象列表为空。
                - `500`：读取 CA Secret、查询对象存储、生成下载票据或流式转发失败。
            operationId: get_apis_appbackups_testudo_softcdata_com_v1_appbackups_name_backups_backupname_download
            parameters:
                - description: 路径参数 `name`，AppBackup 名称，必传。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: 路径参数 `backupName`，Velero 备份名称，必传。
                  in: path
                  name: backupName
                  required: true
                  schema:
                    type: string
                - description: 查询参数 `type`，下载类型，默认 `resource`，可取 `resource`、`data`、`all`。
                  in: query
                  name: type
                  required: false
                  schema:
                    enum:
                        - resource
                        - data
                        - all
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/BackupDownloadEnvelope'
                    description: 返回平台代理下载地址
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 应用备份 GET /apis/appbackups.testudo.softcdata.com/v1/appbackups/:name/backups/:backupName/download
            tags:
                - 应用备份
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - AppBackup、StorageRepository、Velero Backup/Schedule
                downstream:
                    - 对象存储中的备份包、kopia 数据块、restic 数据块
                scope: 管理集群与对象存储之间的下载代理链路
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/app_backup/v1/router.go
                serverHandler: c.downloadBackup
            x-runapi-target-id: example-runapi-target-id
    /apis/appbackups.testudo.softcdata.com/v1/appbackups/{name}/backups/{backupName}/download/stream:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.downloadBackupStream`。它不再返回对象存储地址，而是校验 `downloadToken` 后直接从对象存储读取内容并代理给浏览器。客户端只需要打开 `/download` 返回的同源地址即可完成下载。

                ## 2. 控制哪些资源

                直接操作资源：
                - AppBackup、StorageRepository、对象存储对象流

                下层影响资源：
                - 对象存储中的 `<cluster>/backups/<backupName>/<backupName>.tar.gz`
                - 对象存储中的 `<cluster>/kopia/<namespace>/`
                - 对象存储中的 `<cluster>/restic/<namespace>/`

                资源作用范围：
                - 管理集群中的 AppBackup 与 StorageRepository
                - AppBackup `spec.cluster`
                - AppBackup `spec.template.storageLocation`
                - AppBackup `status.history`
                - `/download` 签发的短期 `downloadToken`

                ## 3. 入参的详细说明

                路径参数：
                - `name`：AppBackup 名称，必传，必须与票据中的名称一致。
                - `backupName`：Velero 备份名称，必传，必须与票据中的备份名一致。

                查询参数：
                - `downloadToken`：短期下载票据，必传，由 `/download` 接口签发。票据携带 AppBackup 名称、备份名、下载类型、AppBackup UID、StorageRepository UID 与过期时间。

                ## 4. 返回的详细说明

                该接口直接返回文件流，不再返回 JSON 下载地址。

                | 状态 | Content-Type | 说明 |
                |---|---|---|
                | `200` | `application/gzip` 或对象存储原始类型 | `type=resource` 时返回单文件代理流，响应头包含 `Content-Disposition`、`ETag`、`Last-Modified`、`Accept-Ranges`。 |
                | `200` | `application/x-tar` | `type=data` 或 `type=all` 时返回 tar 归档流。 |
                | `206` | `application/gzip` 或对象存储原始类型 | `type=resource` 且客户端携带 `Range` 时返回部分内容。 |

                ## 5. 可能返回的错误

                - `400`：路径参数缺失、`downloadToken` 缺失、票据中下载类型非法、AppBackup 缺少 `spec.cluster`、`spec.template.storageLocation` 为空。
                - `403`：下载票据失效、签名不合法、票据与当前请求或资源不匹配。
                - `404`：AppBackup 不存在、指定历史备份不存在、StorageRepository 不存在、对象列表为空。
                - `502`：读取对象存储失败、读取资源包失败、读取归档条目失败。
                - `500`：读取 CA Secret 失败或流式写出过程中发生未分类错误。
            operationId: get_apis_appbackups_testudo_softcdata_com_v1_appbackups_name_backups_backupname_download_stream
            parameters:
                - description: 路径参数 `name`，AppBackup 名称，必传，必须与票据一致。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: 路径参数 `backupName`，Velero 备份名称，必传，必须与票据一致。
                  in: path
                  name: backupName
                  required: true
                  schema:
                    type: string
                - description: 查询参数 `downloadToken`，短期下载票据，必传，由 `/download` 接口签发。
                  in: query
                  name: downloadToken
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/gzip:
                            schema:
                                type: string
                                format: binary
                        application/x-tar:
                            schema:
                                type: string
                                format: binary
                    description: 返回代理下载文件流
                "206":
                    content:
                        application/gzip:
                            schema:
                                type: string
                                format: binary
                    description: 返回资源包部分内容
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 票据无效
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "502":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 上游对象存储错误
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security: []
            summary: 应用备份 GET /apis/appbackups.testudo.softcdata.com/v1/appbackups/:name/backups/:backupName/download/stream
            tags:
                - 应用备份
            x-controlled-resources:
                direct:
                    - AppBackup
                    - StorageRepository
                    - 对象存储对象流
                downstream:
                    - 对象存储中的备份包、kopia 数据块、restic 数据块
                scope: 管理集群与对象存储之间的平台代理下载链路
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/app_backup/v1/router.go
                serverHandler: c.downloadBackupStream
            x-runapi-target-id: example-runapi-target-id
    /apis/appbackups.testudo.softcdata.com/v1/appbackups/{name}/history:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.getBackupHistory`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - AppBackup、Velero Backup/Schedule

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: get_apis_appbackups_testudo_softcdata_com_v1_appbackups_name_history
            parameters:
                - description: 路径参数 `name`，中文含义待逐接口调用链确认。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/BackupHistoryEnvelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 应用备份 GET /apis/appbackups.testudo.softcdata.com/v1/appbackups/:name/history
            tags:
                - 应用备份
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - AppBackup、Velero Backup/Schedule
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/app_backup/v1/router.go
                serverHandler: c.getBackupHistory
            x-runapi-target-id: example-runapi-target-id
    /apis/appbackups.testudo.softcdata.com/v1/appbackups/clusters:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.getAppBackupClusters`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - AppBackup、Velero Backup/Schedule

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: get_apis_appbackups_testudo_softcdata_com_v1_appbackups_clusters
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 应用备份 GET /apis/appbackups.testudo.softcdata.com/v1/appbackups/clusters
            tags:
                - 应用备份
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - AppBackup、Velero Backup/Schedule
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/app_backup/v1/router.go
                serverHandler: c.getAppBackupClusters
            x-runapi-target-id: example-runapi-target-id
    /apis/appbackups.testudo.softcdata.com/v1/velero/backups/{backupName}/includes:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.getVeleroBackupIncludes`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - AppBackup、Velero Backup/Schedule

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: get_apis_appbackups_testudo_softcdata_com_v1_velero_backups_backupname_includes
            parameters:
                - description: 路径参数 `backupName`，中文含义待逐接口调用链确认。
                  in: path
                  name: backupName
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 应用备份 GET /apis/appbackups.testudo.softcdata.com/v1/velero/backups/:backupName/includes
            tags:
                - 应用备份
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - AppBackup、Velero Backup/Schedule
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/app_backup/v1/router.go
                serverHandler: c.getVeleroBackupIncludes
            x-runapi-target-id: example-runapi-target-id
    /apis/appbackups.testudo.softcdata.com/v1/watch/appbackups:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.watchAppBackups`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - AppBackup、Velero Backup/Schedule

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 该接口为 WebSocket 连接，服务端推送消息使用 `Envelope` 包裹 `WatchEventDTO`。
            operationId: get_apis_appbackups_testudo_softcdata_com_v1_watch_appbackups
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "101":
                    description: WebSocket 连接升级成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: '[WebSocket] 应用备份 GET /apis/appbackups.testudo.softcdata.com/v1/watch/appbackups'
            tags:
                - 应用备份
            x-controlled-resources:
                direct:
                    - AppBackup、Velero Backup/Schedule
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-disaster-protocol: websocket
            x-message-schema:
                $ref: '#/components/schemas/WatchEnvelope'
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/app_backup/v1/router.go
                serverHandler: c.watchAppBackups
            x-runapi-target-id: example-runapi-target-id
    /apis/appbackups.testudo.softcdata.com/v1/watch/appbackups/{name}:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.watchAppBackup`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - AppBackup、Velero Backup/Schedule

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 该接口为 WebSocket 连接，服务端推送消息使用 `Envelope` 包裹 `WatchEventDTO`。
            operationId: get_apis_appbackups_testudo_softcdata_com_v1_watch_appbackups_name
            parameters:
                - description: 路径参数 `name`，中文含义待逐接口调用链确认。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "101":
                    description: WebSocket 连接升级成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: '[WebSocket] 应用备份 GET /apis/appbackups.testudo.softcdata.com/v1/watch/appbackups/:name'
            tags:
                - 应用备份
            x-controlled-resources:
                direct:
                    - AppBackup、Velero Backup/Schedule
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-disaster-protocol: websocket
            x-message-schema:
                $ref: '#/components/schemas/WatchEnvelope'
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/app_backup/v1/router.go
                serverHandler: c.watchAppBackup
            x-runapi-target-id: example-runapi-target-id
    /apis/apprestores.testudo.softcdata.com/v1/apprestores:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.appRestores`。返回 AppRestore 列表，`data.items[].spec.hooks` 回显 CRD template.hooks，`data.items[].status.phase` 透传 operator 顶层恢复状态；当 Velero Restore 为 `PartiallyFailed` 时，该字段返回 `PartiallyFailed`，属于非成功终态，不会被 server 映射为 `Succeeded`。`status.restoreStatus.hookStatus` 展示 Velero Restore Hook attempted/failed。

                ## 2. 控制哪些资源

                直接操作资源：
                - AppRestore、Velero Restore

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: get_apis_apprestores_testudo_softcdata_com_v1_apprestores
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/AppRestoreCollectionEnvelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 应用恢复 GET /apis/apprestores.testudo.softcdata.com/v1/apprestores
            tags:
                - 应用恢复
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - AppRestore、Velero Restore
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/app_restore/v1/router.go
                serverHandler: c.appRestores
            x-runapi-target-id: example-runapi-target-id
        post:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.createAppRestore`。创建 AppRestore 时可传 `hooks`，server 校验后原样写入 `AppRestore.spec.template.hooks`，operator 后续透传到 Velero Restore。请求中 `restorePVs=true` 或 `cleanVolumes=true` 时，server 会自动注入 PVC `spec.volumeName` 清理 ResourceModifierRule；该规则必须使用幂等 `add /spec/volumeName` 空值 patch，不得使用字段不存在时会失败的 `remove /spec/volumeName`。

                ## 2. 控制哪些资源

                直接操作资源：
                - AppRestore、Velero Restore

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: post_apis_apprestores_testudo_softcdata_com_v1_apprestores
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/AppRestoreCreateRequest'
                description: 创建 AppRestore 请求体；`hooks` 会写入 `spec.template.hooks` 并透传给 Velero Restore；`restorePVs=true` 或 `cleanVolumes=true` 会注入幂等 PVC volumeName 清理规则。
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/AppRestoreEnvelope'
                    description: 创建成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 应用恢复 POST /apis/apprestores.testudo.softcdata.com/v1/apprestores
            tags:
                - 应用恢复
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - AppRestore、Velero Restore
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/app_restore/v1/router.go
                serverHandler: c.createAppRestore
            x-runapi-target-id: example-runapi-target-id
    /apis/apprestores.testudo.softcdata.com/v1/apprestores/{name}:
        delete:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.deleteAppRestore`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - AppRestore、Velero Restore

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: delete_apis_apprestores_testudo_softcdata_com_v1_apprestores_name
            parameters:
                - description: 路径参数 `name`，中文含义待逐接口调用链确认。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 应用恢复 DELETE /apis/apprestores.testudo.softcdata.com/v1/apprestores/:name
            tags:
                - 应用恢复
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - AppRestore、Velero Restore
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/app_restore/v1/router.go
                serverHandler: c.deleteAppRestore
            x-runapi-target-id: example-runapi-target-id
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.appRestore`。查询单个 AppRestore 详情，`data.spec.hooks` 回显当前 template.hooks，`data.status.phase` 透传 operator 顶层恢复状态；当 Velero Restore 为 `PartiallyFailed` 时，该字段返回 `PartiallyFailed`，属于非成功终态，不会被 server 映射为 `Succeeded`。`status.restoreStatus.hookStatus` 回显 Velero Hook attempted/failed。

                ## 2. 控制哪些资源

                直接操作资源：
                - AppRestore、Velero Restore

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: get_apis_apprestores_testudo_softcdata_com_v1_apprestores_name
            parameters:
                - description: 路径参数 `name`，中文含义待逐接口调用链确认。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/AppRestoreEnvelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 应用恢复 GET /apis/apprestores.testudo.softcdata.com/v1/apprestores/:name
            tags:
                - 应用恢复
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - AppRestore、Velero Restore
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/app_restore/v1/router.go
                serverHandler: c.appRestore
            x-runapi-target-id: example-runapi-target-id
        put:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.updateAppRestore`。更新 AppRestore 时 `hooks` 未出现保持原值，传对象整体替换，传 null 或空对象清空 template.hooks。请求中 `restorePVs=true` 或 `cleanVolumes=true` 时，server 会标准化现有 PVC `spec.volumeName` 清理 ResourceModifierRule：替换 legacy `remove /spec/volumeName`，并保留一个幂等 `add /spec/volumeName` 空值 patch。

                ## 2. 控制哪些资源

                直接操作资源：
                - AppRestore、Velero Restore

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: put_apis_apprestores_testudo_softcdata_com_v1_apprestores_name
            parameters:
                - description: 路径参数 `name`，中文含义待逐接口调用链确认。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/AppRestoreUpdateRequest'
                description: 更新 AppRestore 请求体；`hooks` 未出现保持原值，传对象整体替换，传 null 或空对象清空；`restorePVs=true` 或 `cleanVolumes=true` 会标准化 PVC volumeName 清理规则并替换 legacy remove。
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/AppRestoreEnvelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 应用恢复 PUT /apis/apprestores.testudo.softcdata.com/v1/apprestores/:name
            tags:
                - 应用恢复
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - AppRestore、Velero Restore
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/app_restore/v1/router.go
                serverHandler: c.updateAppRestore
            x-runapi-target-id: example-runapi-target-id
    /apis/apprestores.testudo.softcdata.com/v1/apprestores/{name}/actions/{type}:
        post:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.executeAction`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - AppRestore、Velero Restore

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: post_apis_apprestores_testudo_softcdata_com_v1_apprestores_name_actions_type
            parameters:
                - description: 路径参数 `name`，中文含义待逐接口调用链确认。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: 路径参数 `type`，中文含义待逐接口调用链确认。
                  in: path
                  name: type
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 请求体字段待逐接口按 server request struct 与 operator 调用链补齐。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 应用恢复 POST /apis/apprestores.testudo.softcdata.com/v1/apprestores/:name/actions/:type
            tags:
                - 应用恢复
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - AppRestore、Velero Restore
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/app_restore/v1/router.go
                serverHandler: c.executeAction
            x-runapi-target-id: example-runapi-target-id
    /apis/apprestores.testudo.softcdata.com/v1/apprestores/preflight/validate:
        post:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.validateRestorePreflight`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - AppRestore、Velero Restore

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: post_apis_apprestores_testudo_softcdata_com_v1_apprestores_preflight_validate
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 请求体字段待逐接口按 server request struct 与 operator 调用链补齐。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 应用恢复 POST /apis/apprestores.testudo.softcdata.com/v1/apprestores/preflight/validate
            tags:
                - 应用恢复
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - AppRestore、Velero Restore
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/app_restore/v1/router.go
                serverHandler: c.validateRestorePreflight
            x-runapi-target-id: example-runapi-target-id
    /apis/apprestores.testudo.softcdata.com/v1/watch/apprestores:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.watchAppRestores`。订阅 AppRestore 列表变化；推送对象的 `data.object.status.phase` 透传 operator 顶层恢复状态。当 Velero Restore 为 `PartiallyFailed` 时，该字段返回 `PartiallyFailed`，属于非成功终态，前端不得按成功处理。

                ## 2. 控制哪些资源

                直接操作资源：
                - AppRestore、Velero Restore

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 该接口为 WebSocket 连接，服务端推送消息使用 `Envelope` 包裹 `WatchEventDTO`。
            operationId: get_apis_apprestores_testudo_softcdata_com_v1_watch_apprestores
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "101":
                    description: WebSocket 连接升级成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: '[WebSocket] 应用恢复 GET /apis/apprestores.testudo.softcdata.com/v1/watch/apprestores'
            tags:
                - 应用恢复
            x-controlled-resources:
                direct:
                    - AppRestore、Velero Restore
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-disaster-protocol: websocket
            x-message-schema:
                $ref: '#/components/schemas/WatchEnvelope'
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/app_restore/v1/router.go
                serverHandler: c.watchAppRestores
            x-runapi-target-id: example-runapi-target-id
    /apis/apprestores.testudo.softcdata.com/v1/watch/apprestores/{name}:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.watchAppRestore`。订阅单个 AppRestore 变化；推送对象的 `data.object.status.phase` 透传 operator 顶层恢复状态。当 Velero Restore 为 `PartiallyFailed` 时，该字段返回 `PartiallyFailed`，属于非成功终态，前端不得按成功处理。

                ## 2. 控制哪些资源

                直接操作资源：
                - AppRestore、Velero Restore

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 该接口为 WebSocket 连接，服务端推送消息使用 `Envelope` 包裹 `WatchEventDTO`。
            operationId: get_apis_apprestores_testudo_softcdata_com_v1_watch_apprestores_name
            parameters:
                - description: 路径参数 `name`，中文含义待逐接口调用链确认。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "101":
                    description: WebSocket 连接升级成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: '[WebSocket] 应用恢复 GET /apis/apprestores.testudo.softcdata.com/v1/watch/apprestores/:name'
            tags:
                - 应用恢复
            x-controlled-resources:
                direct:
                    - AppRestore、Velero Restore
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-disaster-protocol: websocket
            x-message-schema:
                $ref: '#/components/schemas/WatchEnvelope'
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/app_restore/v1/router.go
                serverHandler: c.watchAppRestore
            x-runapi-target-id: example-runapi-target-id
    /apis/backuprestorestatistics.testudo.softcdata.com/v1/autobackups/execution-summary:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.GetAutoBackupExecutionSummary`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - BackupRestoreStatistics、AppBackup、AppRestore、DisasterOperation、DisasterInstance、DisasterPolicy

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: get_apis_backuprestorestatistics_testudo_softcdata_com_v1_autobackups_execution_summary
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 统计 GET /apis/backuprestorestatistics.testudo.softcdata.com/v1/autobackups/execution-summary
            tags:
                - 统计
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - BackupRestoreStatistics、AppBackup、AppRestore、DisasterOperation、DisasterInstance、DisasterPolicy
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/statistics/v1/router.go
                serverHandler: h.GetAutoBackupExecutionSummary
            x-runapi-target-id: example-runapi-target-id
    /apis/backuprestorestatistics.testudo.softcdata.com/v1/backups:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.GetBackupStatistics`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - BackupRestoreStatistics、AppBackup、AppRestore、DisasterOperation、DisasterInstance、DisasterPolicy

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: get_apis_backuprestorestatistics_testudo_softcdata_com_v1_backups
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 统计 GET /apis/backuprestorestatistics.testudo.softcdata.com/v1/backups
            tags:
                - 统计
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - BackupRestoreStatistics、AppBackup、AppRestore、DisasterOperation、DisasterInstance、DisasterPolicy
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/statistics/v1/router.go
                serverHandler: h.GetBackupStatistics
            x-runapi-target-id: example-runapi-target-id
    /apis/backuprestorestatistics.testudo.softcdata.com/v1/backups/success-rate:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.GetBackupSuccessRate`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - BackupRestoreStatistics、AppBackup、AppRestore、DisasterOperation、DisasterInstance、DisasterPolicy

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: get_apis_backuprestorestatistics_testudo_softcdata_com_v1_backups_success_rate
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 统计 GET /apis/backuprestorestatistics.testudo.softcdata.com/v1/backups/success-rate
            tags:
                - 统计
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - BackupRestoreStatistics、AppBackup、AppRestore、DisasterOperation、DisasterInstance、DisasterPolicy
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/statistics/v1/router.go
                serverHandler: h.GetBackupSuccessRate
            x-runapi-target-id: example-runapi-target-id
    /apis/backuprestorestatistics.testudo.softcdata.com/v1/instances:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.GetInstanceStatistics`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - BackupRestoreStatistics、AppBackup、AppRestore、DisasterOperation、DisasterInstance、DisasterPolicy

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: get_apis_backuprestorestatistics_testudo_softcdata_com_v1_instances
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 统计 GET /apis/backuprestorestatistics.testudo.softcdata.com/v1/instances
            tags:
                - 统计
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - BackupRestoreStatistics、AppBackup、AppRestore、DisasterOperation、DisasterInstance、DisasterPolicy
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/statistics/v1/router.go
                serverHandler: h.GetInstanceStatistics
            x-runapi-target-id: example-runapi-target-id
    /apis/backuprestorestatistics.testudo.softcdata.com/v1/operations:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.GetOperationStatistics`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - BackupRestoreStatistics、AppBackup、AppRestore、DisasterOperation、DisasterInstance、DisasterPolicy

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: get_apis_backuprestorestatistics_testudo_softcdata_com_v1_operations
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 统计 GET /apis/backuprestorestatistics.testudo.softcdata.com/v1/operations
            tags:
                - 统计
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - BackupRestoreStatistics、AppBackup、AppRestore、DisasterOperation、DisasterInstance、DisasterPolicy
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/statistics/v1/router.go
                serverHandler: h.GetOperationStatistics
            x-runapi-target-id: example-runapi-target-id
    /apis/backuprestorestatistics.testudo.softcdata.com/v1/operations/by-time:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.GetOperationStatisticsByTime`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - BackupRestoreStatistics、AppBackup、AppRestore、DisasterOperation、DisasterInstance、DisasterPolicy

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: get_apis_backuprestorestatistics_testudo_softcdata_com_v1_operations_by_time
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 统计 GET /apis/backuprestorestatistics.testudo.softcdata.com/v1/operations/by-time
            tags:
                - 统计
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - BackupRestoreStatistics、AppBackup、AppRestore、DisasterOperation、DisasterInstance、DisasterPolicy
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/statistics/v1/router.go
                serverHandler: h.GetOperationStatisticsByTime
            x-runapi-target-id: example-runapi-target-id
    /apis/backuprestorestatistics.testudo.softcdata.com/v1/restores:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.GetRestoreStatistics`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - BackupRestoreStatistics、AppBackup、AppRestore、DisasterOperation、DisasterInstance、DisasterPolicy

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: get_apis_backuprestorestatistics_testudo_softcdata_com_v1_restores
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 统计 GET /apis/backuprestorestatistics.testudo.softcdata.com/v1/restores
            tags:
                - 统计
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - BackupRestoreStatistics、AppBackup、AppRestore、DisasterOperation、DisasterInstance、DisasterPolicy
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/statistics/v1/router.go
                serverHandler: h.GetRestoreStatistics
            x-runapi-target-id: example-runapi-target-id
    /apis/backuprestorestatistics.testudo.softcdata.com/v1/storage:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.GetStorageStatistics`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - BackupRestoreStatistics、AppBackup、AppRestore、DisasterOperation、DisasterInstance、DisasterPolicy

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: get_apis_backuprestorestatistics_testudo_softcdata_com_v1_storage
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 统计 GET /apis/backuprestorestatistics.testudo.softcdata.com/v1/storage
            tags:
                - 统计
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - BackupRestoreStatistics、AppBackup、AppRestore、DisasterOperation、DisasterInstance、DisasterPolicy
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/statistics/v1/router.go
                serverHandler: h.GetStorageStatistics
            x-runapi-target-id: example-runapi-target-id
    /apis/backuprestorestatistics.testudo.softcdata.com/v1/tasks/progress:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.GetTaskProgressTrend`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - BackupRestoreStatistics、AppBackup、AppRestore、DisasterOperation、DisasterInstance、DisasterPolicy

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: get_apis_backuprestorestatistics_testudo_softcdata_com_v1_tasks_progress
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 统计 GET /apis/backuprestorestatistics.testudo.softcdata.com/v1/tasks/progress
            tags:
                - 统计
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - BackupRestoreStatistics、AppBackup、AppRestore、DisasterOperation、DisasterInstance、DisasterPolicy
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/statistics/v1/router.go
                serverHandler: h.GetTaskProgressTrend
            x-runapi-target-id: example-runapi-target-id
    /apis/cluster.testudo.softcdata.com/v1/clusters:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.clusters`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - Cluster

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回统一 Envelope。`data` 为分页后的 `DisasterClusterDTO` 列表，`meta` 包含分页与链接信息。每个集群的 `spec.has_token` 与 `spec.has_kube_config` 只表示凭据是否存在，不回显 token 或 kubeconfig 原文。`spec.veleroInstall` 存在时返回 `imageRegistry`、`credentialConfigured`，并在管理平面 dockerconfigjson Secret 中可解析用户名时返回 `username`；接口永不返回 `password` 与 dockerconfigjson 原文。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: get_apis_cluster_testudo_softcdata_com_v1_clusters
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 集群管理 GET /apis/cluster.testudo.softcdata.com/v1/clusters
            tags:
                - 集群管理
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - Cluster
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/disaster_cluster/v1/router.go
                serverHandler: c.clusters
            x-runapi-target-id: example-runapi-target-id
        post:
            description: |-
                ## 1. 接口用来干什么

                创建一个容灾平台管理的目标集群记录，用于把业务 Kubernetes 集群纳入容灾平台。接口会在写入 Cluster CR 前执行平台 License 前置门禁：读取 `disaster-platform-license` Secret、重新计算当前平台指纹并按创建前未删除 Cluster 数量判断额度。无有效企业 License 时最多允许 2 个 Cluster；有效企业 License 且 `limits.maxClusters=-1` 时不限制数量。

                ## 2. 控制哪些资源

                直接操作资源：
                - Cluster
                - 可选创建或更新 `disaster-system/cluster-velero-regcred-<clusterName>` Secret，用于 Velero 私有镜像仓库拉取凭据
                - 只读 License Secret、License install-id Secret、Namespace、API Server CA 与 Cluster 列表，用于可信 License 校验

                下层影响资源：
                - disaster-operator 后续通过 ClusterReconciler 连接目标集群，检查或安装 Velero，读取节点、命名空间、Velero 状态并回写 Cluster status。

                资源作用范围：
                - Cluster 为 cluster-scoped CR；Velero registry Secret 位于管理面 `disaster-system` 命名空间；License Secret 位于配置的 license namespace，默认 `disaster-system`。

                ## 3. 入参的详细说明

                请求体为 JSON。`name` 必传，作为 Cluster CR 名称，长度 1-50 并必须满足 Kubernetes metadata.name 规则。`description` 非必传，写入 `testudo.softcdata.com/description` annotation。`tag` 非必传，写入 `testudo.softcdata.com/cluster-tag` label。`kubeConfig`、`token`、`endpoint` 至少需要满足一种目标集群连接方式：kubeconfig 方式传 `kubeConfig`，token 方式传 `token` 与 `endpoint`。`imageSources` 非必传，用于保存可供后续实例级镜像改写引用的 registry 别名列表。`veleroInstall` 非必传，用于指定 Velero 安装镜像仓库和可选拉取凭据；`username/password` 必须成对出现，携带凭据时必须提供 `imageRegistry`。`username` 会写入管理平面 dockerconfigjson Secret 并可在响应 DTO 中回显，`password` 只用于生成 Secret，不会出现在响应中。

                ## 4. 返回的详细说明

                成功时 HTTP 状态码为 `201`，返回统一 Envelope。`data` 为创建后的 `DisasterClusterDTO`，包含 `id`、`name`、`description`、`labels`、`creation_timestamp`、脱敏后的 `spec` 和当前 `status`。`spec.has_token` 与 `spec.has_kube_config` 只表示凭据是否存在，不回显 token 或 kubeconfig 原文。`spec.veleroInstall` 存在时返回 `imageRegistry`、`credentialConfigured`，并在管理平面 dockerconfigjson Secret 中可解析用户名时返回 `username`；响应不返回 `password` 与 dockerconfigjson 原文。创建响应是 CR 刚写入后的即时结果，operator 尚未 reconcile 时 status 字段可能为空，后续状态需要通过详情、列表或 watch 接口读取。

                ## 5. 可能返回的错误

                `400 code=1000`：JSON 绑定失败、字段校验失败、kubeconfig 解析失败、endpoint 非法、imageSources 非法、Velero registry 配置非法或 Kubernetes API 返回对象校验失败。
                `401 code=2001`：未通过 JWT 认证。
                `403 code=2003`：License 门禁拒绝创建。无有效企业 License 且创建前未删除 Cluster 数量达到 2 时返回，`meta.reason=LicenseLimitExceeded`，`meta.maxClusters` 为当前额度，`meta.currentClusters` 为创建前数量，`meta.licenseReason` 表示 License 自身失败原因。
                `409 code=3009`：同名 Cluster 已存在，或归一化后的目标 endpoint 与已有集群冲突。
                `500 code=5000`：列出现有 Cluster 失败、License 校验依赖读取失败、Velero registry Secret 写入失败或 Cluster CR 创建失败。
                接口成功后，目标集群连接失败、Velero 安装失败、版本不兼容、节点或命名空间统计失败不会改变本次 HTTP 创建结果，由 operator 写入 `Cluster.status.status/reason/message` 并产生事件。
            operationId: post_apis_cluster_testudo_softcdata_com_v1_clusters
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 请求体字段待逐接口按 server request struct 与 operator 调用链补齐。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足，或 LicenseLimitExceeded 导致集群创建被拒绝
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 集群管理 POST /apis/cluster.testudo.softcdata.com/v1/clusters
            tags:
                - 集群管理
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - Cluster
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/disaster_cluster/v1/router.go
                serverHandler: c.createCluster
            x-runapi-target-id: example-runapi-target-id
    /apis/cluster.testudo.softcdata.com/v1/clusters/{name}:
        delete:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.deleteCluster`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - Cluster

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回统一 Envelope。删除请求成功提交后，`data` 至少包含被删除的 Cluster 名称；实际 finalizer 清理状态需要通过后续查询、事件或 Kubernetes CR 删除进度确认。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: delete_apis_cluster_testudo_softcdata_com_v1_clusters_name
            parameters:
                - description: 路径参数 `name`，中文含义待逐接口调用链确认。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 集群管理 DELETE /apis/cluster.testudo.softcdata.com/v1/clusters/:name
            tags:
                - 集群管理
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - Cluster
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/disaster_cluster/v1/router.go
                serverHandler: c.deleteCluster
            x-runapi-target-id: example-runapi-target-id
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.cluster`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - Cluster

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                通过 path 参数 `name` 指定要查询的 Cluster 名称；该接口不使用请求体。

                ## 4. 返回的详细说明

                返回统一 Envelope。`data` 为指定 Cluster 的 `DisasterClusterDTO`，包含 `id`、`name`、`description`、`labels`、`creation_timestamp`、脱敏后的 `spec` 和当前 `status`。`spec.veleroInstall` 存在时返回 `imageRegistry`、`credentialConfigured`，并在管理平面 dockerconfigjson Secret 中可解析用户名时返回 `username`；接口永不返回 `password` 与 dockerconfigjson 原文。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: get_apis_cluster_testudo_softcdata_com_v1_clusters_name
            parameters:
                - description: 路径参数 `name`，中文含义待逐接口调用链确认。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 集群管理 GET /apis/cluster.testudo.softcdata.com/v1/clusters/:name
            tags:
                - 集群管理
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - Cluster
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/disaster_cluster/v1/router.go
                serverHandler: c.cluster
            x-runapi-target-id: example-runapi-target-id
        patch:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.patchCluster`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - Cluster

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求体为 JSON，所有字段均为局部更新。支持 `token`、`tag`、`description`、`imageSources`、`veleroInstall`。未携带字段表示保持原值。`veleroInstall.imageRegistry` 传非空值时修改 Velero 安装镜像仓库前缀，传空字符串时删除 server 管理的 registry Secret 并清空整段 `Cluster.spec.veleroInstall`。`veleroInstall.password` 传非空值时表示轮换管理平面 dockerconfigjson Secret，此时 `veleroInstall.username` 也必须非空，且有效镜像源前缀不能为空；`veleroInstall.password` 未携带或传空字符串时表示不修改既有凭据，可与编辑态回显的 `username` 一起提交，不会创建、更新或删除 Secret。`veleroInstall.removeCredential=true` 表示显式删除凭据，server 删除管理平面 registry Secret 并清空 `Cluster.spec.veleroInstall.registryCredentialSecretRef`，但保留非空 `imageRegistry`。未携带 username/password 不代表删除凭据，空 username/password 也不代表删除凭据。

                ## 4. 返回的详细说明

                返回统一 Envelope。`data` 为更新后的 `DisasterClusterDTO`。`spec.veleroInstall` 存在时返回 `imageRegistry`、`credentialConfigured`，并在管理平面 dockerconfigjson Secret 中可解析用户名时返回 `username`；接口永不返回 `password` 与 dockerconfigjson 原文。若请求体没有任何支持字段，接口返回当前 DTO，不执行 Kubernetes Update。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: patch_apis_cluster_testudo_softcdata_com_v1_clusters_name
            parameters:
                - description: 路径参数 `name`，中文含义待逐接口调用链确认。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 请求体字段待逐接口按 server request struct 与 operator 调用链补齐。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 集群管理 PATCH /apis/cluster.testudo.softcdata.com/v1/clusters/:name
            tags:
                - 集群管理
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - Cluster
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/disaster_cluster/v1/router.go
                serverHandler: c.patchCluster
            x-runapi-target-id: example-runapi-target-id
    /apis/cluster.testudo.softcdata.com/v1/clusters/{name}/actions/refresh-namespaces:
        post:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.refreshNamespaces`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - Cluster

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                成功时返回 HTTP `202` 统一 Envelope。`data.cluster` 为写入刷新 annotation 后的 `DisasterClusterDTO`，其 `spec.veleroInstall` 与详情接口保持一致：存在时返回 `imageRegistry`、`credentialConfigured`，并在管理平面 dockerconfigjson Secret 中可解析用户名时返回 `username`；接口不返回 `password` 与 dockerconfigjson 原文。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: post_apis_cluster_testudo_softcdata_com_v1_clusters_name_actions_refresh_namespaces
            parameters:
                - description: 路径参数 `name`，中文含义待逐接口调用链确认。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 请求体字段待逐接口按 server request struct 与 operator 调用链补齐。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 集群管理 POST /apis/cluster.testudo.softcdata.com/v1/clusters/:name/actions/refresh-namespaces
            tags:
                - 集群管理
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - Cluster
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/disaster_cluster/v1/router.go
                serverHandler: c.refreshNamespaces
            x-runapi-target-id: example-runapi-target-id
    /apis/cluster.testudo.softcdata.com/v1/clusters/{name}/protected-namespaces:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.protectedNamespaces`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - Cluster

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: get_apis_cluster_testudo_softcdata_com_v1_clusters_name_protected_namespaces
            parameters:
                - description: 路径参数 `name`，中文含义待逐接口调用链确认。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 集群管理 GET /apis/cluster.testudo.softcdata.com/v1/clusters/:name/protected-namespaces
            tags:
                - 集群管理
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - Cluster
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/disaster_cluster/v1/router.go
                serverHandler: c.protectedNamespaces
            x-runapi-target-id: example-runapi-target-id
    /apis/cluster.testudo.softcdata.com/v1/clusters/{name}/restore-classes:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.listRestoreClasses`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - Cluster

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: get_apis_cluster_testudo_softcdata_com_v1_clusters_name_restore_classes
            parameters:
                - description: 路径参数 `name`，中文含义待逐接口调用链确认。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 集群管理 GET /apis/cluster.testudo.softcdata.com/v1/clusters/:name/restore-classes
            tags:
                - 集群管理
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - Cluster
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/disaster_cluster/v1/router.go
                serverHandler: c.listRestoreClasses
            x-runapi-target-id: example-runapi-target-id
    /apis/cluster.testudo.softcdata.com/v1/clusters/{name}/validate:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.validateCluster`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - Cluster

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: get_apis_cluster_testudo_softcdata_com_v1_clusters_name_validate
            parameters:
                - description: 路径参数 `name`，中文含义待逐接口调用链确认。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 集群管理 GET /apis/cluster.testudo.softcdata.com/v1/clusters/:name/validate
            tags:
                - 集群管理
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - Cluster
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/disaster_cluster/v1/router.go
                serverHandler: c.validateCluster
            x-runapi-target-id: example-runapi-target-id
    /apis/cluster.testudo.softcdata.com/v1/clusters/kubeconfig/validate:
        post:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.validateKubeConfig`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - Cluster

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: post_apis_cluster_testudo_softcdata_com_v1_clusters_kubeconfig_validate
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 请求体字段待逐接口按 server request struct 与 operator 调用链补齐。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 集群管理 POST /apis/cluster.testudo.softcdata.com/v1/clusters/kubeconfig/validate
            tags:
                - 集群管理
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - Cluster
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/disaster_cluster/v1/router.go
                serverHandler: c.validateKubeConfig
            x-runapi-target-id: example-runapi-target-id
    /apis/cluster.testudo.softcdata.com/v1/clusters/names:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.clusterNames`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - Cluster

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: get_apis_cluster_testudo_softcdata_com_v1_clusters_names
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 集群管理 GET /apis/cluster.testudo.softcdata.com/v1/clusters/names
            tags:
                - 集群管理
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - Cluster
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/disaster_cluster/v1/router.go
                serverHandler: c.clusterNames
            x-runapi-target-id: example-runapi-target-id
    /apis/cluster.testudo.softcdata.com/v1/watch/clusters:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.watchClusters`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - Cluster

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                WebSocket 推送统一 Envelope 包裹的 watch event。事件 data 中的 Cluster DTO 与详情接口保持一致：`spec.veleroInstall` 存在时返回 `imageRegistry`、`credentialConfigured`，并在管理平面 dockerconfigjson Secret 中可解析用户名时返回 `username`；事件不返回 `password` 与 dockerconfigjson 原文。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 该接口为 WebSocket 连接，服务端推送消息使用 `Envelope` 包裹 `WatchEventDTO`。
            operationId: get_apis_cluster_testudo_softcdata_com_v1_watch_clusters
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "101":
                    description: WebSocket 连接升级成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: '[WebSocket] 集群管理 GET /apis/cluster.testudo.softcdata.com/v1/watch/clusters'
            tags:
                - 集群管理
            x-controlled-resources:
                direct:
                    - Cluster
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-disaster-protocol: websocket
            x-message-schema:
                $ref: '#/components/schemas/WatchEnvelope'
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/disaster_cluster/v1/router.go
                serverHandler: c.watchClusters
            x-runapi-target-id: example-runapi-target-id
    /apis/cluster.testudo.softcdata.com/v1/watch/clusters/{name}:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `c.watchCluster`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - Cluster

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                WebSocket 推送统一 Envelope 包裹的 watch event。事件 data 中的 Cluster DTO 与详情接口保持一致：`spec.veleroInstall` 存在时返回 `imageRegistry`、`credentialConfigured`，并在管理平面 dockerconfigjson Secret 中可解析用户名时返回 `username`；事件不返回 `password` 与 dockerconfigjson 原文。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 该接口为 WebSocket 连接，服务端推送消息使用 `Envelope` 包裹 `WatchEventDTO`。
            operationId: get_apis_cluster_testudo_softcdata_com_v1_watch_clusters_name
            parameters:
                - description: 路径参数 `name`，中文含义待逐接口调用链确认。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "101":
                    description: WebSocket 连接升级成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: '[WebSocket] 集群管理 GET /apis/cluster.testudo.softcdata.com/v1/watch/clusters/:name'
            tags:
                - 集群管理
            x-controlled-resources:
                direct:
                    - Cluster
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-disaster-protocol: websocket
            x-message-schema:
                $ref: '#/components/schemas/WatchEnvelope'
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/disaster_cluster/v1/router.go
                serverHandler: c.watchCluster
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterbackups.testudo.softcdata.com/v1/backups:
        get:
            description: |-
                ## 1. 接口用来干什么

                查询 `disaster-system` namespace 下的 `DisasterBackup` 备份配置列表。server 从 informer lister 读取 CRD，按动态 label query 做内存包含过滤，支持按 `name` 和 `creationTimestamp` 排序，再分页返回。

                该接口是只读查询，不创建、不更新、不删除备份配置，也不触发 operator reconcile。返回中的 `status.phase/resources/workload/conditions/updateTime` 是 `DisasterBackupReconciler` 后续写入的观测状态。

                ## 2. 控制哪些资源

                直接操作资源：
                - 只读 `DisasterBackup` CRD informer 缓存。

                下层影响资源：
                - 无。列表接口本身不访问源集群、目标集群、S3 和 Velero。
                - 返回状态间接反映 operator 对 `DisasterConfig`、源 `Cluster`、源集群 discovery/dynamic client 和命名空间资源扫描的处理结果。

                资源作用范围：
                - 读取配置的管理命名空间（默认 `disaster-system`）。
                - `DisasterBackupReconciler` 会同步 dependency labels，包含自身 dependency token 和 `spec.disasterConfig` 指向的 `DisasterConfig`。
                - operator 读取 `DisasterConfig.spec.sourceCluster` 指向的 `Cluster`，用 kubeconfig 或 token/endpoint 连接源集群，扫描 `spec.namespace` 下所有可列出的 namespaced API 资源并写入 `status.resources`。

                ## 3. 入参的详细说明

                | 字段 | 位置 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|---|
                | `limit` | query | integer | 否 | `-1` 或正整数 | 每页数量 | 控制内存分页数量 | 默认 `10`；`-1` 返回全部。 |
                | `page` | query | integer | 否 | 正整数 | 页码 | 控制内存分页起点 | 默认 `1`；小于等于 `0` 时按 `1` 处理。 |
                | `sort` | query | string | 否 | `name`、`creationTimestamp` | 排序字段 | 控制内存排序字段 | 其他值不报错，比较结果恒为 0，等同不排序。 |
                | `order` | query | string | 否 | `desc`、其他字符串 | 排序方向 | 控制升序和降序 | 仅 `desc` 触发倒序，其他值按正序。 |
                | `keyword` | query | string | 否 | 任意字符串 | 关键字兼容参数 | 当前 handler 解析但不参与过滤 | 不影响结果。 |
                | `<labelKey>` | query | string | 否 | 任意 label key | 动态标签过滤 | 对 `item.Labels[labelKey]` 做包含匹配 | 空值和 `*` 表示不过滤；前后 `*` 会被去掉后做包含匹配。 |

                ## 4. 返回的详细说明

                成功响应使用统一 `Envelope`，`data.items` 为 `DisasterBackupDTO[]`，`meta` 为集合分页元信息。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | 成功固定为 `0`。 |
                | `data.items[]` | array | `DisasterBackupDTO[]` | 当前页备份配置列表 | server DTO | 字段含义同详情接口。 |
                | `meta.type` | string | `collection` | 响应类型 | `BuildCollectionResponse` | 固定集合响应。 |
                | `meta.resourceType` | string | `disasterBackup` | 资源类型 | server | 固定值。 |
                | `meta.pagination.limit` | integer | 请求 limit | 每页数量 | query | `-1` 时 `next` 为空。 |
                | `meta.pagination.total` | integer | 总数 | 过滤后的总数量 | server 内存过滤结果 | 分页前数量。 |
                | `meta.links.self/first/previous/next/last` | string | URL | 分页链接 | server | 根据当前 path 和分页参数生成。 |
                | `meta.links.<name>` | string | URL | 单项详情链接 | server | key 为每个 backup name，value 为详情路径。 |
                | `meta.sort.name` | string | 请求 sort | 排序字段回显 | query | 只回显，不代表字段一定生效。 |
                | `meta.filters` | object | query 动态过滤项 | 过滤条件回显 | query | 只包含动态 label 过滤项。 |
                | `trace_id` | string | trace id | 请求追踪 ID | trace middleware | 用于排查。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 列表读取失败 | informer lister `List` 返回错误。 |
                | 401 | `2001` | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。响应由 JWT 中间件返回统一信封，包含本地化 `message` 与稳定 `message_key`。 |

                `keyword` 不生效和不支持的 `sort` 字段不会返回错误。operator 读取 `DisasterConfig`、源集群连接或资源扫描失败也不会使列表接口失败，只会表现为 `data.items[].status.phase=Failed`。
            operationId: get_apis_disasterbackups_testudo_softcdata_com_v1_backups
            parameters:
                - description: 每页数量。默认 10；传 -1 时返回全部。
                  in: query
                  name: limit
                  required: false
                  schema:
                    type: integer
                - description: 页码。默认 1；小于等于 0 时按 1 处理。
                  in: query
                  name: page
                  required: false
                  schema:
                    type: integer
                - description: 排序字段。当前列表接口只识别 name、creationTimestamp；其他值不报错，等同不排序。
                  in: query
                  name: sort
                  required: false
                  schema:
                    type: string
                - description: 排序方向。desc 表示倒序，其他值按正序处理。
                  in: query
                  name: order
                  required: false
                  schema:
                    type: string
                    enum:
                        - asc
                        - desc
                - description: 关键字。当前 handler 会解析该参数，但不参与过滤。
                  in: query
                  name: keyword
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterBackupCollectionEnvelope'
                    description: 成功返回 DisasterBackup 集合。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 列表读取失败。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
            security:
                - bearerAuth: []
            summary: 容灾备份 GET /apis/disasterbackups.testudo.softcdata.com/v1/backups
            tags:
                - 容灾备份
            x-async-failure-status:
                readVia:
                    - GET /apis/disasterbackups.testudo.softcdata.com/v1/backups/{name}
                    - GET /apis/disasterbackups.testudo.softcdata.com/v1/backups
                    - GET /apis/disasterbackups.testudo.softcdata.com/v1/watch/backups
                    - GET /apis/v1/{resource}/{name}/history
                statusFields:
                    - status.phase
                    - status.resources
                    - status.workload
                    - status.conditions
            x-controlled-resources:
                direct:
                    - DisasterBackup CRD informer cache
                downstream:
                    - 无
                scope: disaster-system namespace 下的 DisasterBackup informer 缓存
            x-operator-chain:
                controller: DisasterBackupReconciler syncs dependency labels, reads DisasterConfig and source Cluster, scans namespace resources, and writes status
                crd: DisasterBackup
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterbackup_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterbackup_controller.go
                serverFile: internal/apis/disaster_backup/v1/router.go
                serverHandler: c.backups
            x-runapi-target-id: example-runapi-target-id
        post:
            description: |-
                ## 1. 接口用来干什么

                创建一个 `DisasterBackup` 备份配置。server 校验请求体后，在配置的管理命名空间（默认 `disaster-system`） 创建 CRD，并把当前请求 trace id 注入 annotation 供 operator 事件关联。

                HTTP 201 只表示 CRD 创建成功，不表示源集群资源扫描已完成。创建后由 `DisasterBackupReconciler` 异步读取关联 `DisasterConfig` 与源 `Cluster`，扫描 `spec.namespace` 下的命名空间级资源，并把结果写入 `status.resources`。

                ## 2. 控制哪些资源

                直接操作资源：
                - 创建 `DisasterBackup` CRD。
                - 写入 trace annotation。

                下层影响资源：
                - operator 同步 dependency token 和 `spec.disasterConfig` 依赖标签。
                - operator 读取 `DisasterConfig` 与 `DisasterConfig.spec.sourceCluster` 指向的 `Cluster`。
                - operator 使用源集群 discovery/dynamic client 扫描命名空间资源，成功后写 `status.phase=Ready`，失败写 `status.phase=Failed`。
                - 后续 `DisasterJobReconciler` 会读取该备份，并把 `spec.namespace/labelSelector/newNamespace/newStorageName` 用于 Velero `Backup/Restore` 链路。

                资源作用范围：
                - CRD 写入配置的管理命名空间（默认 `disaster-system`）。
                - 源集群访问范围来自关联 `DisasterConfig.spec.sourceCluster`。
                - 业务资源扫描范围来自 `body.namespace` 指定的源集群 namespace；server 不强制必传，但业务上应传入。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `name` | string | 是 | Kubernetes 资源名称 | 备份配置名称 | 写入 `metadata.name` | binding 要求必传；同名资源已存在返回 409。 |
                | `disasterConfig` | string | 是 | DisasterConfig 名称 | 关联容灾配置 | 写入 `spec.disasterConfig`，operator 据此读取源集群和存储配置 | binding 要求必传；不存在时 operator 写 `status.phase=Failed`。 |
                | `namespace` | string | 否 | 源集群 namespace | 要扫描和后续备份的业务 namespace | 写入 `spec.namespace` | server 未强制；operator 资源扫描和后续 DisasterJob Velero Backup 都依赖该值。 |
                | `labelSelector.matchLabels` | object | 否 | label key/value | 标签选择器 | 写入 `spec.labelSelector`，后续 DisasterJob 创建 Velero Backup 时使用 | 当前 DisasterBackupReconciler 资源扫描不使用该字段。 |
                | `labelSelector.matchExpressions[]` | array | 否 | `In`、`NotIn`、`Exists`、`DoesNotExist` | 标签表达式 | 写入 `spec.labelSelector` | 由 Kubernetes LabelSelector 语义定义。 |
                | `newNamespace` | string | 否 | namespace | 恢复目标新 namespace | 写入 `spec.newNamespace` 供后续链路使用 | 当前 DisasterBackupReconciler 不使用。 |
                | `newStorageName` | string | 否 | 存储名称 | 新存储名称兼容字段 | 写入 `spec.newStorageName` 供后续链路使用 | 当前 DisasterBackupReconciler 不使用。 |

                ## 4. 返回的详细说明

                成功响应 HTTP 状态码为 201，使用统一 `Envelope`，`data` 为创建后的 `DisasterBackupDTO`。由于 operator 异步处理，创建响应中的 `status.phase/resources` 可能为空，随后会被 operator 更新。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | 成功固定为 `0`。 |
                | `data.id` | string | Kubernetes UID | 备份配置 UID | DisasterBackup metadata | 用于前端唯一标识和 dependency token 派生。 |
                | `data.name` | string | 资源名称 | 备份配置名称 | DisasterBackup metadata | 创建时来自 `body.name`。 |
                | `data.namespace` | string | `disaster-system` | CRD 所在 namespace | server 配置的管理命名空间 | 不等同于要备份的业务 namespace。 |
                | `data.creationTimestamp` | string | RFC3339 时间 | 创建时间 | Kubernetes metadata | 当前 DTO 使用 camelCase。 |
                | `data.labels` | object | label map | 资源标签 | DisasterBackup metadata | operator 后续会补 dependency labels。 |
                | `data.annotations` | object | annotation map | 资源注解 | DisasterBackup metadata | 写操作会写入 trace annotation。 |
                | `data.spec.namespace` | string | 源业务 namespace | 要备份的 namespace | DisasterBackup spec | 后续 Velero Backup includedNamespaces 使用该值。 |
                | `data.spec.disasterConfig` | string | DisasterConfig 名称 | 关联容灾配置 | DisasterBackup spec | operator 用它读取源集群。 |
                | `data.spec.labelSelector` | object | Kubernetes LabelSelector | 标签选择器 | DisasterBackup spec | 后续 Velero Backup labelSelector 使用该值。 |
                | `data.status.phase` | string | `Pending`、`Ready`、`Failed` 等 | 扫描阶段 | operator status | 创建响应中可能为空。 |
                | `data.status.resources` | object | namespace 到资源组列表 | 源命名空间资源清单 | operator status | 扫描完成后写入。 |
                | `message` | string | `OK` | 响应消息 | server | 成功固定为 `OK`。 |
                | `trace_id` | string | trace id | 请求追踪 ID | trace middleware | 同步写入 CR annotation。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 请求体非法 | JSON/binding 失败，包含缺少 `name` 或 `disasterConfig`。 |
                | 401 | 无固定业务码 | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。 |
                | 409 | `3009` | 同名备份已存在 | Kubernetes Create 返回 AlreadyExists。 |
                | 500 | `5000` | CRD 创建失败 | 非 AlreadyExists 的 Kubernetes API server、RBAC、资源名称非法等创建错误。 |

                `DisasterConfig` 不存在、源 `Cluster` 不存在、源集群凭据不可用、discovery 或 dynamic list 失败不会在创建接口同步返回，而是由 operator 写入 `status.phase=Failed`。
            operationId: post_apis_disasterbackups_testudo_softcdata_com_v1_backups
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/DisasterBackupCreateRequest'
                description: 创建容灾备份配置请求体。
                required: true
            responses:
                '201':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterBackupEnvelope'
                    description: 成功创建 DisasterBackup。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求体非法。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
                '409':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 同名 DisasterBackup 已存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: DisasterBackup 创建失败。
            security:
                - bearerAuth: []
            summary: 容灾备份 POST /apis/disasterbackups.testudo.softcdata.com/v1/backups
            tags:
                - 容灾备份
            x-async-failure-status:
                readVia:
                    - GET /apis/disasterbackups.testudo.softcdata.com/v1/backups/{name}
                    - GET /apis/disasterbackups.testudo.softcdata.com/v1/backups
                    - GET /apis/disasterbackups.testudo.softcdata.com/v1/watch/backups/{name}
                    - GET /apis/v1/{resource}/{name}/history
                statusFields:
                    - status.phase
                    - status.resources
                    - status.workload
                    - status.conditions
            x-controlled-resources:
                direct:
                    - DisasterBackup CRD
                downstream:
                    - Dependency labels on DisasterBackup
                    - Source Cluster REST config and namespace resources observed by operator
                    - Later DisasterJob Velero Backup and Restore inputs
                scope: disaster-system namespace 下的 DisasterBackup，以及关联 DisasterConfig 指定的源集群业务 namespace
            x-operator-chain:
                controller: DisasterBackupReconciler syncs dependency labels, reads DisasterConfig and source Cluster, scans namespace resources, and writes status
                crd: DisasterBackup
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterbackup_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterbackup_controller.go
                serverFile: internal/apis/disaster_backup/v1/router.go
                serverHandler: c.createBackup
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterbackups.testudo.softcdata.com/v1/backups/{name}:
        delete:
            description: |-
                ## 1. 接口用来干什么

                删除指定 `DisasterBackup` CRD。server 会先尽力读取目标对象并写入 trace annotation，再提交 Kubernetes Delete 请求，成功后返回 `{ "name": "<name>" }`。

                当前 `DisasterBackupReconciler` 没有设置专门 finalizer；删除接口只删除 CRD，不直接清理源集群扫描结果、历史 `DisasterJob`、Velero `Backup/Restore` 或 S3 对象。

                ## 2. 控制哪些资源

                直接操作资源：
                - 尽力更新 `DisasterBackup disaster-system/{name}` 的 trace annotation。
                - 删除 `DisasterBackup disaster-system/{name}`。

                下层影响资源：
                - 无直接下层清理。operator 在对象已删除后只会收到 NotFound 并返回。
                - 已经由后续 `DisasterJob` 创建的 Velero 资源不会被该接口清理。

                资源作用范围：
                - CRD 删除配置的管理命名空间（默认 `disaster-system`）下的对象。
                - 删除前读取或 annotation 更新失败当前会被忽略，不影响后续 Delete。

                ## 3. 入参的详细说明

                | 字段 | 位置 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|---|
                | `name` | path | string | 是 | DisasterBackup 名称 | 要删除的备份配置名称 | 用于 Kubernetes Get/Update/Delete | 为空时正常路由通常不会匹配；handler 未额外检查空值。 |

                ## 4. 返回的详细说明

                成功响应使用统一 `Envelope`，`data` 为删除提交结果。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | 成功固定为 `0`。 |
                | `data.name` | string | DisasterBackup 名称 | 已提交删除的备份配置名称 | path 参数 | 表示 Kubernetes Delete 请求已成功返回。 |
                | `message` | string | `OK` | 响应消息 | server | 成功固定为 `OK`。 |
                | `trace_id` | string | trace id | 请求追踪 ID | trace middleware | 用于排查。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 401 | 无固定业务码 | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。 |
                | 404 | `3004` | 目标备份配置不存在 | Kubernetes Delete 返回 NotFound。 |
                | 500 | `5000` | 删除请求失败 | Kubernetes Delete 返回非 NotFound 错误。 |

                删除前 Get 或 annotation Update 失败当前被忽略。删除成功不代表下层 Velero 备份产物已经清理。
            operationId: delete_apis_disasterbackups_testudo_softcdata_com_v1_backups_name
            parameters:
                - description: 要删除的 DisasterBackup 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterBackupDeleteEnvelope'
                    description: 成功提交 DisasterBackup 删除请求。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 目标 DisasterBackup 不存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: DisasterBackup 删除请求失败。
            security:
                - bearerAuth: []
            summary: 容灾备份 DELETE /apis/disasterbackups.testudo.softcdata.com/v1/backups/:name
            tags:
                - 容灾备份
            x-controlled-resources:
                direct:
                    - DisasterBackup CRD
                downstream:
                    - 无
                scope: disaster-system namespace 下的指定 DisasterBackup
            x-operator-chain:
                controller: DisasterBackupReconciler has no active finalizer cleanup path for DisasterBackup deletion
                crd: DisasterBackup
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterbackup_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterbackup_controller.go
                serverFile: internal/apis/disaster_backup/v1/router.go
                serverHandler: c.deleteBackup
            x-runapi-target-id: example-runapi-target-id
        get:
            description: |-
                ## 1. 接口用来干什么

                查询单个 `DisasterBackup` 详情。server 直接读取 `DisasterBackup disaster-system/{name}`，转换为 `DisasterBackupDTO` 返回。

                该接口不触发 operator reconcile。返回中的 `status.phase/resources/workload/conditions/updateTime` 用于查看 operator 对源集群命名空间资源扫描的结果。

                ## 2. 控制哪些资源

                直接操作资源：
                - 只读 `DisasterBackup` CRD。

                下层影响资源：
                - 无。详情接口本身不访问源集群、目标集群、S3 和 Velero。
                - 返回状态间接来源于 operator 使用源集群 discovery/dynamic client 对 `spec.namespace` 的扫描。

                资源作用范围：
                - 读取配置的管理命名空间（默认 `disaster-system`）。
                - `status.phase` 成功扫描后为 `Ready`，依赖读取、源集群连接或资源扫描失败时为 `Failed`；当前状态结构没有 `reason/message` 字段。

                ## 3. 入参的详细说明

                | 字段 | 位置 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|---|
                | `name` | path | string | 是 | DisasterBackup 名称 | 要查询的备份配置名称 | 用于 Kubernetes Get | 为空时 handler 返回 `400 code=1000`。 |

                ## 4. 返回的详细说明

                成功响应使用统一 `Envelope`，`data` 为 `DisasterBackupDTO`。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | 成功固定为 `0`。 |
                | `data.id` | string | Kubernetes UID | 备份配置 UID | DisasterBackup metadata | 用于前端唯一标识和 dependency token 派生。 |
                | `data.name` | string | 资源名称 | 备份配置名称 | DisasterBackup metadata | 路径中的 name 对应资源名。 |
                | `data.namespace` | string | `disaster-system` | CRD 所在 namespace | server 配置的管理命名空间 | 不等同于要备份的业务 namespace。 |
                | `data.creationTimestamp` | string | RFC3339 时间 | 创建时间 | Kubernetes metadata | 当前 DTO 使用 camelCase。 |
                | `data.labels` | object | label map | 资源标签 | DisasterBackup metadata | operator 同步 dependency labels 后会出现在这里。 |
                | `data.annotations` | object | annotation map | 资源注解 | DisasterBackup metadata | 写接口会写入 trace annotation。 |
                | `data.spec.namespace` | string | 源业务 namespace | 要扫描和备份的 namespace | DisasterBackup spec | 后续 Velero Backup includedNamespaces 使用该值。 |
                | `data.spec.disasterConfig` | string | DisasterConfig 名称 | 关联容灾配置 | DisasterBackup spec | operator 用它读取源集群。 |
                | `data.spec.labelSelector` | object | Kubernetes LabelSelector | 标签选择器 | DisasterBackup spec | 后续 Velero Backup labelSelector 使用该值。 |
                | `data.spec.newNamespace` | string | namespace、空 | 目标新 namespace | DisasterBackup spec | 当前 BackupReconciler 不使用。 |
                | `data.spec.newStorageName` | string | 存储名称、空 | 新存储名称 | DisasterBackup spec | 当前 BackupReconciler 不使用。 |
                | `data.status.phase` | string | `Pending`、`Ready`、`Failed` 等 | 扫描阶段 | operator status | 初始为空或 Pending，成功扫描后 Ready。 |
                | `data.status.resources.<namespace>[]` | array | 资源组列表 | 源命名空间资源清单 | operator status | 每个元素包含 `apiVersion/kind/names`。 |
                | `data.status.workload` | object | 资源组 map、空 | workload 清单兼容字段 | operator status | 当前 BackupReconciler 未写入。 |
                | `data.status.conditions[]` | array | condition 列表 | 条件列表 | operator status | 当前 DTO 透传 CRD 条件。 |
                | `data.status.updateTime` | string | RFC3339 时间、空 | 状态更新时间 | operator status | 当前 controller 写入代码被注释，可能为空。 |
                | `message` | string | `OK` | 响应消息 | server | 成功固定为 `OK`。 |
                | `trace_id` | string | trace id | 请求追踪 ID | trace middleware | 用于排查。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 路径参数非法 | `name` 为空。 |
                | 401 | 无固定业务码 | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。 |
                | 404 | `3004` | 目标备份配置不存在 | Kubernetes Get 返回 NotFound。 |
                | 500 | `5000` | 读取失败 | Kubernetes Get 返回非 NotFound 错误。 |

                operator 读取 `DisasterConfig`、源集群连接或源命名空间扫描失败不作为详情接口同步错误返回，而是体现在 `status.phase=Failed`。
            operationId: get_apis_disasterbackups_testudo_softcdata_com_v1_backups_name
            parameters:
                - description: 要查询的 DisasterBackup 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterBackupEnvelope'
                    description: 成功返回 DisasterBackup 详情。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 路径参数非法。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 目标 DisasterBackup 不存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: DisasterBackup 读取失败。
            security:
                - bearerAuth: []
            summary: 容灾备份 GET /apis/disasterbackups.testudo.softcdata.com/v1/backups/:name
            tags:
                - 容灾备份
            x-async-failure-status:
                readVia:
                    - GET /apis/disasterbackups.testudo.softcdata.com/v1/backups/{name}
                    - GET /apis/disasterbackups.testudo.softcdata.com/v1/backups
                    - GET /apis/disasterbackups.testudo.softcdata.com/v1/watch/backups/{name}
                    - GET /apis/v1/{resource}/{name}/history
                statusFields:
                    - status.phase
                    - status.resources
                    - status.workload
                    - status.conditions
            x-controlled-resources:
                direct:
                    - DisasterBackup CRD
                downstream:
                    - 无
                scope: disaster-system namespace 下的指定 DisasterBackup
            x-operator-chain:
                controller: DisasterBackupReconciler syncs dependency labels, reads DisasterConfig and source Cluster, scans namespace resources, and writes status
                crd: DisasterBackup
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterbackup_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterbackup_controller.go
                serverFile: internal/apis/disaster_backup/v1/router.go
                serverHandler: c.backup
            x-runapi-target-id: example-runapi-target-id
        put:
            description: |-
                ## 1. 接口用来干什么

                更新一个 `DisasterBackup` 备份配置。当前 handler 虽然路由带 `:name`，但实际使用请求体 `name` 字段定位资源；请求体中的非空字段合并到现有 CRD spec，并写入新的 trace annotation。

                更新 CRD 后会触发 `DisasterBackupReconciler` 重新同步依赖标签、读取 `DisasterConfig` 和源 `Cluster`，并重新扫描 `spec.namespace` 下资源。

                ## 2. 控制哪些资源

                直接操作资源：
                - 更新 `DisasterBackup` CRD spec 的部分字段。
                - 写入 trace annotation。

                下层影响资源：
                - operator 重新同步 dependency token 和 `spec.disasterConfig` 依赖标签。
                - operator 重新读取 `DisasterConfig.spec.sourceCluster` 指向的 `Cluster`，用源集群 discovery/dynamic client 扫描命名空间资源。
                - 后续 `DisasterJobReconciler` 会使用更新后的 `spec.namespace/labelSelector/newNamespace/newStorageName` 创建 Velero `Backup/Restore`。

                资源作用范围：
                - CRD 更新配置的管理命名空间（默认 `disaster-system`）。
                - path `name` 当前只参与路由匹配，不参与 Get；调用方必须让 path `name` 与 body `name` 保持一致，避免误更新 body 指向的资源。
                - 字符串字段只在非空时覆盖，因此不能用空字符串清空 `namespace/disasterConfig/newNamespace/newStorageName`。
                - Update 使用 `retry.RetryOnConflict`，多次资源版本冲突后返回 409。

                ## 3. 入参的详细说明

                | 字段 | 位置 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|---|
                | `name` | path | string | 是 | DisasterBackup 名称 | 路由匹配 | 当前 handler 不使用该值定位资源 | 应与 body `name` 一致。 |
                | `name` | body | string | 是 | DisasterBackup 名称 | 实际要更新的资源名称 | 用于 Kubernetes Get/Update | binding 要求必传。 |
                | `labelSelector.matchLabels` | body | object | 否 | label key/value | 标签选择器 | 非 null 时整体覆盖 `spec.labelSelector` | 可传空对象来设置空 selector。 |
                | `labelSelector.matchExpressions[]` | body | array | 否 | `In`、`NotIn`、`Exists`、`DoesNotExist` | 标签表达式 | 非 null 时整体覆盖 `spec.labelSelector` | 由 Kubernetes LabelSelector 语义定义。 |
                | `namespace` | body | string | 否 | 源集群 namespace | 要扫描和后续备份的业务 namespace | 非空时覆盖 `spec.namespace` | 空字符串表示不更新，不能清空。 |
                | `disasterConfig` | body | string | 否 | DisasterConfig 名称 | 关联容灾配置 | 非空时覆盖 `spec.disasterConfig` | 空字符串表示不更新，不能清空。 |
                | `newNamespace` | body | string | 否 | namespace | 恢复目标新 namespace | 非空时覆盖 `spec.newNamespace` | 空字符串表示不更新，不能清空。 |
                | `newStorageName` | body | string | 否 | 存储名称 | 新存储名称兼容字段 | 非空时覆盖 `spec.newStorageName` | 空字符串表示不更新，不能清空。 |

                ## 4. 返回的详细说明

                成功响应使用统一 `Envelope`，`data` 为更新后的 `DisasterBackupDTO`。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | 成功固定为 `0`。 |
                | `data.id` | string | Kubernetes UID | 备份配置 UID | DisasterBackup metadata | 用于前端唯一标识和 dependency token 派生。 |
                | `data.name` | string | 资源名称 | 备份配置名称 | DisasterBackup metadata | 来自 body `name` 定位的资源。 |
                | `data.namespace` | string | `disaster-system` | CRD 所在 namespace | server 配置的管理命名空间 | 不等同于要备份的业务 namespace。 |
                | `data.creationTimestamp` | string | RFC3339 时间 | 创建时间 | Kubernetes metadata | 当前 DTO 使用 camelCase。 |
                | `data.labels` | object | label map | 资源标签 | DisasterBackup metadata | operator 后续会同步 dependency labels。 |
                | `data.annotations` | object | annotation map | 资源注解 | DisasterBackup metadata | 本次写操作会更新 trace annotation。 |
                | `data.spec.*` | object | 更新后的 spec | 备份配置 | DisasterBackup spec | 字段含义同详情接口。 |
                | `data.status.*` | object | 当前 operator 状态 | 观测状态 | DisasterBackup status | Update 返回时可能仍是旧状态，随后由 operator 异步刷新。 |
                | `message` | string | `OK` | 响应消息 | server | 成功固定为 `OK`。 |
                | `trace_id` | string | trace id | 请求追踪 ID | trace middleware | 同步写入 CR annotation。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 请求体非法 | JSON/binding 失败，包含缺少 body `name`。 |
                | 401 | 无固定业务码 | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。 |
                | 404 | `3004` | 目标备份配置不存在 | 按 body `name` Get 现有 DisasterBackup 返回 NotFound。 |
                | 409 | `3009` | 资源版本冲突 | `RetryOnConflict` 重试后仍收到 Kubernetes Conflict。 |
                | 500 | `5000` | 更新失败 | Get 或 Update 返回非 NotFound、非 Conflict 错误。 |

                `DisasterConfig` 不存在、源集群不可访问、资源扫描失败不会在更新接口同步返回，而是由 operator 后续写入 `status.phase=Failed`。
            operationId: put_apis_disasterbackups_testudo_softcdata_com_v1_backups_name
            parameters:
                - description: 路由中的 DisasterBackup 名称；当前 handler 实际用 body.name 定位资源，调用方应保持一致。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/DisasterBackupUpdateRequest'
                description: 更新容灾备份配置请求体。
                required: true
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterBackupEnvelope'
                    description: 成功返回更新后的 DisasterBackup。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求体非法。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 目标 DisasterBackup 不存在。
                '409':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源版本冲突。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: DisasterBackup 更新失败。
            security:
                - bearerAuth: []
            summary: 容灾备份 PUT /apis/disasterbackups.testudo.softcdata.com/v1/backups/:name
            tags:
                - 容灾备份
            x-async-failure-status:
                readVia:
                    - GET /apis/disasterbackups.testudo.softcdata.com/v1/backups/{name}
                    - GET /apis/disasterbackups.testudo.softcdata.com/v1/backups
                    - GET /apis/disasterbackups.testudo.softcdata.com/v1/watch/backups/{name}
                    - GET /apis/v1/{resource}/{name}/history
                statusFields:
                    - status.phase
                    - status.resources
                    - status.workload
                    - status.conditions
            x-controlled-resources:
                direct:
                    - DisasterBackup CRD
                downstream:
                    - Dependency labels on DisasterBackup
                    - Source Cluster REST config and namespace resources observed by operator
                    - Later DisasterJob Velero Backup and Restore inputs
                scope: disaster-system namespace 下的 DisasterBackup，以及关联 DisasterConfig 指定的源集群业务 namespace
            x-operator-chain:
                controller: DisasterBackupReconciler syncs dependency labels, reads DisasterConfig and source Cluster, scans namespace resources, and writes status
                crd: DisasterBackup
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterbackup_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterbackup_controller.go
                serverFile: internal/apis/disaster_backup/v1/router.go
                serverHandler: c.updateBackup
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterbackups.testudo.softcdata.com/v1/watch/backups:
        get:
            description: |-
                ## 1. 接口用来干什么

                建立 WebSocket 连接，实时推送 `disaster-system` namespace 下全部 `DisasterBackup` 的 Kubernetes watch 事件。operator 更新备份配置 status 或 dependency labels 时，会通过该 watch 表现为 `MODIFIED` 事件。

                handler 不先执行 List 快照，只在 watch 建立后的事件到达时推送。

                ## 2. 控制哪些资源

                直接操作资源：
                - 只监听 `DisasterBackup` CRD watch 流。

                下层影响资源：
                - 无。该接口不创建、不更新、不删除 CRD，不访问源集群、目标集群、S3 和 Velero。
                - operator 后续写入 `status.phase/resources/workload/conditions/updateTime` 时会推送为 watch 事件。

                资源作用范围：
                - 监听配置的管理命名空间（默认 `disaster-system`） 的全部 DisasterBackup。
                - WebSocket 流由 `StreamWatch` 发送连接成功帧、事件帧、30 秒 heartbeat、关闭帧和 30 分钟 timeout 帧。

                ## 3. 入参的详细说明

                | 字段 | 位置 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|---|
                | `Authorization` | header | string | 非 dev 必传 | `Bearer <JWT>` | JWT 令牌 | WebSocket 握手前认证 | 三种传入方式任选一种。 |
                | `token` | query | string | 非 dev 可选 | JWT 原文 | JWT 令牌 | `WebSocketTokenAdapter` 会转换为 Authorization header | 适合浏览器 WebSocket 无法设置 header 的场景。 |
                | `Sec-WebSocket-Protocol` | header | string | 非 dev 可选 | JWT 原文或子协议 | JWT 令牌 | adapter 会尝试提取 token | 服务端仍会完成 WebSocket 升级。 |

                ## 4. 返回的详细说明

                该接口升级为 WebSocket 后，所有消息均为统一 `Envelope` JSON 字符串。

                | 帧 | `code` | `data` | `meta` | 说明 |
                |---|---|---|---|---|
                | 连接成功 | `0` | `{"status":"connected"}` | `type=connected` | 升级成功后第一帧。 |
                | 资源事件 | `0` | `{"type":"ADDED|MODIFIED|DELETED|ERROR","object":DisasterBackupDTO}` | 可能为空 | `object` 字段来自 `ConvertToDisasterBackupDTO`。 |
                | 心跳 | `0` | `null` | `type=heartbeat` | 每 30 秒发送一次。 |
                | watcher 错误 | `5000` | `null` | 空 | `DisasterBackups(disaster-system).Watch` 创建失败时发送。 |
                | 正常关闭 | `0` | `null` | `type=closed, reason=watcher closed` | watch channel 关闭。 |
                | 超时关闭 | `0` | `null` | `type=timeout, reason=connection timeout` | 连接达到 30 分钟上限。 |

                `DisasterBackupDTO` 字段含义与详情接口一致。

                ## 5. 可能返回的错误

                | 阶段 | HTTP 状态码或业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 握手前 | HTTP 401 | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。 |
                | 升级阶段 | HTTP 500 | WebSocket 升级失败 | upgrader.Upgrade 返回错误，响应为 `{"message":"..."}`。 |
                | WebSocket 内 | `code=5000` | watcher 创建失败 | `DisasterBackups(disaster-system).Watch` 返回错误。 |

                operator 资源扫描失败不会关闭 WebSocket，只会通过后续 `MODIFIED` 事件中的 `status.phase=Failed` 体现。
            operationId: get_apis_disasterbackups_testudo_softcdata_com_v1_watch_backups
            parameters:
                - description: 浏览器 WebSocket 兼容鉴权 token。
                  in: query
                  name: token
                  required: false
                  schema:
                    type: string
                - description: JWT Authorization header，格式为 Bearer <JWT>。
                  in: header
                  name: Authorization
                  required: false
                  schema:
                    type: string
                - description: 可携带 JWT 原文的 WebSocket 子协议 header。
                  in: header
                  name: Sec-WebSocket-Protocol
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '101':
                    description: WebSocket 连接升级成功。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MessageResponse'
                    description: WebSocket 升级失败。
            security:
                - bearerAuth: []
            summary: '[WebSocket] 容灾备份 GET /apis/disasterbackups.testudo.softcdata.com/v1/watch/backups'
            tags:
                - 容灾备份
            x-controlled-resources:
                direct:
                    - DisasterBackup CRD watch stream
                downstream:
                    - 无
                scope: disaster-system namespace 下全部 DisasterBackup watch 事件
            x-disaster-protocol: websocket
            x-message-schema:
                $ref: '#/components/schemas/DisasterBackupWatchEnvelope'
            x-operator-chain:
                controller: DisasterBackupReconciler syncs dependency labels, reads DisasterConfig and source Cluster, scans namespace resources, and writes status
                crd: DisasterBackup
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterbackup_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterbackup_controller.go
                serverFile: internal/apis/disaster_backup/v1/router.go
                serverHandler: c.watchBackups
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterbackups.testudo.softcdata.com/v1/watch/backups/{name}:
        get:
            description: |-
                ## 1. 接口用来干什么

                建立 WebSocket 连接，实时推送指定 `DisasterBackup` 的 Kubernetes watch 事件。server 使用 `fieldSelector=metadata.name=<name>` 过滤 watch 流。

                该接口不会先执行详情 Get，也不会因为目标当前不存在而同步返回 404；连接建立后只会推送匹配名称的后续 watch 事件。

                ## 2. 控制哪些资源

                直接操作资源：
                - 只监听指定名称的 `DisasterBackup` CRD watch 流。

                下层影响资源：
                - 无。该接口不创建、不更新、不删除 CRD，不访问源集群、目标集群、S3 和 Velero。
                - operator 后续更新该备份状态或 dependency labels 时会推送为 `MODIFIED` 事件。

                资源作用范围：
                - 监听配置的管理命名空间（默认 `disaster-system`）。
                - field selector 固定为 `metadata.name=<name>`。
                - WebSocket 流由 `StreamWatch` 发送连接成功帧、事件帧、30 秒 heartbeat、关闭帧和 30 分钟 timeout 帧。

                ## 3. 入参的详细说明

                | 字段 | 位置 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|---|
                | `name` | path | string | 是 | DisasterBackup 名称 | 要监听的备份配置名称 | 组成 Kubernetes field selector `metadata.name=<name>` | 为空时握手前返回 `400 code=1000`。 |
                | `Authorization` | header | string | 非 dev 必传 | `Bearer <JWT>` | JWT 令牌 | WebSocket 握手前认证 | 三种传入方式任选一种。 |
                | `token` | query | string | 非 dev 可选 | JWT 原文 | JWT 令牌 | `WebSocketTokenAdapter` 会转换为 Authorization header | 适合浏览器 WebSocket 无法设置 header 的场景。 |
                | `Sec-WebSocket-Protocol` | header | string | 非 dev 可选 | JWT 原文或子协议 | JWT 令牌 | adapter 会尝试提取 token | 服务端仍会完成 WebSocket 升级。 |

                ## 4. 返回的详细说明

                该接口升级为 WebSocket 后，所有消息均为统一 `Envelope` JSON 字符串。

                | 帧 | `code` | `data` | `meta` | 说明 |
                |---|---|---|---|---|
                | 连接成功 | `0` | `{"status":"connected"}` | `type=connected` | 升级成功后第一帧。 |
                | 资源事件 | `0` | `{"type":"ADDED|MODIFIED|DELETED|ERROR","object":DisasterBackupDTO}` | 可能为空 | 只推送 `metadata.name=<name>` 的事件。 |
                | 心跳 | `0` | `null` | `type=heartbeat` | 每 30 秒发送一次。 |
                | watcher 错误 | `5000` | `null` | 空 | watch 创建失败时发送。 |
                | 正常关闭 | `0` | `null` | `type=closed, reason=watcher closed` | watch channel 关闭。 |
                | 超时关闭 | `0` | `null` | `type=timeout, reason=connection timeout` | 连接达到 30 分钟上限。 |

                `DisasterBackupDTO` 字段含义与详情接口一致。目标不存在时连接也可能成功，但不会收到现有对象快照。

                ## 5. 可能返回的错误

                | 阶段 | HTTP 状态码或业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 握手前 | HTTP 400 / `code=1000` | 路径参数非法 | `name` 为空。 |
                | 握手前 | HTTP 401 | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。 |
                | 升级阶段 | HTTP 500 | WebSocket 升级失败 | upgrader.Upgrade 返回错误，响应为 `{"message":"..."}`。 |
                | WebSocket 内 | `code=5000` | watcher 创建失败 | `DisasterBackups(disaster-system).Watch` 返回错误。 |

                目标不存在不会同步返回 404。operator 资源扫描失败不会关闭 WebSocket，只会通过后续 `MODIFIED` 事件中的 `status.phase=Failed` 体现。
            operationId: get_apis_disasterbackups_testudo_softcdata_com_v1_watch_backups_name
            parameters:
                - description: 要监听的 DisasterBackup 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: 浏览器 WebSocket 兼容鉴权 token。
                  in: query
                  name: token
                  required: false
                  schema:
                    type: string
                - description: JWT Authorization header，格式为 Bearer <JWT>。
                  in: header
                  name: Authorization
                  required: false
                  schema:
                    type: string
                - description: 可携带 JWT 原文的 WebSocket 子协议 header。
                  in: header
                  name: Sec-WebSocket-Protocol
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '101':
                    description: WebSocket 连接升级成功。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 路径参数非法。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MessageResponse'
                    description: WebSocket 升级失败。
            security:
                - bearerAuth: []
            summary: '[WebSocket] 容灾备份 GET /apis/disasterbackups.testudo.softcdata.com/v1/watch/backups/:name'
            tags:
                - 容灾备份
            x-controlled-resources:
                direct:
                    - DisasterBackup CRD watch stream
                downstream:
                    - 无
                scope: disaster-system namespace 下指定名称的 DisasterBackup watch 事件
            x-disaster-protocol: websocket
            x-message-schema:
                $ref: '#/components/schemas/DisasterBackupWatchEnvelope'
            x-operator-chain:
                controller: DisasterBackupReconciler syncs dependency labels, reads DisasterConfig and source Cluster, scans namespace resources, and writes status
                crd: DisasterBackup
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterbackup_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterbackup_controller.go
                serverFile: internal/apis/disaster_backup/v1/router.go
                serverHandler: c.watchBackup
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterconfigs.testudo.softcdata.com/v1/configs:
        get:
            description: |-
                ## 1. 接口用来干什么

                查询 DisasterConfig 列表，用于容灾配置列表页展示源集群、目标集群、存储仓库、同步策略、镜像源重写和 operator 当前校验状态。server 使用 informer lister 读取对象，按 `keyword` 对配置名称做不区分大小写的包含搜索，按动态 label query 做内存模糊过滤，按 name/creationTimestamp 排序分页，并在 DTO 中尽力补充策略 cron。响应 `meta.summary` 按过滤后、分页前的结果统计健康配置和异常配置数量。

                ## 2. 控制哪些资源

                直接读取资源：
                - `DisasterConfig` CRD：cluster-scoped，通过 informer lister 列举。
                - `DisasterPolicy(disaster-system)`：仅在 dataSyncPolicy/resourceSyncPolicy 非空时读取，用于补充 `dataSyncCron/resourceSyncCron`。

                下层影响资源：
                - 列表接口只读，不访问业务集群、Velero、S3、DataSync、ResourceSync、AppBackup 或 DisasterInstance。
                - `DisasterConfigReconciler` 会添加 finalizer、同步依赖 labels、校验 source/target `Cluster` 和 `StorageRepository`，并通过 `DefaultBSL.ApplyStorageRepository` 在源/目标集群维护 Velero credential `Secret` 和 `BackupStorageLocation`。

                资源作用范围：
                - DisasterConfig 为 cluster-scoped；StorageRepository 由 operator 在配置的管理命名空间（默认 `disaster-system`） 查找。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `page` | integer | 否 | 大于 0 | 页码 | 内存分页 | 默认 1。 |
                | `limit` | integer | 否 | 正整数、`-1` | 每页数量 | 内存分页 | 默认 10；`-1` 返回全部。 |
                | `sort` | string | 否 | `name`、`creationTimestamp` | 排序字段 | 内存排序 | 空值保持 lister 原顺序；其他值 compare=0。 |
                | `order` | string | 否 | `asc`、`desc` | 排序方向 | 配合 sort | 只有 `desc` 触发倒序，其他按升序。 |
                | `keyword` | string | 否 | 任意字符串 | 配置名称关键字 | 按 `metadata.name` 搜索容灾基础配置 | 不区分大小写；会被 ParseOptions 排除在动态 label filters 外。 |
                | `environment` | string | 否 | label value 片段 | 示例动态 label 过滤 | 匹配 `metadata.labels.environment` 的值 | 任意非通用 query 都进入动态 filters；空或 `*` 不过滤。 |
                | `<labelKey>` | string | 否 | label value 片段 | 动态 label 过滤 | 匹配 `metadata.labels[labelKey]` 的值 | 使用包含匹配，区分大小写。 |

                ## 4. 返回的详细说明

                成功返回 `DisasterConfigCollectionEnvelope`，真实结构为 `data.items[] + meta`。`data.items[]` 的条目 schema 为 `DisasterConfigDTO`，其中 `spec` 为 `DisasterConfigSpecDTO`，`status` 为 `DisasterConfigStatusDTO`。

                | 字段 | 类型/取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|
                | `data.items[].id` | string | UID | metadata.uid | 唯一标识。 |
                | `data.items[].name` | string | 配置名 | metadata.name | cluster-scoped 对象名。 |
                | `data.items[].description` | string | 配置说明 | annotation | `testudo.softcdata.com/description`。 |
                | `data.items[].labels` | object | labels | metadata.labels | operator 会写依赖 labels。 |
                | `data.items[].creation_timestamp` | RFC3339 offset | 创建时间 | metadata.creationTimestamp | server 按进程本地时区（time.Local/TZ）格式化，例如 `2026-05-15T11:08:19+08:00`；排序仍使用 Kubernetes 原始 creationTimestamp。 |
                | `data.items[].spec.sourceCluster` | string | 源集群 | spec | operator 后续读取 Cluster CR。 |
                | `data.items[].spec.targetCluster` | string | 目标集群 | spec | operator 后续读取 Cluster CR。 |
                | `data.items[].spec.storageRepository` | string | 存储仓库 | spec | operator 在 disaster-system 查找 StorageRepository。 |
                | `data.items[].spec.dataSyncType` | string | 数据同步类型 | spec | 常见值 fsb/snapshot/none/external，server 不校验枚举。 |
                | `data.items[].spec.dataSyncPolicy` | string | 数据同步策略 | spec | 可为空。 |
                | `data.items[].spec.resourceSyncPolicy` | string | 资源同步策略 | spec | 可为空。 |
                | `data.items[].spec.resourcesSyncPolicy` | string | 兼容字段 | DTO | 与 resourceSyncPolicy 同源。 |
                | `data.items[].spec.dataSyncCron/resourceSyncCron` | string | 策略 cron | server 补充 | 策略不存在、读取失败或 Disabled 时为空。 |
                | `data.items[].spec.imageRewrite` | object | 镜像源重写 | spec | 可能省略。 |
                | `data.items[].status.status` | Pending/Ready/NotReady/Error/空 | 配置状态 | operator status | 新建后 operator 未回写时可能为空。 |
                | `data.items[].status.reason/message` | string | 状态原因/说明 | operator status | 常见 reason 包括 `SourceClusterNotFound`、`TargetClusterNotFound`、`StorageRepositoryNotFound`、`ClusterNotReady`、`QueryDependencyFailed`、`UpdateConfigFailed`、`ApplyStorageRepositoryFailed`。 |
                | `meta.pagination` | object | 分页信息 | BuildCollectionResponse | total 为过滤后总数。 |
                | `meta.summary.healthyCount` | integer | 健康配置数量 | server 过滤结果统计 | 统计 `status.status=Ready` 的配置数，统计范围为过滤后、分页前。 |
                | `meta.summary.abnormalCount` | integer | 异常配置数量 | server 过滤结果统计 | 统计 `status.status=NotReady/Error` 的配置数，统计范围为过滤后、分页前；Pending 或空状态不计入异常。 |
                | `meta.filters` | object | 动态过滤回显 | query filters | 不包含 page/limit/sort/order/keyword。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 列表失败 | `DisasterConfigLister.List` 返回错误。 |
                | 401 | 无 | 认证失败 | JWT 缺失、过期或无效。 |

                无匹配项返回 `200 data.items=[]`。策略 cron 读取失败只会让 cron 字段为空；operator 校验失败不影响列表 HTTP 状态，只体现在 `status.status/reason/message` 与 `meta.summary.abnormalCount`。
            operationId: get_apis_disasterconfigs_testudo_softcdata_com_v1_configs
            parameters:
                - description: 页码，默认 1。
                  in: query
                  name: page
                  required: false
                  schema:
                    default: 1
                    type: integer
                - description: 每页数量，默认 10；-1 返回全部。
                  in: query
                  name: limit
                  required: false
                  schema:
                    default: 10
                    type: integer
                - description: 排序字段，仅 name 和 creationTimestamp 有实际比较逻辑。
                  in: query
                  name: sort
                  required: false
                  schema:
                    enum: [name, creationTimestamp]
                    type: string
                - description: 排序方向；desc 倒序，其他值按升序。
                  in: query
                  name: order
                  required: false
                  schema:
                    enum: [asc, desc]
                    type: string
                - description: 配置名称关键字，不区分大小写匹配 metadata.name。
                  in: query
                  name: keyword
                  required: false
                  schema:
                    type: string
                - description: 示例动态 label 过滤；任意非通用 query 都会按同样规则匹配 metadata.labels。
                  in: query
                  name: environment
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterConfigCollectionEnvelope'
                    description: 查询成功。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: Lister 列表失败。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
            security:
                - bearerAuth: []
            summary: 查询容灾配置列表
            tags:
                - 容灾配置
            x-async-failure-status:
                readVia:
                    - GET /apis/disasterconfigs.testudo.softcdata.com/v1/configs
                    - GET /apis/disasterconfigs.testudo.softcdata.com/v1/configs/{name}
                    - GET /apis/disasterconfigs.testudo.softcdata.com/v1/watch/configs
                statusFields:
                    - status.status
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - DisasterConfig CRD informer cache
                    - DisasterPolicy CRD read for cron enrichment
                downstream:
                    - 无同步下游写入
                    - operator 异步维护源/目标集群 Velero Secret 和 BackupStorageLocation
                scope: cluster-scoped DisasterConfig
            x-operator-chain:
                controller: DisasterConfigReconciler validates Cluster and StorageRepository dependencies and applies Velero BSL; list endpoint only reads DTO snapshots
                crd: DisasterConfig, DisasterPolicy, Cluster, StorageRepository
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterconfig_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterconfig_controller.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/BSL.go
                serverFile: internal/apis/disaster_config/v1/router.go
                serverHandler: c.configs
            x-runapi-target-id: example-runapi-target-id
        post:
            description: |-
                ## 1. 接口用来干什么

                创建 DisasterConfig，描述源集群、目标集群、存储仓库、同步类型、同步策略和可选镜像源重写规则。HTTP 201 只表示 CRD 创建成功；Cluster/StorageRepository 是否存在且 Ready、Velero Secret/BackupStorageLocation 是否应用成功由 operator 异步写入 status。

                ## 2. 控制哪些资源

                直接写入资源：
                - `DisasterConfig` CRD：cluster-scoped，metadata.name 来自 body.name。
                - annotations：description 写入 `testudo.softcdata.com/description`，trace 写入 `testudo.softcdata.com/trace-id`。
                - `Cluster` CRD：只有当 imageRewrite/imageSources 产生 mappings 时同步读取 source/target Cluster，用于校验镜像源别名。

                下层影响资源：
                - `DisasterConfigReconciler` 添加 finalizer、同步依赖 labels，并校验 source/target Cluster、StorageRepository。
                - operator 通过 `GetKubeClientSetWithCluster` 连接源/目标集群，并由 `DefaultBSL.ApplyStorageRepository` 维护 Velero namespace 下的 credential `Secret` 和 `BackupStorageLocation`。

                资源作用范围：
                - DisasterConfig 和 Cluster 为 cluster-scoped；StorageRepository 从配置的管理命名空间（默认 `disaster-system`） 读取；策略依赖 labels 默认在 `disaster-system` 查找。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `name` | string | 是 | Kubernetes 名称 | 配置名称 | 写入 metadata.name | binding required；同名返回 409。 |
                | `description` | string | 否 | 任意字符串 | 配置说明 | 写入 annotation | 空字符串不写 annotation。 |
                | `sourceCluster` | string | 是 | Cluster 名称 | 源集群 | 写入 spec.sourceCluster | imageRewrite 映射校验可能同步读取；其他依赖由 operator 校验。 |
                | `targetCluster` | string | 是 | Cluster 名称 | 目标集群 | 写入 spec.targetCluster | imageRewrite 映射校验可能同步读取；其他依赖由 operator 校验。 |
                | `storageRepository` | string | 是 | StorageRepository 名称 | 存储仓库 | 写入 spec.storageRepository | operator 在 disaster-system 查找。 |
                | `dataSyncType` | string | 是 | `fsb`、`snapshot`、`none`、`external` 等 | 数据同步类型 | 写入 spec.dataSyncType | server 不校验枚举。 |
                | `dataSyncPolicy` | string/null | 否 | DisasterPolicy 名称、空字符串 | 数据同步策略 | 写入 spec.dataSyncPolicy | nil 或空字符串表示不绑定。 |
                | `resourcesSyncPolicy` | string/null | 否 | DisasterPolicy 名称、空字符串 | 资源同步策略兼容字段 | 优先写入 spec.resourceSyncPolicy | 字段出现即优先于 resourceSyncPolicy，即使为空也不回退。 |
                | `resourceSyncPolicy` | string/null | 否 | DisasterPolicy 名称、空字符串 | 资源同步策略 | 写入 spec.resourceSyncPolicy | 仅当 resourcesSyncPolicy 未出现时使用。 |
                | `imageRewrite.enabled` | boolean | 否 | true/false | 是否启用镜像重写 | 写入 spec.imageRewrite.enabled | true 时 mappings 至少一条。 |
                | `imageRewrite.applyTo[]` | string[] | 否 | `resourceSync`、`drill` | 生效场景 | 控制哪些恢复链路做镜像重写 | 不允许空值、未知值或重复值。 |
                | `imageRewrite.unmatchedPolicy` | string | 否 | `Fail`、`Keep` | 未匹配策略 | 控制未命中映射的镜像 | 空值默认 `Fail`。 |
                | `imageRewrite.mappings[]` | object[] | 否 | source/target 别名对 | 镜像源映射 | 校验 Cluster.spec.imageSources | source 不能为空且不能重复；target 不能为空；允许正向或反向配对。 |
                | `imageSources` | object | 否 | map[sourceAlias]=targetAlias | 旧版映射字段 | 转换为 imageRewrite | 仅 imageRewrite 未传且 map 非空时生效；空 key/value 会跳过。 |
                | `syncPolicy` | any | 否 | 任意值 | 不支持旧字段 | 无 | 顶层出现即返回 400，提示使用 dataSyncPolicy/resourcesSyncPolicy。 |

                ## 4. 返回的详细说明

                成功返回 HTTP 201 和 `DisasterConfigEnvelope`，`data` 为创建后的 `DisasterConfigDTO`。`data.creation_timestamp` 源自 Kubernetes `metadata.creationTimestamp`，server 按进程本地时区（time.Local/TZ）格式化为 RFC3339 offset 字符串，例如 `2026-05-15T11:08:19+08:00`。创建响应中的 status 可能为空，因为 operator 还未完成 Pending/Ready/NotReady/Error 回写；policy cron 只在关联策略存在且未 Disabled 时补充。

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 不支持旧字段 | 顶层包含 `syncPolicy`。 |
                | 400 | `1000` | JSON 绑定失败 | body 非法、必填字段缺失或类型不匹配。 |
                | 400 | `1000` | imageRewrite 校验失败 | applyTo/unmatchedPolicy/mappings 非法，Cluster 不存在，或别名对无法在源/目标 Cluster 中匹配。 |
                | 401 | 无 | 认证失败 | JWT 缺失、过期或无效。 |
                | 409 | `3009` | 同名配置已存在 | Kubernetes Create 返回 AlreadyExists。 |
                | 500 | `5000` | 创建失败 | Kubernetes Create 返回非冲突错误。 |

                Cluster 未 Ready、StorageRepository 不存在、Velero Secret/BSL 应用失败不会改变 HTTP 201，只会在后续 status 中体现。
            operationId: post_apis_disasterconfigs_testudo_softcdata_com_v1_configs
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateDisasterConfigRequest'
                description: 创建容灾配置请求体。
                required: true
            responses:
                '201':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterConfigEnvelope'
                    description: 创建成功。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求体或 imageRewrite 校验失败。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '409':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 同名 DisasterConfig 已存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: Kubernetes Create 失败。
            security:
                - bearerAuth: []
            summary: 创建容灾配置
            tags:
                - 容灾配置
            x-async-failure-status:
                readVia:
                    - GET /apis/disasterconfigs.testudo.softcdata.com/v1/configs/{name}
                    - GET /apis/disasterconfigs.testudo.softcdata.com/v1/watch/configs/{name}
                statusFields:
                    - status.status
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - DisasterConfig CRD create
                    - Cluster CRD read when imageRewrite mappings exist
                downstream:
                    - DisasterConfigReconciler finalizer/dependency labels/status
                    - Velero credential Secret in source and target clusters
                    - Velero BackupStorageLocation in source and target clusters
                scope: cluster-scoped DisasterConfig plus source/target clusters
            x-operator-chain:
                controller: DisasterConfigReconciler validates dependencies and applies StorageRepository to both clusters after create
                crd: DisasterConfig, Cluster, StorageRepository, DisasterPolicy
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterconfig_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterconfig_controller.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/BSL.go
                serverFile: internal/apis/disaster_config/v1/router.go
                serverHandler: c.createConfig
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterconfigs.testudo.softcdata.com/v1/configs/{name}:
        delete:
            description: |-
                ## 1. 接口用来干什么

                删除指定 DisasterConfig。server 会先 best-effort 读取对象并写入 trace annotation，再调用 Delete；删除结果以 Delete 调用为准。成功返回 200 只表示删除请求已提交，带 finalizer 的对象可能仍处于 Terminating。

                ## 2. 控制哪些资源

                直接写入资源：
                - `DisasterConfig` CRD：按 path.name 删除 cluster-scoped 对象。
                - annotations：删除前尽力写入 trace-id；Get/Update 失败会被忽略。

                下层影响资源：
                - `DisasterConfigReconciler.handleDelete` 当前只移除 `testudo.softcdata.com/config-finalizer`。
                - 旧的 DisasterInstance 引用检查代码已注释，当前不会因为实例引用阻止删除。
                - 当前删除分支不清理已应用到源/目标集群的 Velero credential `Secret` 或 `BackupStorageLocation`。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `name` | string | 是 | DisasterConfig 名称 | 删除目标 | 用于 Get/Update/Delete | 不存在返回 404。 |

                ## 4. 返回的详细说明

                成功返回 `DisasterConfigDeleteEnvelope`，`data.name` 为 path.name。该响应不表示引用它的 DisasterInstance 已更新，也不表示下层 Velero 资源已清理。

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 401 | 无 | 认证失败 | JWT 缺失、过期或无效。 |
                | 404 | `3004` | 配置不存在 | Delete 返回 NotFound。 |
                | 500 | `5000` | 删除失败 | Delete 返回非 NotFound 错误。 |

                删除前 best-effort Get/Update 失败不会改变 HTTP 结果。operator finalizer 处理失败也不会改变本次 HTTP 响应。
            operationId: delete_apis_disasterconfigs_testudo_softcdata_com_v1_configs_name
            parameters:
                - description: 要删除的 DisasterConfig 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterConfigDeleteEnvelope'
                    description: 删除请求提交成功。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 目标配置不存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: Kubernetes Delete 失败。
            security:
                - bearerAuth: []
            summary: 删除容灾配置
            tags:
                - 容灾配置
            x-controlled-resources:
                direct:
                    - DisasterConfig CRD delete
                downstream:
                    - DisasterConfigReconciler removes config finalizer
                    - 不同步清理下层 Velero Secret/BackupStorageLocation
                scope: cluster-scoped DisasterConfig by name
            x-operator-chain:
                controller: DisasterConfigReconciler handleDelete removes finalizer; reference protection is currently disabled
                crd: DisasterConfig
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterconfig_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterconfig_controller.go
                serverFile: internal/apis/disaster_config/v1/router.go
                serverHandler: c.deleteConfig
            x-runapi-target-id: example-runapi-target-id
        get:
            description: |-
                ## 1. 接口用来干什么

                查询单个 DisasterConfig 详情。server 读取 CRD 后转换为 `DisasterConfigDTO`，并根据 dataSyncPolicy/resourceSyncPolicy 尽力读取 `DisasterPolicy(disaster-system)` 补充 cron。

                ## 2. 控制哪些资源

                直接读取资源：
                - `DisasterConfig` CRD：按 path.name Get。
                - `DisasterPolicy(disaster-system)`：只用于补充策略 cron，读取失败会忽略。

                下层影响资源：
                - 详情接口只读，不访问业务集群、Velero、S3、DataSync、ResourceSync 或 AppBackup。
                - operator 后续维护 `status.status/reason/message`，详情只展示状态快照。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `name` | string | 是 | DisasterConfig 名称 | 查询目标 | Kubernetes Get | 不存在返回 404。 |

                ## 4. 返回的详细说明

                成功返回 `DisasterConfigEnvelope`，`data` 的 schema 为 `DisasterConfigDTO`，其中 `spec` 为 `DisasterConfigSpecDTO`，`status` 为 `DisasterConfigStatusDTO`。`data.creation_timestamp` 源自 Kubernetes `metadata.creationTimestamp`，server 按进程本地时区（time.Local/TZ）格式化为 RFC3339 offset 字符串，例如 `2026-05-15T11:08:19+08:00`。`spec.resourcesSyncPolicy` 与 `spec.resourceSyncPolicy` 当前同源；`dataSyncCron/resourceSyncCron` 只在关联策略存在且未 Disabled 时有值；operator 依赖校验异常通过 `status.status/reason/message` 表示。

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 401 | 无 | 认证失败 | JWT 缺失、过期或无效。 |
                | 404 | `3004` | 配置不存在 | Get 返回 NotFound。 |
                | 500 | `5000` | 查询失败 | Get 返回非 NotFound 错误。 |

                关联策略读取失败不影响详情 HTTP 状态；只会让 cron 字段为空。
            operationId: get_apis_disasterconfigs_testudo_softcdata_com_v1_configs_name
            parameters:
                - description: 要查询的 DisasterConfig 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterConfigEnvelope'
                    description: 查询成功。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 目标配置不存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: Kubernetes Get 失败。
            security:
                - bearerAuth: []
            summary: 查询容灾配置详情
            tags:
                - 容灾配置
            x-async-failure-status:
                readVia:
                    - GET /apis/disasterconfigs.testudo.softcdata.com/v1/configs/{name}
                    - GET /apis/disasterconfigs.testudo.softcdata.com/v1/watch/configs/{name}
                statusFields:
                    - status.status
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - DisasterConfig CRD read
                    - DisasterPolicy CRD read for cron enrichment
                downstream:
                    - 无同步下游写入
                scope: cluster-scoped DisasterConfig by name
            x-operator-chain:
                controller: DisasterConfigReconciler writes status; detail endpoint reads current snapshot only
                crd: DisasterConfig, DisasterPolicy
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterconfig_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterconfig_controller.go
                serverFile: internal/apis/disaster_config/v1/router.go
                serverHandler: c.config
            x-runapi-target-id: example-runapi-target-id
        put:
            description: |-
                ## 1. 接口用来干什么

                更新 DisasterConfig。虽然 HTTP 方法是 PUT，当前实现是部分合并更新：只更新请求体中有效出现的字段，资源身份只来自 path.name，不读取 body.name。

                ## 2. 控制哪些资源

                直接写入资源：
                - `DisasterConfig` CRD：RetryOnConflict 中读取现有对象并合并更新 spec/annotation。
                - `Cluster` CRD：只有更新 imageRewrite/imageSources 且产生 mappings 时，同步读取有效 source/target Cluster 校验别名。

                下层影响资源：
                - 更新 spec 后触发 `DisasterConfigReconciler` 重新同步依赖 labels，校验 Cluster/StorageRepository，并重新应用 Velero credential `Secret` 和 `BackupStorageLocation`。

                ## 3. 入参的详细说明

                | 字段 | 位置 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|---|
                | `name` | path | string | 是 | DisasterConfig 名称 | 更新目标 | Kubernetes Get/Update | 不存在返回 404。 |
                | `description` | body | string/null | 否 | 任意字符串 | 配置说明 | 覆盖 annotation | 字段出现即覆盖，空字符串可清空。 |
                | `sourceCluster` | body | string | 否 | Cluster 名称 | 源集群 | 覆盖 spec.sourceCluster | 非空才覆盖；空字符串不生效。 |
                | `targetCluster` | body | string | 否 | Cluster 名称 | 目标集群 | 覆盖 spec.targetCluster | 非空才覆盖；空字符串不生效。 |
                | `storageRepository` | body | string | 否 | StorageRepository 名称 | 存储仓库 | 覆盖 spec.storageRepository | 非空才覆盖；空字符串不生效。 |
                | `dataSyncType` | body | string | 否 | fsb/snapshot/none/external 等 | 数据同步类型 | 覆盖 spec.dataSyncType | 非空才覆盖；server 不校验枚举。 |
                | `dataSyncPolicy` | body | string/null | 否 | 策略名或空字符串 | 数据同步策略 | 覆盖 spec.dataSyncPolicy | 字段出现时覆盖；空字符串可清空。 |
                | `resourcesSyncPolicy` | body | string/null | 否 | 策略名或空字符串 | 资源同步策略兼容字段 | 优先覆盖 spec.resourceSyncPolicy | 字段出现即优先于 resourceSyncPolicy，空字符串可清空。 |
                | `resourceSyncPolicy` | body | string/null | 否 | 策略名或空字符串 | 资源同步策略 | 覆盖 spec.resourceSyncPolicy | 仅 resourcesSyncPolicy 未出现时使用；空字符串可清空。 |
                | `imageRewrite` | body | object/null | 否 | 镜像源重写配置 | 替换 spec.imageRewrite | 不传保持原值；传空对象会保存禁用空配置，不会清除为 nil。 |
                | `imageSources` | body | object | 否 | map[sourceAlias]=targetAlias | 旧版映射字段 | 转换为 imageRewrite | 仅 imageRewrite 未传且 map 非空时生效。 |
                | `syncPolicy` | body | any | 否 | 任意值 | 不支持旧字段 | 无 | 顶层出现即返回 400。 |

                ## 4. 返回的详细说明

                成功返回 `DisasterConfigEnvelope`。响应为更新后的 CRD 快照加策略 cron 尽力补充；`data.creation_timestamp` 源自 Kubernetes `metadata.creationTimestamp`，server 按进程本地时区（time.Local/TZ）格式化为 RFC3339 offset 字符串，例如 `2026-05-15T11:08:19+08:00`；operator 对依赖和下层 Velero 资源的处理是异步的，成功响应中的 status 可能仍是旧状态。

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | path name 为空 | handler 获取不到 path.name。 |
                | 400 | `1000` | 不支持旧字段 | 顶层包含 syncPolicy。 |
                | 400 | `1000` | JSON 绑定或 imageRewrite 校验失败 | body 非法、类型不匹配、镜像源配置非法或别名对不匹配。 |
                | 401 | 无 | 认证失败 | JWT 缺失、过期或无效。 |
                | 404 | `3004` | 配置不存在 | RetryOnConflict 中 Get 返回 NotFound。 |
                | 409 | `3009` | 资源冲突 | RetryOnConflict 最终返回 Conflict。 |
                | 500 | `5000` | 查询或更新失败 | Get/Update 返回其他错误。 |

                Cluster/StorageRepository 未 Ready 或 Velero 应用失败不会改变 HTTP 200，只会由 operator 后续写入 status。
            operationId: put_apis_disasterconfigs_testudo_softcdata_com_v1_configs_name
            parameters:
                - description: 要更新的 DisasterConfig 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateDisasterConfigRequest'
                description: 更新容灾配置请求体。当前为部分合并更新。
                required: true
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterConfigEnvelope'
                    description: 更新成功。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求体、path 或 imageRewrite 校验失败。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 目标配置不存在。
                '409':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: Kubernetes Get/Update 失败。
            security:
                - bearerAuth: []
            summary: 更新容灾配置
            tags:
                - 容灾配置
            x-async-failure-status:
                readVia:
                    - GET /apis/disasterconfigs.testudo.softcdata.com/v1/configs/{name}
                    - GET /apis/disasterconfigs.testudo.softcdata.com/v1/watch/configs/{name}
                statusFields:
                    - status.status
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - DisasterConfig CRD update
                    - Cluster CRD read when imageRewrite mappings exist
                downstream:
                    - DisasterConfigReconciler dependency labels/status
                    - Velero credential Secret and BackupStorageLocation reapplied in source/target clusters
                scope: cluster-scoped DisasterConfig plus source/target clusters
            x-operator-chain:
                controller: DisasterConfigReconciler revalidates dependencies and reapplies StorageRepository after spec update
                crd: DisasterConfig, Cluster, StorageRepository, DisasterPolicy
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterconfig_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterconfig_controller.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/BSL.go
                serverFile: internal/apis/disaster_config/v1/router.go
                serverHandler: c.updateConfig
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterconfigs.testudo.softcdata.com/v1/configs/names:
        get:
            description: |-
                ## 1. 接口用来干什么

                查询 DisasterConfig 轻量名称列表，用于下拉选择。该接口只返回数组，不返回 collection meta，也不分页。

                ## 2. 控制哪些资源

                直接读取 `DisasterConfig` informer cache。接口不读取 DisasterPolicy，不访问业务集群、Velero、S3、DataSync、ResourceSync、AppBackup 或 DisasterInstance。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `keyword` | string | 否 | 任意字符串 | 通用关键字 | 当前不使用 | 被 ParseOptions 排除在 filters 外，因此不影响返回结果。 |
                | `page/limit/sort/order` | string/integer | 否 | 任意值 | 通用参数 | 当前不使用 | 不分页、不排序、不返回 meta。 |
                | `environment` | string | 否 | label value 片段 | 示例动态 label 过滤 | 匹配 labels.environment | 任意非通用 query 都进入动态 filters。 |
                | `<labelKey>` | string | 否 | label value 片段 | 动态 label 过滤 | 匹配 metadata.labels[labelKey] | 空或 `*` 不过滤。 |

                ## 4. 返回的详细说明

                成功返回 `DisasterConfigNamesEnvelope`，`data` 是数组，每个条目的 schema 为 `DisasterConfigNameDTO`。

                | 字段 | 类型/取值 | 中文含义 | 字段来源 |
                |---|---|---|---|
                | `data[].name` | string | 配置名称 | metadata.name |
                | `data[].id` | string | UID | metadata.uid |
                | `data[].sourceCluster` | string | 源集群 | spec.sourceCluster |
                | `data[].targetCluster` | string | 目标集群 | spec.targetCluster |
                | `data[].status` | Pending/Ready/NotReady/Error/空 | 配置状态 | status.status |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 列表失败 | `DisasterConfigLister.List` 返回错误。 |
                | 401 | 无 | 认证失败 | JWT 缺失、过期或无效。 |

                无匹配项返回 `200 data=[]`。
            operationId: get_apis_disasterconfigs_testudo_softcdata_com_v1_configs_names
            parameters:
                - description: 当前接口不使用 keyword 过滤结果。
                  in: query
                  name: keyword
                  required: false
                  schema:
                    type: string
                - description: 示例动态 label 过滤；任意非通用 query 都会按同样规则匹配 metadata.labels。
                  in: query
                  name: environment
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterConfigNamesEnvelope'
                    description: 查询成功。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: Lister 列表失败。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
            security:
                - bearerAuth: []
            summary: 查询容灾配置名称列表
            tags:
                - 容灾配置
            x-controlled-resources:
                direct:
                    - DisasterConfig CRD informer cache
                downstream:
                    - 无同步下游写入
                scope: cluster-scoped DisasterConfig
            x-operator-chain:
                controller: DisasterConfigReconciler maintains status.status used by names endpoint
                crd: DisasterConfig
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterconfig_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterconfig_controller.go
                serverFile: internal/apis/disaster_config/v1/router.go
                serverHandler: c.configNames
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterconfigs.testudo.softcdata.com/v1/watch/configs:
        get:
            description: |-
                ## 1. 接口用来干什么

                建立 WebSocket，监听全部 DisasterConfig 变化。handler 直接 Watch 空 ListOptions，不先 List 当前快照；客户端如需初始数据，应先调用列表接口再建立 WebSocket 接收增量事件。

                ## 2. 控制哪些资源

                直接监听 `DisasterConfig` CRD。事件对象转换为 DTO 时会按 dataSyncPolicy/resourceSyncPolicy 尽力读取 `DisasterPolicy(disaster-system)` 补充 cron。接口本身不访问业务集群、Velero 或 S3；下层处理结果通过 status 字段变化推送。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `token` | string | 否 | JWT | WebSocket 鉴权 | query token | 与 Authorization/Sec-WebSocket-Protocol 任一方式携带。 |
                | `Authorization` | header | 否 | `Bearer <JWT>` | 鉴权 | JWT 中间件读取 | 非 dev 环境需要。 |
                | `Sec-WebSocket-Protocol` | header | 否 | JWT 原文 | 浏览器兼容鉴权 | JWT 中间件读取 | 可替代 Authorization。 |

                ## 4. 返回的详细说明

                握手成功返回 101。WebSocket 帧统一为 `DisasterConfigWatchEnvelope`：连接成功帧 `data.status=connected, meta.type=connected`；心跳帧 `data=null, meta.type=heartbeat`；事件帧 `data.type=ADDED/MODIFIED/DELETED/ERROR, data.object=DisasterConfigDTO`，其中 `data.object.creation_timestamp` 按 server 进程本地时区格式化为 RFC3339 offset 字符串；关闭/超时帧通过 meta 标识。

                ## 5. 可能返回的错误

                | 阶段 | HTTP 状态码或业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 握手前 | HTTP 401 | 认证失败 | JWT 缺失、过期或无效。 |
                | 升级阶段 | HTTP 500 | WebSocket 升级失败 | upgrader.Upgrade 返回错误。 |
                | WebSocket 内 | `code=5000` | watcher 创建失败 | `DisasterConfigs().Watch` 返回错误。 |

                连接默认 30 分钟超时，心跳间隔 30 秒。
            operationId: get_apis_disasterconfigs_testudo_softcdata_com_v1_watch_configs
            parameters:
                - description: 浏览器 WebSocket 兼容鉴权 token。
                  in: query
                  name: token
                  required: false
                  schema:
                    type: string
                - description: JWT Authorization header，格式 Bearer <JWT>。
                  in: header
                  name: Authorization
                  required: false
                  schema:
                    type: string
                - description: 可携带 JWT 原文的 WebSocket 子协议 header。
                  in: header
                  name: Sec-WebSocket-Protocol
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '101':
                    description: WebSocket 连接升级成功。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MessageResponse'
                    description: WebSocket 升级失败。
            security:
                - bearerAuth: []
            summary: '[WebSocket] 监听容灾配置'
            tags:
                - 容灾配置
            x-controlled-resources:
                direct:
                    - DisasterConfig CRD watch stream
                    - DisasterPolicy CRD read for cron enrichment during conversion
                downstream:
                    - 无同步下游写入
                scope: all cluster-scoped DisasterConfigs
            x-disaster-protocol: websocket
            x-message-schema:
                $ref: '#/components/schemas/DisasterConfigWatchEnvelope'
            x-operator-chain:
                controller: DisasterConfigReconciler writes labels/finalizer/status that produce watch events
                crd: DisasterConfig, DisasterPolicy
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterconfig_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterconfig_controller.go
                serverFile: internal/apis/disaster_config/v1/router.go
                serverHandler: c.watchConfigs
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterconfigs.testudo.softcdata.com/v1/watch/configs/{name}:
        get:
            description: |-
                ## 1. 接口用来干什么

                建立 WebSocket，监听指定 DisasterConfig 名称的变化。handler 不先 Get 校验目标存在，也不推当前对象快照，而是直接按 FieldSelector `metadata.name=<name>` Watch。

                ## 2. 控制哪些资源

                直接监听指定 `DisasterConfig`。事件对象转换为 DTO 时会尽力读取 `DisasterPolicy(disaster-system)` 补充 cron。接口本身不访问业务集群、Velero 或 S3。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `name` | string | 是 | DisasterConfig 名称 | 监听目标 | FieldSelector metadata.name | 目标不存在也可建立连接，只是没有业务事件。 |
                | `token` | string | 否 | JWT | WebSocket 鉴权 | query token | 与 Authorization/Sec-WebSocket-Protocol 任一方式携带。 |
                | `Authorization` | header | 否 | `Bearer <JWT>` | 鉴权 | JWT 中间件读取 | 非 dev 环境需要。 |
                | `Sec-WebSocket-Protocol` | header | 否 | JWT 原文 | 浏览器兼容鉴权 | JWT 中间件读取 | 可替代 Authorization。 |

                ## 4. 返回的详细说明

                握手成功返回 101。事件帧 `data.object` 为 `DisasterConfigDTO`，其中 `data.object.creation_timestamp` 按 server 进程本地时区格式化为 RFC3339 offset 字符串；连接成功、心跳、closed、timeout、error 帧遵循 `StreamWatch` 通用结构。

                ## 5. 可能返回的错误

                | 阶段 | HTTP 状态码或业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 握手前 | HTTP 400 / `1000` | path 参数缺失 | handler 获取到空 name。 |
                | 握手前 | HTTP 401 | 认证失败 | JWT 缺失、过期或无效。 |
                | 升级阶段 | HTTP 500 | WebSocket 升级失败 | upgrader.Upgrade 返回错误。 |
                | WebSocket 内 | `code=5000` | watcher 创建失败 | `DisasterConfigs().Watch(FieldSelector...)` 返回错误。 |

                目标不存在不会返回 404。
            operationId: get_apis_disasterconfigs_testudo_softcdata_com_v1_watch_configs_name
            parameters:
                - description: 要监听的 DisasterConfig 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: 浏览器 WebSocket 兼容鉴权 token。
                  in: query
                  name: token
                  required: false
                  schema:
                    type: string
                - description: JWT Authorization header，格式 Bearer <JWT>。
                  in: header
                  name: Authorization
                  required: false
                  schema:
                    type: string
                - description: 可携带 JWT 原文的 WebSocket 子协议 header。
                  in: header
                  name: Sec-WebSocket-Protocol
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '101':
                    description: WebSocket 连接升级成功。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: path name 为空。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MessageResponse'
                    description: WebSocket 升级失败。
            security:
                - bearerAuth: []
            summary: '[WebSocket] 监听指定容灾配置'
            tags:
                - 容灾配置
            x-controlled-resources:
                direct:
                    - DisasterConfig CRD watch stream
                    - DisasterPolicy CRD read for cron enrichment during conversion
                downstream:
                    - 无同步下游写入
                scope: cluster-scoped DisasterConfig by metadata.name
            x-disaster-protocol: websocket
            x-message-schema:
                $ref: '#/components/schemas/DisasterConfigWatchEnvelope'
            x-operator-chain:
                controller: DisasterConfigReconciler writes labels/finalizer/status that produce target watch events
                crd: DisasterConfig, DisasterPolicy
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterconfig_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterconfig_controller.go
                serverFile: internal/apis/disaster_config/v1/router.go
                serverHandler: c.watchConfig
            x-runapi-target-id: example-runapi-target-id

    /apis/disasterdrills.testudo.softcdata.com/v1/drills:
        get:
            description: |-
                ## 1. 接口用来干什么

                查询 DisasterDrill 列表。server 使用 `transport.ParseOptions` 解析分页和过滤参数，按 namespace 读取 Drill CRD，再在内存中过滤 keyword、type、instanceName、groupName、state，最后固定按 creationTimestamp 倒序分页返回。该接口只读，不触发 operator reconcile。

                ## 2. 控制哪些资源

                直接只读 DisasterDrill。返回状态来自 DisasterDrillReconciler 和 DisasterOperationReconciler 已写入的 status；列表接口不读取实例、组、Operation、DataSync、ResourceSync、AppRestore、业务集群、Velero 或 S3。

                ## 3. 入参的详细说明

                | 字段 | 是否必传 | 中文含义 | 目的/约束 |
                |---|---|---|---|
                | `namespace` | 否 | Drill namespace | 为空读取全部 namespace；指定值只读取该 namespace。 |
                | `keyword` | 否 | 名称关键字 | 只匹配 `metadata.name`，大小写敏感。 |
                | `type` | 否 | 演练类型 | `Instance` 要求 `spec.instanceName` 非空；`Group` 要求 `spec.groupName` 非空；其他值当前不生效。 |
                | `state` | 否 | 状态过滤 | `NotStarted` 匹配 Pending/Ready；`InProgress` 匹配 Executing；其他值按 CRD `status.state` 精确匹配。 |
                | `instanceName/groupName` | 否 | 目标对象名称 | 按 spec 字段精确匹配。 |
                | `limit/page` | 否 | 分页参数 | 由 transport.Paginate 处理，`limit=-1` 返回全部。 |
                | `sort/order` | 否 | 排序参数 | handler 当前不使用这两个参数；响应 meta 会回显。 |

                ## 4. 返回的详细说明

                | 字段 | 类型/取值 | 中文含义 | 来源/说明 |
                |---|---|---|---|
                | `data.items[]` | DisasterDrillDTO[] | 当前页演练 | 含 status、validationResults、steps、groupProgress。 |
                | `data.items[].status.state` | Pending/Ready/Executing/Completed/CleaningUp/CleanedUp/Failed | 演练状态 | 来自 operator。 |
                | `meta.pagination.total` | integer | 过滤后总数 | 分页前数量。 |

                ## 5. 可能返回的错误

                | 状态 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 401 | `2001` | 认证失败 | JWT 缺失、过期或无效。 |
                | 500 | `5000` | 列表失败 | `DisasterDrills(ns).List` 返回错误。 |
            operationId: get_apis_disasterdrills_testudo_softcdata_com_v1_drills
            parameters:
                - description: Drill namespace。
                  in: query
                  name: namespace
                  required: false
                  schema:
                    type: string
                - description: 名称关键字。
                  in: query
                  name: keyword
                  required: false
                  schema:
                    type: string
                - description: Instance 或 Group。
                  in: query
                  name: type
                  required: false
                  schema:
                    type: string
                - description: 状态过滤。
                  in: query
                  name: state
                  required: false
                  schema:
                    type: string
                - description: 实例名称。
                  in: query
                  name: instanceName
                  required: false
                  schema:
                    type: string
                - description: 容灾组名称。
                  in: query
                  name: groupName
                  required: false
                  schema:
                    type: string
                - description: 每页数量。
                  in: query
                  name: limit
                  required: false
                  schema:
                    type: string
                - description: 页码。
                  in: query
                  name: page
                  required: false
                  schema:
                    type: string
                - description: 排序字段，当前只进入 meta。
                  in: query
                  name: sort
                  required: false
                  schema:
                    type: string
                - description: 排序方向，当前只进入 meta。
                  in: query
                  name: order
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterDrillCollectionEnvelope'
                    description: 成功返回演练集合。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误。
            security:
                - bearerAuth: []
            summary: 容灾演练 GET /apis/disasterdrills.testudo.softcdata.com/v1/drills
            tags:
                - 容灾演练
            x-controlled-resources:
                direct:
                    - DisasterDrill
                    - DisasterOperation
                downstream:
                    - AppRestore
                    - DataSync
                    - ResourceSync
                    - 目标集群 Namespace/Deployment/StatefulSet/Pod/PVC
                scope: 管理集群 Drill namespace；实例或组按 server namespace 解析结果定位
            x-operator-chain:
                controller: DisasterDrillReconciler / DisasterOperationReconciler
                crd: DisasterDrill, DisasterOperation, DisasterInstance, DisasterGroup, DisasterConfig, DataSync, ResourceSync, AppRestore
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterdrill_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterdrill/controller.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_drill/v1/router.go
                serverHandler: h.listDrills
            x-runapi-target-id: example-runapi-target-id
        post:
            description: |-
                ## 1. 接口用来干什么

                创建 DisasterDrill。server 校验 instanceName/groupName 二选一、自定义 name 长度、目标实例或组存在性、演练级 restorePolicy 和 veleroHooks.dataRestore 后创建 Drill CRD。显式传入 `veleroHooks:{}` 表示清空演练级 Hook 覆盖并阻断实例级 dataRestore Hook 继承。201 只表示 CRD 创建成功；operator 后续初始化 Pending、执行前置校验并进入 Ready，确认后才创建 drill DisasterOperation。

                ## 2. 控制哪些资源

                直接创建 DisasterDrill；同步写入目标对象 label、trace annotation、可选 restorePolicy 和可选 veleroHooks。operator 后续创建 DisasterOperation，Operation 再读取 DataSync/ResourceSync 最近备份、创建 AppRestore，并在目标集群执行恢复和 ScaleUp；演练级 dataRestore hooks 会覆盖实例级 dataRestore hooks，显式空 veleroHooks 会作为清空覆盖复制到 Operation。

                ## 3. 入参的详细说明

                | 字段 | 是否必传 | 中文含义 | 目的/约束 |
                |---|---|---|---|
                | `instanceName` | 条件必传 | 实例演练目标 | 与 `groupName` 必须且只能传一个。 |
                | `groupName` | 条件必传 | 容灾组演练目标 | 与 `instanceName` 必须且只能传一个。 |
                | `name` | 否 | 自定义 Drill 名称 | 长度不能超过 63；为空时按 `drill-<目标名>-<MMDDHHMM>` 生成。 |
                | `namespace` | 否 | 初始查询 namespace | 为空默认 disaster-system；NotFound 后全 namespace 回退并切换到目标对象真实 namespace。 |
                | `targetCluster` | 否 | 目标集群 | 实例演练为空时使用实例 secondaryCluster；组演练为空时 status.targetCluster 为 `(Auto)`。 |
                | `namespaceMapping` | 否 | namespace 映射 | 空映射表示复用原 namespace；非空时恢复写入映射 namespace，cleanup 删除映射 namespace。 |
                | `skipValidation` | 否 | 跳过前置校验 | true 时目标 Cluster 非 Ready 不阻断，但实例/组存在性仍由 operator 校验。 |
                | `waitUntilReady` | 否 | 是否等待 Pod 就绪 | operator 写入 Drill Operation waitUntilReady，并把 skipPodReadyCheck 设为相反值。 |
                | `restorePolicy` | 否 | 演练级恢复策略覆盖 | 解析 modifierRulesText/bulkModifierActionsText，校验 include/exclude、bulk action、modifier rule；modifier rule live path 按 JSON Patch 语义校验，`add` 允许最终一级 map key 不存在但父路径必须存在，`replace/remove` 要求完整路径存在；组演练要求成员实例 sourceCluster 唯一。 |
                | `veleroHooks.dataRestore` | 否 | 演练级恢复 Hook 覆盖 | Velero RestoreHooks 原生结构，写入 Drill spec 并复制到 Drill Operation；只影响演练恢复创建的 AppRestore template.hooks。 |
                | `veleroHooks:{}` | 否 | 清空演练级 Hook 覆盖 | 顶层空对象会保留为非 nil 空 CRD 对象，表示本次 Drill 不继承实例级 dataRestore Hook。 |
                | `veleroHooks.dataBackup` | 禁止 | 演练级备份 Hook | Drill 不创建数据备份；请求中只要出现 dataBackup 就返回 400。 |

                ## 4. 返回的详细说明

                | 字段 | 类型/取值 | 中文含义 | 来源/说明 |
                |---|---|---|---|
                | `data` | DisasterDrillDTO | 刚创建的 Drill | Kubernetes Create 后立即返回，status 可能尚未由 operator 初始化。 |
                | `data.veleroHooks` | object/null | 演练级 Hook 回显 | 来自 DisasterDrill spec；仅包含 dataRestore。 |
                | `data.status.state` | string | 演练状态 | 初始同步响应可能为空，随后 watch/详情变为 Pending/Ready。 |

                ## 5. 可能返回的错误

                | 状态 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 请求、restorePolicy 或 Hook 非法 | JSON 错误、二选一错误、name 超长、文本入口冲突、modifier/bulk/live 校验失败、Hook 结构非法、Hook timeout 超过上限、出现 `veleroHooks.dataBackup`、组成员 sourceCluster 不唯一。 |
                | 400 | `1000` + meta.errorCode=`VeleroHookSensitiveParameter` | Hook 明文敏感参数 | dataRestore command/args/env.value 中包含明显敏感片段；meta.fieldPath 返回字段路径。 |
                | 404 | `3004` | 目标不存在 | 指定实例或组在指定 namespace 与全 namespace 回退中都未找到。 |
                | 409 | `3009` | 同名 Drill 冲突 | Kubernetes Create 返回 AlreadyExists。 |
                | 500 | `5000` | 创建失败 | 目标对象读取非 NotFound 错误或 Create 非 AlreadyExists 错误。 |
            operationId: post_apis_disasterdrills_testudo_softcdata_com_v1_drills
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateDisasterDrillRequest'
                description: 创建容灾演练请求体。
                required: true
            responses:
                '201':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterDrillEnvelope'
                    description: 成功创建演练。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在。
                '409':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误。
            security:
                - bearerAuth: []
            summary: 容灾演练 POST /apis/disasterdrills.testudo.softcdata.com/v1/drills
            tags:
                - 容灾演练
            x-controlled-resources:
                direct:
                    - DisasterDrill
                    - DisasterOperation
                downstream:
                    - AppRestore
                    - DataSync
                    - ResourceSync
                    - 目标集群 Namespace/Deployment/StatefulSet/Pod/PVC
                scope: 管理集群 Drill namespace；实例或组按 server namespace 解析结果定位
            x-operator-chain:
                controller: DisasterDrillReconciler / DisasterOperationReconciler
                crd: DisasterDrill, DisasterOperation, DisasterInstance, DisasterGroup, DisasterConfig, DataSync, ResourceSync, AppRestore
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterdrill_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterdrill/controller.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_drill/v1/router.go
                serverHandler: h.createDrill
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterdrills.testudo.softcdata.com/v1/drills/{name}:
        delete:
            description: |-
                ## 1. 接口用来干什么

                删除 DisasterDrill CR。namespace 为空时先按名称全 namespace 定位；定位失败、目标不存在或 Delete NotFound 都按幂等成功返回 200。删除 Drill 不等同于清理演练恢复出的业务资源，业务资源清理需调用 cleanup。

                ## 2. 控制哪些资源

                直接删除 DisasterDrill；operator handleDeletion 只移除 finalizer，Drill ownerReference 下的 DisasterOperation 由 Kubernetes GC 级联。接口不直接访问目标集群、AppRestore、Velero、S3、Namespace 或工作负载。

                ## 3. 入参的详细说明

                | 字段 | 是否必传 | 中文含义 | 目的/约束 |
                |---|---|---|---|
                | `name` | 是 | Drill 名称 | path 参数。 |
                | `namespace` | 否 | Drill namespace | 为空时自动解析；解析失败也返回幂等成功。 |

                ## 4. 返回的详细说明

                | 字段 | 类型/取值 | 中文含义 | 来源/说明 |
                |---|---|---|---|
                | `data.name` | string | 删除目标名称 | 始终回显 path name。 |
                | `data.deleted` | boolean | 是否真的提交 Delete | 仅 Kubernetes Delete 成功时为 true。 |

                ## 5. 可能返回的错误

                | 状态 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 401 | `2001` | 认证失败 | JWT 缺失、过期或无效。 |
                | 500 | `5000` | 删除失败 | 定位成功后 Delete 返回非 NotFound 错误。 |
            operationId: delete_apis_disasterdrills_testudo_softcdata_com_v1_drills_name
            parameters:
                - description: DisasterDrill 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: Drill、目标实例或目标组所在 namespace。多数接口为空时自动解析。
                  in: query
                  name: namespace
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterDrillDeleteEnvelope'
                    description: 删除提交成功或目标已不存在。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误。
            security:
                - bearerAuth: []
            summary: 容灾演练 DELETE /apis/disasterdrills.testudo.softcdata.com/v1/drills/:name
            tags:
                - 容灾演练
            x-controlled-resources:
                direct:
                    - DisasterDrill
                    - DisasterOperation
                downstream:
                    - AppRestore
                    - DataSync
                    - ResourceSync
                    - 目标集群 Namespace/Deployment/StatefulSet/Pod/PVC
                scope: 管理集群 Drill namespace；实例或组按 server namespace 解析结果定位
            x-operator-chain:
                controller: DisasterDrillReconciler / DisasterOperationReconciler
                crd: DisasterDrill, DisasterOperation, DisasterInstance, DisasterGroup, DisasterConfig, DataSync, ResourceSync, AppRestore
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterdrill_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterdrill/controller.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_drill/v1/router.go
                serverHandler: h.deleteDrill
            x-runapi-target-id: example-runapi-target-id
        get:
            description: |-
                ## 1. 接口用来干什么

                查询单个 DisasterDrill 详情。server 根据 namespace query 或 findNamespace 定位后读取 Drill CRD，并转换为 DisasterDrillDTO；接口只读，不触发 reconcile。

                ## 2. 控制哪些资源

                只读 DisasterDrill。status、validationResults、steps、groupProgress 均来自 operator 已写入状态；接口不直接读取 Operation、DataSync、ResourceSync、AppRestore、业务集群、Velero 或 S3。

                ## 3. 入参的详细说明

                | 字段 | 是否必传 | 中文含义 | 目的/约束 |
                |---|---|---|---|
                | `name` | 是 | Drill 名称 | path 参数，用于 namespace 自动解析和 Get。 |
                | `namespace` | 否 | Drill namespace | 为空时自动解析。 |

                ## 4. 返回的详细说明

                | 字段 | 类型/取值 | 中文含义 | 来源/说明 |
                |---|---|---|---|
                | `data` | DisasterDrillDTO | 演练详情 | 含 spec 投影、operator status、步骤和组进度。 |
                | `data.status.operationName` | string | 关联 Operation | 确认执行或清理后由 operator 写入。 |

                ## 5. 可能返回的错误

                | 状态 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 404 | `3004` | Drill 不存在 | namespace 自动解析未找到或指定 namespace 下 Get NotFound。 |
                | 500 | `5000` | 读取失败 | 自动解析 List 失败或 Get 非 NotFound 错误。 |
            operationId: get_apis_disasterdrills_testudo_softcdata_com_v1_drills_name
            parameters:
                - description: DisasterDrill 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: Drill、目标实例或目标组所在 namespace。多数接口为空时自动解析。
                  in: query
                  name: namespace
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterDrillEnvelope'
                    description: 成功返回演练详情。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误。
            security:
                - bearerAuth: []
            summary: 容灾演练 GET /apis/disasterdrills.testudo.softcdata.com/v1/drills/:name
            tags:
                - 容灾演练
            x-controlled-resources:
                direct:
                    - DisasterDrill
                    - DisasterOperation
                downstream:
                    - AppRestore
                    - DataSync
                    - ResourceSync
                    - 目标集群 Namespace/Deployment/StatefulSet/Pod/PVC
                scope: 管理集群 Drill namespace；实例或组按 server namespace 解析结果定位
            x-operator-chain:
                controller: DisasterDrillReconciler / DisasterOperationReconciler
                crd: DisasterDrill, DisasterOperation, DisasterInstance, DisasterGroup, DisasterConfig, DataSync, ResourceSync, AppRestore
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterdrill_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterdrill/controller.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_drill/v1/router.go
                serverHandler: h.getDrill
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterdrills.testudo.softcdata.com/v1/drills/{name}/cleanup:
        post:
            description: |-
                ## 1. 接口用来干什么

                触发演练资源清理。HTTP handler 只校验 Drill 当前状态为 Completed 且 spec.cleanup 尚未触发，然后写入 `spec.cleanup=true`；真正清理由 DisasterDrillReconciler 创建 `drill-cleanup` DisasterOperation 后异步执行。

                ## 2. 控制哪些资源

                直接更新 DisasterDrill spec.cleanup。operator 创建 drill-cleanup Operation；无 namespaceMapping 时在目标集群缩容工作负载为 0，有 namespaceMapping 时删除映射后的目标 Namespace。

                ## 3. 入参的详细说明

                | 字段 | 是否必传 | 中文含义 | 目的/约束 |
                |---|---|---|---|
                | `name` | 是 | Drill 名称 | path 参数。 |
                | `namespace` | 否 | Drill namespace | 为空时自动解析。 |

                ## 4. 返回的详细说明

                | 字段 | 类型/取值 | 中文含义 | 来源/说明 |
                |---|---|---|---|
                | `data.cleanup` | boolean | 清理标记 | 成功响应为 true，但 status.state 可能仍是 Completed。 |
                | `data.status.state` | Completed/CleaningUp/CleanedUp/Failed | 异步状态 | 后续由 operator 推进。 |

                ## 5. 可能返回的错误

                | 状态 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 状态不允许 | Drill 不是 Completed、cleanup 已触发，或 RetryOnConflict 中部分错误 result 为空。 |
                | 404 | `3004` | Drill 不存在 | 自动解析或 Get NotFound。 |
                | 500 | `5000` | 定位失败 | namespace 自动解析非 NotFound 错误。 |
            operationId: post_apis_disasterdrills_testudo_softcdata_com_v1_drills_name_cleanup
            parameters:
                - description: DisasterDrill 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: Drill、目标实例或目标组所在 namespace。多数接口为空时自动解析。
                  in: query
                  name: namespace
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterDrillEnvelope'
                    description: 成功更新 Drill。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误。
            security:
                - bearerAuth: []
            summary: 容灾演练 POST /apis/disasterdrills.testudo.softcdata.com/v1/drills/:name/cleanup
            tags:
                - 容灾演练
            x-controlled-resources:
                direct:
                    - DisasterDrill
                    - DisasterOperation
                downstream:
                    - AppRestore
                    - DataSync
                    - ResourceSync
                    - 目标集群 Namespace/Deployment/StatefulSet/Pod/PVC
                scope: 管理集群 Drill namespace；实例或组按 server namespace 解析结果定位
            x-operator-chain:
                controller: DisasterDrillReconciler / DisasterOperationReconciler
                crd: DisasterDrill, DisasterOperation, DisasterInstance, DisasterGroup, DisasterConfig, DataSync, ResourceSync, AppRestore
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterdrill_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterdrill/controller.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_drill/v1/router.go
                serverHandler: h.cleanupDrill
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterdrills.testudo.softcdata.com/v1/drills/{name}/confirm:
        post:
            description: |-
                ## 1. 接口用来干什么

                确认执行已 Ready 的演练。HTTP handler 只校验 Drill 当前状态为 Ready 并写入 `spec.confirmed=true`；真正执行由 operator 创建 `operationType=drill` 的 DisasterOperation 后异步执行。

                ## 2. 控制哪些资源

                直接更新 DisasterDrill spec.confirmed。operator 后续创建 Drill Operation，并执行 RestoreResource、RestoreData、ScaleUp；实例演练写 instanceName，组演练写 groupName 并按 levels 创建子 Operation。

                ## 3. 入参的详细说明

                | 字段 | 是否必传 | 中文含义 | 目的/约束 |
                |---|---|---|---|
                | `name` | 是 | Drill 名称 | path 参数。 |
                | `namespace` | 否 | Drill namespace | 为空时自动解析。 |

                ## 4. 返回的详细说明

                | 字段 | 类型/取值 | 中文含义 | 来源/说明 |
                |---|---|---|---|
                | `data.confirmed` | boolean | 确认标记 | 成功响应为 true，但 status.state 可能仍是 Ready。 |
                | `data.status.operationName` | string | Drill Operation | 后续由 operator 写入。 |

                ## 5. 可能返回的错误

                | 状态 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 状态不允许 | Drill 不是 Ready，或 RetryOnConflict 中部分错误 result 为空。 |
                | 404 | `3004` | Drill 不存在 | 自动解析或 Get NotFound。 |
                | 500 | `5000` | 定位失败 | namespace 自动解析非 NotFound 错误。 |
            operationId: post_apis_disasterdrills_testudo_softcdata_com_v1_drills_name_confirm
            parameters:
                - description: DisasterDrill 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: Drill、目标实例或目标组所在 namespace。多数接口为空时自动解析。
                  in: query
                  name: namespace
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterDrillEnvelope'
                    description: 成功更新 Drill。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误。
            security:
                - bearerAuth: []
            summary: 容灾演练 POST /apis/disasterdrills.testudo.softcdata.com/v1/drills/:name/confirm
            tags:
                - 容灾演练
            x-controlled-resources:
                direct:
                    - DisasterDrill
                    - DisasterOperation
                downstream:
                    - AppRestore
                    - DataSync
                    - ResourceSync
                    - 目标集群 Namespace/Deployment/StatefulSet/Pod/PVC
                scope: 管理集群 Drill namespace；实例或组按 server namespace 解析结果定位
            x-operator-chain:
                controller: DisasterDrillReconciler / DisasterOperationReconciler
                crd: DisasterDrill, DisasterOperation, DisasterInstance, DisasterGroup, DisasterConfig, DataSync, ResourceSync, AppRestore
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterdrill_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterdrill/controller.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_drill/v1/router.go
                serverHandler: h.confirmDrill
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterdrills.testudo.softcdata.com/v1/drills/{name}/restart:
        post:
            description: |-
                ## 1. 接口用来干什么

                重跑已完成或失败的演练。HTTP handler 只校验 Drill 当前状态为 Completed 或 Failed，并写入 restart timestamp annotation；operator 在后续 reconcile 中清空 status、把 confirmed 重置为 false，并重新进入 Pending 校验。

                ## 2. 控制哪些资源

                直接更新 DisasterDrill annotation `testudo.softcdata.com/restart-timestamp` 和 trace annotation。接口不直接创建 Operation、不访问业务集群；重置后仍需再次 confirm 才执行新的 Drill Operation。

                ## 3. 入参的详细说明

                | 字段 | 是否必传 | 中文含义 | 目的/约束 |
                |---|---|---|---|
                | `name` | 是 | Drill 名称 | path 参数。 |
                | `namespace` | 否 | Drill namespace | 为空时自动解析。 |

                ## 4. 返回的详细说明

                | 字段 | 类型/取值 | 中文含义 | 来源/说明 |
                |---|---|---|---|
                | `data.status.state` | Completed/Failed/Pending | 即时或异步状态 | HTTP 成功后响应可能仍为 Completed/Failed，后续由 operator 重置为 Pending。 |

                ## 5. 可能返回的错误

                | 状态 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 状态不允许 | Drill 不是 Completed 或 Failed，或 RetryOnConflict 中部分错误 result 为空。 |
                | 404 | `3004` | Drill 不存在 | 自动解析或 Get NotFound。 |
                | 500 | `5000` | 定位失败 | namespace 自动解析非 NotFound 错误。 |
            operationId: post_apis_disasterdrills_testudo_softcdata_com_v1_drills_name_restart
            parameters:
                - description: DisasterDrill 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: Drill、目标实例或目标组所在 namespace。多数接口为空时自动解析。
                  in: query
                  name: namespace
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterDrillEnvelope'
                    description: 成功更新 Drill。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误。
            security:
                - bearerAuth: []
            summary: 容灾演练 POST /apis/disasterdrills.testudo.softcdata.com/v1/drills/:name/restart
            tags:
                - 容灾演练
            x-controlled-resources:
                direct:
                    - DisasterDrill
                    - DisasterOperation
                downstream:
                    - AppRestore
                    - DataSync
                    - ResourceSync
                    - 目标集群 Namespace/Deployment/StatefulSet/Pod/PVC
                scope: 管理集群 Drill namespace；实例或组按 server namespace 解析结果定位
            x-operator-chain:
                controller: DisasterDrillReconciler / DisasterOperationReconciler
                crd: DisasterDrill, DisasterOperation, DisasterInstance, DisasterGroup, DisasterConfig, DataSync, ResourceSync, AppRestore
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterdrill_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterdrill/controller.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_drill/v1/router.go
                serverHandler: h.restartDrill
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterdrills.testudo.softcdata.com/v1/drills/actions/protected-namespaces:
        get:
            description: |-
                ## 1. 接口用来干什么

                查询实例或容灾组当前保护的 namespace，用于创建演练前给前端展示或辅助填写 namespaceMapping。server 要求 instanceName 与 groupName 必须且只能传一个；容灾实例和容灾组均按平台管理命名空间中的 CR 查询，namespace 为空时使用配置的管理命名空间（默认 disaster-system）。实例模式读取 DisasterInstance.spec.namespaces，组模式读取 DisasterGroup.spec.levels 后逐实例聚合 namespaces。

                ## 2. 控制哪些资源

                只读平台管理命名空间中的 DisasterInstance 和 DisasterGroup；不创建 Drill、不创建 Operation、不访问 DataSync、ResourceSync、AppRestore、业务集群、Velero 或 S3。组内实例 NotFound 不失败，而是进入 missingInstances。

                ## 3. 入参的详细说明

                | 字段 | 是否必传 | 中文含义 | 目的/约束 |
                |---|---|---|---|
                | `instanceName` | 条件必传 | 实例名称 | 与 groupName 必须且只能传一个。 |
                | `groupName` | 条件必传 | 容灾组名称 | 与 instanceName 必须且只能传一个。 |
                | `namespace` | 否 | 平台管理 namespace | 为空时使用配置的管理命名空间（默认 disaster-system）；传入时使用指定 namespace。该接口不再全 namespace 回退查找实例或组，组内实例读取使用组所在 namespace。 |

                ## 4. 返回的详细说明

                | 字段 | 类型/取值 | 中文含义 | 来源/说明 |
                |---|---|---|---|
                | `data.type` | Instance/Group | 查询类型 | 根据入参决定。 |
                | `data.namespaces` | string[] | 保护 namespace | 去空、去重、排序。 |
                | `data.missingInstances` | string[] | 缺失实例 | 仅组模式可返回。 |

                ## 5. 可能返回的错误

                | 状态 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 参数非法 | instanceName/groupName 都空或同传。 |
                | 404 | `3004` | 目标不存在 | 实例或组在平台管理 namespace 中未找到。 |
                | 500 | `5000` | 查询失败 | CRD Get 非 NotFound 错误，或组内实例读取非 NotFound 错误。 |
            operationId: get_apis_disasterdrills_testudo_softcdata_com_v1_drills_actions_protected_namespaces
            parameters:
                - description: 实例名称。
                  in: query
                  name: instanceName
                  required: false
                  schema:
                    type: string
                - description: 容灾组名称。
                  in: query
                  name: groupName
                  required: false
                  schema:
                    type: string
                - description: 目标实例或目标组所在的平台管理 namespace。为空时使用配置的管理命名空间（默认 disaster-system）。
                  in: query
                  name: namespace
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ProtectedNamespacesEnvelope'
                    description: 成功返回受保护 namespace。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误。
            security:
                - bearerAuth: []
            summary: 容灾演练 GET /apis/disasterdrills.testudo.softcdata.com/v1/drills/actions/protected-namespaces
            tags:
                - 容灾演练
            x-controlled-resources:
                direct:
                    - DisasterDrill
                    - DisasterOperation
                downstream:
                    - AppRestore
                    - DataSync
                    - ResourceSync
                    - 目标集群 Namespace/Deployment/StatefulSet/Pod/PVC
                scope: 管理集群 Drill namespace；实例或组按 server namespace 解析结果定位
            x-operator-chain:
                controller: DisasterDrillReconciler / DisasterOperationReconciler
                crd: DisasterDrill, DisasterOperation, DisasterInstance, DisasterGroup, DisasterConfig, DataSync, ResourceSync, AppRestore
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterdrill_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterdrill/controller.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_drill/v1/router.go
                serverHandler: h.getProtectedNamespaces
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterdrills.testudo.softcdata.com/v1/watch/drills:
        get:
            description: |-
                ## 1. 接口用来干什么

                建立 WebSocket 连接，监听所有 namespace 的 DisasterDrill watch 事件。当前 handler 不读取 namespace、instanceName、groupName、state 等业务过滤 query，固定推送全部 Drill 变更。

                ## 2. 控制哪些资源

                只监听管理集群 DisasterDrill watch 流；无写入，不直接访问业务集群、Velero、S3、DataSync、ResourceSync 或 AppRestore。

                ## 3. 入参的详细说明

                | 字段 | 是否必传 | 中文含义 | 目的/约束 |
                |---|---|---|---|
                | `Authorization` | 非 dev 必传 | JWT | Bearer token。 |
                | `token` | 非 dev 可选 | JWT | 浏览器 WebSocket 兼容入口。 |
                | `Sec-WebSocket-Protocol` | 非 dev 可选 | JWT/子协议 | adapter 会尝试提取 token。 |

                ## 4. 返回的详细说明

                | 字段 | 类型/取值 | 中文含义 | 来源/说明 |
                |---|---|---|---|
                | 连接成功帧 | code=0 | data.status=connected | 升级成功后第一帧。 |
                | 事件帧 | code=0 | DisasterDrill watch event | object 为 DisasterDrillDTO；非 Drill 对象时 object 为 null。 |
                | 心跳/关闭/超时 | code=0 | meta.type | StreamWatch 固定帧，默认 30 秒心跳和 30 分钟超时。 |

                ## 5. 可能返回的错误

                | 状态 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 握手前 401 | `2001` | 认证失败 | JWT 缺失、过期或无效。 |
                | 升级 500 | message | 升级失败 | upgrader.Upgrade 返回错误。 |
                | WebSocket 内 | `5000` | watcher 创建失败 | DisasterDrills Watch 返回错误。 |
            operationId: get_apis_disasterdrills_testudo_softcdata_com_v1_watch_drills
            parameters:
                - description: 浏览器 WebSocket 兼容鉴权 token。
                  in: query
                  name: token
                  required: false
                  schema:
                    type: string
                - description: JWT Authorization header，格式为 Bearer <JWT>。
                  in: header
                  name: Authorization
                  required: false
                  schema:
                    type: string
                - description: 可携带 JWT 原文的 WebSocket 子协议 header。
                  in: header
                  name: Sec-WebSocket-Protocol
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '101':
                    description: WebSocket 连接升级成功。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MessageResponse'
                    description: WebSocket 升级失败。
            security:
                - bearerAuth: []
            summary: '[WebSocket] 容灾演练 GET /apis/disasterdrills.testudo.softcdata.com/v1/watch/drills'
            tags:
                - 容灾演练
            x-controlled-resources:
                direct:
                    - DisasterDrill
                    - DisasterOperation
                downstream:
                    - AppRestore
                    - DataSync
                    - ResourceSync
                    - 目标集群 Namespace/Deployment/StatefulSet/Pod/PVC
                scope: 管理集群 Drill namespace；实例或组按 server namespace 解析结果定位
            x-disaster-protocol: websocket
            x-message-schema:
                $ref: '#/components/schemas/DisasterDrillWatchEnvelope'
            x-operator-chain:
                controller: DisasterDrillReconciler / DisasterOperationReconciler
                crd: DisasterDrill, DisasterOperation, DisasterInstance, DisasterGroup, DisasterConfig, DataSync, ResourceSync, AppRestore
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterdrill_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterdrill/controller.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_drill/v1/router.go
                serverHandler: h.watchDrills
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterdrills.testudo.softcdata.com/v1/watch/drills/{name}:
        get:
            description: |-
                ## 1. 接口用来干什么

                建立 WebSocket 连接，监听指定名称的 DisasterDrill watch 事件。watcher 创建时调用 findNamespace，失败会忽略并使用空 namespace，因此目标不存在不会在握手阶段返回 404；query.namespace 当前不参与 watcher 选择。

                ## 2. 控制哪些资源

                只监听管理集群 DisasterDrill watch 流；无写入，不直接访问业务集群、Velero、S3、DataSync、ResourceSync 或 AppRestore。

                ## 3. 入参的详细说明

                | 字段 | 是否必传 | 中文含义 | 目的/约束 |
                |---|---|---|---|
                | `name` | 是 | Drill 名称 | 用于 FieldSelector `metadata.name=<name>`。 |
                | `Authorization/token/Sec-WebSocket-Protocol` | 非 dev | JWT | 三种 WebSocket 鉴权入口。 |

                ## 4. 返回的详细说明

                | 字段 | 类型/取值 | 中文含义 | 来源/说明 |
                |---|---|---|---|
                | 连接成功帧 | code=0 | data.status=connected | 升级成功后第一帧。 |
                | 事件帧 | code=0 | DisasterDrill watch event | object 为 DisasterDrillDTO；非 Drill 对象时 object 为 null。 |
                | 心跳/关闭/超时 | code=0 | meta.type | StreamWatch 固定帧，默认 30 秒心跳和 30 分钟超时。 |

                ## 5. 可能返回的错误

                | 状态 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 握手前 401 | `2001` | 认证失败 | JWT 缺失、过期或无效。 |
                | 升级 500 | message | 升级失败 | upgrader.Upgrade 返回错误。 |
                | WebSocket 内 | `5000` | watcher 创建失败 | DisasterDrills Watch 返回错误。 |
            operationId: get_apis_disasterdrills_testudo_softcdata_com_v1_watch_drills_name
            parameters:
                - description: DisasterDrill 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: 浏览器 WebSocket 兼容鉴权 token。
                  in: query
                  name: token
                  required: false
                  schema:
                    type: string
                - description: JWT Authorization header，格式为 Bearer <JWT>。
                  in: header
                  name: Authorization
                  required: false
                  schema:
                    type: string
                - description: 可携带 JWT 原文的 WebSocket 子协议 header。
                  in: header
                  name: Sec-WebSocket-Protocol
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '101':
                    description: WebSocket 连接升级成功。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MessageResponse'
                    description: WebSocket 升级失败。
            security:
                - bearerAuth: []
            summary: '[WebSocket] 容灾演练 GET /apis/disasterdrills.testudo.softcdata.com/v1/watch/drills/:name'
            tags:
                - 容灾演练
            x-controlled-resources:
                direct:
                    - DisasterDrill
                    - DisasterOperation
                downstream:
                    - AppRestore
                    - DataSync
                    - ResourceSync
                    - 目标集群 Namespace/Deployment/StatefulSet/Pod/PVC
                scope: 管理集群 Drill namespace；实例或组按 server namespace 解析结果定位
            x-disaster-protocol: websocket
            x-message-schema:
                $ref: '#/components/schemas/DisasterDrillWatchEnvelope'
            x-operator-chain:
                controller: DisasterDrillReconciler / DisasterOperationReconciler
                crd: DisasterDrill, DisasterOperation, DisasterInstance, DisasterGroup, DisasterConfig, DataSync, ResourceSync, AppRestore
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterdrill_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterdrill/controller.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_drill/v1/router.go
                serverHandler: h.watchDrill
            x-runapi-target-id: example-runapi-target-id
    /apis/disastergroups.testudo.softcdata.com/v1/groups:
        get:
            description: |-
                ## 1. 接口用来干什么

                查询 `disaster-system` 中的 DisasterGroup 列表，用于容灾组列表页展示分层实例、组聚合状态和可用操作。server 会预加载 `DisasterInstance` 与 `DisasterConfig`，为每个组补齐成员摘要，再按 keyword/status 做内存过滤。响应 `meta.summary.instanceCount` 按过滤后、分页前的结果统计列表中所有组成员实例条目数量，`meta.summary.abnormalCount` 按过滤后、分页前的结果统计异常容灾组个数。

                ## 2. 控制哪些资源

                直接读取资源：
                - `DisasterGroup` CRD：列举配置的管理命名空间（默认 `disaster-system`）。
                - `DisasterInstance` CRD：预加载 `disaster-system` 内实例，用于成员摘要和展示态推导。
                - `DisasterConfig` CRD：预加载全部配置，用于成员 `storageRepository` 展示。

                下层影响资源：
                - 列表接口只读，不创建 `DisasterOperation`，不访问业务集群、Velero、S3、DataSync、ResourceSync 或 AppBackup。
                - `DisasterGroupReconciler` 监听组和成员实例，按 `spec.levels` 聚合 total/ready，缺失成员写 `InstanceNotFound`，成员失败或 reason 非空写 `InstanceFailed`。

                资源作用范围：
                - 组对象读取配置的管理命名空间（默认 `disaster-system`）。
                - 成员摘要只来自 `disaster-system` 预加载缓存；缓存读取失败会降级为空缓存，不让列表接口失败。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `keyword` | string | 否 | 任意字符串 | 模糊关键字 | 匹配组名、描述、成员实例名、成员 namespaces | 不区分大小写。 |
                | `status` | string | 否 | `all`、`running`、`paused`、`error` | 成员状态过滤 | 判断组内是否存在匹配状态的成员 | `all` 或空不过滤；running 匹配 Protected/Initializing，error 匹配 Failed/ConfigError/NotFound/FailingOver/FailingBack。 |
                | `page` | integer | 否 | 大于 0 | 页码 | 内存分页 | 默认 1。 |
                | `limit` | integer | 否 | 正整数、`-1` | 每页数量 | 内存分页 | 默认 10；`-1` 返回全部。 |
                | `sort` | string | 否 | 任意值 | 通用排序参数 | 当前 handler 不使用 | 列表固定按 creation_timestamp 倒序。 |
                | `order` | string | 否 | 任意值 | 通用排序方向 | 当前 handler 不使用 | 仅在 meta 中随 ParseOptions 回显。 |

                ## 4. 返回的详细说明

                成功返回 `DisasterGroupCollectionEnvelope`，真实结构为 `data.items[] + meta`。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `data.items[].id` | string | Kubernetes UID | 组 UID | metadata | 唯一标识。 |
                | `data.items[].name` | string | 组名 | 组名称 | metadata | 与详情 path.name 对应。 |
                | `data.items[].description` | string | 任意字符串 | 组描述 | annotation | 来自 `testudo.softcdata.com/description`。 |
                | `data.items[].creation_timestamp` | string | RFC3339 | 创建时间 | metadata | server 格式化为 snake_case 字段。 |
                | `data.items[].levels` | string[][] | 分层实例名 | 执行层级 | CRD Spec | 外层顺序即组操作执行顺序。 |
                | `data.items[].policy.failPolicy` | string | `Continue`、`Stop`、空 | 失败策略 | CRD Spec | 由组动作 operator 使用。 |
                | `data.items[].status.totalInstances` | integer | 0+ | 实例总数 | CRD Status | operator 按 levels 统计。 |
                | `data.items[].status.readyInstances` | integer | 0+ | Protected 成员数 | CRD Status | operator 统计 Protected。 |
                | `data.items[].status.reason` | string | `InstanceNotFound`、`InstanceFailed`、空 | 聚合原因 | CRD Status | 缺失或失败成员时写入。 |
                | `data.items[].status.summary` | string | 如 `3 Levels, 10 Instances` | 展示摘要 | server | 根据 levels 和 totalInstances 计算。 |
                | `data.items[].status.fsmState` | string | `Protected`、`Active`、`Paused`、`Degraded` 等 | 组展示态 | server | 根据成员 fsmState 优先级推导。 |
                | `data.items[].status.availableOperations` | string[] | 动作列表 | 可执行操作 | server | Protected 通常有 failover/pause/synconce/syncdata/syncresource。 |
                | `data.items[].instances[]` | object | 成员摘要 | 组内实例 | server 读取成员实例 | 读取失败时成员 fsmState=NotFound。 |
                | `meta.summary.instanceCount` | integer | 列表内所有实例条目数量 | server 过滤结果统计 | 对过滤后、分页前所有组的 `instances[]` 长度求和；缺失成员会以 `fsmState=NotFound` 占位并计入。 |
                | `meta.summary.abnormalCount` | integer | 异常容灾组个数 | server 过滤结果统计 | 对过滤后、分页前所有组判定异常。组级 reason 非空、Error=True condition、组展示态 Degraded、成员 Failed/ConfigError/NotFound、成员 reason 非空均计入；纯 FailingOver/FailingBack 不计入。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 401 | 无 | 认证失败 | JWT 缺失、过期或无效。 |
                | 500 | `5000` | 列举组失败 | `DisasterGroups(disaster-system).List` 返回错误。 |

                实例或配置预加载失败不返回错误；查询不到匹配组返回 `200 data.items=[]`。operator 聚合异常通过 `status.reason/message/conditions` 体现。
            operationId: get_apis_disastergroups_testudo_softcdata_com_v1_groups
            parameters:
                - description: 模糊搜索组名、描述、成员实例名和成员 namespace。
                  in: query
                  name: keyword
                  required: false
                  schema:
                    type: string
                - description: 成员状态过滤。running 匹配 Protected/Initializing；paused 匹配 Paused；error 匹配 Failed/ConfigError/NotFound/FailingOver/FailingBack。
                  in: query
                  name: status
                  required: false
                  schema:
                    enum: [all, running, paused, error]
                    type: string
                - description: 页码，默认 1。
                  in: query
                  name: page
                  required: false
                  schema:
                    default: 1
                    type: integer
                - description: 每页数量，默认 10；-1 返回全部。
                  in: query
                  name: limit
                  required: false
                  schema:
                    default: 10
                    type: integer
                - description: 当前 handler 不使用该字段控制排序。
                  in: query
                  name: sort
                  required: false
                  schema:
                    type: string
                - description: 当前 handler 不使用该字段控制排序方向。
                  in: query
                  name: order
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterGroupCollectionEnvelope'
                    description: 查询成功。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 列举 DisasterGroup 失败。
            security:
                - bearerAuth: []
            summary: 查询容灾组列表
            tags:
                - 容灾组
            x-async-failure-status:
                readVia:
                    - GET /apis/disastergroups.testudo.softcdata.com/v1/groups
                    - GET /apis/disastergroups.testudo.softcdata.com/v1/groups/{name}
                    - GET /apis/disastergroups.testudo.softcdata.com/v1/watch/groups/status
                statusFields:
                    - status.totalInstances
                    - status.readyInstances
                    - status.reason
                    - status.message
                    - status.conditions
            x-controlled-resources:
                direct:
                    - DisasterGroup CRD in configured management namespace (default disaster-system)
                    - DisasterInstance CRD summaries
                    - DisasterConfig CRD storageRepository lookup
                downstream:
                    - 无同步下游写入
                scope: disaster-system 容灾组列表
            x-operator-chain:
                controller: DisasterGroupReconciler aggregates member status and dependency labels; listGroups only reads and enriches
                crd: DisasterGroup, DisasterInstance, DisasterConfig
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disastergroup_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disastergroup/controller.go
                serverFile: internal/apis/disaster_group/v1/router.go
                serverHandler: h.listGroups
            x-runapi-target-id: example-runapi-target-id
        post:
            description: |-
                ## 1. 接口用来干什么

                创建 DisasterGroup，用二维 levels 定义多个 DisasterInstance 的组级执行顺序，并写入组执行策略。创建接口只写组 CRD，不执行动作；组动作必须通过 `POST /groups/{name}/actions` 触发。

                ## 2. 控制哪些资源

                直接写入资源：
                - `DisasterGroup` CRD：创建到配置的管理命名空间（默认 `disaster-system`）。
                - annotations：description 写入 `testudo.softcdata.com/description`，trace 写入 `testudo.softcdata.com/trace-id`。

                下层影响资源：
                - `DisasterGroupReconciler` 后续同步 dependency labels，发射创建任务 Started/Finished 事件，并聚合组内实例状态。
                - 不创建 `DisasterOperation`，不访问业务集群、Velero、S3、DataSync、ResourceSync 或 AppBackup。

                资源作用范围：
                - 配置的管理命名空间（默认 `disaster-system`）。
                - 创建阶段不校验 levels 中的实例是否存在。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `name` | string | 是 | 组名 | 容灾组名称 | 写入 metadata.name | 结构体 tag 不是 binding required，空值校验依赖后续 Kubernetes 行为。 |
                | `description` | string | 否 | 任意字符串 | 组描述 | 写入 annotation | 为空不写 annotation。 |
                | `levels` | string[][] | 是 | 实例名二维数组 | 分层执行顺序 | 写入 `spec.levels` | 外层 index 0 最先执行；同层实例并行。server 不校验成员存在。 |
                | `policy.failPolicy` | string | 否 | `Continue`、`Stop` | 失败处理策略 | 写入 `spec.policy.failPolicy` | operator 组动作中解释；server 不校验枚举。 |
                | `policy.timeoutMin` | integer | 否 | 分钟数 | 单 Level 超时配置 | 写入 CRD | 当前 handleGroupOperation 未独立执行 Level 超时。 |
                | `policy.parallelism` | integer | 否 | 0 或正整数 | 并行度配置 | 写入 CRD | 当前组操作未显式使用该字段限流。 |
                | `policy.retryPolicy` | object | 否 | maxRetries/retryIntervalSeconds | 子操作重试策略 | 创建组动作时复制到父级与子操作 | 不传表示不重试。 |

                ## 4. 返回的详细说明

                成功返回 HTTP 201 和 `DisasterGroupDTO`。创建响应直接使用 `ConvertToDisasterGroupDTO`，通常还没有 operator 聚合后的 summary、fsmState、availableOperations 和 instances。

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | JSON 绑定失败 | body 不是合法 JSON 或字段类型不匹配。 |
                | 401 | 无 | 认证失败 | JWT 缺失、过期或无效。 |
                | 409 | `3009` | 同名组已存在 | Kubernetes Create 返回 AlreadyExists。 |
                | 500 | `5000` | 创建失败 | Kubernetes Create 返回其他错误。 |

                operator 后续成员缺失或失败只会写入 status，不改变 HTTP 201。
            operationId: post_apis_disastergroups_testudo_softcdata_com_v1_groups
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateDisasterGroupRequest'
                description: 创建容灾组请求体。
                required: true
            responses:
                '201':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterGroupEnvelope'
                    description: 创建成功。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: JSON 绑定失败。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '409':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 同名组已存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: Kubernetes Create 失败。
            security:
                - bearerAuth: []
            summary: 创建容灾组
            tags:
                - 容灾组
            x-async-failure-status:
                readVia:
                    - GET /apis/disastergroups.testudo.softcdata.com/v1/groups/{name}
                    - GET /apis/disastergroups.testudo.softcdata.com/v1/watch/groups/status/{name}
                    - GET /apis/v1/events
                statusFields:
                    - status.totalInstances
                    - status.readyInstances
                    - status.reason
                    - status.message
                    - status.conditions
            x-controlled-resources:
                direct:
                    - DisasterGroup CRD in configured management namespace (default disaster-system)
                downstream:
                    - DisasterGroupReconciler status and dependency labels
                    - 无同步 DisasterOperation 创建
                scope: disaster-system namespace
            x-operator-chain:
                controller: DisasterGroupReconciler syncs dependency labels, emits create task events, aggregates member status
                crd: DisasterGroup
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disastergroup_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disastergroup/controller.go
                serverFile: internal/apis/disaster_group/v1/router.go
                serverHandler: h.createGroup
            x-runapi-target-id: example-runapi-target-id
    /apis/disastergroups.testudo.softcdata.com/v1/groups/{name}:
        delete:
            description: |-
                ## 1. 接口用来干什么

                删除指定 DisasterGroup。当前删除是幂等语义：目标不存在时也返回 200，并回显 `{name}`。

                ## 2. 控制哪些资源

                直接写入资源：
                - `DisasterGroup` CRD：删除配置的管理命名空间（默认 `disaster-system`）下的 `{name}`。

                下层影响资源：
                - 当前没有组删除 finalizer，也没有专门的组删除 Started/Finished 事件。
                - 删除组不会删除成员 `DisasterInstance`、已创建的 `DisasterOperation`、`DisasterDrill`、历史事件、业务集群资源、Velero、S3、DataSync、ResourceSync 或 AppBackup。

                资源作用范围：
                - 配置的管理命名空间（默认 `disaster-system`）。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `name` | string | 是 | 容灾组名称 | 要删除的组名 | 用于 Kubernetes Delete | 不存在时仍返回成功。 |

                ## 4. 返回的详细说明

                成功返回统一信封，`data.name` 为 path.name。该响应仅表示 Delete 已成功或目标本就不存在，不表示历史操作或成员资源被清理。

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 401 | 无 | 认证失败 | JWT 缺失、过期或无效。 |
                | 500 | `5000` | 删除失败 | Kubernetes Delete 返回非 NotFound 错误。 |

                NotFound 返回 `200 code=0`，不会返回 404。
            operationId: delete_apis_disastergroups_testudo_softcdata_com_v1_groups_name
            parameters:
                - description: 要删除的容灾组名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterGroupDeleteEnvelope'
                    description: 删除成功或目标不存在。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: Kubernetes Delete 失败。
            security:
                - bearerAuth: []
            summary: 删除容灾组
            tags:
                - 容灾组
            x-controlled-resources:
                direct:
                    - DisasterGroup CRD in configured management namespace (default disaster-system)
                downstream:
                    - 不删除 DisasterInstance/DisasterOperation/DisasterDrill 或业务集群资源
                scope: disaster-system namespace
            x-operator-chain:
                controller: DisasterGroupReconciler ignores NotFound; no delete finalizer
                crd: DisasterGroup
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disastergroup_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disastergroup/controller.go
                serverFile: internal/apis/disaster_group/v1/router.go
                serverHandler: h.deleteGroup
            x-runapi-target-id: example-runapi-target-id
        get:
            description: |-
                ## 1. 接口用来干什么

                查询指定容灾组详情，补齐组内成员实例摘要、组聚合展示态和可用操作。

                ## 2. 控制哪些资源

                直接读取资源：
                - `DisasterGroup` CRD：读取配置的管理命名空间（默认 `disaster-system`）下的 `{name}`。
                - `DisasterInstance` CRD：按 `spec.levels` 逐个读取 `disaster-system` 中的成员。
                - `DisasterConfig` CRD：按成员 `spec.config` 尽力读取 storageRepository。

                下层影响资源：
                - 详情接口只读，不访问业务集群、Velero、S3、DataSync、ResourceSync、AppBackup 或 DisasterOperation。

                资源作用范围：
                - 配置的管理命名空间（默认 `disaster-system`）。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `name` | string | 是 | 容灾组名称 | 要查询的组名 | 用于 Kubernetes Get | 不存在返回 404。 |

                ## 4. 返回的详细说明

                返回 `DisasterGroupDTO`。成员实例读取失败不会让接口失败，而是在 `instances[]` 中返回占位条目：`name`、`fsmState=NotFound`、`level`。

                关键字段：`levels` 来自 CRD spec；`policy` 来自 CRD spec；`status.totalInstances/readyInstances/reason/message/conditions` 来自 operator；`status.summary/fsmState/availableOperations` 由 server 现场计算；`instances[].storageRepository` 来自成员 config，读取失败时为空。

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 401 | 无 | 认证失败 | JWT 缺失、过期或无效。 |
                | 404 | `3004` | 组不存在 | `DisasterGroups(disaster-system).Get` 返回 NotFound。 |
                | 500 | `5000` | 查询组失败 | 读取组本身发生非 NotFound 错误。 |

                成员实例或配置读取失败不改变 HTTP 状态。
            operationId: get_apis_disastergroups_testudo_softcdata_com_v1_groups_name
            parameters:
                - description: 要查询的容灾组名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterGroupEnvelope'
                    description: 查询成功。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 容灾组不存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: Kubernetes Get 失败。
            security:
                - bearerAuth: []
            summary: 查询容灾组详情
            tags:
                - 容灾组
            x-async-failure-status:
                readVia:
                    - GET /apis/disastergroups.testudo.softcdata.com/v1/groups/{name}
                    - GET /apis/disastergroups.testudo.softcdata.com/v1/watch/groups/status/{name}
                statusFields:
                    - status.totalInstances
                    - status.readyInstances
                    - status.reason
                    - status.message
                    - status.conditions
            x-controlled-resources:
                direct:
                    - DisasterGroup CRD in configured management namespace (default disaster-system)
                    - DisasterInstance CRD member summaries
                    - DisasterConfig CRD storageRepository lookup
                downstream:
                    - 无同步下游写入
                scope: disaster-system namespace
            x-operator-chain:
                controller: DisasterGroupReconciler aggregates group status; getGroup enriches DTO by reading members
                crd: DisasterGroup, DisasterInstance, DisasterConfig
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disastergroup_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disastergroup/controller.go
                serverFile: internal/apis/disaster_group/v1/router.go
                serverHandler: h.getGroup
            x-runapi-target-id: example-runapi-target-id
        patch:
            description: |-
                ## 1. 接口用来干什么

                部分更新容灾组。PATCH 与 PUT 共用 `h.updateGroup`，当前两者都是部分更新语义：只更新请求体中存在的 description、levels、policy。

                ## 2. 控制哪些资源

                直接写入资源：
                - `DisasterGroup` CRD：更新配置的管理命名空间（默认 `disaster-system`）下的 `{name}`。
                - annotations：description 字段存在时覆盖 `testudo.softcdata.com/description`；写入 trace annotation。

                下层影响资源：
                - 更新后 `DisasterGroupReconciler` 重新同步 dependency labels 并聚合 status。
                - 不创建 DisasterOperation，不执行组动作，不访问业务集群或 Velero。

                资源作用范围：
                - 配置的管理命名空间（默认 `disaster-system`）。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `name` path | string | 是 | 容灾组名称 | 要更新的组 | 用于 Kubernetes Get/Update | 目标不存在当前返回 500，不返回 404。 |
                | `description` | string/null | 否 | 任意字符串 | 组描述 | 覆盖 annotation | 字段存在即覆盖；空字符串会清空。 |
                | `levels` | string[][] | 否 | 分层实例列表 | 分层执行顺序 | 整体替换 `spec.levels` | 不校验成员存在。 |
                | `policy` | object/null | 否 | DisasterGroupPolicy | 执行策略 | 整体替换 `spec.policy` | 不做子字段 merge，不校验 failPolicy。 |

                ## 4. 返回的详细说明

                返回 `DisasterGroupDTO`，但因为使用 `ConvertToDisasterGroupDTO`，成功响应不补齐 `instances[]`，`status.summary/fsmState/availableOperations` 也可能为空；完整展示态需再次查询详情或列表。

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | JSON 绑定失败 | body 非法或字段类型不匹配。 |
                | 401 | 无 | 认证失败 | JWT 缺失、过期或无效。 |
                | 500 | `5000` | 读取或更新失败 | Get NotFound、Get 非 NotFound、Update 失败或 conflict 重试耗尽都会进入 500。 |

                operator 异步聚合失败不改变本次 HTTP 成功响应。
            operationId: patch_apis_disastergroups_testudo_softcdata_com_v1_groups_name
            parameters:
                - description: 要更新的容灾组名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateDisasterGroupRequest'
                description: 部分更新请求体。
                required: true
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterGroupEnvelope'
                    description: 更新成功。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: JSON 绑定失败。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 读取或更新 DisasterGroup 失败。
            security:
                - bearerAuth: []
            summary: 部分更新容灾组
            tags:
                - 容灾组
            x-async-failure-status:
                readVia:
                    - GET /apis/disastergroups.testudo.softcdata.com/v1/groups/{name}
                    - GET /apis/disastergroups.testudo.softcdata.com/v1/watch/groups/status/{name}
                statusFields:
                    - status.totalInstances
                    - status.readyInstances
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - DisasterGroup CRD in configured management namespace (default disaster-system)
                downstream:
                    - DisasterGroupReconciler status recalculation
                scope: disaster-system namespace
            x-operator-chain:
                controller: DisasterGroupReconciler recalculates labels and status after update
                crd: DisasterGroup
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disastergroup_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disastergroup/controller.go
                serverFile: internal/apis/disaster_group/v1/router.go
                serverHandler: h.updateGroup
            x-runapi-target-id: example-runapi-target-id
        put:
            description: |-
                ## 1. 接口用来干什么

                更新容灾组配置。虽然方法是 PUT，但当前实现与 PATCH 完全相同，都是部分更新而非严格全量替换。

                ## 2. 控制哪些资源

                直接写入 `disaster-system/{name}` 的 DisasterGroup CRD；按字段存在性更新 description annotation、整体替换 levels、整体替换 policy，并写入 trace annotation。不会创建 DisasterOperation，也不会执行组动作。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `name` path | string | 是 | 容灾组名称 | 要更新的组 | 用于 Kubernetes Get/Update | 目标不存在当前返回 500。 |
                | `description` | string/null | 否 | 任意字符串 | 组描述 | 覆盖 annotation | 字段存在即覆盖。 |
                | `levels` | string[][] | 否 | 分层实例列表 | 分层执行顺序 | 整体替换 spec.levels | 不校验成员存在。 |
                | `policy` | object/null | 否 | DisasterGroupPolicy | 执行策略 | 整体替换 spec.policy | 不做子字段 merge。 |

                ## 4. 返回的详细说明

                成功返回 `DisasterGroupDTO`。与 PATCH 一样，响应 DTO 只做基础转换，不补齐成员摘要和 server 计算展示态；完整展示态需查详情或列表。

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | JSON 绑定失败 | body 非法或字段类型不匹配。 |
                | 401 | 无 | 认证失败 | JWT 缺失、过期或无效。 |
                | 500 | `5000` | 读取或更新失败 | Get NotFound、Get 非 NotFound、Update 失败或 conflict 重试耗尽。 |
            operationId: put_apis_disastergroups_testudo_softcdata_com_v1_groups_name
            parameters:
                - description: 要更新的容灾组名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateDisasterGroupRequest'
                description: 更新请求体，当前为部分更新语义。
                required: true
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterGroupEnvelope'
                    description: 更新成功。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: JSON 绑定失败。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 读取或更新 DisasterGroup 失败。
            security:
                - bearerAuth: []
            summary: 更新容灾组
            tags:
                - 容灾组
            x-async-failure-status:
                readVia:
                    - GET /apis/disastergroups.testudo.softcdata.com/v1/groups/{name}
                    - GET /apis/disastergroups.testudo.softcdata.com/v1/watch/groups/status/{name}
                statusFields:
                    - status.totalInstances
                    - status.readyInstances
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - DisasterGroup CRD in configured management namespace (default disaster-system)
                downstream:
                    - DisasterGroupReconciler status recalculation
                scope: disaster-system namespace
            x-operator-chain:
                controller: DisasterGroupReconciler recalculates labels and status after update
                crd: DisasterGroup
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disastergroup_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disastergroup/controller.go
                serverFile: internal/apis/disaster_group/v1/router.go
                serverHandler: h.updateGroup
            x-runapi-target-id: example-runapi-target-id
    /apis/disastergroups.testudo.softcdata.com/v1/groups/{name}/actions:
        post:
            description: |-
                ## 1. 接口用来干什么

                触发容灾组动作。server 校验组存在后创建父级 `DisasterOperation`，返回 HTTP 202；真正的分层编排和实例级执行由 operator 异步完成。

                ## 2. 控制哪些资源

                直接读写资源：
                - 读取 `DisasterGroup(disaster-system/{name})` 确认组存在并读取 retryPolicy。
                - 创建父级 `DisasterOperation(disaster-system)`，设置 label `testudo.softcdata.com/group=<name>` 和 `testudo.softcdata.com/operation=<operationType>`。

                下层影响资源：
                - `DisasterOperationReconciler.handleGroupOperation` 读取 group.spec.levels，按 Level 创建实例级子 `DisasterOperation`。
                - 子操作继承 force、skipFinalSync、skipScaleDownSource、skipPodReadyCheck、waitUntilReady、retryPolicy 和 trace annotation。
                - 真正访问业务集群、DataSync、ResourceSync、AppBackup/AppRestore、Velero、Deployment/StatefulSet 的是每个实例级子操作。

                资源作用范围：
                - 父级操作和组在配置的管理命名空间（默认 `disaster-system`）。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `name` path | string | 是 | 容灾组名称 | 动作目标组 | 用于读取组和写入 operation label | 组读取失败统一返回 404。 |
                | `operation` | string | 是 | `failover`、`reprotect`、`pause`、`resume`、`synconce`、`sync-data`、`sync-resource`、`undo`、`cancel` | 动作类型 | 写入父级 `spec.operationType` | `sync-data` 映射 `syncdata`，`sync-resource` 映射 `syncresource`；其他值直接写入。 |
                | `config.force`/`Force` | boolean | 否 | true/false | 是否强制 | 写入 `spec.force` | 默认 false。 |
                | `config.skipFinalSync`/`SkipFinalSync` | boolean | 否 | true/false | 是否跳过最终同步 | 写入 `spec.skipFinalSync` | 默认 false。 |
                | `config.skipScaleDownSource`/`SkipScaleDownSource` | boolean | 否 | true/false | failover 是否跳过源端缩容 | 写入 spec 和兼容 annotation | 仅 operation=failover 时读取。 |
                | `config.timeoutMinutes` | number | 否 | 分钟数 | 操作超时 | 写入父级 `spec.timeoutMinutes` | 当前 operator 创建子操作时未透传该字段。 |
                | `config.skipPodReadyCheck`/`SkipPodReadyCheck` | boolean | 否 | true/false | 是否跳过 Pod ready 校验 | 写入 `spec.skipPodReadyCheck` 并反推 waitUntilReady | 优先级高于 waitUntilReady。 |
                | `config.waitUntilReady`/`WaitUntilReady` | boolean | 否 | true/false | 是否等待 Pod ready | 写入 `spec.waitUntilReady` 并反推 skipPodReadyCheck | 当未提供 skipPodReadyCheck 时生效。 |

                ## 4. 返回的详细说明

                成功返回 HTTP 202，`data.operationName` 是父级组操作名，`data.message` 为受理消息。该响应不表示动作完成；进度通过 history、operation detail 或 WebSocket 读取。

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | JSON 绑定失败或 operation 缺失 | body 非法或 `operation` 未传。 |
                | 401 | 无 | 认证失败 | JWT 缺失、过期或无效。 |
                | 404 | `3004` | 组读取失败 | 第一次读取组失败，当前不区分 NotFound 与其他 Get 错误。 |
                | 500 | `5000` | 创建父级操作失败 | Kubernetes Create DisasterOperation 返回错误。 |

                成员实例不存在或子操作失败不会改变 HTTP 202，只体现在父级/子级 operation status。
            operationId: post_apis_disastergroups_testudo_softcdata_com_v1_groups_name_actions
            parameters:
                - description: 要触发动作的容灾组名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/DisasterGroupActionRequest'
                description: 组动作请求体。
                required: true
            responses:
                '202':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterGroupActionEnvelope'
                    description: 动作已受理。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: JSON 绑定失败或 operation 缺失。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 组读取失败。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 创建 DisasterOperation 失败。
            security:
                - bearerAuth: []
            summary: 触发容灾组动作
            tags:
                - 容灾组
            x-async-failure-status:
                readVia:
                    - GET /apis/disastergroups.testudo.softcdata.com/v1/groups/{name}/history
                    - GET /apis/disastergroups.testudo.softcdata.com/v1/groups/{name}/operations/{operationName}
                    - GET /apis/disastergroups.testudo.softcdata.com/v1/watch/groups/operations/{operationName}
                statusFields:
                    - status.state
                    - status.reason
                    - status.currentStep
                    - status.message
                    - status.groupStatus
            x-controlled-resources:
                direct:
                    - DisasterGroup CRD read
                    - DisasterOperation CRD parent operation create
                downstream:
                    - Child DisasterOperation CRDs
                    - DisasterInstance/DataSync/ResourceSync/AppBackup/AppRestore depending on operation
                    - Kubernetes workloads and Velero resources through child operations
                scope: disaster-system namespace parent operation
            x-operator-chain:
                controller: DisasterOperationReconciler.handleGroupOperation creates child operations per level and updates parent progress
                crd: DisasterGroup, DisasterOperation, DisasterInstance
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disastergroup_types.go
                serverFile: internal/apis/disaster_group/v1/router.go
                serverHandler: h.executeAction
            x-runapi-target-id: example-runapi-target-id
    /apis/disastergroups.testudo.softcdata.com/v1/groups/{name}/history:
        get:
            description: |-
                ## 1. 接口用来干什么

                查询容灾组父级操作历史。server 按 label `testudo.softcdata.com/group=<name>` 列举 `DisasterOperation`，转换为 `HistoryDTO[]` 并按时间倒序返回。

                ## 2. 控制哪些资源

                直接读取 `DisasterOperation` CRD；不读取 DisasterGroup 是否存在，不查询子操作列表，不访问业务集群或 Velero。组操作子 Operation 通常没有 `testudo.softcdata.com/group` label，因此不在本列表中。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `name` | string | 是 | 容灾组名称 | 历史所属组 | 构造 label selector | 组不存在也返回空数组。 |

                ## 4. 返回的详细说明

                `data[]` 为 `HistoryDTO`：`time` 来自 operation 创建时间，`type` 来自 operationType，`status.state/reason/message` 来自 operation status，`operationName/operationUID/hasDetail` 用于跳转详情，`operator` 当前固定为 `admin`。

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 401 | 无 | 认证失败 | JWT 缺失、过期或无效。 |
                | 500 | `5000` | 列举操作失败 | `DisasterOperations(disaster-system).List` 返回错误。 |

                组不存在、组已删除或没有历史均返回 `200 data=[]`。
            operationId: get_apis_disastergroups_testudo_softcdata_com_v1_groups_name_history
            parameters:
                - description: 容灾组名称，用于 testudo.softcdata.com/group label selector。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterGroupHistoryEnvelope'
                    description: 查询成功。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 列举 DisasterOperation 失败。
            security:
                - bearerAuth: []
            summary: 查询容灾组操作历史
            tags:
                - 容灾组
            x-controlled-resources:
                direct:
                    - DisasterOperation CRD list by testudo.softcdata.com/group label
                downstream:
                    - 无同步下游写入
                scope: disaster-system namespace
            x-operator-chain:
                controller: DisasterOperationReconciler writes parent group operation status; history only reads persisted operations
                crd: DisasterOperation
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_group/v1/router.go
                serverHandler: h.getHistory
            x-runapi-target-id: example-runapi-target-id
    /apis/disastergroups.testudo.softcdata.com/v1/groups/{name}/instances:
        get:
            description: |-
                ## 1. 接口用来干什么

                查询指定容灾组已选成员实例列表，供编辑页展示当前 levels 中的实例及其派生状态。server 先全 namespace 按 metadata.name 查组，取第一条，再在组 namespace 内读取成员实例。

                ## 2. 控制哪些资源

                直接读取 `DisasterGroup`、成员 `DisasterInstance` 和可选 `DisasterConfig`。不访问业务集群、Velero、S3、DataSync、ResourceSync 或 AppBackup。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `name` | string | 是 | 容灾组名称 | 目标组 | 全 namespace FieldSelector 查组 | 未找到返回 404。 |
                | `keyword` | string | 否 | 任意字符串 | 模糊关键字 | 匹配成员名、namespaces、description | 不区分大小写。 |
                | `status` | string | 否 | `all`、展示状态值 | 状态过滤 | 匹配派生 `status.state` | 空或 all 不过滤。 |
                | `page` | integer | 否 | 页码 | 分页 | `Paginate` | 默认 1。 |
                | `limit` | integer | 否 | 数量或 -1 | 分页 | `Paginate` | 默认 10；-1 返回全部。 |
                | `sort`/`order` | string | 否 | 任意值 | 通用参数 | 当前不排序 | 仅可能在 meta 回显。 |

                ## 4. 返回的详细说明

                返回 `GroupMemberInstanceCollectionEnvelope`，其中 `data.items[]` 的条目 schema 为 `GroupMemberInstanceDTO`。`status.state` 可为原始 fsmState，也可派生为 `ConfigError`、`Failed`、`NotFound`；成员读取失败时 namespaces/description 为空，reason=`InstanceNotFound`。

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 401 | 无 | 认证失败 | JWT 缺失、过期或无效。 |
                | 404 | `3004` | 组不存在 | 全 namespace 查组结果为空。 |
                | 500 | `5000` | 列举组失败 | DisasterGroups("").List 返回错误。 |

                成员实例或配置读取失败不改变 HTTP 状态。
            operationId: get_apis_disastergroups_testudo_softcdata_com_v1_groups_name_instances
            parameters:
                - description: 容灾组名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: 模糊搜索成员名、namespaces 和 description。
                  in: query
                  name: keyword
                  required: false
                  schema:
                    type: string
                - description: 派生成员状态过滤。all 或空不过滤。
                  in: query
                  name: status
                  required: false
                  schema:
                    type: string
                - description: 页码，默认 1。
                  in: query
                  name: page
                  required: false
                  schema:
                    type: integer
                - description: 每页数量，-1 返回全部。
                  in: query
                  name: limit
                  required: false
                  schema:
                    type: integer
                - description: 当前接口不使用该字段排序。
                  in: query
                  name: sort
                  required: false
                  schema:
                    type: string
                - description: 当前接口不使用该字段排序。
                  in: query
                  name: order
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GroupMemberInstanceCollectionEnvelope'
                    description: 查询成功。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 组不存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 列举组失败。
            security:
                - bearerAuth: []
            summary: 查询容灾组成员实例
            tags:
                - 容灾组
            x-controlled-resources:
                direct:
                    - DisasterGroup CRD all-namespace name lookup
                    - DisasterInstance CRD member lookup
                    - DisasterConfig CRD status lookup
                downstream:
                    - 无同步下游写入
                scope: group namespace members
            x-operator-chain:
                controller: DisasterGroupReconciler and DisasterInstanceReconciler maintain source statuses; endpoint derives display status
                crd: DisasterGroup, DisasterInstance, DisasterConfig
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disastergroup_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disastergroup/controller.go
                serverFile: internal/apis/disaster_group/v1/router.go
                serverHandler: h.listGroupInstances
            x-runapi-target-id: example-runapi-target-id
    /apis/disastergroups.testudo.softcdata.com/v1/groups/{name}/operations/{operationName}:
        get:
            description: |-
                ## 1. 接口用来干什么

                查询容灾组父级操作详情。server 读取指定 `DisasterOperation`，并校验 `op.spec.groupName == path.name`；不匹配按 404 处理。

                ## 2. 控制哪些资源

                直接读取 `DisasterOperation(disaster-system/{operationName})`。不读取 DisasterGroup，不查询子操作列表，不访问业务集群或 Velero。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `name` | string | 是 | 容灾组名称 | 归属组校验 | 与 `op.spec.groupName` 比较 | 不匹配返回 404。 |
                | `operationName` | string | 是 | DisasterOperation 名称 | 要查询的操作 | Kubernetes Get | 不存在返回 404。 |

                ## 4. 返回的详细说明

                返回 `OperationDetailDTO`：`ownerKind=DisasterGroup`、`ownerName` 为组名，`operationType/state/reason/currentStep/message/steps/autoCancel/roleStatus/groupStatus/startTime/completionTime` 来自 operation status。父级组操作的 `currentStep` 常见为 `Level-0`、`Level-1` 等；`groupStatus` CRD/DTO 支持，但当前组编排主要通过 `currentStep/message` 表达 Level 进度，不保证稳定写入。

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 401 | 无 | 认证失败 | JWT 缺失、过期或无效。 |
                | 404 | `3004` | 操作不存在或归属组不匹配 | Get NotFound 或 `op.spec.groupName != name`。 |
                | 500 | `5000` | 查询操作失败 | Get 返回非 NotFound 错误。 |
            operationId: get_apis_disastergroups_testudo_softcdata_com_v1_groups_name_operations_operationname
            parameters:
                - description: 容灾组名称，用于校验 operation spec.groupName。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: DisasterOperation 名称。
                  in: path
                  name: operationName
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/OperationDetailEnvelope'
                    description: 查询成功。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 操作不存在或归属组不匹配。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 查询 DisasterOperation 失败。
            security:
                - bearerAuth: []
            summary: 查询容灾组操作详情
            tags:
                - 容灾组
            x-controlled-resources:
                direct:
                    - DisasterOperation CRD read
                downstream:
                    - 无同步下游写入
                scope: disaster-system namespace
            x-operator-chain:
                controller: DisasterOperationReconciler writes operation detail status; endpoint reads parent operation only
                crd: DisasterOperation
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_group/v1/handler_operation.go
                serverHandler: h.getOperationDetail
            x-runapi-target-id: example-runapi-target-id
    /apis/disastergroups.testudo.softcdata.com/v1/groups/instance-picker:
        get:
            description: |-
                ## 1. 接口用来干什么

                查询可供容灾组选择的 DisasterInstance 轻量列表。server 全 namespace 列举实例，读取可选 DisasterConfig，派生成员展示状态，再按 keyword/status 过滤分页。

                ## 2. 控制哪些资源

                直接读取 `DisasterInstance` 和可选 `DisasterConfig`。不读取 DisasterGroup，不访问业务集群、Velero、S3、DataSync、ResourceSync 或 AppBackup。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `keyword` | string | 否 | 任意字符串 | 模糊关键字 | 匹配 name、namespaces、description | 不匹配 labels。 |
                | `status` | string | 否 | `all` 或状态值 | 状态过滤 | 匹配派生 status.state 或原始 fsmState | 空或 all 不过滤。 |
                | `page` | integer | 否 | 页码 | 分页 | `Paginate` | 默认 1。 |
                | `limit` | integer | 否 | 数量或 -1 | 分页 | `Paginate` | 默认 10；-1 返回全部。 |
                | `sort`/`order` | string | 否 | 任意值 | 通用参数 | 当前不排序 | 仅可能在 meta 回显。 |

                ## 4. 返回的详细说明

                返回 `InstancePickerCollectionEnvelope`，其中 `data.items[]` 的条目 schema 为 `InstancePickerItemDTO`。每个条目包含 `name/namespaces/description/status/fsmState`；description 来自实例 annotation；`status.state` 可因配置 NotFound/NotReady/Error 派生为 ConfigError。

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 401 | 无 | 认证失败 | JWT 缺失、过期或无效。 |
                | 500 | `5000` | 列举实例失败 | `DisasterInstances("").List` 返回错误。 |

                配置读取失败不返回 HTTP 错误；查询不到匹配项返回 `200 data.items=[]`。
            operationId: get_apis_disastergroups_testudo_softcdata_com_v1_groups_instance_picker
            parameters:
                - description: 模糊搜索 name、namespaces 和 description。
                  in: query
                  name: keyword
                  required: false
                  schema:
                    type: string
                - description: 状态过滤，匹配派生 status.state 或原始 fsmState。
                  in: query
                  name: status
                  required: false
                  schema:
                    type: string
                - description: 页码。
                  in: query
                  name: page
                  required: false
                  schema:
                    type: integer
                - description: 每页数量，-1 返回全部。
                  in: query
                  name: limit
                  required: false
                  schema:
                    type: integer
                - description: 当前接口不使用该字段排序。
                  in: query
                  name: sort
                  required: false
                  schema:
                    type: string
                - description: 当前接口不使用该字段排序方向。
                  in: query
                  name: order
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/InstancePickerCollectionEnvelope'
                    description: 查询成功。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 列举 DisasterInstance 失败。
            security:
                - bearerAuth: []
            summary: 查询容灾组实例选择器
            tags:
                - 容灾组
            x-controlled-resources:
                direct:
                    - DisasterInstance CRD all-namespace list
                    - DisasterConfig CRD status lookup
                downstream:
                    - 无同步下游写入
                scope: all namespaces DisasterInstance list
            x-operator-chain:
                controller: DisasterInstanceReconciler and DisasterConfigReconciler maintain source statuses; endpoint derives picker status
                crd: DisasterInstance, DisasterConfig
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterinstance_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterinstance/controller.go
                serverFile: internal/apis/disaster_group/v1/router.go
                serverHandler: h.instancePicker
            x-runapi-target-id: example-runapi-target-id
    /apis/disastergroups.testudo.softcdata.com/v1/watch/groups/operations:
        get:
            description: |-
                ## 1. 接口用来干什么

                建立 WebSocket，监听容灾组父级 `DisasterOperation` 变化。可通过 `groupName` 只监听某个组的父级操作；不传则监听全部操作。

                ## 2. 控制哪些资源

                直接读取/监听 `DisasterOperation(disaster-system)`。接口不直接访问业务集群或 Velero；下层执行结果由 operator 写入 operation status 后推送。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `groupName` | string | 否 | 容灾组名称 | 组过滤 | 作为 label selector `testudo.softcdata.com/group=<groupName>` | 不传监听全部操作。 |
                | `token` | string | 否 | JWT | WebSocket 鉴权 | query token | 与 Authorization/Sec-WebSocket-Protocol 三选一。 |
                | `Authorization` | header | 否 | `Bearer <JWT>` | 鉴权 | JWT 中间件读取 | 非 dev 环境需要。 |
                | `Sec-WebSocket-Protocol` | header | 否 | JWT 原文 | 浏览器 WebSocket 鉴权兼容 | JWT 中间件读取 | 可替代 Authorization。 |

                ## 4. 返回的详细说明

                握手成功返回 101。WebSocket 帧统一为 Envelope：连接成功帧 `data.status=connected, meta.type=connected`；心跳帧 `data=null, meta.type=heartbeat`；事件帧 `data.type=ADDED/MODIFIED/DELETED/ERROR, data.object=DisasterGroupOperationDTO`；关闭/超时帧通过 meta 标识。

                ## 5. 可能返回的错误

                | 阶段 | HTTP 状态码或业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 握手前 | HTTP 401 | 认证失败 | JWT 缺失、过期或无效。 |
                | 升级阶段 | HTTP 500 | WebSocket 升级失败 | upgrader.Upgrade 返回错误，响应 `{"message":"WebSocket 升级失败: ..."}`。 |
                | WebSocket 内 | `code=5000` | watcher 创建失败 | 初始 List 或 Watch DisasterOperation 失败。 |

                `groupName` 不存在不会报错，只是没有匹配事件。
            operationId: get_apis_disastergroups_testudo_softcdata_com_v1_watch_groups_operations
            parameters:
                - description: 可选容灾组名称，用于 testudo.softcdata.com/group label selector。
                  in: query
                  name: groupName
                  required: false
                  schema:
                    type: string
                - description: 浏览器 WebSocket 兼容鉴权 token。
                  in: query
                  name: token
                  required: false
                  schema:
                    type: string
                - description: JWT Authorization header，格式 Bearer <JWT>。
                  in: header
                  name: Authorization
                  required: false
                  schema:
                    type: string
                - description: 可携带 JWT 原文的 WebSocket 子协议 header。
                  in: header
                  name: Sec-WebSocket-Protocol
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '101':
                    description: WebSocket 连接升级成功。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MessageResponse'
                    description: WebSocket 升级失败。
            security:
                - bearerAuth: []
            summary: '[WebSocket] 监听容灾组操作'
            tags:
                - 容灾组
            x-controlled-resources:
                direct:
                    - DisasterOperation CRD watch stream
                downstream:
                    - 无同步下游写入
                scope: disaster-system operations, optional group label filter
            x-disaster-protocol: websocket
            x-message-schema:
                $ref: '#/components/schemas/DisasterGroupOperationWatchEnvelope'
            x-operator-chain:
                controller: DisasterOperationReconciler updates parent group operation status and child operation progress
                crd: DisasterOperation
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_group/v1/router.go
                serverHandler: h.watchGroupOperations
            x-runapi-target-id: example-runapi-target-id
    /apis/disastergroups.testudo.softcdata.com/v1/watch/groups/operations/{operationName}:
        get:
            description: |-
                ## 1. 接口用来干什么

                建立 WebSocket，监听指定操作或指定组的全部操作。handler 会先尝试把 `operationName` 当作 DisasterGroup 名称读取；如果组存在，则按 group label 监听该组操作；如果组不存在，则按 metadata.name 监听单个 DisasterOperation。

                ## 2. 控制哪些资源

                直接读取/监听 `DisasterGroup` 和 `DisasterOperation`。接口本身不访问业务集群或 Velero。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `operationName` | string | 是 | 组名或操作名 | 动态 watch 目标 | 先按组名探测，再按操作名 watch | 名称冲突时组名优先。 |
                | `token` | string | 否 | JWT | WebSocket 鉴权 | query token | 与 header 方式任选。 |
                | `Authorization` | header | 否 | `Bearer <JWT>` | 鉴权 | JWT 中间件读取 | 非 dev 环境需要。 |
                | `Sec-WebSocket-Protocol` | header | 否 | JWT 原文 | 浏览器兼容鉴权 | JWT 中间件读取 | 可替代 Authorization。 |

                ## 4. 返回的详细说明

                握手成功返回 101。组名模式使用 List+Watch 并跳过历史 ADDED；单操作模式直接 FieldSelector Watch，可能下发当前对象事件。事件帧对象为 `DisasterGroupOperationDTO`。

                ## 5. 可能返回的错误

                | 阶段 | HTTP 状态码或业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 握手前 | HTTP 401 | 认证失败 | JWT 缺失、过期或无效。 |
                | 升级阶段 | HTTP 500 | WebSocket 升级失败 | upgrader.Upgrade 返回错误。 |
                | WebSocket 内 | `code=5000` | watcher 创建失败 | 组名模式 List/Watch 失败，或单操作 Watch 失败。 |

                目标组或操作不存在不会返回 404；连接可建立但可能无业务事件。
            operationId: get_apis_disastergroups_testudo_softcdata_com_v1_watch_groups_operations_operationname
            parameters:
                - description: 组名或 DisasterOperation 名称。组名存在时监听该组全部操作，否则监听同名操作。
                  in: path
                  name: operationName
                  required: true
                  schema:
                    type: string
                - description: 浏览器 WebSocket 兼容鉴权 token。
                  in: query
                  name: token
                  required: false
                  schema:
                    type: string
                - description: JWT Authorization header，格式 Bearer <JWT>。
                  in: header
                  name: Authorization
                  required: false
                  schema:
                    type: string
                - description: 可携带 JWT 原文的 WebSocket 子协议 header。
                  in: header
                  name: Sec-WebSocket-Protocol
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '101':
                    description: WebSocket 连接升级成功。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MessageResponse'
                    description: WebSocket 升级失败。
            security:
                - bearerAuth: []
            summary: '[WebSocket] 监听指定容灾组操作'
            tags:
                - 容灾组
            x-controlled-resources:
                direct:
                    - DisasterGroup CRD dynamic probe
                    - DisasterOperation CRD watch stream
                downstream:
                    - 无同步下游写入
                scope: disaster-system operations by group label or operation name
            x-disaster-protocol: websocket
            x-message-schema:
                $ref: '#/components/schemas/DisasterGroupOperationWatchEnvelope'
            x-operator-chain:
                controller: DisasterOperationReconciler updates parent group operation status and child operation progress
                crd: DisasterGroup, DisasterOperation
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_group/v1/router.go
                serverHandler: h.watchGroupOperation
            x-runapi-target-id: example-runapi-target-id
    /apis/disastergroups.testudo.softcdata.com/v1/watch/groups/status:
        get:
            description: |-
                ## 1. 接口用来干什么

                建立 WebSocket，监听全部 DisasterGroup 状态变化。server 使用 List+Watch 获取 resourceVersion 后监听，避免连接建立时历史组全量 ADDED 洪泛。

                ## 2. 控制哪些资源

                直接监听 `DisasterGroup(disaster-system)`；事件对象会通过 `buildGroupDTO` 额外读取成员 `DisasterInstance` 和可选 `DisasterConfig` 补齐展示态。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `token` | string | 否 | JWT | WebSocket 鉴权 | query token | 与 header 方式任选。 |
                | `Authorization` | header | 否 | `Bearer <JWT>` | 鉴权 | JWT 中间件读取 | 非 dev 环境需要。 |
                | `Sec-WebSocket-Protocol` | header | 否 | JWT 原文 | 浏览器兼容鉴权 | JWT 中间件读取 | 可替代 Authorization。 |

                ## 4. 返回的详细说明

                握手成功返回 101。事件帧对象为完整 `DisasterGroupDTO`，其中 `status.summary/fsmState/availableOperations` 由 server 现场计算，成员读取失败会在 `instances[]` 中体现。

                ## 5. 可能返回的错误

                | 阶段 | HTTP 状态码或业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 握手前 | HTTP 401 | 认证失败 | JWT 缺失、过期或无效。 |
                | 升级阶段 | HTTP 500 | WebSocket 升级失败 | upgrader.Upgrade 返回错误。 |
                | WebSocket 内 | `code=5000` | watcher 创建失败 | 初始 List 或 Watch DisasterGroup 失败。 |

                当前没有容灾组不会报错，只是无业务事件。
            operationId: get_apis_disastergroups_testudo_softcdata_com_v1_watch_groups_status
            parameters:
                - description: 浏览器 WebSocket 兼容鉴权 token。
                  in: query
                  name: token
                  required: false
                  schema:
                    type: string
                - description: JWT Authorization header，格式 Bearer <JWT>。
                  in: header
                  name: Authorization
                  required: false
                  schema:
                    type: string
                - description: 可携带 JWT 原文的 WebSocket 子协议 header。
                  in: header
                  name: Sec-WebSocket-Protocol
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '101':
                    description: WebSocket 连接升级成功。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MessageResponse'
                    description: WebSocket 升级失败。
            security:
                - bearerAuth: []
            summary: '[WebSocket] 监听容灾组状态'
            tags:
                - 容灾组
            x-controlled-resources:
                direct:
                    - DisasterGroup CRD watch stream
                    - DisasterInstance/DisasterConfig reads for DTO conversion
                downstream:
                    - 无同步下游写入
                scope: all disaster-system DisasterGroups
            x-disaster-protocol: websocket
            x-message-schema:
                $ref: '#/components/schemas/DisasterGroupWatchEnvelope'
            x-operator-chain:
                controller: DisasterGroupReconciler updates group status when group or member instances change
                crd: DisasterGroup, DisasterInstance, DisasterConfig
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disastergroup_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disastergroup/controller.go
                serverFile: internal/apis/disaster_group/v1/router.go
                serverHandler: h.watchGroupStatuses
            x-runapi-target-id: example-runapi-target-id
    /apis/disastergroups.testudo.softcdata.com/v1/watch/groups/status/{name}:
        get:
            description: |-
                ## 1. 接口用来干什么

                建立 WebSocket，监听指定 DisasterGroup 状态变化。handler 不先 Get 校验目标存在，而是直接按 FieldSelector `metadata.name=<name>` Watch。

                ## 2. 控制哪些资源

                直接监听 `DisasterGroup(disaster-system)` 指定名称；事件转换时读取成员 `DisasterInstance` 与可选 `DisasterConfig` 补齐 DTO。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `name` | string | 是 | 容灾组名称 | 监听目标 | FieldSelector metadata.name | 不存在也可连接，只是无事件。 |
                | `token` | string | 否 | JWT | WebSocket 鉴权 | query token | 与 header 方式任选。 |
                | `Authorization` | header | 否 | `Bearer <JWT>` | 鉴权 | JWT 中间件读取 | 非 dev 环境需要。 |
                | `Sec-WebSocket-Protocol` | header | 否 | JWT 原文 | 浏览器兼容鉴权 | JWT 中间件读取 | 可替代 Authorization。 |

                ## 4. 返回的详细说明

                握手成功返回 101。事件帧对象为 `DisasterGroupDTO`；连接成功、心跳、closed、timeout、error 帧遵循 `StreamWatch` 通用结构。

                ## 5. 可能返回的错误

                | 阶段 | HTTP 状态码或业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 握手前 | HTTP 401 | 认证失败 | JWT 缺失、过期或无效。 |
                | 升级阶段 | HTTP 500 | WebSocket 升级失败 | upgrader.Upgrade 返回错误。 |
                | WebSocket 内 | `code=5000` | watcher 创建失败 | Watch DisasterGroup 失败。 |

                目标组不存在不会返回 404。
            operationId: get_apis_disastergroups_testudo_softcdata_com_v1_watch_groups_status_name
            parameters:
                - description: 要监听的容灾组名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: 浏览器 WebSocket 兼容鉴权 token。
                  in: query
                  name: token
                  required: false
                  schema:
                    type: string
                - description: JWT Authorization header，格式 Bearer <JWT>。
                  in: header
                  name: Authorization
                  required: false
                  schema:
                    type: string
                - description: 可携带 JWT 原文的 WebSocket 子协议 header。
                  in: header
                  name: Sec-WebSocket-Protocol
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '101':
                    description: WebSocket 连接升级成功。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MessageResponse'
                    description: WebSocket 升级失败。
            security:
                - bearerAuth: []
            summary: '[WebSocket] 监听指定容灾组状态'
            tags:
                - 容灾组
            x-controlled-resources:
                direct:
                    - DisasterGroup CRD watch stream
                    - DisasterInstance/DisasterConfig reads for DTO conversion
                downstream:
                    - 无同步下游写入
                scope: disaster-system DisasterGroup by metadata.name
            x-disaster-protocol: websocket
            x-message-schema:
                $ref: '#/components/schemas/DisasterGroupWatchEnvelope'
            x-operator-chain:
                controller: DisasterGroupReconciler updates target group status when group or member instances change
                crd: DisasterGroup, DisasterInstance, DisasterConfig
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disastergroup_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disastergroup/controller.go
                serverFile: internal/apis/disaster_group/v1/router.go
                serverHandler: h.watchGroupStatus
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterinstances.testudo.softcdata.com/v1/instances:
        get:
            description: |-
                ## 1. 接口用来干什么

                查询 DisasterInstance 列表。server 从配置的平台管理命名空间读取实例 CR，解析业务 namespace、分页、排序、keyword 和动态 label query 后过滤结果。`namespace` query 用于对实例 `spec.namespaces` 中的受保护业务命名空间做包含匹配，不用于限定 DisasterInstance CR 的存储 namespace；例如 `namespace=test-nginx` 可匹配 `test-nginx-1`。接口按 name/CR namespace/label value 做 keyword 包含匹配，并为当前页尽力聚合 config、DataSync、ResourceSync 和最近 failover autoCancel。响应 `meta.summary.protectedCount` 按过滤后、分页前的结果统计保护中的实例数量。接口只读，不触发 reconcile。

                ## 2. 控制哪些资源

                直接只读 DisasterInstance；当前页会尽力读取 DisasterConfig、DataSync、ResourceSync、DisasterOperation。下层 DataSync/ResourceSync/AppBackup/AppRestore/Velero 状态只通过 status 间接体现。

                ## 3. 入参的详细说明

                | 字段 | 是否必传 | 中文含义 | 目的/约束 |
                |---|---|---|---|
                | `namespace` | 否 | 受保护业务 namespace 片段 | 空或 `*` 表示不过滤业务 namespace；指定值时对 `spec.namespaces` 做包含匹配，例如 `test-nginx` 命中 `test-nginx-1`。该参数不是 CR 存储 namespace。 |
                | `limit/page` | 否 | 分页参数 | `limit=-1` 返回全部；page 控制分页起点。 |
                | `sort/order` | 否 | 排序参数 | sort 支持 `creationTimestamp/name/namespace`；默认 `creationTimestamp desc`。 |
                | `keyword` | 否 | 关键字 | 匹配 name、CR namespace、labels value，区分大小写。 |
                | `<labelKey>` | 否 | 动态 label 过滤 | 对 `item.Labels[labelKey]` 做模糊匹配，空值或 `*` 不过滤。 |

                ## 4. 返回的详细说明

                | 字段 | 类型/取值 | 中文含义 | 来源/说明 |
                |---|---|---|---|
                | `data.items[]` | DisasterInstanceDTO[] | 当前页实例 | 含 spec/status/currentState/sync summary/autoCancel。 |
                | `meta.pagination.total` | integer | 过滤后总数 | 分页前数量。 |
                | `meta.summary.protectedCount` | integer | 保护中的实例数量 | 按过滤后、分页前结果统计 `status.fsmState=Protected` 的实例数；`currentState=Running` 中的 Pending/Initializing 不计入。 |
                | `currentState` | Running/Active/Paused/Transitioning/Failed/Unknown | 前端聚合状态 | `ConfigError` 映射为 `Failed`。 |

                ## 5. 可能返回的错误

                | 状态 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 401 | 无固定业务码 | 认证失败 | JWT 缺失、过期或无效。 |
                | 500 | `5000` | 列表失败 | `DisasterInstances(common.DisasterSystemNamespace).List` 返回错误。 |
            operationId: get_apis_disasterinstances_testudo_softcdata_com_v1_instances
            parameters:
                - description: 每页数量。
                  in: query
                  name: limit
                  required: false
                  schema:
                    type: string
                - description: 页码。
                  in: query
                  name: page
                  required: false
                  schema:
                    type: string
                - description: 排序字段。
                  in: query
                  name: sort
                  required: false
                  schema:
                    type: string
                - description: 排序方向。
                  in: query
                  name: order
                  required: false
                  schema:
                    type: string
                - description: 关键字，匹配实例 name、CR namespace 和 labels value，区分大小写。
                  in: query
                  name: keyword
                  required: false
                  schema:
                    type: string
                - description: 受保护业务 namespace 片段。为空或 * 时不过滤；指定值时对 DisasterInstance.spec.namespaces 做包含匹配，不表示 CR 存储 namespace。
                  in: query
                  name: namespace
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterInstanceCollectionEnvelope'
                    description: 成功返回实例集合。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误。
            security:
                - bearerAuth: []
            summary: 容灾实例 GET /apis/disasterinstances.testudo.softcdata.com/v1/instances
            tags:
                - 容灾实例
            x-controlled-resources:
                direct:
                    - DisasterInstance、DisasterOperation、DataSync、ResourceSync
                downstream:
                    - 详见详细说明中的 operator 调用链
                scope: resolved namespace or all namespaces as described
            x-operator-chain:
                controller: DisasterInstanceReconciler / DisasterOperationReconciler / DataSyncReconciler / ResourceSyncReconciler
                crd: DisasterInstance, DisasterOperation, DataSync, ResourceSync
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterinstance_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterinstance/controller.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_instance/v1/router.go
                serverHandler: h.listInstances
            x-runapi-target-id: example-runapi-target-id
        post:
            description: |-
                ## 1. 接口用来干什么

                创建 DisasterInstance。server 校验 `syncPolicy` 禁用、JSON/binding、DisasterConfig 存在性、restorePolicy 和 veleroHooks，然后创建 CRD。当前临时放开受保护 namespace 冲突校验，同一 sourceCluster 下同一业务 namespace 可被多个 DisasterInstance 选择。201 只表示 CRD 创建成功，operator 后续创建 DataSync `dr-ds-<name>` 与 ResourceSync `dr-rs-<name>` 并推进 fsmState。

                ## 2. 控制哪些资源

                直接创建 DisasterInstance，写入 config label、description annotation、trace annotation 和可选 spec.veleroHooks；operator 后续创建/维护 DataSync、ResourceSync，DataSync 会将 veleroHooks.dataBackup 透传到 AppBackup template.hooks，并将 veleroHooks.dataRestore 透传到 AppRestore template.hooks。

                ## 3. 入参的详细说明

                | 字段 | 是否必传 | 中文含义 | 目的/约束 |
                |---|---|---|---|
                | `name` | 是 | 实例名称 | 写入 metadata.name；同名返回 409。 |
                | `namespace` | 否 | CRD namespace | 空时默认 `disaster-system`。 |
                | `config` | 是 | DisasterConfig 名称 | 校验存在并写入 spec.config；不存在返回 400。 |
                | `dataSyncPolicy/resourceSyncPolicy` | 否 | 实例级策略 override | 空时继承 config 对应策略。 |
                | `namespaces` | 否 | 保护 namespace 列表 | 当前临时允许同一 sourceCluster 下多个 DisasterInstance 选择相同 namespace。 |
                | `labelSelector` | 否 | 资源选择器 | 写入 spec；bulk/live modifier 校验可使用。 |
                | `operationTimeoutMinutes` | 否 | 实例级容灾操作超时时间 | 单位分钟，写入 `spec.operationTimeoutMinutes`；operator 在初始化、DataSync、ResourceSync、Failover/Reprotect 等操作中使用，未传时按 operator 默认 60 分钟处理。大容量 PV/FSB 同步场景应按数据量和并发能力显式设置。 |
                | `podRestoreMethod` | 否 | Pod 恢复方法 | `replica/initContainer`；空时 server 默认 `replica`。 |
                | `restorePolicy` | 否 | 恢复策略 | 支持 resourceSelection、class mapping、bulk actions、modifier rules；文本和结构化入口同时存在时必须语义一致；每实例 `modifierRules` 最多 1000 条，单条 veleroNative patches 最多 50 个；modifier rule live path 按 JSON Patch 语义校验，`add` 允许最终一级 map key 不存在但父路径必须存在，`replace/remove` 要求完整路径存在；bulk 静态快照生成会跳过 `/status/**`、`/metadata/finalizers/**`、`/metadata/ownerReferences/**` 等受保护路径，不要求通过排除 pods 规避 status 命中；`bulkModifierActions[].action=rewriteImage` 只校验并保存 `imageRewrite.sourcePrefix/targetPrefix` 等运行时动态镜像重写意图，不在创建时生成长期 `modifierRuleSnapshot`。 |
                | `veleroHooks.dataBackup` | 否 | DataSync 数据备份 Hook | Velero BackupHooks 原生结构，整体写入 DataSync 生成的 AppBackup `spec.template.hooks`；command argv 透明透传。 |
                | `veleroHooks.dataRestore` | 否 | DataSync 数据恢复 Hook | Velero RestoreHooks 原生结构，整体写入 DataSync 生成的 AppRestore `spec.template.hooks`；restore initContainer 可通过 Secret env/envFrom 传敏感值。 |
                | `skipPodReadyCheck` | 否 | Pod ready 默认校验策略 | 可被 action config 覆盖。 |
                | `description` | 否 | 实例描述 | 写入 annotation。 |

                ## 4. 返回的详细说明

                | 字段 | 类型/取值 | 中文含义 | 来源/说明 |
                |---|---|---|---|
                | `data` | DisasterInstanceDTO | 刚创建的实例 | operator 通常尚未回写 status，`currentState` 可能为 `Unknown`。 |
                | `data.spec.operationTimeoutMinutes` | integer | 实例级操作超时时间 | 来自 `DisasterInstance.spec.operationTimeoutMinutes`；未设置时响应中省略但 operator 默认按 60 分钟处理。 |
                | `data.spec.veleroHooks` | object/null | Hook 配置回显 | 来自刚写入的 DisasterInstance spec；未配置时省略。 |
                | `trace_id` | string | 请求追踪 ID | 同步写入 CR annotation。 |

                ## 5. 可能返回的错误

                | 状态 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 请求、策略或 Hook 非法 | binding、Config 不存在、ResourceSelectionInvalid、ModifierRulesTextInvalid、BulkModifierActionsTextInvalid、ModifierRuleRejected、Hook 结构非法、Hook timeout 超过上限、Kubernetes Invalid/BadRequest。 |
                | 400 | `1000` + meta.errorCode=`VeleroHookSensitiveParameter` | Hook 明文敏感参数 | command/args/env.value 中包含明显敏感片段，如 `password=`、`token=`、`access_key=`、`secret=`；meta.fieldPath 返回字段路径。 |
                | 403 | `2003` | 权限不足 | Kubernetes Create Forbidden。 |
                | 409 | `3009` | 冲突 | 同名实例。 |
                | 500 | `5000` | 创建失败 | 配置读取非 NotFound 或 Create 非分类错误。 |
            operationId: post_apis_disasterinstances_testudo_softcdata_com_v1_instances
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/DisasterInstanceCreateRequest'
                description: 创建容灾实例请求体。
                required: true
            responses:
                '201':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterInstanceEnvelope'
                    description: 成功创建实例。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '403':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足。
                '409':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误。
            security:
                - bearerAuth: []
            summary: 容灾实例 POST /apis/disasterinstances.testudo.softcdata.com/v1/instances
            tags:
                - 容灾实例
            x-controlled-resources:
                direct:
                    - DisasterInstance、DataSync、ResourceSync、BackupRestoreStatistics
                downstream:
                    - 详见详细说明中的 operator 调用链
                scope: resolved namespace or all namespaces as described
            x-operator-chain:
                controller: DisasterInstanceReconciler / DisasterOperationReconciler / DataSyncReconciler / ResourceSyncReconciler
                crd: DisasterInstance, DisasterOperation, DataSync, ResourceSync
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterinstance_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterinstance/controller.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_instance/v1/router.go
                serverHandler: h.createInstance
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterinstances.testudo.softcdata.com/v1/instances/{name}:
        delete:
            description: |-
                ## 1. 接口用来干什么

                删除 DisasterInstance。server 根据 namespace query 或 findNamespace 定位后提交 Delete；目标不存在或自动解析失败当前按幂等成功返回 200。

                ## 2. 控制哪些资源

                直接删除 DisasterInstance；operator handleDeletion 移除 finalizer。DataSync/ResourceSync 依赖 ownerReference 由 Kubernetes GC 级联删除；不直接清理 AppBackup/AppRestore/Velero/S3/业务集群资源。

                ## 3. 入参的详细说明

                | 字段 | 是否必传 | 中文含义 | 目的/约束 |
                |---|---|---|---|
                | `name` | 是 | 实例名称 | path 参数，用于定位和删除。 |
                | `namespace` | 否 | 实例 namespace | 为空时自动解析。 |

                ## 4. 返回的详细说明

                | 字段 | 类型/取值 | 中文含义 | 来源/说明 |
                |---|---|---|---|
                | `data.name` | string | 删除目标名称 | Delete 成功或目标视为不存在时返回。 |

                ## 5. 可能返回的错误

                | 状态 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 401 | 无固定业务码 | 认证失败 | JWT 缺失、过期或无效。 |
                | 500 | `5000` | 删除失败 | Kubernetes Delete 返回非 NotFound 错误。 |
            operationId: delete_apis_disasterinstances_testudo_softcdata_com_v1_instances_name
            parameters:
                - description: DisasterInstance 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: 实例所在 namespace。为空时自动解析；列表中空或 * 表示全部 namespace。
                  in: query
                  name: namespace
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterInstanceDeleteEnvelope'
                    description: 删除提交成功或目标已不存在。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误。
            security:
                - bearerAuth: []
            summary: 容灾实例 DELETE /apis/disasterinstances.testudo.softcdata.com/v1/instances/:name
            tags:
                - 容灾实例
            x-controlled-resources:
                direct:
                    - DisasterInstance、DisasterOperation、DataSync、ResourceSync
                downstream:
                    - 详见详细说明中的 operator 调用链
                scope: resolved namespace or all namespaces as described
            x-operator-chain:
                controller: DisasterInstanceReconciler / DisasterOperationReconciler / DataSyncReconciler / ResourceSyncReconciler
                crd: DisasterInstance, DisasterOperation, DataSync, ResourceSync
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterinstance_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterinstance/controller.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_instance/v1/router.go
                serverHandler: h.deleteInstance
            x-runapi-target-id: example-runapi-target-id
        get:
            description: |-
                ## 1. 接口用来干什么

                查询单个 DisasterInstance 详情。server 自动解析 namespace 后读取实例，并尽力聚合 DisasterConfig、StorageRepository 和最近 failover DisasterOperation 的 autoCancel 摘要。

                ## 2. 控制哪些资源

                只读 DisasterInstance、DisasterConfig、StorageRepository、DisasterOperation；无写入。状态来自 operator 已写入的 fsmState、主备集群、availableOperations、sync 子资源名称和 conditions。

                ## 3. 入参的详细说明

                | 字段 | 是否必传 | 中文含义 | 目的/约束 |
                |---|---|---|---|
                | `name` | 是 | 实例名称 | path 参数，用于 findNamespace 和 Get。 |
                | `namespace` | 否 | 实例 namespace | 为空时自动解析。 |

                ## 4. 返回的详细说明

                | 字段 | 类型/取值 | 中文含义 | 来源/说明 |
                |---|---|---|---|
                | `data.spec` | DisasterInstanceSpecDTO | 实例配置 | 含策略继承结果和 restorePolicy 文本回显。 |
                | `data.status` | DisasterInstanceStatusDTO | operator 状态 | 包含 fsmState/reason/message/cluster roles/sync names。 |
                | `data.currentState` | enum | 聚合状态 | 由 CurrentStateFromFSM 计算。 |
                | `data.config/storage/autoCancel` | object/null | 聚合字段 | 读取失败时省略。 |

                ## 5. 可能返回的错误

                | 状态 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 404 | `3004` | 实例不存在 | namespace 自动解析未找到或 Get 返回 NotFound。 |
                | 500 | `5000` | 读取失败 | 自动解析 List 失败或 Get 非 NotFound 错误。 |
            operationId: get_apis_disasterinstances_testudo_softcdata_com_v1_instances_name
            parameters:
                - description: DisasterInstance 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: 实例所在 namespace。为空时自动解析；列表中空或 * 表示全部 namespace。
                  in: query
                  name: namespace
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterInstanceEnvelope'
                    description: 成功返回实例详情。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误。
            security:
                - bearerAuth: []
            summary: 容灾实例 GET /apis/disasterinstances.testudo.softcdata.com/v1/instances/:name
            tags:
                - 容灾实例
            x-controlled-resources:
                direct:
                    - DisasterInstance、DisasterOperation、DataSync、ResourceSync
                downstream:
                    - 详见详细说明中的 operator 调用链
                scope: resolved namespace or all namespaces as described
            x-operator-chain:
                controller: DisasterInstanceReconciler / DisasterOperationReconciler / DataSyncReconciler / ResourceSyncReconciler
                crd: DisasterInstance, DisasterOperation, DataSync, ResourceSync
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterinstance_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterinstance/controller.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_instance/v1/router.go
                serverHandler: h.getInstance
            x-runapi-target-id: example-runapi-target-id
        put:
            description: |-
                ## 1. 接口用来干什么

                更新 DisasterInstance 的保护范围、策略 override、restorePolicy、veleroHooks、podRestoreMethod、skipPodReadyCheck 和 description。server 使用 RetryOnConflict 读取现有实例并按请求中出现的字段合并更新。

                ## 2. 控制哪些资源

                直接更新 DisasterInstance spec/annotations/trace；operator 后续同步依赖标签、DataSync/ResourceSync schedule 和 veleroHooks。DataSync 会在后续同步前对齐既有 ds-* AppBackup template.hooks，确保实例 hooks 更新影响下一次备份。

                ## 3. 入参的详细说明

                | 字段 | 是否必传 | 中文含义 | 目的/约束 |
                |---|---|---|---|
                | `name` | 是 | 实例名称 | path 参数；body 不含 name。 |
                | `namespace` | 否 | 实例 namespace | 为空时自动解析。 |
                | `dataSyncPolicy/resourceSyncPolicy` | 否 | 策略 override | 未传或 null 保持，空字符串清空。 |
                | `namespaces` | 否 | 保护 namespace | 未传或 null 保持，空数组清空；当前临时允许同一 sourceCluster 下多个 DisasterInstance 选择相同 namespace。 |
                | `labelSelector` | 否 | 资源选择器 | 传对象整体替换；未传或 null 保持。 |
                | `restorePolicy` | 否 | 恢复策略 | 未传或 null 不更新；传对象时合并出现的子字段；每实例 `modifierRules` 最多 1000 条，单条 veleroNative patches 最多 50 个；modifier rule live path 按 JSON Patch 语义校验，`add` 允许最终一级 map key 不存在但父路径必须存在，`replace/remove` 要求完整路径存在；bulk 静态快照生成会跳过 `/status/**`、`/metadata/finalizers/**`、`/metadata/ownerReferences/**` 等受保护路径，不要求通过排除 pods 规避 status 命中；`bulkModifierActions[].action=rewriteImage` 只校验并保存 `imageRewrite.sourcePrefix/targetPrefix` 等运行时动态镜像重写意图，不在更新时生成长期 `modifierRuleSnapshot`。 |
                | `veleroHooks` | 否 | 容灾 Hook 配置 | 未出现保持原值；传 null 或 `{}` 清空全部；`dataBackup`/`dataRestore` 子字段未出现保持对应子字段，传 null 或 `{}` 清空对应子字段，传对象整体替换。 |
                | `podRestoreMethod/skipPodReadyCheck/description` | 否 | 其他可更新字段 | description 空字符串清空 annotation。 |

                ## 4. 返回的详细说明

                | 字段 | 类型/取值 | 中文含义 | 来源/说明 |
                |---|---|---|---|
                | `data` | DisasterInstanceDTO | 更新后的实例 | operator status 可能尚未刷新。 |
                | `data.spec.veleroHooks` | object/null | 更新后的 Hook 配置 | 直接来自 DisasterInstance spec。 |
                | `data.spec.restorePolicy.modifierRuleSnapshot` | array | bulk 快照 | replaceExactValue/removeKey 等提交期静态 bulk action 生效时写入；仅包含 rewriteImage 时为空，因为镜像规则由 operator 在 ResourceSync/Drill 恢复构建阶段运行时动态编译。 |

                ## 5. 可能返回的错误

                | 状态 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 请求或校验非法 | syncPolicy、binding、config lister 不存在、restorePolicy 校验、Hook 结构非法、Hook timeout 超过上限、Kubernetes Invalid/BadRequest。 |
                | 400 | `1000` + meta.errorCode=`VeleroHookSensitiveParameter` | Hook 明文敏感参数 | command/args/env.value 中包含明显敏感片段；meta.fieldPath 返回字段路径。 |
                | 403 | `2003` | 权限不足 | Kubernetes Update Forbidden。 |
                | 404 | `3004` | 实例不存在 | findNamespace 或 Get 返回 NotFound。 |
                | 409 | `3009` | 资源冲突 | 当前 handler 正常路径不再因受保护 namespace 冲突返回 409；保留该响应用于兼容 Kubernetes 冲突类文档。 |
                | 500 | `5000` | 更新失败 | RetryOnConflict 后仍失败或非分类错误。 |
            operationId: put_apis_disasterinstances_testudo_softcdata_com_v1_instances_name
            parameters:
                - description: DisasterInstance 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: 实例所在 namespace。为空时自动解析；列表中空或 * 表示全部 namespace。
                  in: query
                  name: namespace
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/DisasterInstanceUpdateRequest'
                description: 更新容灾实例请求体。
                required: true
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterInstanceEnvelope'
                    description: 成功返回更新后的实例。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '403':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足。
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在。
                '409':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误。
            security:
                - bearerAuth: []
            summary: 容灾实例 PUT /apis/disasterinstances.testudo.softcdata.com/v1/instances/:name
            tags:
                - 容灾实例
            x-controlled-resources:
                direct:
                    - DisasterInstance、DisasterOperation、DataSync、ResourceSync
                downstream:
                    - 详见详细说明中的 operator 调用链
                scope: resolved namespace or all namespaces as described
            x-operator-chain:
                controller: DisasterInstanceReconciler / DisasterOperationReconciler / DataSyncReconciler / ResourceSyncReconciler
                crd: DisasterInstance, DisasterOperation, DataSync, ResourceSync
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterinstance_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterinstance/controller.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_instance/v1/router.go
                serverHandler: h.updateInstance
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterinstances.testudo.softcdata.com/v1/instances/{name}/actions:
        post:
            description: |-
                ## 1. 接口用来干什么

                对实例发起动作并创建 DisasterOperation。server 校验 operation 是否在实例的有效 availableOperations 中，支持 failover/reprotect/undo/pause/resume/synconce/sync-data/sync-resource/cancel；sync-data/sync-resource 会映射为 CRD syncdata/syncresource。为兼容存量对象，当实例处于 Failed 且 reason 属于 DataSyncFailed、ResourceSyncFailed、InitializationFailed、BackupFailed、BuildRestoreSpecFailed、RestoreFailed、DependencyFailed 或 StorageUnavailable 时，server 会把对应同步重试视为有效操作，即使旧 operator 仍只上报 reset。当实例处于 FailingOver 时，server 会把 cancel 视为有效操作，即使 operator 在进行中的 failover 期间暂时清空 availableOperations。

                ## 2. 控制哪些资源

                直接读取 DisasterInstance 并创建 DisasterOperation；DisasterOperationReconciler 后续修改实例状态、DataSync/ResourceSync trigger 或 paused、源/目标集群 workload 副本，并驱动下层同步链路。

                ## 3. 入参的详细说明

                | 字段 | 是否必传 | 中文含义 | 目的/约束 |
                |---|---|---|---|
                | `name` | 是 | 实例名称 | path 参数。 |
                | `namespace` | 否 | 实例 namespace | 为空时自动解析。 |
                | `operation` | 是 | 动作类型 | 不支持直接传 syncdata/syncresource；不支持 reset。 |
                | `config.force/skipFinalSync/skipScaleDownSource/timeoutMinutes/skipPodReadyCheck/waitUntilReady` | 否 | 动作参数 | 写入 DisasterOperation spec 或 annotation。 |

                ## 4. 返回的详细说明

                | 字段 | 类型/取值 | 中文含义 | 来源/说明 |
                |---|---|---|---|
                | `data.operationID` | string | Operation 名称 | 用于详情和 watch。 |
                | `data.status` | Processing | 受理状态 | 不代表动作完成。 |

                ## 5. 可能返回的错误

                | 状态 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 动作非法 | binding 或未知 operation。 |
                | 404 | `3004` | 实例不存在 | 自动解析或 Get 失败。 |
                | 409 | `3009` | 状态不允许 | operation 不在有效 availableOperations；可恢复同步失败的 sync-data/sync-resource 重试、FailingOver 下的 cancel 不走该错误。 |
                | 500 | `5000` | 创建失败 | 创建 DisasterOperation 失败。 |
            operationId: post_apis_disasterinstances_testudo_softcdata_com_v1_instances_name_actions
            parameters:
                - description: DisasterInstance 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: 实例所在 namespace。为空时自动解析；列表中空或 * 表示全部 namespace。
                  in: query
                  name: namespace
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/DisasterInstanceActionRequest'
                description: 实例动作请求体。
                required: true
            responses:
                '202':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterInstanceActionEnvelope'
                    description: 操作已受理。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在。
                '409':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误。
            security:
                - bearerAuth: []
            summary: 容灾实例 POST /apis/disasterinstances.testudo.softcdata.com/v1/instances/:name/actions
            tags:
                - 容灾实例
            x-controlled-resources:
                direct:
                    - DisasterInstance、DisasterOperation、DataSync、ResourceSync
                downstream:
                    - 详见详细说明中的 operator 调用链
                scope: resolved namespace or all namespaces as described
            x-operator-chain:
                controller: DisasterInstanceReconciler / DisasterOperationReconciler / DataSyncReconciler / ResourceSyncReconciler
                crd: DisasterInstance, DisasterOperation, DataSync, ResourceSync
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterinstance_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterinstance/controller.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_instance/v1/router.go
                serverHandler: h.executeAction
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterinstances.testudo.softcdata.com/v1/instances/{name}/groups:
        get:
            description: |-
                ## 1. 接口用来干什么

                查询实例属于哪些 DisasterGroup。server 确认实例存在后列出同 namespace 的 DisasterGroup，遍历 spec.levels 查找实例名，返回去重排序的 groups。

                ## 2. 控制哪些资源

                只读 DisasterInstance 和 DisasterGroup；不读取组 status，不访问 DataSync/ResourceSync/业务集群/Velero/S3。

                ## 3. 入参的详细说明

                | 字段 | 是否必传 | 中文含义 | 目的/约束 |
                |---|---|---|---|
                | `name` | 是 | 实例名称 | path 参数。 |
                | `namespace` | 否 | 实例 namespace | 为空时自动解析。 |

                ## 4. 返回的详细说明

                | 字段 | 类型/取值 | 中文含义 | 来源/说明 |
                |---|---|---|---|
                | `data.inGroup` | boolean | 是否属于组 | groups 非空时为 true。 |
                | `data.groups` | string[] | 组名列表 | 来自 DisasterGroup.spec.levels。 |

                ## 5. 可能返回的错误

                | 状态 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 404 | `3004` | 实例不存在 | 自动解析或 Get NotFound。 |
                | 500 | `5000` | 查询失败 | 列出 DisasterGroup 失败。 |
            operationId: get_apis_disasterinstances_testudo_softcdata_com_v1_instances_name_groups
            parameters:
                - description: DisasterInstance 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: 实例所在 namespace。为空时自动解析；列表中空或 * 表示全部 namespace。
                  in: query
                  name: namespace
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterInstanceGroupsEnvelope'
                    description: 成功返回组归属。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误。
            security:
                - bearerAuth: []
            summary: 容灾实例 GET /apis/disasterinstances.testudo.softcdata.com/v1/instances/:name/groups
            tags:
                - 容灾实例
            x-controlled-resources:
                direct:
                    - DisasterInstance、DisasterOperation、DataSync、ResourceSync
                downstream:
                    - 详见详细说明中的 operator 调用链
                scope: resolved namespace or all namespaces as described
            x-operator-chain:
                controller: DisasterInstanceReconciler / DisasterOperationReconciler / DataSyncReconciler / ResourceSyncReconciler
                crd: DisasterInstance, DisasterOperation, DataSync, ResourceSync
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterinstance_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterinstance/controller.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_instance/v1/router.go
                serverHandler: h.getInstanceGroups
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterinstances.testudo.softcdata.com/v1/instances/{name}/history:
        get:
            description: |-
                ## 1. 接口用来干什么

                查询实例动作历史。server 按 label `testudo.softcdata.com/instance=<name>` 列出 DisasterOperation，转换为 HistoryDTO 后按 time 倒序返回。当前不读取 DataSync/ResourceSync history，不分页。

                ## 2. 控制哪些资源

                只读 DisasterOperation；不直接访问业务集群、Velero、S3、DataSync 或 ResourceSync。

                ## 3. 入参的详细说明

                | 字段 | 是否必传 | 中文含义 | 目的/约束 |
                |---|---|---|---|
                | `name` | 是 | 实例名称 | 组成 Operation labelSelector。 |
                | `namespace` | 否 | Operation namespace | 为空时按实例自动解析；显式传入时不校验实例存在。 |

                ## 4. 返回的详细说明

                | 字段 | 类型/取值 | 中文含义 | 来源/说明 |
                |---|---|---|---|
                | `data[]` | HistoryDTO[] | 操作历史 | 数组响应，不是分页对象。 |
                | `result/reason/operator/note` | string | 兼容字段 | result=status.state，operator 固定 admin。 |

                ## 5. 可能返回的错误

                | 状态 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 404 | `3004` | 自动解析失败 | 未传 namespace 且 findNamespace 失败。 |
                | 500 | `5000` | 历史读取失败 | DisasterOperations List 失败。 |
            operationId: get_apis_disasterinstances_testudo_softcdata_com_v1_instances_name_history
            parameters:
                - description: DisasterInstance 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: 实例所在 namespace。为空时自动解析；列表中空或 * 表示全部 namespace。
                  in: query
                  name: namespace
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterInstanceHistoryEnvelope'
                    description: 成功返回历史数组。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误。
            security:
                - bearerAuth: []
            summary: 容灾实例 GET /apis/disasterinstances.testudo.softcdata.com/v1/instances/:name/history
            tags:
                - 容灾实例
            x-controlled-resources:
                direct:
                    - DisasterInstance、DisasterOperation、DataSync、ResourceSync
                downstream:
                    - 详见详细说明中的 operator 调用链
                scope: resolved namespace or all namespaces as described
            x-operator-chain:
                controller: DisasterInstanceReconciler / DisasterOperationReconciler / DataSyncReconciler / ResourceSyncReconciler
                crd: DisasterInstance, DisasterOperation, DataSync, ResourceSync
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterinstance_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterinstance/controller.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_instance/v1/router.go
                serverHandler: h.getHistory
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterinstances.testudo.softcdata.com/v1/instances/{name}/operations/{operationName}:
        get:
            description: |-
                ## 1. 接口用来干什么

                查询实例动作的 DisasterOperation 详情。server 读取 operation 后校验 `op.spec.instanceName == path.name`，不匹配按 404 返回。

                ## 2. 控制哪些资源

                只读 DisasterOperation status；不直接读取 DataSync/ResourceSync、业务集群、Velero 或 S3。

                ## 3. 入参的详细说明

                | 字段 | 是否必传 | 中文含义 | 目的/约束 |
                |---|---|---|---|
                | `name` | 是 | 实例名称 | 校验 operation 归属。 |
                | `operationName` | 是 | Operation 名称 | 用于 Get。 |
                | `namespace` | 否 | Operation namespace | 为空时按实例自动解析。 |

                ## 4. 返回的详细说明

                | 字段 | 类型/取值 | 中文含义 | 来源/说明 |
                |---|---|---|---|
                | `data.state/currentStep/steps` | Operation 状态 | operator 写入 | 展示动作进度。 |
                | `data.autoCancel/roleStatus/groupStatus` | object | 补充状态 | groupStatus 对实例 operation 通常为空。 |

                ## 5. 可能返回的错误

                | 状态 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 404 | `3004` | 不存在或归属不匹配 | findNamespace 失败、Operation NotFound、instanceName 不匹配。 |
                | 500 | `5000` | 读取失败 | Operation Get 非 NotFound 错误。 |
            operationId: get_apis_disasterinstances_testudo_softcdata_com_v1_instances_name_operations_operationname
            parameters:
                - description: DisasterInstance 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: DisasterOperation 名称。
                  in: path
                  name: operationName
                  required: true
                  schema:
                    type: string
                - description: 实例所在 namespace。为空时自动解析；列表中空或 * 表示全部 namespace。
                  in: query
                  name: namespace
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/OperationDetailEnvelope'
                    description: 成功返回 Operation 详情。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误。
            security:
                - bearerAuth: []
            summary: 容灾实例 GET /apis/disasterinstances.testudo.softcdata.com/v1/instances/:name/operations/:operationName
            tags:
                - 容灾实例
            x-controlled-resources:
                direct:
                    - DisasterInstance、DisasterOperation、DataSync、ResourceSync
                downstream:
                    - 详见详细说明中的 operator 调用链
                scope: resolved namespace or all namespaces as described
            x-operator-chain:
                controller: DisasterInstanceReconciler / DisasterOperationReconciler / DataSyncReconciler / ResourceSyncReconciler
                crd: DisasterInstance, DisasterOperation, DataSync, ResourceSync
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterinstance_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterinstance/controller.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_instance/v1/router.go
                serverHandler: h.getOperationDetail
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterinstances.testudo.softcdata.com/v1/instances/{name}/restore-classes/validate:
        post:
            description: |-
                ## 1. 接口用来干什么

                预检实例恢复策略中的 StorageClass/IngressClass 映射。server 解析目标集群后读取目标集群 StorageClassList 和 IngressClassList，返回缺失 class 和 valid 结果。

                ## 2. 控制哪些资源

                读取 DisasterInstance、必要时读取 DisasterConfig、读取目标集群 StorageClass/IngressClass；不创建 DataSync、ResourceSync、AppRestore、Velero Restore，也不修改实例。

                ## 3. 入参的详细说明

                | 字段 | 是否必传 | 中文含义 | 目的/约束 |
                |---|---|---|---|
                | `name` | 是 | 实例名称 | 定位实例。 |
                | `namespace` | 否 | 实例 namespace | 为空时自动解析。 |
                | `targetCluster` | 否 | 目标集群 | 为空时使用 status.secondaryCluster，再回退 config.spec.targetCluster。 |
                | `storageClassMapping/ingressClassMapping` | 条件必传 | class 映射 | 至少传一个；unmatchedPolicy 仅接受 Keep/Fail/空。 |
                | `mappings[].sourceClass/targetClass` | 是 | 源/目标 class | 为空或重复 source 冲突返回 400。 |
                | `strictTargetValidation` | 否 | 严格检查 | 缺失目标且 true 时 HTTP 仍 200，但 data.valid=false。 |

                ## 4. 返回的详细说明

                | 字段 | 类型/取值 | 中文含义 | 来源/说明 |
                |---|---|---|---|
                | `data.valid` | boolean | 预检结果 | strict=true 且目标缺失时 false。 |
                | `missingTargets` | string[] | 缺失 class | strict=false 时也会返回。 |

                ## 5. 可能返回的错误

                | 状态 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 预检非法 | 映射缺失、unmatchedPolicy 非法、目标集群解析/client/List 失败。 |
                | 404 | `3004` | 实例不存在 | 自动解析或 Get NotFound。 |
                | 500 | `5000` | 读取失败 | 实例读取非 NotFound 错误。 |
            operationId: post_apis_disasterinstances_testudo_softcdata_com_v1_instances_name_restore_classes_validate
            parameters:
                - description: DisasterInstance 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: 实例所在 namespace。为空时自动解析；列表中空或 * 表示全部 namespace。
                  in: query
                  name: namespace
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/ValidateRestoreClassesRequest'
                description: 恢复 class 预检请求体。
                required: true
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ValidateRestoreClassesEnvelope'
                    description: 预检完成。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误。
            security:
                - bearerAuth: []
            summary: 容灾实例 POST /apis/disasterinstances.testudo.softcdata.com/v1/instances/:name/restore-classes/validate
            tags:
                - 容灾实例
            x-controlled-resources:
                direct:
                    - DisasterInstance、DisasterOperation、DataSync、ResourceSync
                downstream:
                    - 详见详细说明中的 operator 调用链
                scope: resolved namespace or all namespaces as described
            x-operator-chain:
                controller: DisasterInstanceReconciler / DisasterOperationReconciler / DataSyncReconciler / ResourceSyncReconciler
                crd: DisasterInstance, DisasterOperation, DataSync, ResourceSync
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterinstance_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterinstance/controller.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_instance/v1/router.go
                serverHandler: h.validateRestoreClasses
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterinstances.testudo.softcdata.com/v1/instances/{name}/sync-status:
        get:
            description: |-
                ## 1. 接口用来干什么

                查询实例关联 DataSync 和 ResourceSync 的明细状态，并按子资源 UID label 读取 BackupRestoreStatistics 聚合成功/失败次数。响应会在 `data.dataSync.lastSyncStatus` 与 `data.resourceSync.lastSyncStatus` 中返回对应子资源最近一次 `status.history` 记录的执行结果。

                ## 2. 控制哪些资源

                读取 DisasterInstance、DataSync、ResourceSync、BackupRestoreStatistics；不直接访问业务集群、Velero 或 S3。

                ## 3. 入参的详细说明

                | 字段 | 是否必传 | 中文含义 | 目的/约束 |
                |---|---|---|---|
                | `name` | 是 | 实例名称 | 定位实例。 |
                | `namespace` | 否 | 实例 namespace | 为空时自动解析。 |

                ## 4. 返回的详细说明

                | 字段 | 类型/取值 | 中文含义 | 来源/说明 |
                |---|---|---|---|
                | `data.dataSync/resourceSync` | SubResourceStatusDTO | 子资源状态 | 子资源读取失败时省略；`status` 表示当前子资源状态。 |
                | `data.dataSync.lastSyncStatus` | LastSyncStatusDTO | 最近一次数据同步结果 | 来自 DataSync `status.history` 最新记录；history 为空时省略。 |
                | `data.resourceSync.lastSyncStatus` | LastSyncStatusDTO | 最近一次资源同步结果 | 来自 ResourceSync `status.history` 最新记录；history 为空时省略。 |
                | `lastSyncStatus.backupHookStatus/restoreHookStatus` | object/null | Hook 执行汇总 | DataSync 来源记录从 AppBackup/AppRestore Velero hookStatus 复制；ResourceSync 未投影 hooks 时通常为空。 |
                | `failureCount/syncSuccessCount/syncFailureCount` | integer | 统计 | 从 BackupRestoreStatistics 聚合；Failed 且统计为 0 时 failureCount 至少 1。 |

                ## 5. 可能返回的错误

                | 状态 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 404 | `3004` | 实例不存在 | 自动解析失败或实例 Get 失败。 |
                | 401 | 无固定业务码 | 认证失败 | JWT 缺失、过期或无效。 |
            operationId: get_apis_disasterinstances_testudo_softcdata_com_v1_instances_name_sync_status
            parameters:
                - description: DisasterInstance 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: 实例所在 namespace。为空时自动解析；列表中空或 * 表示全部 namespace。
                  in: query
                  name: namespace
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/SyncStatusEnvelope'
                    description: 成功返回同步状态。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在。
            security:
                - bearerAuth: []
            summary: 容灾实例 GET /apis/disasterinstances.testudo.softcdata.com/v1/instances/:name/sync-status
            tags:
                - 容灾实例
            x-controlled-resources:
                direct:
                    - DisasterInstance、DisasterOperation、DataSync、ResourceSync
                downstream:
                    - 详见详细说明中的 operator 调用链
                scope: resolved namespace or all namespaces as described
            x-operator-chain:
                controller: DisasterInstanceReconciler / DisasterOperationReconciler / DataSyncReconciler / ResourceSyncReconciler
                crd: DisasterInstance, DisasterOperation, DataSync, ResourceSync
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterinstance_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterinstance/controller.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_instance/v1/router.go
                serverHandler: h.getSyncStatus
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterinstances.testudo.softcdata.com/v1/instances/{name}/sync-history:
        get:
            description: |-
                ## 1. 接口用来干什么

                查询指定 DisasterInstance 的同步历史，服务实例详情的“资源与数据同步”页面。默认读取 DataSync 与 ResourceSync 的 `status.history` 记录；当 `source=operation` 时读取现有 DisasterOperation 历史，并只返回 `syncdata`、`syncresource`、`synconce` 三类同步操作。

                ## 2. 控制哪些资源

                读取 DisasterInstance、DataSync、ResourceSync 和 DisasterOperation；不创建同步操作，不访问业务集群、Velero、S3 或 AppBackup/AppRestore。

                ## 3. 入参的详细说明

                | 字段 | 是否必传 | 中文含义 | 目的/约束 |
                |---|---|---|---|
                | `name` | 是 | 实例名称 | 定位 DisasterInstance。 |
                | `namespace` | 否 | 实例 namespace | 为空时自动解析。 |
                | `syncType` | 否 | 同步类型过滤 | `all`、`dataSync`、`resourceSync`、`syncOnce`；默认 `all`。 |
                | `status` | 否 | 状态过滤 | `all`、`Pending`、`Running`、`Completed`、`Failed`、`Unknown`；默认 `all`。 |
                | `source` | 否 | 历史来源 | `syncRecord`、`operation`、`all`；默认 `syncRecord`。 |
                | `page` | 否 | 页码 | 默认 1。 |
                | `limit` | 否 | 每页数量 | 默认 10，`-1` 返回全部。 |

                ## 4. 返回的详细说明

                返回标准 collection envelope：`data.items[] + meta`。

                | 字段 | 类型/取值 | 中文含义 | 来源/说明 |
                |---|---|---|---|
                | `data.items[].syncType` | `dataSync/resourceSync/syncOnce` | 同步类型 | syncRecord 来源按子资源区分；operation 来源按 operationType 映射。 |
                | `data.items[].source` | `syncRecord/operation` | 历史来源 | `syncRecord` 来自子资源 status.history；`operation` 来自 DisasterOperation。 |
                | `data.items[].status.state` | `Pending/Running/Completed/Failed/Unknown` | 历史状态 | syncRecord 使用 record.status；operation 使用 operation.status.state。 |
                | `data.items[].backupHookStatus/restoreHookStatus` | object/null | Hook 执行汇总 | 仅 syncRecord 来源稳定返回；来自 DataSync/ResourceSync status.history 中的 `backupHookStatus` / `restoreHookStatus`。 |
                | `data.items[].operationName` | string | 操作名称 | 仅 source=operation 时返回，可继续查询 operation detail。 |
                | `data.items[].hasOperationDetail` | boolean | 是否有详情 | source=operation 时为 true。 |
                | `meta.summary.totalCount` | integer | 记录总数 | 过滤后、分页前统计。 |
                | `meta.summary.dataSyncCount/resourceSyncCount` | integer | 数据/资源同步记录数 | 过滤后、分页前统计。 |
                | `meta.summary.completedCount/failedCount` | integer | 成功/失败记录数 | 过滤后、分页前统计。 |

                ## 5. 可能返回的错误

                | 状态 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 参数非法 | source、syncType 或 status 不在允许值中。 |
                | 401 | 无固定业务码 | 认证失败 | JWT 缺失、过期或无效。 |
                | 404 | `3004` | 实例不存在 | 自动解析 namespace 失败或实例 Get 失败。 |
                | 500 | `5000` | 操作历史读取失败 | `source=operation/all` 时 DisasterOperation List 失败。 |
            operationId: get_apis_disasterinstances_testudo_softcdata_com_v1_instances_name_sync_history
            parameters:
                - description: DisasterInstance 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: 实例所在 namespace。为空时自动解析。
                  in: query
                  name: namespace
                  required: false
                  schema:
                    type: string
                - description: 同步类型过滤。
                  in: query
                  name: syncType
                  required: false
                  schema:
                    default: all
                    enum: [all, dataSync, resourceSync, syncOnce]
                    type: string
                - description: 状态过滤。
                  in: query
                  name: status
                  required: false
                  schema:
                    default: all
                    enum: [all, Pending, Running, Completed, Failed, Unknown]
                    type: string
                - description: 历史来源。
                  in: query
                  name: source
                  required: false
                  schema:
                    default: syncRecord
                    enum: [syncRecord, operation, all]
                    type: string
                - description: 页码，默认 1。
                  in: query
                  name: page
                  required: false
                  schema:
                    default: 1
                    type: integer
                - description: 每页数量，默认 10；-1 返回全部。
                  in: query
                  name: limit
                  required: false
                  schema:
                    default: 10
                    type: integer
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/SyncHistoryCollectionEnvelope'
                    description: 成功返回同步历史集合。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误。
            security:
                - bearerAuth: []
            summary: 容灾实例 GET /apis/disasterinstances.testudo.softcdata.com/v1/instances/:name/sync-history
            tags:
                - 容灾实例
            x-controlled-resources:
                direct:
                    - DisasterInstance、DataSync、ResourceSync、DisasterOperation
                downstream:
                    - 详见详细说明中的 operator 调用链
                scope: resolved namespace
            x-operator-chain:
                controller: DisasterInstanceReconciler / DisasterOperationReconciler / DataSyncReconciler / ResourceSyncReconciler
                crd: DisasterInstance, DisasterOperation, DataSync, ResourceSync
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterinstance_types.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/datasync_types.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/resourcesync_types.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                serverFile: internal/apis/disaster_instance/v1/router.go
                serverHandler: h.getSyncHistory
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterinstances.testudo.softcdata.com/v1/instances/{name}/validate-target:
        get:
            description: |-
                ## 1. 接口用来干什么

                执行实例动作前校验目标是否允许指定 operation。server 读取实例、组归属和 status.availableOperations，返回 ValidateTargetDTO；不创建 DisasterOperation。Failed 且 reason 为可恢复同步失败时，sync-data/sync-resource 会按同步重试兼容规则参与校验。FailingOver 状态下 cancel 会被补入有效操作集，用于允许正在进行的 failover 被用户取消。

                ## 2. 控制哪些资源

                读取 DisasterInstance 和 DisasterGroup；不访问 DataSync、ResourceSync、Velero、S3 或业务集群。

                ## 3. 入参的详细说明

                | 字段 | 是否必传 | 中文含义 | 目的/约束 |
                |---|---|---|---|
                | `name` | 是 | 实例名称 | 定位实例。 |
                | `namespace` | 否 | 实例 namespace | 为空时自动解析。 |
                | `operation` | 否 | 待校验动作 | 空值直接 valid=true；sync-data/sync-resource 归一化为 syncdata/syncresource。 |

                ## 4. 返回的详细说明

                | 字段 | 类型/取值 | 中文含义 | 来源/说明 |
                |---|---|---|---|
                | `data.valid` | boolean | 是否可执行 | false 时 HTTP 仍为 200。 |
                | `availableOperations` | string[] | 当前可执行动作 | operator 根据 fsmState 写入；server 可能为可恢复同步失败的 Failed 对象补入 syncdata/syncresource 兼容项，并为 FailingOver 对象补入 cancel。 |
                | `inGroup/groups` | boolean/string[] | 组归属 | 当前 server 不因 inGroup=true 自动禁止单实例动作。 |

                ## 5. 可能返回的错误

                | 状态 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 404 | `3004` | 实例不存在 | 自动解析或 Get NotFound。 |
                | 500 | `5000` | 校验失败 | 列出 DisasterGroup 失败等。 |
            operationId: get_apis_disasterinstances_testudo_softcdata_com_v1_instances_name_validate_target
            parameters:
                - description: DisasterInstance 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: 实例所在 namespace。为空时自动解析；列表中空或 * 表示全部 namespace。
                  in: query
                  name: namespace
                  required: false
                  schema:
                    type: string
                - description: 要校验的操作名。
                  in: query
                  name: operation
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ValidateTargetEnvelope'
                    description: 成功返回目标校验结果。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误。
            security:
                - bearerAuth: []
            summary: 容灾实例 GET /apis/disasterinstances.testudo.softcdata.com/v1/instances/:name/validate-target
            tags:
                - 容灾实例
            x-controlled-resources:
                direct:
                    - DisasterInstance、DisasterOperation、DataSync、ResourceSync
                downstream:
                    - 详见详细说明中的 operator 调用链
                scope: resolved namespace or all namespaces as described
            x-operator-chain:
                controller: DisasterInstanceReconciler / DisasterOperationReconciler / DataSyncReconciler / ResourceSyncReconciler
                crd: DisasterInstance, DisasterOperation, DataSync, ResourceSync
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterinstance_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterinstance/controller.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_instance/v1/router.go
                serverHandler: h.validateTarget
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterinstances.testudo.softcdata.com/v1/watch/instances:
        get:
            description: |-
                ## 1. 接口用来干什么

                建立 WebSocket 连接，聚合推送全 namespace 的 DisasterInstance、DataSync、ResourceSync watch 事件。DataSync/ResourceSync 事件按 `dr-ds-`/`dr-rs-` 名称前缀反查父实例并转换为 DisasterInstanceDTO。

                ## 2. 控制哪些资源

                只监听管理集群 CRD watch 流；无写入，不直接访问业务集群、Velero 或 S3。

                ## 3. 入参的详细说明

                | 字段 | 是否必传 | 中文含义 | 目的/约束 |
                |---|---|---|---|
                | `Authorization` | 非 dev 必传 | JWT | Bearer token。 |
                | `token` | 非 dev 可选 | JWT | 浏览器 WebSocket 兼容入口。 |
                | `Sec-WebSocket-Protocol` | 非 dev 可选 | JWT/子协议 | adapter 会尝试提取 token。 |

                ## 4. 返回的详细说明

                | 字段 | 类型/取值 | 中文含义 | 来源/说明 |
                |---|---|---|---|
                | 连接成功帧 | code=0 | data.status=connected | 升级成功后第一帧。 |
                | 事件帧 | code=0 | watch event object | 实例 watch 推 DisasterInstanceDTO；operation watch 推 OperationDetailDTO。 |
                | 心跳/关闭/超时 | code=0 | meta.type | StreamWatch 固定帧。 |

                ## 5. 可能返回的错误

                | 状态 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 握手前 401 | 无固定业务码 | 认证失败 | JWT 缺失、过期或无效。 |
                | 升级 500 | message | 升级失败 | upgrader.Upgrade 返回错误。 |
                | WebSocket 内 | `5000` | watcher 创建失败 | 任一 watcher 创建失败。 |
            operationId: get_apis_disasterinstances_testudo_softcdata_com_v1_watch_instances
            parameters:
                - description: 浏览器 WebSocket 兼容鉴权 token。
                  in: query
                  name: token
                  required: false
                  schema:
                    type: string
                - description: JWT Authorization header，格式为 Bearer <JWT>。
                  in: header
                  name: Authorization
                  required: false
                  schema:
                    type: string
                - description: 可携带 JWT 原文的 WebSocket 子协议 header。
                  in: header
                  name: Sec-WebSocket-Protocol
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '101':
                    description: WebSocket 连接升级成功。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MessageResponse'
                    description: WebSocket 升级失败。
            security:
                - bearerAuth: []
            summary: '[WebSocket] 容灾实例 GET /apis/disasterinstances.testudo.softcdata.com/v1/watch/instances'
            tags:
                - 容灾实例
            x-controlled-resources:
                direct:
                    - DisasterInstance、DisasterOperation、DataSync、ResourceSync
                downstream:
                    - 详见详细说明中的 operator 调用链
                scope: resolved namespace or all namespaces as described
            x-disaster-protocol: websocket
            x-message-schema:
                $ref: '#/components/schemas/DisasterInstanceWatchEnvelope'
            x-operator-chain:
                controller: DisasterInstanceReconciler / DisasterOperationReconciler / DataSyncReconciler / ResourceSyncReconciler
                crd: DisasterInstance, DisasterOperation, DataSync, ResourceSync
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterinstance_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterinstance/controller.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_instance/v1/router.go
                serverHandler: h.watchInstances
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterinstances.testudo.softcdata.com/v1/watch/instances/{name}:
        get:
            description: |-
                ## 1. 接口用来干什么

                建立 WebSocket 连接，推送指定 DisasterInstance 以及固定子资源 DataSync `dr-ds-<name>`、ResourceSync `dr-rs-<name>` 的 watch 事件。findNamespace 失败不会同步 404，而是退化为 all-namespaces watch。

                ## 2. 控制哪些资源

                只监听管理集群 CRD watch 流；无写入，不直接访问业务集群、Velero 或 S3。

                ## 3. 入参的详细说明

                | 字段 | 是否必传 | 中文含义 | 目的/约束 |
                |---|---|---|---|
                | `name` | 是 | 实例名称 | 组成三个 fieldSelector。 |
                | `Authorization/token/Sec-WebSocket-Protocol` | 非 dev | JWT | 三种 WebSocket 鉴权入口。 |

                ## 4. 返回的详细说明

                | 字段 | 类型/取值 | 中文含义 | 来源/说明 |
                |---|---|---|---|
                | 连接成功帧 | code=0 | data.status=connected | 升级成功后第一帧。 |
                | 事件帧 | code=0 | watch event object | 实例 watch 推 DisasterInstanceDTO；operation watch 推 OperationDetailDTO。 |
                | 心跳/关闭/超时 | code=0 | meta.type | StreamWatch 固定帧。 |

                ## 5. 可能返回的错误

                | 状态 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 握手前 401 | 无固定业务码 | 认证失败 | JWT 缺失、过期或无效。 |
                | 升级 500 | message | 升级失败 | upgrader.Upgrade 返回错误。 |
                | WebSocket 内 | `5000` | watcher 创建失败 | 任一 watcher 创建失败。 |
            operationId: get_apis_disasterinstances_testudo_softcdata_com_v1_watch_instances_name
            parameters:
                - description: DisasterInstance 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: 浏览器 WebSocket 兼容鉴权 token。
                  in: query
                  name: token
                  required: false
                  schema:
                    type: string
                - description: JWT Authorization header，格式为 Bearer <JWT>。
                  in: header
                  name: Authorization
                  required: false
                  schema:
                    type: string
                - description: 可携带 JWT 原文的 WebSocket 子协议 header。
                  in: header
                  name: Sec-WebSocket-Protocol
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '101':
                    description: WebSocket 连接升级成功。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MessageResponse'
                    description: WebSocket 升级失败。
            security:
                - bearerAuth: []
            summary: '[WebSocket] 容灾实例 GET /apis/disasterinstances.testudo.softcdata.com/v1/watch/instances/:name'
            tags:
                - 容灾实例
            x-controlled-resources:
                direct:
                    - DisasterInstance、DisasterOperation、DataSync、ResourceSync
                downstream:
                    - 详见详细说明中的 operator 调用链
                scope: resolved namespace or all namespaces as described
            x-disaster-protocol: websocket
            x-message-schema:
                $ref: '#/components/schemas/DisasterInstanceWatchEnvelope'
            x-operator-chain:
                controller: DisasterInstanceReconciler / DisasterOperationReconciler / DataSyncReconciler / ResourceSyncReconciler
                crd: DisasterInstance, DisasterOperation, DataSync, ResourceSync
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterinstance_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterinstance/controller.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_instance/v1/router.go
                serverHandler: h.watchInstance
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterinstances.testudo.softcdata.com/v1/watch/instances/operations/{operationName}:
        get:
            description: |-
                ## 1. 接口用来干什么

                建立 WebSocket 连接，监听指定 DisasterOperation 并推送 OperationDetailDTO。handler 只有 operationName，不校验实例名，因此也可能推送 ownerKind=DisasterGroup 的 operation。

                ## 2. 控制哪些资源

                只监听管理集群 CRD watch 流；无写入，不直接访问业务集群、Velero 或 S3。

                ## 3. 入参的详细说明

                | 字段 | 是否必传 | 中文含义 | 目的/约束 |
                |---|---|---|---|
                | `operationName` | 是 | Operation 名称 | fieldSelector metadata.name。 |
                | `namespace` | 否 | Operation namespace | 为空时先查 disaster-system，再跨 namespace 查找。 |
                | `Authorization/token/Sec-WebSocket-Protocol` | 非 dev | JWT | 三种 WebSocket 鉴权入口。 |

                ## 4. 返回的详细说明

                | 字段 | 类型/取值 | 中文含义 | 来源/说明 |
                |---|---|---|---|
                | 连接成功帧 | code=0 | data.status=connected | 升级成功后第一帧。 |
                | 事件帧 | code=0 | watch event object | 实例 watch 推 DisasterInstanceDTO；operation watch 推 OperationDetailDTO。 |
                | 心跳/关闭/超时 | code=0 | meta.type | StreamWatch 固定帧。 |

                ## 5. 可能返回的错误

                | 状态 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 握手前 401 | 无固定业务码 | 认证失败 | JWT 缺失、过期或无效。 |
                | 握手前 404 | `3004` | 自动查找失败 | 未传 namespace 且找不到 operation。 |
                | 升级 500 | message | 升级失败 | upgrader.Upgrade 返回错误。 |
                | WebSocket 内 | `5000` | watcher 创建失败 | Operation watch 创建失败。 |
            operationId: get_apis_disasterinstances_testudo_softcdata_com_v1_watch_instances_operations_operationname
            parameters:
                - description: DisasterOperation 名称。
                  in: path
                  name: operationName
                  required: true
                  schema:
                    type: string
                - description: 实例所在 namespace。为空时自动解析；列表中空或 * 表示全部 namespace。
                  in: query
                  name: namespace
                  required: false
                  schema:
                    type: string
                - description: 浏览器 WebSocket 兼容鉴权 token。
                  in: query
                  name: token
                  required: false
                  schema:
                    type: string
                - description: JWT Authorization header，格式为 Bearer <JWT>。
                  in: header
                  name: Authorization
                  required: false
                  schema:
                    type: string
                - description: 可携带 JWT 原文的 WebSocket 子协议 header。
                  in: header
                  name: Sec-WebSocket-Protocol
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '101':
                    description: WebSocket 连接升级成功。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MessageResponse'
                    description: WebSocket 升级失败。
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在。
            security:
                - bearerAuth: []
            summary: '[WebSocket] 容灾实例 GET /apis/disasterinstances.testudo.softcdata.com/v1/watch/instances/operations/:operationName'
            tags:
                - 容灾实例
            x-controlled-resources:
                direct:
                    - DisasterInstance、DisasterOperation、DataSync、ResourceSync
                downstream:
                    - 详见详细说明中的 operator 调用链
                scope: resolved namespace or all namespaces as described
            x-disaster-protocol: websocket
            x-message-schema:
                $ref: '#/components/schemas/OperationWatchEnvelope'
            x-operator-chain:
                controller: DisasterInstanceReconciler / DisasterOperationReconciler / DataSyncReconciler / ResourceSyncReconciler
                crd: DisasterInstance, DisasterOperation, DataSync, ResourceSync
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterinstance_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterinstance/controller.go
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasteroperation_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasteroperation/controller.go
                serverFile: internal/apis/disaster_instance/v1/router.go
                serverHandler: h.watchOperation
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterjobs.testudo.softcdata.com/v1/jobs:
        get:
            description: |-
                ## 1. 接口用来干什么

                查询 `disaster-system` namespace 下的 `DisasterJob` 容灾任务列表。server 从 informer lister 读取 CRD，按动态 label query 做内存包含过滤，支持按 `name` 和 `creationTimestamp` 排序，再分页返回。

                该接口是只读查询，不创建、不更新、不删除任务，也不触发 operator reconcile。返回中的 `status.phase/reason/startTime/conditions` 是 `DisasterJobReconciler` 后续写入的观测状态。

                ## 2. 控制哪些资源

                直接操作资源：
                - 只读 `DisasterJob` CRD informer 缓存。

                下层影响资源：
                - 无。该接口本身不访问源集群、目标集群和 Velero 资源。
                - 返回状态间接反映 operator 对 `DisasterBackup`、`DisasterConfig`、源集群 Velero `Backup` 和目标集群 Velero `Restore` 的处理结果。

                资源作用范围：
                - 读取配置的管理命名空间（默认 `disaster-system`）。
                - `DisasterJobReconciler` 会同步 dependency labels，包含自身 dependency token、`spec.disasterBackup` 指向的 `DisasterBackup`、以及 label `testudo.softcdata.com/disaster-policy-name` 指向的 `DisasterPolicy`。
                - operator 按 `syncType` 决定同步方向：`forward` 表示 DisasterConfig sourceCluster 到 targetCluster；非 `forward` 值当前都会走 targetCluster 到 sourceCluster。

                ## 3. 入参的详细说明

                | 字段 | 位置 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|---|
                | `limit` | query | integer | 否 | `-1` 或正整数 | 每页数量 | 控制内存分页数量 | 默认 `10`；`-1` 返回全部。 |
                | `page` | query | integer | 否 | 正整数 | 页码 | 控制内存分页起点 | 默认 `1`；小于等于 `0` 时按 `1` 处理。 |
                | `sort` | query | string | 否 | `name`、`creationTimestamp` | 排序字段 | 控制内存排序字段 | 其他值不报错，比较结果恒为 0，等同不排序。 |
                | `order` | query | string | 否 | `desc`、其他字符串 | 排序方向 | 控制升序和降序 | 仅 `desc` 触发倒序，其他值按正序。 |
                | `keyword` | query | string | 否 | 任意字符串 | 关键字兼容参数 | 当前 handler 解析但不参与过滤 | 不影响结果。 |
                | `<labelKey>` | query | string | 否 | 任意 label key | 动态标签过滤 | 对 `item.Labels[labelKey]` 做包含匹配 | 空值和 `*` 表示不过滤；前后 `*` 会被去掉后做包含匹配。 |

                ## 4. 返回的详细说明

                成功响应使用统一 `Envelope`，`data.items` 为 `DisasterJobDTO[]`，`meta` 为集合分页元信息。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | 成功固定为 `0`。 |
                | `data.items[]` | array | `DisasterJobDTO[]` | 当前页容灾任务列表 | server DTO | 字段含义同详情接口。 |
                | `meta.type` | string | `collection` | 响应类型 | `BuildCollectionResponse` | 固定集合响应。 |
                | `meta.resourceType` | string | `disasterJob` | 资源类型 | server | 固定值。 |
                | `meta.pagination.limit` | integer | 请求 limit | 每页数量 | query | `-1` 时 `next` 为空。 |
                | `meta.pagination.total` | integer | 总数 | 过滤后的总数量 | server 内存过滤结果 | 分页前数量。 |
                | `meta.links.self/first/previous/next/last` | string | URL | 分页链接 | server | 根据当前 path 和分页参数生成。 |
                | `meta.links.<name>` | string | URL | 单项详情链接 | server | key 为每个 job name，value 为详情路径。 |
                | `meta.sort.name` | string | 请求 sort | 排序字段回显 | query | 只回显，不代表字段一定生效。 |
                | `meta.filters` | object | query 动态过滤项 | 过滤条件回显 | query | 只包含动态 label 过滤项。 |
                | `trace_id` | string | trace id | 请求追踪 ID | trace middleware | 用于排查。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 列表读取失败 | informer lister `List` 返回错误。 |
                | 401 | `2001` | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。响应由 JWT 中间件返回统一信封，包含本地化 `message` 与稳定 `message_key`。 |

                `keyword` 不生效和不支持的 `sort` 字段不会返回错误。operator 备份、同步、恢复失败也不会使该列表接口失败，只会表现为 `data.items[].status.phase=Failed` 与 `status.reason`。
            operationId: get_apis_disasterjobs_testudo_softcdata_com_v1_jobs
            parameters:
                - description: 每页数量。默认 10；传 -1 时返回全部。
                  in: query
                  name: limit
                  required: false
                  schema:
                    type: integer
                - description: 页码。默认 1；小于等于 0 时按 1 处理。
                  in: query
                  name: page
                  required: false
                  schema:
                    type: integer
                - description: 排序字段。当前列表接口只识别 name、creationTimestamp；其他值不报错，等同不排序。
                  in: query
                  name: sort
                  required: false
                  schema:
                    type: string
                - description: 排序方向。desc 表示倒序，其他值按正序处理。
                  in: query
                  name: order
                  required: false
                  schema:
                    type: string
                    enum:
                        - asc
                        - desc
                - description: 关键字。当前 handler 会解析该参数，但不参与过滤。
                  in: query
                  name: keyword
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterJobCollectionEnvelope'
                    description: 成功返回 DisasterJob 集合。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 列表读取失败。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
            security:
                - bearerAuth: []
            summary: 容灾任务 GET /apis/disasterjobs.testudo.softcdata.com/v1/jobs
            tags:
                - 容灾任务
            x-async-failure-status:
                readVia:
                    - GET /apis/disasterjobs.testudo.softcdata.com/v1/jobs/{name}
                    - GET /apis/disasterjobs.testudo.softcdata.com/v1/jobs
                    - GET /apis/disasterjobs.testudo.softcdata.com/v1/watch/jobs
                    - GET /apis/v1/{resource}/{name}/history
                statusFields:
                    - status.phase
                    - status.reason
                    - status.conditions
            x-controlled-resources:
                direct:
                    - DisasterJob CRD informer cache
                downstream:
                    - 无
                scope: disaster-system namespace 下的 DisasterJob informer 缓存
            x-operator-chain:
                controller: DisasterJobReconciler syncs dependency labels, defaults syncType and phase, creates Velero Backup and Restore, and writes status
                crd: DisasterJob
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterjob_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterjob_controller.go
                serverFile: internal/apis/disaster_jobs/v1/router.go
                serverHandler: c.configs
            x-runapi-target-id: example-runapi-target-id
        post:
            description: |-
                ## 1. 接口用来干什么

                创建一个 `DisasterJob` 容灾任务。server 将请求体转换为 `DisasterJob` CRD，写入配置的管理命名空间（默认 `disaster-system`），并把当前请求 trace id 注入 annotation 供 operator 事件关联。

                HTTP 201 只表示 CRD 创建成功，不表示备份和恢复已完成。创建后由 `DisasterJobReconciler` 异步读取关联的 `DisasterBackup` 与 `DisasterConfig`，再创建或复用 Velero `Backup/Restore`。

                ## 2. 控制哪些资源

                直接操作资源：
                - 创建 `DisasterJob` CRD。
                - 写入 trace annotation。

                下层影响资源：
                - operator 同步 dependency labels，并在缺省时写回 `spec.syncType=forward`、`status.phase=Pending`。
                - operator 读取 `DisasterBackup` 和 `DisasterConfig`，按 `syncType` 计算源集群和目标集群。
                - operator 在源集群 `velero` namespace 创建或复用 `yaoshi-backup-<jobName>` Velero `Backup`；备份完成并同步到目标集群后，在目标集群创建或复用 `yaoshi-restore-<jobName>` Velero `Restore`。

                资源作用范围：
                - CRD 写入配置的管理命名空间（默认 `disaster-system`）。
                - Velero `Backup` 的 `includedNamespaces` 来自关联 `DisasterBackup.spec.namespace`，`labelSelector` 来自关联 `DisasterBackup.spec.labelSelector`，`storageLocation` 来自关联 `DisasterConfig.spec.storageRepository`。
                - `syncType=forward` 表示 sourceCluster 到 targetCluster；`reverse` 表示 targetCluster 到 sourceCluster。当前 operator 对其他非 `forward` 字符串也走反向链路。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `name` | string | 是 | Kubernetes 资源名称 | 容灾任务名称 | 写入 `metadata.name`，并参与生成 Velero 资源名 `yaoshi-backup-<name>`、`yaoshi-restore-<name>` | request struct 未设置 binding tag；为空或非法名称会由 Kubernetes API server 拒绝。 |
                | `disasterBackup` | string | 是 | DisasterBackup 名称 | 关联备份配置 | 写入 `spec.disasterBackup`，operator 据此读取备份定义 | server 不同步校验存在性；不存在时 operator 写 `status.phase=Failed`。 |
                | `syncType` | string | 否 | 建议 `forward`、`reverse`、空 | 同步方向 | 控制 operator 使用 source 到 target，还是 target 到 source | 空值由 operator 写回 `forward`；当前非 `forward` 都按反向处理，不建议传其他值。 |
                | `scheduleType` | string | 否 | 代码注释为 `once`、`cron` | 调度类型兼容字段 | 写入 CRD spec 供前端展示或后续扩展 | 当前 `DisasterJobReconciler` 不按该字段分支。 |

                ## 4. 返回的详细说明

                成功响应 HTTP 状态码为 201，使用统一 `Envelope`，`data` 为创建后的 `DisasterJobDTO`。由于 operator 异步处理，创建响应中的 `status.phase` 可能为空，随后会被 operator 更新为 `Pending/Backuping/Restoring/Succeed/Failed` 等阶段。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | 成功固定为 `0`。 |
                | `data.id` | string | Kubernetes UID | 任务 UID | DisasterJob metadata | 用于前端唯一标识和 dependency token 派生。 |
                | `data.name` | string | 资源名称 | 任务名称 | DisasterJob metadata | 创建时来自 `body.name`。 |
                | `data.namespace` | string | `disaster-system` | 所在 namespace | server 配置的管理命名空间 | 当前 handler 写入配置的管理命名空间（默认 `disaster-system`）。 |
                | `data.labels` | object | label map | 资源标签 | DisasterJob metadata | operator 后续会补 dependency labels。 |
                | `data.creation_timestamp` | string | RFC3339 时间 | 创建时间 | Kubernetes metadata | 列表可按该字段排序。 |
                | `data.spec.disasterBackup` | string | DisasterBackup 名称 | 关联备份配置 | DisasterJob spec | operator 用它读取备份定义。 |
                | `data.spec.syncType` | string | `forward`、`reverse`、空 | 同步方向 | DisasterJob spec | 空值后续会被 operator 写回 `forward`。 |
                | `data.spec.scheduleType` | string | `once`、`cron`、空 | 调度类型 | DisasterJob spec | 当前 controller 不按该字段分支。 |
                | `data.status.phase` | string | `Pending`、`Backuping`、`Restoring`、`Succeed`、`Failed` 等 | 执行阶段 | operator status | 创建响应中可能为空。 |
                | `data.status.reason` | string | 任意错误原因 | 失败原因 | operator status | 备份、恢复、集群连接失败时写入。 |
                | `message` | string | `OK` | 响应消息 | server | 成功固定为 `OK`。 |
                | `trace_id` | string | trace id | 请求追踪 ID | trace middleware | 同步写入 CR annotation。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 请求体非法 | JSON 绑定失败。 |
                | 401 | 无固定业务码 | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。 |
                | 409 | `3009` | 同名任务已存在 | Kubernetes Create 返回 AlreadyExists。 |
                | 500 | `5000` | CRD 创建失败 | 非 AlreadyExists 的 Kubernetes API server、RBAC、资源名称非法等创建错误。 |

                `DisasterBackup` 不存在、`DisasterConfig` 不存在、源/目标集群不可访问、Velero `Backup/Restore` 创建失败不会在创建接口同步返回，而是由 operator 写入 `status.phase=Failed` 和 `status.reason`。
            operationId: post_apis_disasterjobs_testudo_softcdata_com_v1_jobs
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/DisasterJobCreateRequest'
                description: 创建容灾任务请求体。
                required: true
            responses:
                '201':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterJobEnvelope'
                    description: 成功创建 DisasterJob。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求体非法。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
                '409':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 同名 DisasterJob 已存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: DisasterJob 创建失败。
            security:
                - bearerAuth: []
            summary: 容灾任务 POST /apis/disasterjobs.testudo.softcdata.com/v1/jobs
            tags:
                - 容灾任务
            x-async-failure-status:
                readVia:
                    - GET /apis/disasterjobs.testudo.softcdata.com/v1/jobs/{name}
                    - GET /apis/disasterjobs.testudo.softcdata.com/v1/jobs
                    - GET /apis/disasterjobs.testudo.softcdata.com/v1/watch/jobs/{name}
                    - GET /apis/v1/{resource}/{name}/history
                statusFields:
                    - status.phase
                    - status.reason
                    - status.conditions
            x-controlled-resources:
                direct:
                    - DisasterJob CRD
                downstream:
                    - Dependency labels on DisasterJob
                    - Source cluster Velero Backup yaoshi-backup-<jobName>
                    - Target cluster Velero Restore yaoshi-restore-<jobName>
                    - Target cluster Velero Backup observed before restore
                scope: disaster-system namespace 下的 DisasterJob，以及关联 DisasterConfig 指定的源集群和目标集群 velero namespace
            x-operator-chain:
                controller: DisasterJobReconciler syncs dependency labels, defaults syncType and phase, creates Velero Backup and Restore, and writes status
                crd: DisasterJob
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterjob_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterjob_controller.go
                serverFile: internal/apis/disaster_jobs/v1/router.go
                serverHandler: c.createConfig
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterjobs.testudo.softcdata.com/v1/jobs/{name}:
        delete:
            description: |-
                ## 1. 接口用来干什么

                删除指定 `DisasterJob` CRD。server 同步提交 Kubernetes Delete 请求并返回 `{ "name": "<name>" }`；如果资源带有 `disasterjob.disaster.io/finalizer`，实际删除会等待 operator 删除分支完成。

                删除接口返回 200 只表示删除请求已提交，不表示下层 Velero `Backup/Restore` 已清理完成。

                ## 2. 控制哪些资源

                直接操作资源：
                - 删除 `DisasterJob disaster-system/{name}`。

                下层影响资源：
                - operator 删除分支会将 `status.phase` 置为 `Deleting`。
                - operator 根据关联 `DisasterBackup` 和 `DisasterConfig` 计算源集群、目标集群和存储仓库。
                - operator 在源集群和目标集群 `velero` namespace 创建 `DeleteBackupRequest`，`spec.backupName=yaoshi-backup-<jobName>`，并尝试删除目标集群 `yaoshi-restore-<jobName>` Velero `Restore`。
                - operator 最后移除 `disasterjob.disaster.io/finalizer`。

                资源作用范围：
                - CRD 删除配置的管理命名空间（默认 `disaster-system`）下的对象。
                - Velero 清理范围由关联 `DisasterConfig.spec.sourceCluster/targetCluster` 决定。
                - 源/目标集群不存在时 operator 会跳过对应集群的外部资源删除；非 NotFound 的 client 创建失败会写 `status.phase=Failed`。

                ## 3. 入参的详细说明

                | 字段 | 位置 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|---|
                | `name` | path | string | 是 | DisasterJob 名称 | 要删除的容灾任务名称 | 用于 Kubernetes Delete 和 operator 生成 Velero 清理资源名 | 为空时 handler 返回 400；正常路由通常不会匹配空值。 |

                ## 4. 返回的详细说明

                成功响应使用统一 `Envelope`，`data` 为删除提交结果。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | 成功固定为 `0`。 |
                | `data.name` | string | DisasterJob 名称 | 已提交删除的任务名称 | path 参数 | 表示 Kubernetes Delete 请求已成功返回。 |
                | `message` | string | `OK` | 响应消息 | server | 成功固定为 `OK`。 |
                | `trace_id` | string | trace id | 请求追踪 ID | trace middleware | 用于排查。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 路径参数非法 | `name` 为空。 |
                | 401 | 无固定业务码 | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。 |
                | 404 | `3004` | 目标任务不存在 | Kubernetes Delete 返回 NotFound。 |
                | 500 | `5000` | 删除请求失败 | Kubernetes Delete 返回非 NotFound 错误。 |

                operator finalizer 阶段删除 Velero `Backup/Restore` 失败不作为本接口同步错误返回；需要通过详情、watch 和历史事件观察后续状态。
            operationId: delete_apis_disasterjobs_testudo_softcdata_com_v1_jobs_name
            parameters:
                - description: 要删除的 DisasterJob 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterJobDeleteEnvelope'
                    description: 成功提交 DisasterJob 删除请求。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 路径参数非法。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 目标 DisasterJob 不存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: DisasterJob 删除请求失败。
            security:
                - bearerAuth: []
            summary: 容灾任务 DELETE /apis/disasterjobs.testudo.softcdata.com/v1/jobs/:name
            tags:
                - 容灾任务
            x-async-failure-status:
                readVia:
                    - GET /apis/disasterjobs.testudo.softcdata.com/v1/jobs/{name}
                    - GET /apis/disasterjobs.testudo.softcdata.com/v1/watch/jobs/{name}
                    - GET /apis/v1/{resource}/{name}/history
                statusFields:
                    - status.phase
                    - status.reason
                    - status.conditions
            x-controlled-resources:
                direct:
                    - DisasterJob CRD
                downstream:
                    - Source and target cluster Velero DeleteBackupRequest
                    - Target cluster Velero Restore yaoshi-restore-<jobName>
                scope: disaster-system namespace 下的 DisasterJob，以及关联 DisasterConfig 指定的源集群和目标集群 velero namespace
            x-operator-chain:
                controller: DisasterJobReconciler delete branch creates Velero DeleteBackupRequest, deletes target Restore, and removes disasterjob finalizer
                crd: DisasterJob
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterjob_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterjob_controller.go
                serverFile: internal/apis/disaster_jobs/v1/router.go
                serverHandler: c.deleteConfig
            x-runapi-target-id: example-runapi-target-id
        get:
            description: |-
                ## 1. 接口用来干什么

                查询单个 `DisasterJob` 详情。server 直接读取 `DisasterJob disaster-system/{name}`，转换为 `DisasterJobDTO` 返回。

                该接口不触发 operator reconcile。返回中的 `status.phase/reason/startTime/conditions` 用于查看 operator 异步备份、同步和恢复链路当前结果。

                ## 2. 控制哪些资源

                直接操作资源：
                - 只读 `DisasterJob` CRD。

                下层影响资源：
                - 无。详情接口本身不访问 Velero 和远端集群。
                - 返回状态间接反映 operator 已读取的 `DisasterBackup`、`DisasterConfig`、源集群 Velero `Backup` 和目标集群 Velero `Restore` 状态。

                资源作用范围：
                - 读取配置的管理命名空间（默认 `disaster-system`）。
                - `status.phase` 可能为 `Pending`、`Backuping`、`Restoring`、`Succeed`、`Failed`、`Deleting` 等。

                ## 3. 入参的详细说明

                | 字段 | 位置 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|---|
                | `name` | path | string | 是 | DisasterJob 名称 | 要查询的任务名称 | 用于 Kubernetes Get | 为空时正常路由通常不会匹配。 |

                ## 4. 返回的详细说明

                成功响应使用统一 `Envelope`，`data` 为 `DisasterJobDTO`。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | 成功固定为 `0`。 |
                | `data.id` | string | Kubernetes UID | 任务 UID | DisasterJob metadata | 用于前端唯一标识和 dependency token 派生。 |
                | `data.name` | string | 资源名称 | 任务名称 | DisasterJob metadata | 路径中的 name 对应资源名。 |
                | `data.namespace` | string | `disaster-system` | 所在 namespace | server 配置的管理命名空间 | 当前 handler 读取配置的管理命名空间（默认 `disaster-system`）。 |
                | `data.labels` | object | label map | 资源标签 | DisasterJob metadata | operator 同步 dependency labels 后会出现在这里。 |
                | `data.creation_timestamp` | string | RFC3339 时间 | 创建时间 | Kubernetes metadata | 列表可按该字段排序。 |
                | `data.spec.disasterBackup` | string | DisasterBackup 名称 | 关联备份配置 | DisasterJob spec | operator 用它读取备份定义。 |
                | `data.spec.syncType` | string | `forward`、`reverse`、其他字符串、空 | 同步方向 | DisasterJob spec | 空值会被 operator 写回 `forward`。 |
                | `data.spec.scheduleType` | string | `once`、`cron`、其他字符串、空 | 调度类型 | DisasterJob spec | 当前 controller 不按该字段分支。 |
                | `data.status.phase` | string | `Pending`、`Ready`、`Backuping`、`Backuped`、`Restoring`、`Restored`、`Succeed`、`Failed`、`Deleting`、空 | 执行阶段 | operator status | 新建后 operator 尚未回写时可能为空。 |
                | `data.status.reason` | string | Kubernetes StatusReason 文本或空 | 失败原因 | operator status | 读取依赖、连接集群、创建 Velero 资源失败时写入。 |
                | `data.status.startTime` | string | RFC3339 时间、空 | 开始时间 | operator status | 当前 controller 未显式设置时为空。 |
                | `data.status.conditions[]` | array | condition 列表 | 条件列表 | operator status | 当前 DTO 透传 CRD 条件。 |
                | `message` | string | `OK` | 响应消息 | server | 成功固定为 `OK`。 |
                | `trace_id` | string | trace id | 请求追踪 ID | trace middleware | 用于排查。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 401 | 无固定业务码 | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。 |
                | 404 | `3004` | 目标任务不存在 | Kubernetes Get 返回 NotFound。 |
                | 500 | `5000` | 读取失败 | Kubernetes Get 返回非 NotFound 错误。 |

                下层备份、恢复、集群连接失败不作为详情接口同步错误返回，而是体现在 `status.phase=Failed` 和 `status.reason`。
            operationId: get_apis_disasterjobs_testudo_softcdata_com_v1_jobs_name
            parameters:
                - description: 要查询的 DisasterJob 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterJobEnvelope'
                    description: 成功返回 DisasterJob 详情。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 目标 DisasterJob 不存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: DisasterJob 读取失败。
            security:
                - bearerAuth: []
            summary: 容灾任务 GET /apis/disasterjobs.testudo.softcdata.com/v1/jobs/:name
            tags:
                - 容灾任务
            x-async-failure-status:
                readVia:
                    - GET /apis/disasterjobs.testudo.softcdata.com/v1/jobs/{name}
                    - GET /apis/disasterjobs.testudo.softcdata.com/v1/jobs
                    - GET /apis/disasterjobs.testudo.softcdata.com/v1/watch/jobs/{name}
                    - GET /apis/v1/{resource}/{name}/history
                statusFields:
                    - status.phase
                    - status.reason
                    - status.conditions
            x-controlled-resources:
                direct:
                    - DisasterJob CRD
                downstream:
                    - 无
                scope: disaster-system namespace 下的指定 DisasterJob
            x-operator-chain:
                controller: DisasterJobReconciler syncs dependency labels, defaults syncType and phase, creates Velero Backup and Restore, and writes status
                crd: DisasterJob
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterjob_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterjob_controller.go
                serverFile: internal/apis/disaster_jobs/v1/router.go
                serverHandler: c.config
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterjobs.testudo.softcdata.com/v1/watch/jobs:
        get:
            description: |-
                ## 1. 接口用来干什么

                建立 WebSocket 连接，实时推送 `disaster-system` namespace 下全部 `DisasterJob` 的 Kubernetes watch 事件。operator 更新任务 status 时，会通过该 watch 表现为 `MODIFIED` 事件。

                handler 不先执行 List 快照，只在 watch 建立后的事件到达时推送。

                ## 2. 控制哪些资源

                直接操作资源：
                - 只监听 `DisasterJob` CRD watch 流。

                下层影响资源：
                - 无。该接口不创建、不更新、不删除 CRD，不访问 Velero、源集群和目标集群。
                - operator 后续写入 `status.phase/reason/conditions` 时会推送为 watch 事件。

                资源作用范围：
                - 监听配置的管理命名空间（默认 `disaster-system`） 的全部 DisasterJob。
                - WebSocket 流由 `StreamWatch` 发送连接成功帧、事件帧、30 秒 heartbeat、关闭帧和 30 分钟 timeout 帧。

                ## 3. 入参的详细说明

                | 字段 | 位置 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|---|
                | `Authorization` | header | string | 非 dev 必传 | `Bearer <JWT>` | JWT 令牌 | WebSocket 握手前认证 | 三种传入方式任选一种。 |
                | `token` | query | string | 非 dev 可选 | JWT 原文 | JWT 令牌 | `WebSocketTokenAdapter` 会转换为 Authorization header | 适合浏览器 WebSocket 无法设置 header 的场景。 |
                | `Sec-WebSocket-Protocol` | header | string | 非 dev 可选 | JWT 原文或子协议 | JWT 令牌 | adapter 会尝试提取 token | 服务端仍会完成 WebSocket 升级。 |

                ## 4. 返回的详细说明

                该接口升级为 WebSocket 后，所有消息均为统一 `Envelope` JSON 字符串。

                | 帧 | `code` | `data` | `meta` | 说明 |
                |---|---|---|---|---|
                | 连接成功 | `0` | `{"status":"connected"}` | `type=connected` | 升级成功后第一帧。 |
                | 资源事件 | `0` | `{"type":"ADDED|MODIFIED|DELETED|ERROR","object":DisasterJobDTO}` | 可能为空 | `object` 字段来自 `ConvertToDisasterJobDTO`。 |
                | 心跳 | `0` | `null` | `type=heartbeat` | 每 30 秒发送一次。 |
                | watcher 错误 | `5000` | `null` | 空 | `DisasterJobs(disaster-system).Watch` 创建失败时发送。 |
                | 正常关闭 | `0` | `null` | `type=closed, reason=watcher closed` | watch channel 关闭。 |
                | 超时关闭 | `0` | `null` | `type=timeout, reason=connection timeout` | 连接达到 30 分钟上限。 |

                `DisasterJobDTO` 字段含义与详情接口一致。

                ## 5. 可能返回的错误

                | 阶段 | HTTP 状态码或业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 握手前 | HTTP 401 | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。 |
                | 升级阶段 | HTTP 500 | WebSocket 升级失败 | upgrader.Upgrade 返回错误，响应为 `{"message":"..."}`。 |
                | WebSocket 内 | `code=5000` | watcher 创建失败 | `DisasterJobs(disaster-system).Watch` 返回错误。 |

                operator 备份和恢复失败不会关闭 WebSocket，只会通过后续 `MODIFIED` 事件中的 `status.phase=Failed` 和 `status.reason` 体现。
            operationId: get_apis_disasterjobs_testudo_softcdata_com_v1_watch_jobs
            parameters:
                - description: 浏览器 WebSocket 兼容鉴权 token。
                  in: query
                  name: token
                  required: false
                  schema:
                    type: string
                - description: JWT Authorization header，格式为 Bearer <JWT>。
                  in: header
                  name: Authorization
                  required: false
                  schema:
                    type: string
                - description: 可携带 JWT 原文的 WebSocket 子协议 header。
                  in: header
                  name: Sec-WebSocket-Protocol
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '101':
                    description: WebSocket 连接升级成功。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MessageResponse'
                    description: WebSocket 升级失败。
            security:
                - bearerAuth: []
            summary: '[WebSocket] 容灾任务 GET /apis/disasterjobs.testudo.softcdata.com/v1/watch/jobs'
            tags:
                - 容灾任务
            x-controlled-resources:
                direct:
                    - DisasterJob CRD watch stream
                downstream:
                    - 无
                scope: disaster-system namespace 下全部 DisasterJob watch 事件
            x-disaster-protocol: websocket
            x-message-schema:
                $ref: '#/components/schemas/DisasterJobWatchEnvelope'
            x-operator-chain:
                controller: DisasterJobReconciler syncs dependency labels, defaults syncType and phase, creates Velero Backup and Restore, and writes status
                crd: DisasterJob
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterjob_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterjob_controller.go
                serverFile: internal/apis/disaster_jobs/v1/router.go
                serverHandler: c.watchJobs
            x-runapi-target-id: example-runapi-target-id
    /apis/disasterjobs.testudo.softcdata.com/v1/watch/jobs/{name}:
        get:
            description: |-
                ## 1. 接口用来干什么

                建立 WebSocket 连接，实时推送指定 `DisasterJob` 的 Kubernetes watch 事件。server 使用 `fieldSelector=metadata.name=<name>` 过滤 watch 流。

                该接口不会先执行详情 Get，也不会因为目标当前不存在而同步返回 404；连接建立后只会推送匹配名称的后续 watch 事件。

                ## 2. 控制哪些资源

                直接操作资源：
                - 只监听指定名称的 `DisasterJob` CRD watch 流。

                下层影响资源：
                - 无。该接口不创建、不更新、不删除 CRD，不访问 Velero、源集群和目标集群。
                - operator 后续更新该任务 status 时会推送为 `MODIFIED` 事件。

                资源作用范围：
                - 监听配置的管理命名空间（默认 `disaster-system`）。
                - field selector 固定为 `metadata.name=<name>`。
                - WebSocket 流由 `StreamWatch` 发送连接成功帧、事件帧、30 秒 heartbeat、关闭帧和 30 分钟 timeout 帧。

                ## 3. 入参的详细说明

                | 字段 | 位置 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|---|
                | `name` | path | string | 是 | DisasterJob 名称 | 要监听的任务名称 | 组成 Kubernetes field selector `metadata.name=<name>` | 为空时握手前返回 HTTP 400 `{"message":"name is required"}`。 |
                | `Authorization` | header | string | 非 dev 必传 | `Bearer <JWT>` | JWT 令牌 | WebSocket 握手前认证 | 三种传入方式任选一种。 |
                | `token` | query | string | 非 dev 可选 | JWT 原文 | JWT 令牌 | `WebSocketTokenAdapter` 会转换为 Authorization header | 适合浏览器 WebSocket 无法设置 header 的场景。 |
                | `Sec-WebSocket-Protocol` | header | string | 非 dev 可选 | JWT 原文或子协议 | JWT 令牌 | adapter 会尝试提取 token | 服务端仍会完成 WebSocket 升级。 |

                ## 4. 返回的详细说明

                该接口升级为 WebSocket 后，所有消息均为统一 `Envelope` JSON 字符串。

                | 帧 | `code` | `data` | `meta` | 说明 |
                |---|---|---|---|---|
                | 连接成功 | `0` | `{"status":"connected"}` | `type=connected` | 升级成功后第一帧。 |
                | 资源事件 | `0` | `{"type":"ADDED|MODIFIED|DELETED|ERROR","object":DisasterJobDTO}` | 可能为空 | 只推送 `metadata.name=<name>` 的事件。 |
                | 心跳 | `0` | `null` | `type=heartbeat` | 每 30 秒发送一次。 |
                | watcher 错误 | `5000` | `null` | 空 | watch 创建失败时发送。 |
                | 正常关闭 | `0` | `null` | `type=closed, reason=watcher closed` | watch channel 关闭。 |
                | 超时关闭 | `0` | `null` | `type=timeout, reason=connection timeout` | 连接达到 30 分钟上限。 |

                `DisasterJobDTO` 字段含义与详情接口一致。目标不存在时连接也可能成功，但不会收到现有对象快照。

                ## 5. 可能返回的错误

                | 阶段 | HTTP 状态码或业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 握手前 | HTTP 400 | 路径参数非法 | `name` 为空，响应体为 `{"message":"name is required"}`。 |
                | 握手前 | HTTP 401 | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。 |
                | 升级阶段 | HTTP 500 | WebSocket 升级失败 | upgrader.Upgrade 返回错误，响应为 `{"message":"..."}`。 |
                | WebSocket 内 | `code=5000` | watcher 创建失败 | `DisasterJobs(disaster-system).Watch` 返回错误。 |

                目标不存在不会同步返回 404。operator 备份和恢复失败不会关闭 WebSocket，只会通过后续 `MODIFIED` 事件中的 `status.phase=Failed` 和 `status.reason` 体现。
            operationId: get_apis_disasterjobs_testudo_softcdata_com_v1_watch_jobs_name
            parameters:
                - description: 要监听的 DisasterJob 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: 浏览器 WebSocket 兼容鉴权 token。
                  in: query
                  name: token
                  required: false
                  schema:
                    type: string
                - description: JWT Authorization header，格式为 Bearer <JWT>。
                  in: header
                  name: Authorization
                  required: false
                  schema:
                    type: string
                - description: 可携带 JWT 原文的 WebSocket 子协议 header。
                  in: header
                  name: Sec-WebSocket-Protocol
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '101':
                    description: WebSocket 连接升级成功。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MessageResponse'
                    description: 路径参数非法。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MessageResponse'
                    description: WebSocket 升级失败。
            security:
                - bearerAuth: []
            summary: '[WebSocket] 容灾任务 GET /apis/disasterjobs.testudo.softcdata.com/v1/watch/jobs/:name'
            tags:
                - 容灾任务
            x-controlled-resources:
                direct:
                    - DisasterJob CRD watch stream
                downstream:
                    - 无
                scope: disaster-system namespace 下指定名称的 DisasterJob watch 事件
            x-disaster-protocol: websocket
            x-message-schema:
                $ref: '#/components/schemas/DisasterJobWatchEnvelope'
            x-operator-chain:
                controller: DisasterJobReconciler syncs dependency labels, defaults syncType and phase, creates Velero Backup and Restore, and writes status
                crd: DisasterJob
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterjob_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterjob_controller.go
                serverFile: internal/apis/disaster_jobs/v1/router.go
                serverHandler: c.watchJob
            x-runapi-target-id: example-runapi-target-id
    /apis/policies.testudo.softcdata.com/v1/policies:
        get:
            description: |-
                ## 1. 接口用来干什么

                查询 `disaster-system` 中的 DisasterPolicy 列表，用于策略管理页展示自动备份策略和同步策略。接口只读 informer cache，不创建、不更新、不删除 CRD，也不触发 operator reconcile。

                server 执行链路为 `ParseOptions` 解析 query -> `BuildLabelSelector` 返回空 selector -> `DisasterPolicyLister.List` 从 informer 读取全量策略 -> 对剩余 query filters 按 label 做 `MatchFuzzy` 包含匹配 -> 按 `name` 或 `creationTimestamp` 排序 -> 内存分页 -> `ConvertToDisasterPolicyDTO` -> `BuildCollectionResponse`。

                ## 2. 控制哪些资源

                直接读取资源：
                - `DisasterPolicy` CRD：通过 informer lister 读取所有 namespace 可见缓存对象，当前策略实际由 server 写入配置的管理命名空间（默认 `disaster-system`）。

                下层影响资源：
                - 无同步写入。列表接口不会访问业务集群、Velero、S3、DataSync、ResourceSync、AppBackup 或 DisasterInstance。
                - 返回的状态来自 `DisasterPolicyReconciler` 异步维护：该 controller 添加 finalizer，校验 `spec.schedule` 和 AutoBackup `spec.ttl`，同步 `testudo.softcdata.com/disaster-policy-type/name/state` labels，成功写 `status.phase=Active`，失败写 `status.reason=InvalidSchedule` 或 `InvalidTTL`。
                - AutoBackup 策略后续由 AppBackup controller 消费并应用到 AppBackup、Velero Schedule、备份 TTL 和 paused 状态；SyncPolicy 后续由 DisasterInstance controller 消费并写入 DataSync/ResourceSync 的 `spec.trigger.schedule`。

                资源作用范围：
                - HTTP 层无 namespace query；返回 lister 可见范围内的 DisasterPolicy。
                - 当前创建、详情、更新、删除接口均使用配置的管理命名空间（默认 `disaster-system`），因此正常业务策略应在该 namespace 下。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `page` | integer | 否 | 大于 0 的页码 | 当前页码 | 内存分页 | 默认 1；小于等于 0 会被修正为 1。 |
                | `limit` | integer | 否 | 正整数、`-1` | 每页数量 | 内存分页 | 默认 10；`-1` 表示返回全部。 |
                | `sort` | string | 否 | `name`、`creationTimestamp` | 排序字段 | 控制内存排序 | 空值不排序；未知字段按 name 比较。 |
                | `order` | string | 否 | `asc`、`desc` | 排序方向 | 控制升序或降序 | 仅 `desc` 触发降序，其他值按升序处理。 |
                | `keyword` | string | 否 | 任意字符串 | 预留关键字 | 当前 handler 不使用该字段 | `ParseOptions` 会读取但不会进入 label filters，因此当前对结果无过滤效果。 |
                | `testudo.softcdata.com/disaster-policy-name` | string | 否 | 策略名称片段 | 策略名称 label 模糊过滤 | 匹配 operator 同步的策略名称 label | 使用包含匹配，传 `backup` 可匹配 `daily-backup`。 |
                | `testudo.softcdata.com/disaster-policy-state` | string | 否 | `Enabled`、`Disabled` 或片段 | 策略状态 label 模糊过滤 | 按 operator 同步的状态 label 过滤 | 使用包含匹配。 |
                | `testudo.softcdata.com/disaster-policy-type` | string | 否 | `AutoBackup`、`DataSync`、`ResourceSync` 或片段 | 底层策略类型 label 模糊过滤 | 按 operator 同步的底层类型 label 过滤 | 过滤发生在 DTO 转换前；传 `SyncPolicy` 通常匹配不到底层 DataSync/ResourceSync label。 |
                | 其他 query | string | 否 | 任意 label key/value | 自定义 label 过滤 | 按 `item.Labels[key]` 做包含匹配 | `limit/page/sort/order/keyword` 之外的 query 都会进入 filters。 |

                ## 4. 返回的详细说明

                成功返回统一信封，`data.items[]` 为 `DisasterPolicyDTO`，`meta` 为 `CollectionMeta`。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | 成功固定为 0。 |
                | `message` | string | `OK` | 响应消息 | server | `transport.WriteSuccess` 固定写入 OK。 |
                | `data.items[].id` | string | Kubernetes UID | 策略 UID | Kubernetes metadata | 用于前端唯一标识。 |
                | `data.items[].name` | string | 策略名称 | 策略名称 | Kubernetes metadata | 与详情、更新、删除 path.name 对应。 |
                | `data.items[].namespace` | string | 通常为 `disaster-system` | 所在 namespace | Kubernetes metadata | server 写操作使用配置的管理命名空间（默认 disaster-system）。 |
                | `data.items[].labels` | object | label map | 策略标签 | Kubernetes metadata + server 归一化 | operator 同步 type/name/state；同步类策略返回时 type label 会被归一为 SyncPolicy。 |
                | `data.items[].creation_timestamp` | string | RFC3339 时间 | 创建时间 | Kubernetes metadata | 字段名为 snake_case。 |
                | `data.items[].spec.type` | string | `AutoBackup`、`SyncPolicy` | 对外策略类型 | CRD Spec + server 转换 | 底层 DataSync/ResourceSync 统一返回 SyncPolicy。 |
                | `data.items[].spec.schedule` | string | cron 表达式 | 调度表达式 | CRD Spec | operator 使用 ParseStandard 校验。 |
                | `data.items[].spec.startTime` | string/null | RFC3339 或空 | 开始生效时间 | CRD Spec | 当前 operator 不使用该字段驱动调度。 |
                | `data.items[].spec.ttl` | string/null | 如 `720h` | AutoBackup 备份保留时间 | CRD Spec | 仅 AutoBackup 有意义，下游写入 Velero Schedule template TTL。 |
                | `data.items[].spec.description` | string | 任意说明 | 策略描述 | CRD Spec | 为空时可省略。 |
                | `data.items[].spec.state` | string | `Enabled`、`Disabled` | 启停状态 | CRD Spec | AutoBackup 禁用会使 AppBackup/Velero Schedule paused；SyncPolicy 禁用会使 DataSync/ResourceSync schedule 为空。 |
                | `data.items[].status.phase` | string | `Active`、`Deleting`、空 | 生命周期阶段 | CRD Status | 当前校验失败也保持 Active，并通过 reason/message 表达失败。 |
                | `data.items[].status.reason` | string | `InvalidSchedule`、`InvalidTTL`、空 | 状态原因 | CRD Status | operator 校验失败时写入。 |
                | `data.items[].status.message` | string | 错误详情或空 | 状态消息 | CRD Status | 展示 cron/TTL 校验错误。 |
                | `meta.pagination` | object | 分页信息 | 分页元信息 | server | 包含 total、limit、first、previous、next、last。 |
                | `meta.filters` | object | filters map | 实际 label filters | server | 不包含 `keyword`。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | 无 | lister 或 selector 异常 | `DisasterPolicyLister.List` 返回错误，响应体为 `{"message":"..."}`，不是统一信封。 |
                | 401 | `2001` | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效，由 JWT 中间件返回统一信封，包含本地化 `message` 与稳定 `message_key`。 |

                本 handler 正常路径不显式返回 404、409 或 500。operator 后续校验失败不会改变列表接口 HTTP 状态，只会体现在 `data.items[].status.reason/message`、Kubernetes Event 或任务事件中。
            operationId: get_apis_policies_testudo_softcdata_com_v1_policies
            parameters:
                - description: 页码。默认 1；小于等于 0 会按 1 处理。
                  in: query
                  name: page
                  required: false
                  schema:
                    default: 1
                    minimum: 1
                    type: integer
                - description: 每页数量。默认 10；传 -1 返回全部。
                  in: query
                  name: limit
                  required: false
                  schema:
                    default: 10
                    type: integer
                - description: 排序字段。支持 name、creationTimestamp；未知字段按 name 比较。
                  in: query
                  name: sort
                  required: false
                  schema:
                    enum: [name, creationTimestamp]
                    type: string
                - description: 排序方向。只有 desc 触发降序，其他值按升序处理。
                  in: query
                  name: order
                  required: false
                  schema:
                    enum: [asc, desc]
                    type: string
                - description: 预留关键字；当前 PolicyHandler.policies 不使用该字段过滤结果。
                  in: query
                  name: keyword
                  required: false
                  schema:
                    type: string
                - description: 按策略名称 label 做包含匹配。
                  in: query
                  name: testudo.softcdata.com/disaster-policy-name
                  required: false
                  schema:
                    type: string
                - description: 按策略状态 label 做包含匹配，常用值 Enabled、Disabled。
                  in: query
                  name: testudo.softcdata.com/disaster-policy-state
                  required: false
                  schema:
                    enum: [Enabled, Disabled]
                    type: string
                - description: 按底层策略类型 label 做包含匹配，常用值 AutoBackup、DataSync、ResourceSync；SyncPolicy 是 DTO 归一化值，列表 label 过滤通常不匹配。
                  in: query
                  name: testudo.softcdata.com/disaster-policy-type
                  required: false
                  schema:
                    enum: [AutoBackup, DataSync, ResourceSync]
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterPolicyCollectionEnvelope'
                    description: 查询成功。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MessageResponse'
                    description: lister 或 selector 异常，返回非统一信封。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
            security:
                - bearerAuth: []
            summary: 查询容灾策略列表
            tags:
                - 容灾策略
            x-async-failure-status:
                readVia:
                    - GET /apis/policies.testudo.softcdata.com/v1/policies
                    - GET /apis/policies.testudo.softcdata.com/v1/policies/{name}
                    - GET /apis/v1/events
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - DisasterPolicy CRD informer cache
                downstream:
                    - 无同步下游写入
                    - AutoBackup 后续由 AppBackup controller 消费并影响 Velero Schedule
                    - SyncPolicy 后续由 DisasterInstance controller 消费并影响 DataSync/ResourceSync schedule
                scope: lister 可见范围内的 DisasterPolicy；正常业务写入配置的管理命名空间（默认 disaster-system）
            x-operator-chain:
                controller: DisasterPolicyReconciler validates schedule and ttl, syncs labels, writes status.phase/reason/message; AppBackupReconciler and DisasterInstanceReconciler consume valid policies later
                crd: DisasterPolicy
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterpolicy_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterpolicy_controller.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/appbackup/appbackup_ready.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterinstance/controller.go
                serverFile: internal/apis/disaster_policy/v1/router.go
                serverHandler: h.policies
            x-runapi-target-id: example-runapi-target-id
        post:
            description: |-
                ## 1. 接口用来干什么

                创建 DisasterPolicy，用于定义自动备份策略或同步调度策略。server 接收外部请求后转换为 operator CRD spec，创建到配置的管理命名空间（默认 `disaster-system`），并注入 trace/user annotation 供 operator 事件关联。

                创建接口只完成 CRD 写入，不等待 operator 校验 cron，也不等待 AppBackup、DisasterInstance、DataSync、ResourceSync 或 Velero Schedule 完成收敛。

                ## 2. 控制哪些资源

                直接写入资源：
                - `DisasterPolicy` CRD：创建到 `disaster-system`。
                - CRD annotations：写入 `testudo.softcdata.com/trace-id` 和 `testudo.softcdata.com/user`。

                下层影响资源：
                - `DisasterPolicyReconciler` 添加 `testudo.softcdata.com/policy-finalizer`，校验 cron 与 AutoBackup ttl，同步策略 labels，写入 status。
                - AutoBackup 策略后续由 AppBackup controller 应用到 AppBackup 的 `spec.schedule/spec.template.ttl/spec.paused`，并最终同步到 Velero `Schedule.spec.schedule/spec.template.ttl/spec.paused`。
                - SyncPolicy 创建时底层写为 `DataSync`；后续由 DisasterInstance controller 通过 `dataSyncPolicy/resourceSyncPolicy` 引用并写入 DataSync/ResourceSync 的 `spec.trigger.schedule`。

                资源作用范围：
                - CRD namespace 为配置的管理命名空间（默认 `disaster-system`）。
                - HTTP 创建不直接访问业务集群、Velero、S3、DataSync、ResourceSync 或 AppBackup。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `name` | string | 是 | 长度 1 到 50 的策略名称 | 策略名称 | 写入 `metadata.name` | 由 binding 校验 `required,max=50,min=1`。 |
                | `type` | string | 是 | `AutoBackup`、`SyncPolicy`，兼容 `DataSync`、`ResourceSync` | 对外策略类型 | 写入 `spec.type` | `AutoBackup` 写底层 AutoBackup；`SyncPolicy/DataSync/ResourceSync` 创建时均写底层 DataSync。其他值返回 400。 |
                | `schedule` | string | 是 | 标准五段 cron，如 `0 2 * * *` | 调度表达式 | 写入 `spec.schedule` | server 只校验必传；operator 后续用 ParseStandard 校验，非法写 `InvalidSchedule`。 |
                | `startTime` | string | 否 | RFC3339 时间 | 开始生效时间 | 写入 `spec.startTime` | 当前 operator 不使用该字段驱动调度。 |
                | `ttl` | string | 否 | 如 `24h`、`720h`，必须大于 0 | AutoBackup 备份保留时间 | 写入 `spec.ttl`，后续传递到 Velero Schedule template TTL | 仅 `type=AutoBackup` 支持；SyncPolicy 携带会返回 400。 |
                | `description` | string | 否 | 最大 200 字符 | 策略说明 | 写入 `spec.description` | 超过 200 返回 400。 |
                | `state` | string | 是 | `Enabled`、`Disabled` | 策略启停状态 | 写入 `spec.state` | AutoBackup Disabled 会暂停下游 Schedule；SyncPolicy Disabled 会让下游 sync schedule 为空。 |

                ## 4. 返回的详细说明

                成功返回 HTTP 201 和统一信封，`message` 当前由 `transport.WriteSuccess` 固定为 `OK`。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | 创建成功固定为 0。 |
                | `data` | object | `DisasterPolicyDTO` | 创建后的策略 DTO | Kubernetes Create 返回对象 | 刚创建后 status 和 labels 可能尚未被 operator 回写。 |
                | `data.spec.type` | string | `AutoBackup`、`SyncPolicy` | 对外策略类型 | server 转换 | 创建 `SyncPolicy/DataSync/ResourceSync` 后会返回 SyncPolicy。 |
                | `data.spec.ttl` | string/null | 如 `720h` | TTL | CRD Spec | 仅 AutoBackup 有意义。 |
                | `data.status.phase` | string | `Active`、空 | 状态阶段 | CRD Status | 刚创建后可能为空，需后续查询列表/详情或事件。 |
                | `data.status.reason` | string | `InvalidSchedule`、`InvalidTTL`、空 | 异步校验结果 | CRD Status | cron 非法或 TTL 非法由 operator 后续写入。 |
                | `trace_id` | string | trace id | 请求追踪 ID | trace middleware | 同步写入 CRD annotation 供 operator 上报任务事件。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | JSON 绑定或字段校验失败 | body 非法、必填字段缺失、`name` 长度不合法、`description` 超长、`type` 不是允许值、SyncPolicy 携带 ttl、ttl 小于等于 0。 |
                | 401 | 无 | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。 |
                | 409 | `3009` | 同名策略已存在 | Kubernetes Create 返回 AlreadyExists。 |
                | 500 | `5000` | Kubernetes Create 失败 | API Server、权限或网络等非 AlreadyExists 错误。 |

                operator 后续 cron 校验失败会写 `status.reason=InvalidSchedule`；AutoBackup ttl 异步校验失败会写 `status.reason=InvalidTTL`。这些失败不会回滚 HTTP 201，需要通过详情、列表或任务事件读取。
            operationId: post_apis_policies_testudo_softcdata_com_v1_policies
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateDisasterPolicyRequest'
                description: 创建策略请求体。
                required: true
            responses:
                '201':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterPolicyEnvelope'
                    description: 创建成功。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求体绑定失败、类型非法或 ttl 校验失败。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '409':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 同名策略已存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: Kubernetes Create 失败。
            security:
                - bearerAuth: []
            summary: 创建容灾策略
            tags:
                - 容灾策略
            x-async-failure-status:
                readVia:
                    - GET /apis/policies.testudo.softcdata.com/v1/policies/{name}
                    - GET /apis/policies.testudo.softcdata.com/v1/policies
                    - GET /apis/v1/events
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - DisasterPolicy CRD in configured management namespace (default disaster-system)
                downstream:
                    - AppBackup CRD when AutoBackup policy is referenced
                    - Velero Schedule when AppBackup reconciles AutoBackup policy
                    - DisasterInstance/DataSync/ResourceSync when SyncPolicy is referenced
                scope: disaster-system namespace
            x-operator-chain:
                controller: DisasterPolicyReconciler adds finalizer, validates cron/ttl, syncs labels, writes status; AppBackupReconciler maps AutoBackup to Velero Schedule; DisasterInstanceReconciler maps SyncPolicy to DataSync/ResourceSync schedules
                crd: DisasterPolicy
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterpolicy_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterpolicy_controller.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/appbackup/appbackup_controller.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/appbackup/appbackup_ready.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterinstance/controller.go
                serverFile: internal/apis/disaster_policy/v1/router.go
                serverHandler: h.createPolicy
            x-runapi-target-id: example-runapi-target-id
    /apis/policies.testudo.softcdata.com/v1/policies/{name}:
        delete:
            description: |-
                ## 1. 接口用来干什么

                删除指定 DisasterPolicy。server 会先从配置的管理命名空间（默认 `disaster-system`） 读取目标策略；目标不存在时直接返回 404。读取成功后会 best-effort 写 trace/user annotation，再调用 Kubernetes Delete。

                该删除不是幂等接口：删除前 Get 或 Delete 任一步返回 NotFound，都会同步返回 404。

                ## 2. 控制哪些资源

                直接写入资源：
                - `DisasterPolicy` CRD：删除 `disaster-system/{name}`。
                - 删除前 annotation：best-effort 写入 `testudo.softcdata.com/trace-id` 和 `testudo.softcdata.com/user`，该 Update 失败会被忽略。

                下层影响资源：
                - `DisasterPolicyReconciler.handleDelete` 在 deletionTimestamp 存在时上报删除开始/完成任务事件，然后移除 `testudo.softcdata.com/policy-finalizer`。
                - 当前 operator 中旧的 AppBackup/DisasterJob 引用阻塞逻辑已被注释旁路；删除策略不会同步删除 AppBackup、DataSync、ResourceSync、DisasterInstance、Velero Schedule 或 Velero Backup。
                - 仍引用该策略的下游资源会由各自 controller 后续 reconcile 处理。

                资源作用范围：
                - 配置的管理命名空间（默认 `disaster-system`）。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `name` | string | 是 | DisasterPolicy 名称 | 要删除的策略名称 | 用于 Get、Update annotation 和 Delete | 路由 path 参数；server 不额外校验空值，正常路由匹配下必有值。 |

                该接口不使用 query 或请求体。

                ## 4. 返回的详细说明

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | Delete 调用成功时返回。 |
                | `message` | string | `OK` | 响应消息 | server | `transport.WriteSuccess` 固定写入。 |
                | `data.name` | string | path.name | 已提交删除的策略名称 | server | 仅表示 Kubernetes Delete 已接受，不表示 finalizer 已清理完成。 |
                | `trace_id` | string | trace id | 请求追踪 ID | trace middleware | 删除前 annotation update 成功时会传给 operator。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 401 | 无 | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。 |
                | 404 | `3004` | 策略不存在 | 删除前 Get 返回 NotFound，或 Delete 返回 NotFound。 |
                | 500 | `5000` | Kubernetes Get/Delete 失败 | 非 NotFound 的 Kubernetes 错误。 |

                删除前 best-effort annotation Update 失败不会返回错误。HTTP 200 后如果 operator finalizer 清理失败，需要通过 Kubernetes 对象状态或任务事件排查。
            operationId: delete_apis_policies_testudo_softcdata_com_v1_policies_name
            parameters:
                - description: 要删除的 DisasterPolicy 名称。server 在配置的管理命名空间（默认 disaster-system）查找。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterPolicyDeleteEnvelope'
                    description: 删除请求提交成功。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 策略不存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: Kubernetes Get 或 Delete 失败。
            security:
                - bearerAuth: []
            summary: 删除容灾策略
            tags:
                - 容灾策略
            x-async-failure-status:
                readVia:
                    - GET /apis/v1/events
                    - GET /apis/v1/watch/events
                statusFields:
                    - deletionTimestamp
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - DisasterPolicy CRD in configured management namespace (default disaster-system)
                downstream:
                    - 不同步删除 AppBackup/DataSync/ResourceSync/DisasterInstance/Velero 资源
                    - DisasterPolicyReconciler removes policy finalizer and emits task events
                scope: disaster-system namespace
            x-operator-chain:
                controller: DisasterPolicyReconciler.handleDelete emits delete task events and removes policy finalizer; legacy AppBackup/DisasterJob deletion blocking is currently disabled
                crd: DisasterPolicy
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterpolicy_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterpolicy_controller.go
                serverFile: internal/apis/disaster_policy/v1/router.go
                serverHandler: h.deletePolicy
            x-runapi-target-id: example-runapi-target-id
        get:
            description: |-
                ## 1. 接口用来干什么

                查询指定 DisasterPolicy 详情，用于策略详情页、编辑页回显和排查 operator 异步校验结果。接口从配置的管理命名空间（默认 `disaster-system`） 读取 `path.name` 对应 CRD，并转换为 `DisasterPolicyDTO`。

                详情接口只读 CRD，不触发 operator reconcile。

                ## 2. 控制哪些资源

                直接读取资源：
                - `DisasterPolicy` CRD：通过 Kubernetes client 读取 `disaster-system/{name}`。

                下层影响资源：
                - 无同步下游写入。
                - `status.phase/reason/message` 由 `DisasterPolicyReconciler` 异步维护；AutoBackup 和 SyncPolicy 的实际下游消费结果需要分别看 AppBackup/Velero Schedule 或 DisasterInstance/DataSync/ResourceSync。

                资源作用范围：
                - 配置的管理命名空间（默认 `disaster-system`）。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `name` | string | 是 | DisasterPolicy 名称 | 要查询的策略名称 | 用于 Kubernetes Get | 路由 path 参数；server 配置的管理命名空间 为 disaster-system。 |

                该接口不使用 query 或请求体。

                ## 4. 返回的详细说明

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | 查询成功固定为 0。 |
                | `data.id` | string | Kubernetes UID | 策略 UID | Kubernetes metadata | AppBackup 引用校验使用该 UID label。 |
                | `data.name` | string | 策略名称 | 策略名称 | Kubernetes metadata | 与 path.name 一致。 |
                | `data.namespace` | string | `disaster-system` | 所在 namespace | Kubernetes metadata | server 读取配置的管理命名空间该 namespace。 |
                | `data.labels` | object | label map | 策略标签 | Kubernetes metadata + server 归一化 | operator 同步 type/name/state；同步类策略返回时 type label 可归一为 SyncPolicy。 |
                | `data.creation_timestamp` | string | RFC3339 时间 | 创建时间 | Kubernetes metadata | 字段名为 snake_case。 |
                | `data.spec.type` | string | `AutoBackup`、`SyncPolicy` | 对外策略类型 | CRD Spec + server 转换 | 底层 DataSync/ResourceSync 统一返回 SyncPolicy。 |
                | `data.spec.schedule` | string | cron 表达式 | 调度表达式 | CRD Spec | 下游 controller 使用该值。 |
                | `data.spec.startTime` | string/null | RFC3339 或空 | 开始生效时间 | CRD Spec | 当前 operator 不使用。 |
                | `data.spec.ttl` | string/null | 如 `720h` | TTL | CRD Spec | AutoBackup 后续写入 AppBackup/Velero Schedule template TTL。 |
                | `data.spec.description` | string | 任意说明 | 策略描述 | CRD Spec | 为空时可省略。 |
                | `data.spec.state` | string | `Enabled`、`Disabled` | 启停状态 | CRD Spec | 控制下游调度启停。 |
                | `data.status.phase` | string | `Active`、`Deleting`、空 | 生命周期阶段 | CRD Status | 校验失败仍可能为 Active。 |
                | `data.status.reason` | string | `InvalidSchedule`、`InvalidTTL`、空 | 状态原因 | CRD Status | operator 校验失败时写入。 |
                | `data.status.message` | string | 错误详情或空 | 状态消息 | CRD Status | 展示具体失败原因。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 401 | 无 | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。 |
                | 404 | `3004` | 策略不存在 | `DisasterPolicies(disaster-system).Get` 返回 NotFound。 |
                | 500 | `5000` | Kubernetes Get 失败 | 非 NotFound 的 Kubernetes 错误。 |

                operator 异步校验失败不会让详情接口返回 4xx/5xx；失败通过 `data.status.reason/message` 体现。
            operationId: get_apis_policies_testudo_softcdata_com_v1_policies_name
            parameters:
                - description: 要查询的 DisasterPolicy 名称。server 在配置的管理命名空间（默认 disaster-system）查找。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterPolicyEnvelope'
                    description: 查询成功。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 策略不存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: Kubernetes Get 失败。
            security:
                - bearerAuth: []
            summary: 查询容灾策略详情
            tags:
                - 容灾策略
            x-async-failure-status:
                readVia:
                    - GET /apis/policies.testudo.softcdata.com/v1/policies/{name}
                    - GET /apis/policies.testudo.softcdata.com/v1/policies
                    - GET /apis/v1/events
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - DisasterPolicy CRD in configured management namespace (default disaster-system)
                downstream:
                    - 无同步下游写入
                    - 详情展示 AppBackup/DisasterInstance 后续消费策略后的回写状态
                scope: disaster-system namespace
            x-operator-chain:
                controller: DisasterPolicyReconciler maintains status and labels; this GET only reads the CRD
                crd: DisasterPolicy
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterpolicy_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterpolicy_controller.go
                serverFile: internal/apis/disaster_policy/v1/router.go
                serverHandler: h.policy
            x-runapi-target-id: example-runapi-target-id
        put:
            description: |-
                ## 1. 接口用来干什么

                更新指定 DisasterPolicy。该接口是部分合并更新：只覆盖请求中明确传入且符合合并规则的字段，不是全量替换。server 要求 `body.name` 必须等于 `path.name`，并使用 `RetryOnConflict` 基于最新 CRD 进行更新。

                更新成功只表示 CRD spec 已写入；operator 校验 cron/ttl、同步 labels、下游 AppBackup/DisasterInstance/DataSync/ResourceSync/Velero Schedule 收敛均为异步过程。

                ## 2. 控制哪些资源

                直接读写资源：
                - `DisasterPolicy` CRD：读写配置的管理命名空间（默认 `disaster-system`）下的 `{name}`。
                - `AppBackup` CRD：当当前底层策略类型不是 AutoBackup 时，server 按 `testudo.softcdata.com/disaster-policy-uid=<uid>` 查询 AppBackup 引用；存在引用则拒绝更新。
                - CRD annotations：写入 `testudo.softcdata.com/trace-id` 和 `testudo.softcdata.com/user`。

                下层影响资源：
                - `DisasterPolicyReconciler` 在更新后重新校验 cron 和 AutoBackup ttl，同步 labels，成功写 `status.phase=Active` 并清空错误，失败写 `InvalidSchedule` 或 `InvalidTTL`。
                - AutoBackup 策略即使被 AppBackup 引用也允许更新；AppBackup controller 会重新入队引用该策略的 AppBackup，并更新 AppBackup/Velero Schedule 的 schedule、ttl、paused。
                - SyncPolicy 更新会被 DisasterInstance controller 在稳态 reconcile 中传播到 DataSync/ResourceSync schedule；Disabled 策略会使 schedule 为空。

                资源作用范围：
                - 配置的管理命名空间（默认 `disaster-system`）。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `name` path | string | 是 | DisasterPolicy 名称 | URL 中的目标策略 | 用于路由和 Get | 必须与 body.name 一致。 |
                | `name` body | string | 是 | 与 path.name 相同 | 请求体策略名称 | 防止误更新其他对象 | 不一致返回 400。 |
                | `type` | string | 否 | `AutoBackup`、`SyncPolicy`，兼容 `DataSync`、`ResourceSync` | 策略类型 | 合并到 `spec.type` | 空字符串不覆盖；`SyncPolicy` 在当前底层为 DataSync/ResourceSync 时保留底层类型，否则默认 DataSync。 |
                | `schedule` | string | 否 | 标准五段 cron | 调度表达式 | 合并到 `spec.schedule` | 非空才覆盖；空字符串不能清空。cron 合法性由 operator 异步校验。 |
                | `startTime` | string | 否 | RFC3339 时间或 null | 开始生效时间 | 合并到 `spec.startTime` | 非 null 才覆盖。 |
                | `ttl` | string | 否 | 如 `24h`、`720h`，必须大于 0 | AutoBackup 备份保留时间 | 合并到 `spec.ttl` | 仅最终 type=AutoBackup 支持；SyncPolicy/DataSync/ResourceSync 携带会返回 400。 |
                | `clearTTL` | boolean | 否 | `true`、`false` | 是否清空 TTL | `true` 时将 `spec.ttl` 置空 | 优先级高于 `ttl`。 |
                | `description` | string | 否 | 任意字符串 | 策略说明 | 合并到 `spec.description` | 非空才覆盖；空字符串不能清空。 |
                | `state` | string | 否 | `Enabled`、`Disabled` | 启停状态 | 合并到 `spec.state` | 非 null 才覆盖。 |

                ## 4. 返回的详细说明

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | 更新成功固定为 0。 |
                | `data` | object | `DisasterPolicyDTO` | 更新后的策略 DTO | Kubernetes Update 返回对象 | status 可能仍是更新前的异步状态。 |
                | `data.spec.type` | string | `AutoBackup`、`SyncPolicy` | 对外策略类型 | CRD Spec + server 转换 | 底层 DataSync/ResourceSync 统一返回 SyncPolicy。 |
                | `data.spec.schedule` | string | cron 表达式 | 调度表达式 | CRD Spec | operator 后续校验并传播。 |
                | `data.spec.ttl` | string/null | 如 `720h` 或空 | TTL | CRD Spec | `clearTTL=true` 后为空。 |
                | `data.spec.state` | string | `Enabled`、`Disabled` | 启停状态 | CRD Spec | 控制下游调度启停。 |
                | `data.status.reason` | string | `InvalidSchedule`、`InvalidTTL`、空 | 异步校验原因 | CRD Status | 更新响应中的状态不保证已经反映本次 reconcile。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 请求体或合并校验失败 | JSON 绑定失败、body.name 与 path.name 不一致、type 非法、ttl 用于非 AutoBackup、ttl 小于等于 0。 |
                | 401 | 无 | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。 |
                | 404 | `3004` | 策略不存在 | RetryOnConflict 中 Get 返回 NotFound。 |
                | 409 | `3009` | 更新冲突或引用限制 | Kubernetes conflict，或当前底层类型非 AutoBackup 且已有 AppBackup 按 `disaster-policy-uid` 引用该策略。 |
                | 500 | `5000` | Kubernetes Get/List/Update 失败 | AppBackup List 或 DisasterPolicy Update 等非上述分类错误。 |

                operator 和下游收敛失败不会改变本次 HTTP 200，需要通过详情、列表、事件以及 AppBackup/DataSync/ResourceSync 状态继续排查。
            operationId: put_apis_policies_testudo_softcdata_com_v1_policies_name
            parameters:
                - description: 要更新的 DisasterPolicy 名称。必须与请求体 name 一致。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateDisasterPolicyRequest'
                description: 更新策略请求体。仅合并传入字段。
                required: true
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterPolicyEnvelope'
                    description: 更新成功。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求体绑定失败、名称不一致、类型非法或 ttl 校验失败。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 策略不存在。
                '409':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 更新冲突或被 AppBackup 引用限制。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: Kubernetes Get/List/Update 失败。
            security:
                - bearerAuth: []
            summary: 更新容灾策略
            tags:
                - 容灾策略
            x-async-failure-status:
                readVia:
                    - GET /apis/policies.testudo.softcdata.com/v1/policies/{name}
                    - GET /apis/policies.testudo.softcdata.com/v1/policies
                    - GET /apis/v1/events
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - DisasterPolicy CRD in configured management namespace (default disaster-system)
                    - AppBackup CRD list for non-AutoBackup reference guard
                downstream:
                    - AppBackup and Velero Schedule when AutoBackup policy changes
                    - DisasterInstance/DataSync/ResourceSync when SyncPolicy changes
                scope: disaster-system namespace
            x-operator-chain:
                controller: DisasterPolicyReconciler validates and labels updated policies; AppBackupReconciler requeues AppBackups for AutoBackup policy changes; DisasterInstanceReconciler propagates sync policy schedules
                crd: DisasterPolicy
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterpolicy_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterpolicy_controller.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/appbackup/appbackup_controller.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/appbackup/appbackup_ready.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterinstance/controller.go
                serverFile: internal/apis/disaster_policy/v1/router.go
                serverHandler: h.updatePolicy
            x-runapi-target-id: example-runapi-target-id
    /apis/policies.testudo.softcdata.com/v1/policies/names:
        get:
            description: |-
                ## 1. 接口用来干什么

                查询策略名称下拉列表，用于 AppBackup、DisasterConfig、DisasterInstance 等配置界面选择可引用的策略。接口返回精简数组，不分页，不返回 collection meta。

                server 执行链路为 `ParseOptions` -> `BuildLabelSelector` 返回空 selector -> `DisasterPolicyLister.List` -> 读取并移除 `enabled` 和 `type` filters -> 按 `spec.state`、底层策略类型和 label filters 内存过滤 -> 排除 `status.phase=Deleting` -> 转换为 `DisasterPolicyNameDTO`。

                ## 2. 控制哪些资源

                直接读取资源：
                - `DisasterPolicy` CRD informer cache。

                下层影响资源：
                - 无同步写入。
                - 返回结果通常作为下游配置接口的引用输入；真正消费发生在 AppBackup controller 或 DisasterInstance controller。

                资源作用范围：
                - lister 可见范围内的 DisasterPolicy；正常业务策略由 server 写入 `disaster-system`。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `enabled` | string | 否 | `true`、`false` | 是否只返回启用策略 | 按 `spec.state` 过滤 | `true` 仅保留 Enabled；`false` 仅保留 Disabled；其他值不生效。 |
                | `type` | string | 否 | `AutoBackup`、`SyncPolicy`、兼容 `DataSync`、`ResourceSync` | 策略类型过滤 | 按底层 `spec.type` 过滤 | `SyncPolicy` 同时匹配底层 DataSync 和 ResourceSync；未知值匹配不到任何策略。 |
                | `testudo.softcdata.com/disaster-policy-name` | string | 否 | 策略名称片段 | 策略名称 label 模糊过滤 | 作为剩余 filter 对 label 做包含匹配 | 使用 `MatchFuzzy`。 |
                | `testudo.softcdata.com/disaster-policy-state` | string | 否 | `Enabled`、`Disabled` 或片段 | 策略状态 label 模糊过滤 | 作为剩余 filter 对 label 做包含匹配 | 与 `enabled` 不同，基于 label 包含匹配。 |
                | `testudo.softcdata.com/disaster-policy-type` | string | 否 | `AutoBackup`、`DataSync`、`ResourceSync` 或片段 | 底层策略类型 label 模糊过滤 | 作为剩余 filter 对 label 做包含匹配 | 若需要 SyncPolicy 聚合语义，应使用 `type=SyncPolicy`。 |
                | `page`、`limit`、`sort`、`order`、`keyword` | string/integer | 否 | 任意值 | 通用查询参数 | 当前 names handler 不使用 | `ParseOptions` 会解析并从 filters 排除，但结果不分页、不排序，也不按 keyword 搜索。 |
                | 其他 query | string | 否 | 任意 label key/value | 自定义 label 过滤 | 对 `item.Labels[key]` 做包含匹配 | `enabled` 和 `type` 会先从 filters 删除，其余 query 作为 label 过滤。 |

                ## 4. 返回的详细说明

                成功返回统一信封，`data` 是 `DisasterPolicyNameDTO[]`，没有分页 meta。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | 成功固定为 0。 |
                | `data[].name` | string | 策略名称 | 策略名称 | Kubernetes metadata | 用作引用字段值。 |
                | `data[].id` | string | Kubernetes UID | 策略 UID | Kubernetes metadata | 用于前端唯一标识。 |
                | `data[].type` | string | `AutoBackup`、`SyncPolicy` | 对外策略类型 | CRD Spec + server 转换 | 底层 DataSync/ResourceSync 返回 SyncPolicy。 |
                | `data[].schedule` | string | cron 表达式 | 调度表达式 | CRD Spec | 用于前端展示和选择判断。 |
                | `data[].ttl` | string/null | 如 `720h` | AutoBackup TTL | CRD Spec | 非 AutoBackup 通常为空。 |

                查询不到匹配策略时返回 `200` 且 `data=[]`。

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | 无 | lister 或 selector 异常 | `DisasterPolicyLister.List` 返回错误，响应体为 `{"message":"..."}`，不是统一信封。 |
                | 401 | 无 | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。 |

                本 handler 正常路径不显式返回 404、409 或 500。策略自身 operator 校验失败不会改变本接口 HTTP 状态。
            operationId: get_apis_policies_testudo_softcdata_com_v1_policies_names
            parameters:
                - description: 是否按启用状态过滤。true 仅返回 Enabled，false 仅返回 Disabled，其他值不生效。
                  in: query
                  name: enabled
                  required: false
                  schema:
                    enum: ['true', 'false']
                    type: string
                - description: 策略类型过滤。SyncPolicy 同时匹配底层 DataSync 和 ResourceSync。
                  in: query
                  name: type
                  required: false
                  schema:
                    enum: [AutoBackup, SyncPolicy, DataSync, ResourceSync]
                    type: string
                - description: 按策略名称 label 做包含匹配。
                  in: query
                  name: testudo.softcdata.com/disaster-policy-name
                  required: false
                  schema:
                    type: string
                - description: 按策略状态 label 做包含匹配。
                  in: query
                  name: testudo.softcdata.com/disaster-policy-state
                  required: false
                  schema:
                    enum: [Enabled, Disabled]
                    type: string
                - description: 按底层策略类型 label 做包含匹配。聚合同步策略请使用 type=SyncPolicy。
                  in: query
                  name: testudo.softcdata.com/disaster-policy-type
                  required: false
                  schema:
                    enum: [AutoBackup, DataSync, ResourceSync]
                    type: string
                - description: 通用分页参数会被解析但当前接口不使用。
                  in: query
                  name: page
                  required: false
                  schema:
                    type: integer
                - description: 通用分页参数会被解析但当前接口不使用。
                  in: query
                  name: limit
                  required: false
                  schema:
                    type: integer
                - description: 通用排序参数会被解析但当前接口不使用。
                  in: query
                  name: sort
                  required: false
                  schema:
                    type: string
                - description: 通用排序参数会被解析但当前接口不使用。
                  in: query
                  name: order
                  required: false
                  schema:
                    type: string
                - description: 通用关键字参数会被解析但当前接口不使用。
                  in: query
                  name: keyword
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterPolicyNamesEnvelope'
                    description: 查询成功。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MessageResponse'
                    description: lister 或 selector 异常，返回非统一信封。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败。
            security:
                - bearerAuth: []
            summary: 查询容灾策略名称列表
            tags:
                - 容灾策略
            x-async-failure-status:
                readVia:
                    - GET /apis/policies.testudo.softcdata.com/v1/policies/{name}
                    - GET /apis/policies.testudo.softcdata.com/v1/policies
                    - GET /apis/v1/events
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - DisasterPolicy CRD informer cache
                downstream:
                    - 无同步下游写入
                    - 返回值作为 AppBackup/DisasterConfig/DisasterInstance 配置引用输入
                scope: lister 可见范围内的 DisasterPolicy；删除中策略会被排除
            x-operator-chain:
                controller: DisasterPolicyReconciler maintains labels and status; this endpoint reads policy spec/status for selector options
                crd: DisasterPolicy
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/disasterpolicy_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/disasterpolicy_controller.go
                serverFile: internal/apis/disaster_policy/v1/router.go
                serverHandler: h.policyNames
            x-runapi-target-id: example-runapi-target-id
    /apis/resources.testudo.softcdata.com/v1/{resource}:
        get:
            description: |-
                ## 1. 接口用来干什么

                查询 server 当前开放的 Kubernetes 原生资源列表。当前代码只注册 `namespaces` 一类资源，因此该接口实际用于读取集群中的 Kubernetes Namespace 列表，供前端选择命名空间、展示集群资源范围。

                ## 2. 控制哪些资源

                直接操作资源：
                - Kubernetes `Namespace`：通过 controller-runtime cache client 执行 List。

                下层影响资源：
                - 无。该接口只读 Kubernetes 原生资源，不创建、不更新、不删除 disaster-operator CRD，也不触发 operator reconcile。

                资源作用范围：
                - 查询当前 server 连接的 Kubernetes 集群。
                - `namespaces` 是集群级资源，`namespace` query 参数当前传入 `NamespaceHandler.List` 后未被使用。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `resource` | string | 是 | `namespaces` | 要查询的 Kubernetes 资源类型 | 选择 `k.resources` 中注册的资源处理器 | 当前代码只注册 `namespaces`。传入其他值会取到空 handler 并由 recovery 处理为内部错误。 |
                | `namespace` | string | 否 | 任意 namespace 名称 | 命名空间查询参数 | 预留给 namespaced 资源过滤 | 对 `namespaces` 无实际过滤效果，因为 Namespace 是集群级资源，当前实现忽略该值。 |

                ## 4. 返回的详细说明

                该接口没有使用统一 `Envelope`，成功时直接返回 Kubernetes `corev1.NamespaceList`。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `apiVersion` | string | `v1` | Kubernetes API 版本 | Kubernetes API | NamespaceList 通常为 `v1`。 |
                | `kind` | string | `NamespaceList` | Kubernetes 资源类型 | Kubernetes API | 表示返回的是 Namespace 列表。 |
                | `metadata` | object | Kubernetes ListMeta | 列表元信息 | Kubernetes metadata | 包含 `resourceVersion` 等列表级元数据。 |
                | `items[]` | array | Namespace 对象数组 | 命名空间列表 | Kubernetes API | 每个元素是 Kubernetes Namespace 对象。 |
                | `items[].metadata.name` | string | namespace 名称 | 命名空间名称 | Kubernetes metadata | 前端通常使用该字段作为选择值。 |
                | `items[].metadata.labels` | object | 标签键值对 | Namespace 标签 | Kubernetes metadata | 由 Kubernetes 或集群管理员维护。 |
                | `items[].status.phase` | string | `Active`、`Terminating` | Namespace 生命周期阶段 | Kubernetes Status | `Active` 表示可用，`Terminating` 表示正在删除。 |
                | `items[].status.conditions` | array | NamespaceCondition 数组 | Namespace 条件 | Kubernetes Status | 删除或资源发现异常时可能出现。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | 无 | Kubernetes List 失败 | `NamespaceHandler.List` 调用 Kubernetes cache client 失败，响应体为 `{"message":"错误内容"}`。 |
                | 401 | 无 | 认证失败 | 非 dev 环境下请求未携带有效 JWT。 |
                | 500 | 无 | 未支持的资源类型或 panic | `resource` 不是 `namespaces` 时，当前代码会对空 handler 调用 `List`，由 recovery 处理为内部错误。 |

                operator 后续回写失败：无。该接口不创建和更新 CRD，不存在异步 reconcile 失败状态。
            operationId: get_apis_resources_testudo_softcdata_com_v1_resource
            parameters:
                - description: 要查询的 Kubernetes 资源类型。当前唯一支持 `namespaces`。
                  in: path
                  name: resource
                  required: true
                  schema:
                    enum:
                        - namespaces
                    type: string
                - description: 命名空间过滤参数。当前 `namespaces` 资源查询会忽略该值。
                  in: query
                  name: namespace
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/KubernetesNamespaceList'
                    description: 成功返回 Kubernetes NamespaceList。
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MessageResponse'
                    description: Kubernetes List 失败。
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: Kubernetes 资源 GET /apis/resources.testudo.softcdata.com/v1/:resource
            tags:
                - Kubernetes 资源
            x-controlled-resources:
                direct:
                    - Kubernetes Namespace
                downstream:
                    - 无
                scope: 当前 server 连接的 Kubernetes 集群
            x-operator-chain:
                controller: 无
                crd: 无
                serverFile: internal/apis/kubernetes_resources/router.go
                serverHandler: k.getResources
            x-runapi-target-id: example-runapi-target-id
    /apis/storage.testudo.softcdata.com/v1/storages:
        get:
            description: |-
                ## 1. 接口用来干什么

                分页查询存储仓库列表。server 从 `StorageRepository` informer 缓存读取 `disaster-system` namespace 下的资源，按 query 中除 `limit/page/sort/order/keyword` 外的动态字段对 `metadata.labels` 的 value 做包含匹配，再按 `name` 或 `creationTimestamp` 做内存排序和分页。

                ## 2. 控制哪些资源

                直接操作资源：
                - 只读 `StorageRepository` CRD，不写入任何对象。

                下层影响资源：
                - 无。该接口本身不访问 S3、不访问 CA Secret、不创建或更新 Velero `Secret/BackupStorageLocation`，也不触发 operator reconcile。

                资源作用范围：
                - 读取配置的管理命名空间（默认 `disaster-system`）。
                - 返回状态来自 `StorageRepositoryReconciler` 已写入的 `status.status/reason/message/lastCheckTime/usedSpaceBytes/totalBackupCount`。
                - operator 的状态来源包括读取同 namespace CA Secret、访问 S3 bucket、必要时创建 bucket，并统计 bucket 对象使用量和备份数量。

                ## 3. 入参的详细说明

                | 字段 | 位置 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|---|
                | `limit` | query | integer | 否 | `-1` 或正整数 | 每页数量 | 控制内存分页数量 | 默认 `10`；`-1` 返回全部。 |
                | `page` | query | integer | 否 | 正整数 | 页码 | 控制内存分页起点 | 默认 `1`；小于等于 `0` 时按 `1` 处理。 |
                | `sort` | query | string | 否 | `name`、`creationTimestamp` | 排序字段 | 控制内存排序字段 | 其他值不报错，比较结果恒为 0，等同不排序。 |
                | `order` | query | string | 否 | `desc`、其他字符串 | 排序方向 | 控制升序或降序 | 仅 `desc` 触发倒序，其他值按正序。 |
                | `keyword` | query | string | 否 | 任意字符串 | 关键字 | 兼容参数 | 当前 handler 解析但不参与过滤。 |
                | `<labelKey>` | query | string | 否 | 任意 label key | 动态标签过滤 | 对 `item.Labels[labelKey]` 做包含匹配 | 空值和 `*` 表示不过滤；前后 `*` 会被去掉后做包含匹配。 |

                ## 4. 返回的详细说明

                成功响应使用统一 `Envelope`，`data.items` 为 `DisasterStorageDTO[]`，`meta` 为集合分页元信息。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | 成功固定为 `0`。 |
                | `data.items[]` | array | `DisasterStorageDTO[]` | 当前页存储仓库列表 | server DTO | 字段含义同详情接口。 |
                | `meta.type` | string | `collection` | 响应类型 | `BuildCollectionResponse` | 固定集合响应。 |
                | `meta.resourceType` | string | `storageRepository` | 资源类型 | server | 固定值。 |
                | `meta.pagination.limit` | integer | 请求 limit | 每页数量 | query | `-1` 时 `next` 为空。 |
                | `meta.pagination.total` | integer | 总数 | 过滤后的总数量 | server 内存过滤结果 | 分页前数量。 |
                | `meta.links.self/first/previous/next/last` | string | URL | 分页链接 | server | 根据当前 path 和分页参数生成。 |
                | `meta.links.<name>` | string | URL | 单项详情链接 | server | key 为每个 storage name，value 为详情路径。 |
                | `meta.sort.name` | string | 请求 sort | 排序字段回显 | query | 只回显，不代表字段一定生效。 |
                | `meta.filters` | object | query 动态过滤项 | 过滤条件回显 | query | 只包含动态 label 过滤项。 |
                | `trace_id` | string | trace id | 请求追踪 ID | trace middleware | 用于排查。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 列表读取失败 | informer lister `List` 返回错误。 |
                | 401 | `2001` | 认证失败 | 非 dev 环境下请求未携带有效 JWT，或 JWT 过期、签名无效。响应由 JWT 中间件返回统一信封，包含本地化 `message` 与稳定 `message_key`。 |

                `keyword` 不生效和不支持的 `sort` 字段不会返回错误。operator 校验 S3 失败也不会使该列表接口失败，只会表现为 `data.items[].status.status=Unavailable` 与对应 `reason/message`。
            operationId: get_apis_storage_testudo_softcdata_com_v1_storages
            parameters:
                - description: 每页数量。默认 10；传 -1 时返回全部。
                  in: query
                  name: limit
                  required: false
                  schema:
                    type: integer
                - description: 页码。默认 1；小于等于 0 时按 1 处理。
                  in: query
                  name: page
                  required: false
                  schema:
                    type: integer
                - description: 排序字段。当前列表接口只识别 name、creationTimestamp；其他值不报错，等同不排序。
                  in: query
                  name: sort
                  required: false
                  schema:
                    type: string
                - description: 排序方向。desc 表示倒序，其他值按正序处理。
                  in: query
                  name: order
                  required: false
                  schema:
                    type: string
                    enum:
                        - asc
                        - desc
                - description: 关键字。当前 handler 会解析该参数，但不参与过滤。
                  in: query
                  name: keyword
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterStorageCollectionEnvelope'
                    description: 成功返回存储仓库集合。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 列表读取失败。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
            security:
                - bearerAuth: []
            summary: 存储仓库 GET /apis/storage.testudo.softcdata.com/v1/storages
            tags:
                - 存储仓库
            x-controlled-resources:
                direct:
                    - StorageRepository CRD
                downstream:
                    - 无
                scope: disaster-system namespace 下的 StorageRepository informer 缓存
            x-operator-chain:
                controller: StorageRepositoryReconciler validates S3 configuration, syncs dependency labels, adds storage finalizer, reports task events, writes status, and updates usage statistics
                crd: StorageRepository
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/storagerepository_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/storagerepository_controller.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/storage_runtime.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/BSL.go
                serverFile: internal/apis/disaster_storage/v1/router.go
                serverHandler: c.storages
            x-runapi-target-id: example-runapi-target-id
        post:
            description: |-
                ## 1. 接口用来干什么

                创建一个 `StorageRepository` 存储仓库。server 校验请求体、endpoint scheme、addressingStyle 和 CA 写入模式后，在 `disaster-system` namespace 创建 CRD；如传入 `caBundle`，server 会同时创建或更新托管 Secret `storage-ca-<name>` 并让 CRD 引用该 Secret。

                ## 2. 控制哪些资源

                直接操作资源：
                - 创建 `StorageRepository` CRD。
                - 可选创建或更新 `Secret disaster-system/storage-ca-<name>`，data key 固定为 `ca.crt`。
                - 可选读取用户指定的 `caSecretRef.name` Secret，确认它存在。

                下层影响资源：
                - 创建 CRD 后触发 `StorageRepositoryReconciler`。operator 会同步 dependency labels、添加 `testudo.softcdata.com/storage-finalizer`、上报创建事件、执行 S3 校验、必要时创建 bucket、统计容量和备份数量。
                - 后续 AppBackup/AppRestore/DisasterConfig/连通性校验链路会基于该仓库在目标集群创建或更新 Velero credential `Secret` 与 `BackupStorageLocation`。

                资源作用范围：
                - CRD 和 CA Secret 写入配置的管理命名空间（默认 `disaster-system`）。
                - S3 访问由 operator 异步完成；HTTP 201 仅表示 CRD 创建成功，不表示 S3 已校验通过。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `name` | string | 是 | 1-50 字符 | 存储仓库名称 | 作为 StorageRepository metadata.name，并用于托管 CA Secret 名称 | binding 要求 `min=1,max=50`；同名资源已存在返回 409。 |
                | `storageType` | string | 是 | 任意非空字符串 | 存储类型 | 写入 `spec.storageType` | 当前 server 不做枚举校验，operator 按 S3 兼容对象存储链路使用。 |
                | `bucket` | string | 是 | 1-50 字符 | bucket 名称 | 写入 `spec.bucket`，operator 用于 `HeadBucket/CreateBucket/ListObjectsV2` | binding 要求 `min=1,max=50`。 |
                | `region` | string | 是 | 1-50 字符 | S3 region | 写入 `spec.region` | binding 要求 `min=1,max=50`。 |
                | `endpoint` | string | 是 | `http://...`、`https://...` | S3 endpoint | 写入 `spec.endpoint` | 必须以 `http://` 或 `https://` 开头。 |
                | `accessKey` | string | 是 | 任意非空字符串 | S3 AccessKey | 写入 `spec.accessKey` | 敏感字段，响应 DTO 不返回。 |
                | `secretKey` | string | 是 | 任意非空字符串 | S3 SecretKey | 写入 `spec.secretKey` | 敏感字段，响应 DTO 不返回。 |
                | `addressingStyle` | string | 否 | `PathStyle`、`VirtualHostedStyle`、空 | S3 地址风格 | 控制 AWS client `UsePathStyle` 和 Velero BSL `s3ForcePathStyle` | 空值归一化为 `PathStyle`；其他值返回 400。 |
                | `caBundle` | string | 否 | PEM 证书 | 内联 CA | 创建或更新 server 托管 CA Secret | 与 `caSecretRef` 互斥。 |
                | `caSecretRef.name` | string | 否 | Secret 名称 | 既有 CA Secret 引用 | 让 operator 读取同 namespace Secret 中的 `ca.crt` | 与 `caBundle` 互斥；Secret 不存在返回 400。 |
                | `quotaBytes` | integer | 否 | int64 字节数 | 容量上限 | 写入 `spec.quotaBytes` 供前端和统计展示 | 0 或缺省表示未设置。 |

                ## 4. 返回的详细说明

                成功响应 HTTP 状态码为 201，使用统一 `Envelope`，`data` 为创建后的 `DisasterStorageDTO`。`accessKey/secretKey/caBundle` 不会返回。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | 成功固定为 `0`。 |
                | `data.id` | string | Kubernetes UID | 存储仓库 UID | StorageRepository metadata | 用于前端唯一标识。 |
                | `data.name` | string | 资源名称 | 存储仓库名称 | StorageRepository metadata | 创建时来自 `body.name`。 |
                | `data.namespace` | string | `disaster-system` | 所在 namespace | server 配置的管理命名空间 | 当前 handler 读写配置的管理命名空间（默认 `disaster-system`）。 |
                | `data.labels` | object | label map | 资源标签 | StorageRepository metadata | 列表动态过滤会按 label value 包含匹配。 |
                | `data.creation_timestamp` | string | RFC3339 时间 | 创建时间 | Kubernetes metadata | 列表可按该字段排序。 |
                | `data.spec.storageType` | string | 任意字符串 | 存储类型 | StorageRepository spec | 当前链路按 S3 兼容对象存储使用。 |
                | `data.spec.bucket` | string | bucket 名称 | 对象存储 bucket | StorageRepository spec | operator 会 HeadBucket，不存在时尝试创建 bucket。 |
                | `data.spec.region` | string | region | 对象存储区域 | StorageRepository spec | S3 client 配置字段。 |
                | `data.spec.endpoint` | string | URL | 对象存储访问地址 | StorageRepository spec | 创建和更新时要求 `http://` 或 `https://` 前缀。 |
                | `data.spec.addressingStyle` | string | `PathStyle`、`VirtualHostedStyle` | S3 地址风格 | StorageRepository spec | 空值会归一化为 `PathStyle`。 |
                | `data.spec.caConfigured` | boolean | `true`、`false` | 是否配置 CA | server DTO | 由 `spec.caSecretRef.name` 是否非空计算。 |
                | `data.spec.caSecretRef.name` | string | Secret 名称 | CA Secret 引用 | StorageRepository spec | Secret 必须在 `disaster-system` 且包含 `ca.crt`。 |
                | `data.spec.quotaBytes` | integer | 字节数 | 容量上限 | StorageRepository spec | `0` 或缺省表示未设置。 |
                | `data.status.status` | string | `Available`、`Unavailable` | 可用状态 | operator status | operator 最近一次 S3 校验结果。 |
                | `data.status.lastCheckTime` | string | RFC3339 时间 | 最近检查时间 | operator status | 每次 reconcile 校验后更新。 |
                | `data.status.reason` | string | `Available`、`ValidationFailed` 等 | 状态原因 | operator status | 失败时用于定位原因。 |
                | `data.status.message` | string | 任意字符串 | 状态消息 | operator status | 成功或失败的详细说明。 |
                | `data.status.usedSpaceBytes` | integer | 字节数 | 已用容量 | operator status | operator 扫描 bucket 对象后统计。 |
                | `data.status.totalBackupCount` | integer | 数量 | 备份对象数量 | operator status | operator 按 backups/ 前缀统计。 |
                | `message` | string | `OK` | 响应消息 | server | 成功固定为 `OK`。 |
                | `trace_id` | string | trace id | 请求追踪 ID | trace middleware | 写操作会把 trace id 注入 CR annotation 供 operator 事件关联。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 请求体或参数非法 | JSON/binding 失败、`addressingStyle` 非法、`caBundle` 与 `caSecretRef` 同时传入、`caSecretRef.name` 为空、CA Secret 不存在、endpoint 不以 `http://` 或 `https://` 开头。 |
                | 401 | 无固定业务码 | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。 |
                | 409 | `3009` | 同名资源已存在 | Kubernetes Create 返回 AlreadyExists。 |
                | 500 | `5000` | 服务端或 Kubernetes 写入失败 | 托管 CA Secret 创建/更新失败，或 CRD Create 返回非 AlreadyExists 错误。 |

                异步 operator 校验失败不会改变本接口 201 响应；需要通过详情、列表、watch 或历史事件读取 `Unavailable/reason/message`。
            operationId: post_apis_storage_testudo_softcdata_com_v1_storages
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/StorageRepositoryCreateRequest'
                description: 创建存储仓库请求体。
                required: true
            responses:
                '201':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterStorageEnvelope'
                    description: 成功创建 StorageRepository。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求体或参数非法。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
                '409':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 同名 StorageRepository 已存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 托管 CA Secret 或 StorageRepository 创建失败。
            security:
                - bearerAuth: []
            summary: 存储仓库 POST /apis/storage.testudo.softcdata.com/v1/storages
            tags:
                - 存储仓库
            x-async-failure-status:
                readVia:
                    - GET /apis/storage.testudo.softcdata.com/v1/storages/{name}
                    - GET /apis/storage.testudo.softcdata.com/v1/storages
                    - GET /apis/storage.testudo.softcdata.com/v1/watch/storages
                    - GET /apis/v1/{resource}/{name}/history
                statusFields:
                    - status.status
                    - status.reason
                    - status.message
                    - status.lastCheckTime
                    - status.usedSpaceBytes
                    - status.totalBackupCount
            x-controlled-resources:
                direct:
                    - StorageRepository CRD
                    - Kubernetes Secret for custom CA when requested
                downstream:
                    - S3-compatible bucket and objects observed by operator
                    - Velero Secret and BackupStorageLocation maintained by downstream backup/restore or connectivity flows
                scope: 管理集群 disaster-system namespace 中的 StorageRepository，以及请求指定的对象存储配置
            x-operator-chain:
                controller: StorageRepositoryReconciler validates S3 configuration, syncs dependency labels, adds storage finalizer, reports task events, writes status, and updates usage statistics
                crd: StorageRepository
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/storagerepository_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/storagerepository_controller.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/storage_runtime.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/BSL.go
                serverFile: internal/apis/disaster_storage/v1/router.go
                serverHandler: c.createStorage
            x-runapi-target-id: example-runapi-target-id
    /apis/storage.testudo.softcdata.com/v1/storages/{name}:
        delete:
            description: |-
                ## 1. 接口用来干什么

                删除指定 `StorageRepository`。server 在删除前尽力读取目标对象、写入 trace/user annotation；如果目标引用的是 server 托管 CA Secret `storage-ca-<name>`，server 会尽力删除该 Secret，然后向 Kubernetes 提交 CRD 删除。

                ## 2. 控制哪些资源

                直接操作资源：
                - 删除 `StorageRepository disaster-system/{name}`。
                - 可能删除 `Secret disaster-system/storage-ca-<name>`。

                下层影响资源：
                - 删除 CRD 后触发 `StorageRepositoryReconciler.handleDelete`。当前 operator 中旧的 DisasterPolicy 引用拦截逻辑已被注释旁路，operator 会上报删除开始/完成事件，然后移除 `testudo.softcdata.com/storage-finalizer`。
                - 本接口不会删除 S3 bucket/object，不会直接删除目标集群 Velero `BackupStorageLocation` 或 Velero credential `Secret`。

                资源作用范围：
                - 操作配置的管理命名空间（默认 `disaster-system`）。
                - 删除前 annotation 更新失败和托管 CA Secret 删除失败当前被忽略，不影响后续 CRD Delete。

                ## 3. 入参的详细说明

                | 字段 | 位置 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|---|
                | `name` | path | string | 是 | StorageRepository 名称 | 要删除的存储仓库名称 | 用于 Get、托管 CA Secret 名称判断和 Delete | 为空时路由通常无法匹配。 |

                ## 4. 返回的详细说明

                成功响应使用统一 `Envelope`，`data` 为删除提交结果。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | 成功固定为 `0`。 |
                | `data.name` | string | path name | 已提交删除的资源名称 | path | 表示 Delete 请求已成功提交。 |
                | `message` | string | `OK` | 响应消息 | server | 成功固定为 `OK`。 |
                | `trace_id` | string | trace id | 请求追踪 ID | trace middleware | server 会尽力写入 CR annotation，供 operator 删除事件关联。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 401 | 无固定业务码 | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。 |
                | 404 | `3004` | 目标资源不存在 | Kubernetes Delete 返回 NotFound。 |
                | 500 | `5000` | 删除失败 | Kubernetes Delete 返回非 NotFound 错误。 |

                删除前 Get、annotation Update 和托管 CA Secret 删除失败当前不会返回给客户端。
            operationId: delete_apis_storage_testudo_softcdata_com_v1_storages_name
            parameters:
                - description: 要删除的 StorageRepository 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterStorageDeleteEnvelope'
                    description: 成功提交 StorageRepository 删除。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 目标 StorageRepository 不存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: StorageRepository 删除失败。
            security:
                - bearerAuth: []
            summary: 存储仓库 DELETE /apis/storage.testudo.softcdata.com/v1/storages/:name
            tags:
                - 存储仓库
            x-async-failure-status:
                readVia:
                    - GET /apis/v1/{resource}/{name}/history
                statusFields:
                    - operator task events for delete start and finish
            x-controlled-resources:
                direct:
                    - StorageRepository CRD
                    - Server-managed CA Secret when referenced
                downstream:
                    - 无直接删除 S3 bucket/object
                    - 无直接删除 Velero Secret/BackupStorageLocation
                scope: disaster-system namespace 中指定名称的 StorageRepository
            x-operator-chain:
                controller: StorageRepositoryReconciler validates S3 configuration, syncs dependency labels, adds storage finalizer, reports task events, writes status, and updates usage statistics
                crd: StorageRepository
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/storagerepository_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/storagerepository_controller.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/storage_runtime.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/BSL.go
                serverFile: internal/apis/disaster_storage/v1/router.go
                serverHandler: c.deleteStorage
            x-runapi-target-id: example-runapi-target-id
        get:
            description: |-
                ## 1. 接口用来干什么

                查询单个存储仓库详情。server 直接读取 `StorageRepository disaster-system/{name}`，转换为 `DisasterStorageDTO` 返回。

                ## 2. 控制哪些资源

                直接操作资源：
                - 只读指定 `StorageRepository` CRD。

                下层影响资源：
                - 无。该接口不触发 operator reconcile，不访问 S3、不读取 CA Secret、不访问 Velero 资源。

                资源作用范围：
                - 读取配置的管理命名空间（默认 `disaster-system`）。
                - 返回的 `status` 字段来自 operator 最近一次 reconcile 写入，间接反映 CA Secret、S3 bucket/object 校验和统计结果。

                ## 3. 入参的详细说明

                | 字段 | 位置 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|---|
                | `name` | path | string | 是 | StorageRepository 名称 | 要查询的存储仓库名称 | 用于 Kubernetes Get | 为空时路由通常无法匹配。 |

                ## 4. 返回的详细说明

                成功响应使用统一 `Envelope`，`data` 为 `DisasterStorageDTO`。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | 成功固定为 `0`。 |
                | `data.id` | string | Kubernetes UID | 存储仓库 UID | StorageRepository metadata | 用于前端唯一标识。 |
                | `data.name` | string | 资源名称 | 存储仓库名称 | StorageRepository metadata | 创建时来自 `body.name`。 |
                | `data.namespace` | string | `disaster-system` | 所在 namespace | server 配置的管理命名空间 | 当前 handler 读写配置的管理命名空间（默认 `disaster-system`）。 |
                | `data.labels` | object | label map | 资源标签 | StorageRepository metadata | 列表动态过滤会按 label value 包含匹配。 |
                | `data.creation_timestamp` | string | RFC3339 时间 | 创建时间 | Kubernetes metadata | 列表可按该字段排序。 |
                | `data.spec.storageType` | string | 任意字符串 | 存储类型 | StorageRepository spec | 当前链路按 S3 兼容对象存储使用。 |
                | `data.spec.bucket` | string | bucket 名称 | 对象存储 bucket | StorageRepository spec | operator 会 HeadBucket，不存在时尝试创建 bucket。 |
                | `data.spec.region` | string | region | 对象存储区域 | StorageRepository spec | S3 client 配置字段。 |
                | `data.spec.endpoint` | string | URL | 对象存储访问地址 | StorageRepository spec | 创建和更新时要求 `http://` 或 `https://` 前缀。 |
                | `data.spec.addressingStyle` | string | `PathStyle`、`VirtualHostedStyle` | S3 地址风格 | StorageRepository spec | 空值会归一化为 `PathStyle`。 |
                | `data.spec.caConfigured` | boolean | `true`、`false` | 是否配置 CA | server DTO | 由 `spec.caSecretRef.name` 是否非空计算。 |
                | `data.spec.caSecretRef.name` | string | Secret 名称 | CA Secret 引用 | StorageRepository spec | Secret 必须在 `disaster-system` 且包含 `ca.crt`。 |
                | `data.spec.quotaBytes` | integer | 字节数 | 容量上限 | StorageRepository spec | `0` 或缺省表示未设置。 |
                | `data.status.status` | string | `Available`、`Unavailable` | 可用状态 | operator status | operator 最近一次 S3 校验结果。 |
                | `data.status.lastCheckTime` | string | RFC3339 时间 | 最近检查时间 | operator status | 每次 reconcile 校验后更新。 |
                | `data.status.reason` | string | `Available`、`ValidationFailed` 等 | 状态原因 | operator status | 失败时用于定位原因。 |
                | `data.status.message` | string | 任意字符串 | 状态消息 | operator status | 成功或失败的详细说明。 |
                | `data.status.usedSpaceBytes` | integer | 字节数 | 已用容量 | operator status | operator 扫描 bucket 对象后统计。 |
                | `data.status.totalBackupCount` | integer | 数量 | 备份对象数量 | operator status | operator 按 backups/ 前缀统计。 |
                | `message` | string | `OK` | 响应消息 | server | 成功固定为 `OK`。 |
                | `trace_id` | string | trace id | 请求追踪 ID | trace middleware | 写操作会把 trace id 注入 CR annotation 供 operator 事件关联。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 401 | 无固定业务码 | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。 |
                | 404 | `3004` | 目标资源不存在 | Kubernetes Get 返回 NotFound。 |
                | 500 | `5000` | 读取失败 | Kubernetes Get 返回非 NotFound 错误。 |
            operationId: get_apis_storage_testudo_softcdata_com_v1_storages_name
            parameters:
                - description: 要查询的 StorageRepository 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterStorageEnvelope'
                    description: 成功返回 StorageRepository 详情。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 目标 StorageRepository 不存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: StorageRepository 读取失败。
            security:
                - bearerAuth: []
            summary: 存储仓库 GET /apis/storage.testudo.softcdata.com/v1/storages/:name
            tags:
                - 存储仓库
            x-controlled-resources:
                direct:
                    - StorageRepository CRD
                downstream:
                    - 无
                scope: disaster-system namespace 中指定名称的 StorageRepository
            x-operator-chain:
                controller: StorageRepositoryReconciler validates S3 configuration, syncs dependency labels, adds storage finalizer, reports task events, writes status, and updates usage statistics
                crd: StorageRepository
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/storagerepository_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/storagerepository_controller.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/storage_runtime.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/BSL.go
                serverFile: internal/apis/disaster_storage/v1/router.go
                serverHandler: c.storage
            x-runapi-target-id: example-runapi-target-id
        patch:
            description: |-
                ## 1. 接口用来干什么

                局部更新指定存储仓库的可变字段。server 读取现有 `StorageRepository`，只按请求体中非 null 字段更新 `accessKey/secretKey/bucket/region/addressingStyle/CA`，然后更新 CRD。当前 PATCH 不支持修改 `storageType`、`endpoint`、`quotaBytes`。

                ## 2. 控制哪些资源

                直接操作资源：
                - 更新 `StorageRepository` CRD spec 的部分字段。
                - 可选读取、创建、更新或删除 CA Secret。

                下层影响资源：
                - CRD 更新后触发 `StorageRepositoryReconciler`。operator 上报“编辑存储”事件，重新执行 S3 校验，更新 `Available/Unavailable`、`reason/message/lastCheckTime/usedSpaceBytes/totalBackupCount`。
                - 后续 Velero `Secret/BackupStorageLocation` 会在相关 operator 链路中使用更新后的凭据和配置。

                资源作用范围：
                - 操作配置的管理命名空间（默认 `disaster-system`）。
                - 如果请求体没有任何可更新字段，server 直接返回现有 DTO，不执行 Update，也不注入新的 trace/user annotation。

                ## 3. 入参的详细说明

                | 字段 | 位置 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|---|
                | `name` | path | string | 是 | StorageRepository 名称 | 要更新的存储仓库名称 | 用于 Kubernetes Get/Update | 为空时路由通常无法匹配。 |
                | `accessKey` | body | string | 否 | 任意字符串 | S3 AccessKey | 非 null 时覆盖 `spec.accessKey` | 敏感字段，响应不返回。 |
                | `secretKey` | body | string | 否 | 任意字符串 | S3 SecretKey | 非 null 时覆盖 `spec.secretKey` | 敏感字段，响应不返回。 |
                | `bucket` | body | string | 否 | bucket 名称 | S3 bucket | 非 null 时覆盖 `spec.bucket` | PATCH 不执行长度校验。 |
                | `region` | body | string | 否 | region | S3 region | 非 null 时覆盖 `spec.region` | 无默认值。 |
                | `addressingStyle` | body | string | 否 | `PathStyle`、`VirtualHostedStyle`、空 | S3 地址风格 | 非 null 时覆盖 `spec.addressingStyle` | 空值归一化为 `PathStyle`；其他值返回 400。 |
                | `caBundle` | body | string | 否 | PEM 证书 | 内联 CA | 创建或更新 server 托管 CA Secret，并覆盖 `spec.caSecretRef` | 与 `caSecretRef`、`clearCa=true` 互斥。 |
                | `caSecretRef.name` | body | string | 否 | Secret 名称 | 既有 CA Secret 引用 | 检查 Secret 存在后覆盖 `spec.caSecretRef` | 与 `caBundle`、`clearCa=true` 互斥。 |
                | `clearCa` | body | boolean | 否 | `true`、`false` | 清除 CA 配置 | `true` 时清空 `spec.caSecretRef`，并在旧引用为托管 Secret 时删除 Secret | 与 `caBundle`、`caSecretRef` 互斥；`false` 不产生更新。 |

                ## 4. 返回的详细说明

                成功响应使用统一 `Envelope`，`data` 为更新后的 `DisasterStorageDTO`。敏感字段不会返回。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | 成功固定为 `0`。 |
                | `data.id` | string | Kubernetes UID | 存储仓库 UID | StorageRepository metadata | 用于前端唯一标识。 |
                | `data.name` | string | 资源名称 | 存储仓库名称 | StorageRepository metadata | 创建时来自 `body.name`。 |
                | `data.namespace` | string | `disaster-system` | 所在 namespace | server 配置的管理命名空间 | 当前 handler 读写配置的管理命名空间（默认 `disaster-system`）。 |
                | `data.labels` | object | label map | 资源标签 | StorageRepository metadata | 列表动态过滤会按 label value 包含匹配。 |
                | `data.creation_timestamp` | string | RFC3339 时间 | 创建时间 | Kubernetes metadata | 列表可按该字段排序。 |
                | `data.spec.storageType` | string | 任意字符串 | 存储类型 | StorageRepository spec | 当前链路按 S3 兼容对象存储使用。 |
                | `data.spec.bucket` | string | bucket 名称 | 对象存储 bucket | StorageRepository spec | operator 会 HeadBucket，不存在时尝试创建 bucket。 |
                | `data.spec.region` | string | region | 对象存储区域 | StorageRepository spec | S3 client 配置字段。 |
                | `data.spec.endpoint` | string | URL | 对象存储访问地址 | StorageRepository spec | 创建和更新时要求 `http://` 或 `https://` 前缀。 |
                | `data.spec.addressingStyle` | string | `PathStyle`、`VirtualHostedStyle` | S3 地址风格 | StorageRepository spec | 空值会归一化为 `PathStyle`。 |
                | `data.spec.caConfigured` | boolean | `true`、`false` | 是否配置 CA | server DTO | 由 `spec.caSecretRef.name` 是否非空计算。 |
                | `data.spec.caSecretRef.name` | string | Secret 名称 | CA Secret 引用 | StorageRepository spec | Secret 必须在 `disaster-system` 且包含 `ca.crt`。 |
                | `data.spec.quotaBytes` | integer | 字节数 | 容量上限 | StorageRepository spec | `0` 或缺省表示未设置。 |
                | `data.status.status` | string | `Available`、`Unavailable` | 可用状态 | operator status | operator 最近一次 S3 校验结果。 |
                | `data.status.lastCheckTime` | string | RFC3339 时间 | 最近检查时间 | operator status | 每次 reconcile 校验后更新。 |
                | `data.status.reason` | string | `Available`、`ValidationFailed` 等 | 状态原因 | operator status | 失败时用于定位原因。 |
                | `data.status.message` | string | 任意字符串 | 状态消息 | operator status | 成功或失败的详细说明。 |
                | `data.status.usedSpaceBytes` | integer | 字节数 | 已用容量 | operator status | operator 扫描 bucket 对象后统计。 |
                | `data.status.totalBackupCount` | integer | 数量 | 备份对象数量 | operator status | operator 按 backups/ 前缀统计。 |
                | `message` | string | `OK` | 响应消息 | server | 成功固定为 `OK`。 |
                | `trace_id` | string | trace id | 请求追踪 ID | trace middleware | 写操作会把 trace id 注入 CR annotation 供 operator 事件关联。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 请求体或参数非法 | JSON/binding 失败、`addressingStyle` 非法、CA 参数互斥、`caSecretRef.name` 为空、CA Secret 不存在。 |
                | 401 | 无固定业务码 | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。 |
                | 404 | `3004` | 目标资源不存在 | Get 现有 StorageRepository 返回 NotFound。 |
                | 500 | `5000` | 更新失败 | Get 返回非 NotFound 错误、托管 CA Secret 操作失败、StorageRepository Update 失败。 |

                异步 operator 校验失败不会改变本接口 200 响应，需要通过详情、列表、watch 或历史事件读取。
            operationId: patch_apis_storage_testudo_softcdata_com_v1_storages_name
            parameters:
                - description: 要局部更新的 StorageRepository 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/StorageRepositoryPatchRequest'
                description: 局部更新存储仓库请求体。
                required: true
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterStorageEnvelope'
                    description: 成功返回更新后的 StorageRepository。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求体或参数非法。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 目标 StorageRepository 不存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: StorageRepository 或托管 CA Secret 更新失败。
            security:
                - bearerAuth: []
            summary: 存储仓库 PATCH /apis/storage.testudo.softcdata.com/v1/storages/:name
            tags:
                - 存储仓库
            x-async-failure-status:
                readVia:
                    - GET /apis/storage.testudo.softcdata.com/v1/storages/{name}
                    - GET /apis/storage.testudo.softcdata.com/v1/storages
                    - GET /apis/storage.testudo.softcdata.com/v1/watch/storages
                    - GET /apis/v1/{resource}/{name}/history
                statusFields:
                    - status.status
                    - status.reason
                    - status.message
                    - status.lastCheckTime
                    - status.usedSpaceBytes
                    - status.totalBackupCount
            x-controlled-resources:
                direct:
                    - StorageRepository CRD
                    - Kubernetes Secret for custom CA when requested
                downstream:
                    - S3-compatible bucket and objects observed by operator
                    - Velero Secret and BackupStorageLocation maintained by downstream backup/restore or connectivity flows
                scope: 管理集群 disaster-system namespace 中的 StorageRepository，以及请求指定的对象存储配置
            x-operator-chain:
                controller: StorageRepositoryReconciler validates S3 configuration, syncs dependency labels, adds storage finalizer, reports task events, writes status, and updates usage statistics
                crd: StorageRepository
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/storagerepository_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/storagerepository_controller.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/storage_runtime.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/BSL.go
                serverFile: internal/apis/disaster_storage/v1/router.go
                serverHandler: c.patchStorage
            x-runapi-target-id: example-runapi-target-id
        put:
            description: |-
                ## 1. 接口用来干什么

                更新存储仓库配置。当前 handler 虽然路由带 `:name`，但实际使用请求体 `name` 字段定位资源；请求体中的非空字段覆盖现有 CRD spec，指针字段 `quotaBytes` 可以显式设置为 0。

                ## 2. 控制哪些资源

                直接操作资源：
                - 更新 `StorageRepository` CRD。
                - 可选读取、创建、更新或删除 CA Secret。

                下层影响资源：
                - CRD 更新后触发 `StorageRepositoryReconciler`。operator 上报“编辑存储”事件，重新执行 S3 校验，更新 `Available/Unavailable`、`reason/message/lastCheckTime/usedSpaceBytes/totalBackupCount`。
                - 后续 Velero `Secret/BackupStorageLocation` 会在相关 operator 链路中使用更新后的配置。

                资源作用范围：
                - 操作配置的管理命名空间（默认 `disaster-system`）。
                - path `name` 当前只参与路由匹配，不参与 Get；调用方必须让 path `name` 与 body `name` 保持一致，避免误更新 body 指向的资源。
                - Update 使用 `retry.RetryOnConflict`，多次资源版本冲突后返回 409。

                ## 3. 入参的详细说明

                | 字段 | 位置 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|---|
                | `name` | path | string | 是 | StorageRepository 名称 | 路由匹配 | 当前 handler 不使用该值定位资源 | 应与 body `name` 一致。 |
                | `name` | body | string | 是 | StorageRepository 名称 | 实际要更新的资源名称 | 用于 Kubernetes Get/Update | binding 要求必传。 |
                | `storageType` | body | string | 否 | 任意字符串 | 存储类型 | 非空时覆盖 `spec.storageType` | 空字符串表示不更新。 |
                | `bucket` | body | string | 否 | bucket 名称 | S3 bucket | 非空时覆盖 `spec.bucket` | 空字符串表示不更新。 |
                | `region` | body | string | 否 | region | S3 region | 非空时覆盖 `spec.region` | 空字符串表示不更新。 |
                | `endpoint` | body | string | 否 | `http://...`、`https://...` | S3 endpoint | 非空时覆盖 `spec.endpoint` | 传入时必须以 `http://` 或 `https://` 开头。 |
                | `accessKey` | body | string | 否 | 任意字符串 | S3 AccessKey | 非空时覆盖 `spec.accessKey` | 敏感字段，响应不返回。 |
                | `secretKey` | body | string | 否 | 任意字符串 | S3 SecretKey | 非空时覆盖 `spec.secretKey` | 敏感字段，响应不返回。 |
                | `addressingStyle` | body | string | 否 | `PathStyle`、`VirtualHostedStyle`、空 | S3 地址风格 | 非 null 时覆盖 `spec.addressingStyle` | 空值归一化为 `PathStyle`；非法值返回 400。 |
                | `caBundle` | body | string | 否 | PEM 证书 | 内联 CA | 创建或更新 server 托管 CA Secret，并覆盖 `spec.caSecretRef` | 与 `caSecretRef`、`clearCa=true` 互斥。 |
                | `caSecretRef.name` | body | string | 否 | Secret 名称 | 既有 CA Secret 引用 | 检查 Secret 存在后覆盖 `spec.caSecretRef` | 与 `caBundle`、`clearCa=true` 互斥。 |
                | `clearCa` | body | boolean | 否 | `true`、`false` | 清除 CA 配置 | `true` 时清空 `spec.caSecretRef`，并在旧引用为托管 Secret 时删除 Secret | 与 `caBundle`、`caSecretRef` 互斥。 |
                | `quotaBytes` | body | integer | 否 | int64 字节数 | 容量上限 | 非 null 时覆盖 `spec.quotaBytes` | 可传 0。 |

                ## 4. 返回的详细说明

                成功响应使用统一 `Envelope`，`data` 为更新后的 `DisasterStorageDTO`。敏感字段不会返回。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | 成功固定为 `0`。 |
                | `data.id` | string | Kubernetes UID | 存储仓库 UID | StorageRepository metadata | 用于前端唯一标识。 |
                | `data.name` | string | 资源名称 | 存储仓库名称 | StorageRepository metadata | 创建时来自 `body.name`。 |
                | `data.namespace` | string | `disaster-system` | 所在 namespace | server 配置的管理命名空间 | 当前 handler 读写配置的管理命名空间（默认 `disaster-system`）。 |
                | `data.labels` | object | label map | 资源标签 | StorageRepository metadata | 列表动态过滤会按 label value 包含匹配。 |
                | `data.creation_timestamp` | string | RFC3339 时间 | 创建时间 | Kubernetes metadata | 列表可按该字段排序。 |
                | `data.spec.storageType` | string | 任意字符串 | 存储类型 | StorageRepository spec | 当前链路按 S3 兼容对象存储使用。 |
                | `data.spec.bucket` | string | bucket 名称 | 对象存储 bucket | StorageRepository spec | operator 会 HeadBucket，不存在时尝试创建 bucket。 |
                | `data.spec.region` | string | region | 对象存储区域 | StorageRepository spec | S3 client 配置字段。 |
                | `data.spec.endpoint` | string | URL | 对象存储访问地址 | StorageRepository spec | 创建和更新时要求 `http://` 或 `https://` 前缀。 |
                | `data.spec.addressingStyle` | string | `PathStyle`、`VirtualHostedStyle` | S3 地址风格 | StorageRepository spec | 空值会归一化为 `PathStyle`。 |
                | `data.spec.caConfigured` | boolean | `true`、`false` | 是否配置 CA | server DTO | 由 `spec.caSecretRef.name` 是否非空计算。 |
                | `data.spec.caSecretRef.name` | string | Secret 名称 | CA Secret 引用 | StorageRepository spec | Secret 必须在 `disaster-system` 且包含 `ca.crt`。 |
                | `data.spec.quotaBytes` | integer | 字节数 | 容量上限 | StorageRepository spec | `0` 或缺省表示未设置。 |
                | `data.status.status` | string | `Available`、`Unavailable` | 可用状态 | operator status | operator 最近一次 S3 校验结果。 |
                | `data.status.lastCheckTime` | string | RFC3339 时间 | 最近检查时间 | operator status | 每次 reconcile 校验后更新。 |
                | `data.status.reason` | string | `Available`、`ValidationFailed` 等 | 状态原因 | operator status | 失败时用于定位原因。 |
                | `data.status.message` | string | 任意字符串 | 状态消息 | operator status | 成功或失败的详细说明。 |
                | `data.status.usedSpaceBytes` | integer | 字节数 | 已用容量 | operator status | operator 扫描 bucket 对象后统计。 |
                | `data.status.totalBackupCount` | integer | 数量 | 备份对象数量 | operator status | operator 按 backups/ 前缀统计。 |
                | `message` | string | `OK` | 响应消息 | server | 成功固定为 `OK`。 |
                | `trace_id` | string | trace id | 请求追踪 ID | trace middleware | 写操作会把 trace id 注入 CR annotation 供 operator 事件关联。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 请求体或参数非法 | JSON/binding 失败、`addressingStyle` 非法、CA 参数互斥、endpoint scheme 非法、`caSecretRef.name` 为空、CA Secret 不存在。 |
                | 401 | 无固定业务码 | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。 |
                | 404 | `3004` | 目标资源不存在 | 按 body `name` Get 现有 StorageRepository 返回 NotFound。 |
                | 409 | `3009` | 资源版本冲突 | `RetryOnConflict` 重试后仍收到 Kubernetes Conflict。 |
                | 500 | `5000` | 更新失败 | 托管 CA Secret 操作失败，或 StorageRepository Update 返回非 NotFound、非 Conflict 错误。 |

                异步 operator 校验失败不会改变本接口 200 响应，需要通过详情、列表、watch 或历史事件读取。
            operationId: put_apis_storage_testudo_softcdata_com_v1_storages_name
            parameters:
                - description: 路由中的 StorageRepository 名称；当前 handler 实际用 body.name 定位资源，调用方应保持两者一致。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/StorageRepositoryUpdateRequest'
                description: 更新存储仓库请求体。
                required: true
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterStorageEnvelope'
                    description: 成功返回更新后的 StorageRepository。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求体或参数非法。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: body.name 指向的 StorageRepository 不存在。
                '409':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源版本冲突。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: StorageRepository 或托管 CA Secret 更新失败。
            security:
                - bearerAuth: []
            summary: 存储仓库 PUT /apis/storage.testudo.softcdata.com/v1/storages/:name
            tags:
                - 存储仓库
            x-async-failure-status:
                readVia:
                    - GET /apis/storage.testudo.softcdata.com/v1/storages/{name}
                    - GET /apis/storage.testudo.softcdata.com/v1/storages
                    - GET /apis/storage.testudo.softcdata.com/v1/watch/storages
                    - GET /apis/v1/{resource}/{name}/history
                statusFields:
                    - status.status
                    - status.reason
                    - status.message
                    - status.lastCheckTime
                    - status.usedSpaceBytes
                    - status.totalBackupCount
            x-controlled-resources:
                direct:
                    - StorageRepository CRD
                    - Kubernetes Secret for custom CA when requested
                downstream:
                    - S3-compatible bucket and objects observed by operator
                    - Velero Secret and BackupStorageLocation maintained by downstream backup/restore or connectivity flows
                scope: 管理集群 disaster-system namespace 中的 StorageRepository，以及请求指定的对象存储配置
            x-operator-chain:
                controller: StorageRepositoryReconciler validates S3 configuration, syncs dependency labels, adds storage finalizer, reports task events, writes status, and updates usage statistics
                crd: StorageRepository
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/storagerepository_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/storagerepository_controller.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/storage_runtime.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/BSL.go
                serverFile: internal/apis/disaster_storage/v1/router.go
                serverHandler: c.updateStorage
            x-runapi-target-id: example-runapi-target-id
    /apis/storage.testudo.softcdata.com/v1/storages/{name}/validate:
        get:
            description: |-
                ## 1. 接口用来干什么

                检查指定存储仓库最近一次 operator 校验状态是否为 `Available`。server 只读取 `StorageRepository.status.status`，不主动连接 S3。

                ## 2. 控制哪些资源

                直接操作资源：
                - 只读指定 `StorageRepository` CRD。

                下层影响资源：
                - 无。该接口不触发 operator reconcile，不访问 S3、不读取 CA Secret、不访问 Velero BSL。

                资源作用范围：
                - 读取配置的管理命名空间（默认 `disaster-system`）。
                - 判定只使用 `item.Status.Status == StorageRepositoryStatusAvailable`。

                ## 3. 入参的详细说明

                | 字段 | 位置 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|---|
                | `name` | path | string | 是 | StorageRepository 名称 | 要检查的存储仓库名称 | 用于 Kubernetes Get | 为空时路由通常无法匹配。 |

                ## 4. 返回的详细说明

                成功响应使用统一 `Envelope`，`data` 为布尔值。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | 成功固定为 `0`。 |
                | `data` | boolean | `true`、`false` | 是否可用 | server 状态判定 | `true` 表示 status 为 `Available`；其他状态返回 `false`。 |
                | `message` | string | `OK` | 响应消息 | server | 成功固定为 `OK`。 |
                | `trace_id` | string | trace id | 请求追踪 ID | trace middleware | 用于排查。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 401 | 无固定业务码 | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。 |
                | 404 | `3004` | 目标资源不存在 | Kubernetes Get 返回 NotFound。 |
                | 500 | `5000` | 读取失败 | Kubernetes Get 返回非 NotFound 错误。 |

                operator 的 S3 校验失败不会让该接口返回 HTTP 错误，只会使 `data=false`。
            operationId: get_apis_storage_testudo_softcdata_com_v1_storages_name_validate
            parameters:
                - description: 要检查可用状态的 StorageRepository 名称。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/StorageBooleanEnvelope'
                    description: 成功返回可用状态布尔值。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
                '404':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 目标 StorageRepository 不存在。
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: StorageRepository 读取失败。
            security:
                - bearerAuth: []
            summary: 存储仓库 GET /apis/storage.testudo.softcdata.com/v1/storages/:name/validate
            tags:
                - 存储仓库
            x-controlled-resources:
                direct:
                    - StorageRepository CRD
                downstream:
                    - 无
                scope: disaster-system namespace 中指定名称的 StorageRepository status
            x-operator-chain:
                controller: StorageRepositoryReconciler validates S3 configuration, syncs dependency labels, adds storage finalizer, reports task events, writes status, and updates usage statistics
                crd: StorageRepository
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/storagerepository_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/storagerepository_controller.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/storage_runtime.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/BSL.go
                serverFile: internal/apis/disaster_storage/v1/router.go
                serverHandler: c.validateStorage
            x-runapi-target-id: example-runapi-target-id
    /apis/storage.testudo.softcdata.com/v1/storages/connectivity/validate:
        post:
            description: |-
                ## 1. 接口用来干什么

                检查指定存储仓库在目标集群中的 Velero `BackupStorageLocation` 是否可用。server 先获取目标集群 kube client，再通过 `BSLVerifier.VerifyBSL` 按 `<storage_name>-<cluster_name>` 查询目标集群 `velero` namespace 的 BSL；如果 BSL 不存在，会 patch 管理集群 `Cluster` 的 `testudo.softcdata.com/ensure-storage` 注解并短轮询等待 BSL 创建和变为 Available。

                ## 2. 控制哪些资源

                直接操作资源：
                - 读取管理集群 `Cluster`，并在 BSL 不存在时 patch `Cluster` annotation `testudo.softcdata.com/ensure-storage=<storage_name>`。
                - 读取目标集群 Velero `BackupStorageLocation`。

                下层影响资源：
                - `ClusterReconciler` 监听 ensure-storage 注解，读取 `StorageRepository`，调用 `DefaultBSL.ApplyStorageRepository` 在目标集群创建或更新 Velero credential `Secret` 和 `BackupStorageLocation`，随后移除注解。
                - `DefaultBSL.ApplyStorageRepository` 会把 StorageRepository 凭据写入 Velero Secret，并设置 BSL bucket、prefix、region、s3Url、s3ForcePathStyle 与 CA 证书。

                资源作用范围：
                - 管理集群：`Cluster` CR、`StorageRepository` CR。
                - 目标集群：`velero` namespace 下的 `BackupStorageLocation` 与 Velero credential Secret。
                - 对象存储：由 Velero BSL 状态反映连接情况。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `cluster_name` | string | 是 | Cluster 名称 | 目标集群名称 | 获取目标集群 client，并组成 BSL 名称后缀 | binding required。 |
                | `storage_name` | string | 是 | StorageRepository 名称 | 存储仓库名称 | 组成 BSL 名称前缀，并作为 ensure-storage 注解值 | binding required。 |

                ## 4. 返回的详细说明

                成功 HTTP 响应统一为 `Envelope`，`data` 为布尔值。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | HTTP 200 时固定为 `0`。 |
                | `data` | boolean | `true`、`false` | 连通性是否通过 | verifier | BSL phase 为 `Available` 返回 true；phase 非空且不是 Available、等待超时返回 false。 |
                | `meta.message` | string | `Connection is available` 等 | 成功说明 | verifier | 仅成功时通常返回。 |
                | `meta.error` | string | 错误文本 | 不可用原因 | verifier | BSL phase 不可用或等待超时时返回。 |
                | `trace_id` | string | trace id | 请求追踪 ID | trace middleware | 用于排查。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 请求体或目标集群 client 获取失败 | JSON/binding 失败，或 `GetKubeClient` 无法为 `cluster_name` 构造 client。 |
                | 401 | 无固定业务码 | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。 |
                | 500 | `5000` | verifier 执行失败 | 读取 BSL 返回非 NotFound 错误、读取 Cluster 失败、patch ensure-storage 注解失败。 |

                BSL phase 不可用和最多 20 次、每次 500ms 等待后仍未 Ready，会返回 HTTP 200 且 `data=false`，错误原因在 `meta.error`。
            operationId: post_apis_storage_testudo_softcdata_com_v1_storages_connectivity_validate
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/ValidateStorageConnectivityRequest'
                description: 目标集群 BSL 连通性校验请求体。
                required: true
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/StorageBooleanEnvelope'
                    description: 成功返回 BSL 连通性结果。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求体非法或目标集群 client 获取失败。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: BSL verifier 执行失败。
            security:
                - bearerAuth: []
            summary: 存储仓库 POST /apis/storage.testudo.softcdata.com/v1/storages/connectivity/validate
            tags:
                - 存储仓库
            x-controlled-resources:
                direct:
                    - Cluster CR annotation
                    - Target cluster Velero BackupStorageLocation
                downstream:
                    - Target cluster Velero Secret
                    - Target cluster Velero BackupStorageLocation
                    - S3 bucket through Velero validation
                scope: 指定 Cluster 与指定 StorageRepository 对应的目标集群 Velero BSL
            x-operator-chain:
                controller: ClusterReconciler processes testudo.softcdata.com/ensure-storage annotation and calls DefaultBSL.ApplyStorageRepository
                crd: Cluster, StorageRepository, Velero BackupStorageLocation
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/internal/controller/cluster_controller.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/BSL.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/storage_runtime.go
                serverFile: internal/apis/disaster_storage/v1/router.go
                serverHandler: c.validateBSLConnectivity
            x-runapi-target-id: example-runapi-target-id
    /apis/storage.testudo.softcdata.com/v1/storages/names:
        get:
            description: |-
                ## 1. 接口用来干什么

                查询存储仓库轻量名称列表，用于下拉选择等场景。server 从 `StorageRepository` informer 缓存读取所有资源，按动态 label 过滤后返回 `name/id/status` 三个字段，不分页、不排序。

                ## 2. 控制哪些资源

                直接操作资源：
                - 只读 `StorageRepository` CRD informer 缓存。

                下层影响资源：
                - 无。该接口不访问 S3、不读取 CA Secret、不访问 Velero 资源，也不触发 operator reconcile。

                资源作用范围：
                - 读取配置的管理命名空间（默认 `disaster-system`）。
                - 返回 `status` 来自 operator 最近一次写入的 `StorageRepository.status.status`。

                ## 3. 入参的详细说明

                | 字段 | 位置 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|---|
                | `limit` | query | integer | 否 | 任意整数 | 兼容参数 | 当前 handler 解析但不分页 | 不影响返回数量。 |
                | `page` | query | integer | 否 | 任意整数 | 兼容参数 | 当前 handler 解析但不分页 | 不影响返回数量。 |
                | `sort` | query | string | 否 | 任意字符串 | 兼容参数 | 当前 handler 解析但不排序 | 不影响返回顺序。 |
                | `order` | query | string | 否 | 任意字符串 | 兼容参数 | 当前 handler 解析但不排序 | 不影响返回顺序。 |
                | `keyword` | query | string | 否 | 任意字符串 | 兼容参数 | 当前 handler 解析但不搜索 | 不影响过滤结果。 |
                | `<labelKey>` | query | string | 否 | 任意 label key | 动态标签过滤 | 对 `item.Labels[labelKey]` 做包含匹配 | 空值和 `*` 表示不过滤。 |

                ## 4. 返回的详细说明

                成功响应使用统一 `Envelope`，`data` 为 `DisasterStorageNameDTO[]`。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | 成功固定为 `0`。 |
                | `data[].name` | string | StorageRepository 名称 | 仓库名称 | metadata.name | 用于后续详情或创建其他资源时引用。 |
                | `data[].id` | string | Kubernetes UID | 仓库 UID | metadata.uid | 用于前端唯一标识。 |
                | `data[].status` | string | `Available`、`Unavailable`、空 | 可用状态 | operator status | 新建后 operator 尚未回写时可能为空。 |
                | `message` | string | `OK` | 响应消息 | server | 成功固定为 `OK`。 |
                | `trace_id` | string | trace id | 请求追踪 ID | trace middleware | 用于排查。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 列表读取失败 | informer lister `List` 返回错误。 |
                | 401 | 无固定业务码 | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。 |

                operator 的 S3/CA/bucket 校验失败不导致该接口失败，只表现为 `data[].status=Unavailable`。
            operationId: get_apis_storage_testudo_softcdata_com_v1_storages_names
            parameters:
                - description: 兼容参数。当前名称列表 handler 会解析但不用于分页。
                  in: query
                  name: limit
                  required: false
                  schema:
                    type: integer
                - description: 兼容参数。当前名称列表 handler 会解析但不用于分页。
                  in: query
                  name: page
                  required: false
                  schema:
                    type: integer
                - description: 兼容参数。当前名称列表 handler 会解析但不用于排序。
                  in: query
                  name: sort
                  required: false
                  schema:
                    type: string
                - description: 兼容参数。当前名称列表 handler 会解析但不用于排序。
                  in: query
                  name: order
                  required: false
                  schema:
                    type: string
                - description: 兼容参数。当前名称列表 handler 会解析但不用于过滤。
                  in: query
                  name: keyword
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisasterStorageNamesEnvelope'
                    description: 成功返回存储仓库轻量名称列表。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 列表读取失败。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
            security:
                - bearerAuth: []
            summary: 存储仓库 GET /apis/storage.testudo.softcdata.com/v1/storages/names
            tags:
                - 存储仓库
            x-controlled-resources:
                direct:
                    - StorageRepository CRD
                downstream:
                    - 无
                scope: disaster-system namespace 下的 StorageRepository informer 缓存
            x-operator-chain:
                controller: StorageRepositoryReconciler validates S3 configuration, syncs dependency labels, adds storage finalizer, reports task events, writes status, and updates usage statistics
                crd: StorageRepository
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/storagerepository_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/storagerepository_controller.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/storage_runtime.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/BSL.go
                serverFile: internal/apis/disaster_storage/v1/router.go
                serverHandler: c.storageNames
            x-runapi-target-id: example-runapi-target-id
    /apis/storage.testudo.softcdata.com/v1/storages/validate/connection:
        post:
            description: |-
                ## 1. 接口用来干什么

                使用请求体中的 S3/MinIO 配置做一次直连校验。该接口不创建、不更新 `StorageRepository`，不触发 disaster-operator reconcile；校验成功或失败都通过 HTTP 200 的布尔 `data` 表达，连接失败原因放在 `meta.error`。

                ## 2. 控制哪些资源

                直接操作资源：
                - 可选读取 `Secret disaster-system/{caSecretRef.name}` 的 `ca.crt`。
                - 使用 AWS S3 client 访问请求指定的 endpoint。

                下层影响资源：
                - 无。不会写入 Kubernetes CRD、Secret、Velero 资源，也不会触发 operator。

                资源作用范围：
                - 如果传入 `bucket`，执行 `HeadBucket` 校验 bucket 存在和权限。
                - 如果未传 `bucket`，执行 `ListBuckets` 校验凭据可用。
                - `addressingStyle` 控制 S3 client `UsePathStyle`，空值按 `PathStyle` 处理。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `endpoint` | string | 是 | S3 endpoint | 对象存储访问地址 | 配置 AWS client BaseEndpoint | 当前 handler 不做 scheme 校验。 |
                | `region` | string | 否 | region | 对象存储区域 | 配置 AWS client region | 为空时默认 `us-east-1`。 |
                | `bucket` | string | 否 | bucket 名称 | 要校验的 bucket | 非空时执行 `HeadBucket` | 为空时执行 `ListBuckets`。 |
                | `accessKey` | string | 是 | 任意字符串 | S3 AccessKey | 配置静态凭据 | binding required。 |
                | `secretKey` | string | 是 | 任意字符串 | S3 SecretKey | 配置静态凭据 | binding required。 |
                | `storageType` | string | 否 | 任意字符串 | 存储类型兼容字段 | 当前 handler 解析但不使用 | 不影响校验。 |
                | `addressingStyle` | string | 否 | `PathStyle`、`VirtualHostedStyle`、空 | S3 地址风格 | 控制 `UsePathStyle` | 空值按 `PathStyle`；非法值返回 400。 |
                | `caBundle` | string | 否 | PEM 证书 | 内联 CA | 构造本次校验 HTTP client | 与 `caSecretRef` 互斥。 |
                | `caSecretRef.name` | string | 否 | Secret 名称 | 既有 CA Secret 引用 | 读取 Secret 中 `ca.crt` 作为 CA | 与 `caBundle` 互斥；Secret 读取失败作为 `data=false` 返回。 |

                ## 4. 返回的详细说明

                成功 HTTP 响应使用统一 `Envelope`，`data` 为布尔值。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | HTTP 200 时固定为 `0`。 |
                | `data` | boolean | `true`、`false` | 校验是否通过 | S3 调用结果 | `HeadBucket/ListBuckets` 成功为 true；失败为 false。 |
                | `meta.error` | string | 错误文本 | 校验失败原因 | server/S3 SDK | CA Secret 读取、PEM 解析、AWS config、HeadBucket/ListBuckets 失败都会写入。 |
                | `trace_id` | string | trace id | 请求追踪 ID | trace middleware | 用于排查。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 请求体或参数非法 | JSON/binding 失败、`addressingStyle` 非法、CA 参数互斥、`caSecretRef.name` 为空。 |
                | 401 | 无固定业务码 | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。 |

                CA Secret 读取失败、PEM 解析失败、AWS config 加载失败、`HeadBucket/ListBuckets` 失败不会返回 HTTP 500，而是返回 HTTP 200、`data=false`，错误原因在 `meta.error`。
            operationId: post_apis_storage_testudo_softcdata_com_v1_storages_validate_connection
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/ValidateS3ConnectionRequest'
                description: S3 直连校验请求体。
                required: true
            responses:
                '200':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/StorageBooleanEnvelope'
                    description: 成功返回 S3 直连校验结果。
                '400':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求体或参数非法。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
            security:
                - bearerAuth: []
            summary: 存储仓库 POST /apis/storage.testudo.softcdata.com/v1/storages/validate/connection
            tags:
                - 存储仓库
            x-controlled-resources:
                direct:
                    - Optional CA Secret
                    - Request-specified S3 endpoint and bucket
                downstream:
                    - 无
                scope: 本次请求提供的 S3 连接参数
            x-operator-chain:
                controller: 无，server 使用 AWS S3 client 直接校验请求参数
                crd: 无
                operatorFiles: []
                serverFile: internal/apis/disaster_storage/v1/router.go
                serverHandler: c.validateS3Connection
            x-runapi-target-id: example-runapi-target-id
    /apis/storage.testudo.softcdata.com/v1/watch/storages:
        get:
            description: |-
                ## 1. 接口用来干什么

                建立 WebSocket 连接，实时推送 `StorageRepository` 的 Kubernetes watch 事件。operator 更新 StorageRepository status 时，会通过该 watch 表现为 `MODIFIED` 事件。

                ## 2. 控制哪些资源

                直接操作资源：
                - 只监听 `StorageRepository` CRD watch 流。

                下层影响资源：
                - 无。该接口不触发 operator reconcile，不访问 S3、不读取 CA Secret、不访问 Velero 资源。

                资源作用范围：
                - 监听配置的管理命名空间（默认 `disaster-system`） 的全部 StorageRepository。
                - handler 不先执行 List 快照，只在 watch 建立后的事件到达时推送。
                - WebSocket 流由 `StreamWatch` 发送连接成功帧、事件帧、30 秒 heartbeat、关闭帧和 30 分钟 timeout 帧。

                ## 3. 入参的详细说明

                | 字段 | 位置 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|---|
                | `Authorization` | header | string | 非 dev 必传 | `Bearer <JWT>` | JWT 令牌 | WebSocket 握手前认证 | 三种传入方式任选一种。 |
                | `token` | query | string | 非 dev 可选 | JWT 原文 | JWT 令牌 | `WebSocketTokenAdapter` 会转换为 Authorization header | 适合浏览器 WebSocket 无法设置 header 的场景。 |
                | `Sec-WebSocket-Protocol` | header | string | 非 dev 可选 | JWT 原文或子协议 | JWT 令牌 | adapter 会尝试提取 token | 服务端仍会完成 WebSocket 升级。 |

                ## 4. 返回的详细说明

                该接口升级为 WebSocket 后，所有消息均为统一 `Envelope` JSON 字符串。

                | 帧 | `code` | `data` | `meta` | 说明 |
                |---|---|---|---|---|
                | 连接成功 | `0` | `{"type":"connected"}` | `protocol=websocket` | 升级成功后第一帧。 |
                | 资源事件 | `0` | `{"type":"ADDED|MODIFIED|DELETED|ERROR","object":DisasterStorageDTO}` | 可能为空 | `object` 字段来自 `ConvertToDisasterStorageDTO`。 |
                | 心跳 | `0` | `null` | `type=heartbeat` | 每 30 秒发送一次。 |
                | watcher 错误 | `5000` | `null` | 错误详情 | Watch 创建失败或流过程中发生错误。 |
                | 正常关闭 | `0` | `null` | `type=closed` | watch channel 关闭。 |
                | 超时关闭 | `0` | `null` | `type=timeout` | 连接达到 30 分钟上限。 |

                `DisasterStorageDTO` 字段含义与详情接口一致。

                ## 5. 可能返回的错误

                | 阶段 | HTTP 状态码或业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 握手前 | HTTP 401 | 认证失败 | 非 dev 环境下 JWT 缺失、过期或无效。 |
                | 升级阶段 | HTTP 500 | WebSocket 升级失败 | upgrader.Upgrade 返回错误，响应为 `{"message":"..."}`。 |
                | WebSocket 内 | `code=5000` | watcher 创建或读取失败 | `StorageRepositories(disaster-system).Watch` 返回错误，或 watch 流内部报错。 |

                连接成功后的 operator S3 校验失败不会关闭 WebSocket，只会通过后续 `MODIFIED` 事件中的 `status` 字段体现。
            operationId: get_apis_storage_testudo_softcdata_com_v1_watch_storages
            parameters:
                - description: 浏览器 WebSocket 兼容鉴权 token。
                  in: query
                  name: token
                  required: false
                  schema:
                    type: string
                - description: JWT Authorization header，格式为 Bearer <JWT>。
                  in: header
                  name: Authorization
                  required: false
                  schema:
                    type: string
                - description: 可携带 JWT 原文的 WebSocket 子协议 header。
                  in: header
                  name: Sec-WebSocket-Protocol
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                '101':
                    description: WebSocket 连接升级成功。
                '401':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
                '500':
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MessageResponse'
                    description: WebSocket 升级失败。
            security:
                - bearerAuth: []
            summary: '[WebSocket] 存储仓库 GET /apis/storage.testudo.softcdata.com/v1/watch/storages'
            tags:
                - 存储仓库
            x-controlled-resources:
                direct:
                    - StorageRepository CRD watch stream
                downstream:
                    - 无
                scope: disaster-system namespace 下全部 StorageRepository watch 事件
            x-disaster-protocol: websocket
            x-message-schema:
                $ref: '#/components/schemas/DisasterStorageWatchEnvelope'
            x-operator-chain:
                controller: StorageRepositoryReconciler validates S3 configuration, syncs dependency labels, adds storage finalizer, reports task events, writes status, and updates usage statistics
                crd: StorageRepository
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/apis/disaster/v1/storagerepository_types.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/storagerepository_controller.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/storage_runtime.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/BSL.go
                serverFile: internal/apis/disaster_storage/v1/router.go
                serverHandler: c.watchStorages
            x-runapi-target-id: example-runapi-target-id
    /apis/v1/{resource}/{name}/history:
        get:
            description: |-
                ## 1. 接口用来干什么

                查询指定资源对象的结构化任务历史事件。server 会按路径中的资源类型和资源名称读取 Kubernetes `Event`，只保留 `testudo.softcdata.com/task-event=true` 的事件，再把 disaster-operator 写入的 JSON 事件载荷聚合为前端使用的 `TaskEvent` 列表。

                ## 2. 控制哪些资源

                直接操作资源：
                - Kubernetes `Event`：通过 `CoreV1().Events(namespace).List` 读取，不写入。

                下层影响资源：
                - 无。该接口是只读历史查询，不创建、不更新、不删除任何 CRD，也不触发 operator reconcile。

                资源作用范围：
                - 默认查询 `disaster-system` namespace，可通过 `namespace` query 参数改为其他 namespace。
                - `resource` 会先通过 `resolveEventResourceKind` 映射为 Kubernetes involvedObject.kind。
                - `name` 会写入 Kubernetes fieldSelector：`involvedObject.name=<name>`。
                - operator 侧事件来源为 `disaster-operator/pkg/helper/event_reporter.go` 的 `ReportTaskStartedWithClient`、`ReportTaskProgressWithClient`、`ReportTaskFinishedWithClient`，调用点分布在 cluster、storage、app backup、app restore、disaster operation、data sync、resource sync、group、drill、instance、policy 等 controller。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `resource` | string | 是 | 见 schema enum | 资源类型别名 | 映射为 Kubernetes Event 的 `involvedObject.kind` | 不支持的值返回 `400 code=1000`。 |
                | `name` | string | 是 | 资源对象名称 | 资源名称 | 组成 `fieldSelector: involvedObject.name=<name>` | 精确匹配 Kubernetes Event 的 involvedObject.name。 |
                | `namespace` | string | 否 | Kubernetes namespace | Event 所在 namespace | 选择读取哪个 namespace 下的 Event | 默认 `disaster-system`。 |
                | `limit` | integer | 否 | `-1` 或正整数 | 每页数量 | 控制内存分页返回数量 | 默认 `10`；`-1` 返回全部。 |
                | `page` | integer | 否 | 正整数 | 页码 | 控制内存分页起始位置 | 默认 `1`；小于等于 `0` 时按 `1` 处理。 |
                | `startTime` | string | 否 | RFC3339 时间，例如 `2026-05-08T10:00:00Z` | 开始时间 | 过滤 `TaskEvent.time` 早于该时间的事件 | 解析失败时当前代码会忽略该过滤条件。 |
                | `endTime` | string | 否 | RFC3339 时间，例如 `2026-05-08T12:00:00Z` | 结束时间 | 过滤 `TaskEvent.time` 晚于该时间的事件 | 解析失败时当前代码会忽略该过滤条件。 |
                | `sort` | string | 否 | 任意字段名 | 排序字段 | 仅进入分页链接和 `meta.sort.name` | 当前 handler 固定按 `TaskEvent.time` 倒序，不使用该字段改变排序。 |
                | `order` | string | 否 | `asc`、`desc` 或任意字符串 | 排序方向 | 仅进入分页链接和 `meta.sort.order` | 当前 handler 不使用该字段改变排序。 |
                | `keyword` | string | 否 | 任意字符串 | 关键字 | 被 `ParseOptions` 解析 | 当前 `listResourceEvents` 未使用该字段做过滤。 |

                ## 4. 返回的详细说明

                成功响应使用统一 `Envelope`，`data.items` 为聚合后的 `TaskEvent` 列表，`meta` 为 `transport.BuildCollectionResponse` 生成的集合元信息。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | 成功固定为 `0`。 |
                | `message` | string | `OK` | 响应消息 | server | `transport.Success` 写入。 |
                | `data.items[]` | array | `TaskEvent[]` | 历史事件列表 | server 聚合 | 先按事件时间聚合，再固定按 `time` 倒序排序。 |
                | `data.items[].id` | string | Kubernetes UID | 关联对象 UID | Kubernetes Event involvedObject | involvedObject.UID 为空时该字段可能为空。 |
                | `data.items[].time` | string | RFC3339 时间 | 最新事件时间 | Kubernetes Event `firstTimestamp` | 排序使用该字段。 |
                | `data.items[].taskType` | string | `AppBackup`、`DisasterOperation` 等 | 任务资源类型 | Kubernetes Event involvedObject.kind | 由 `resource` 映射后的 kind 过滤。 |
                | `data.items[].taskName` | string | 任务名称 | 任务名称 | operator 事件载荷 `task` | 为空时回退 involvedObject.name。 |
                | `data.items[].namespace` | string | namespace | 资源 namespace | Kubernetes Event involvedObject.namespace | 集群级资源事件会被 operator 写入默认 namespace。 |
                | `data.items[].cluster` | string | 集群名称、`-`、空字符串 | 关联集群 | operator 事件载荷 `cluster` | operator 未写入时可能为空。 |
                | `data.items[].status` | string | `InProgress`、`Success`、`Failed`、`Canceled`、`Unknown` | 任务状态 | operator 事件载荷 `status` | 已是终态时后续非终态不会覆盖。 |
                | `data.items[].duration` | string | `1m2s`、`-`、空字符串 | 耗时 | operator 事件载荷或 server 计算 | 结束事件优先使用 operator 载荷。 |
                | `data.items[].triggeredBy` | string | 用户名、`system`、空字符串 | 触发人 | operator 事件载荷 `user` | 聚合历史不会像 watch DTO 一样兜底 `system`。 |
                | `data.items[].traceId` | string | trace id、`-`、空字符串 | 追踪 ID | operator 事件载荷 `traceId` | 用于关联一次任务执行。 |
                | `data.items[].message` | string | 任意消息 | 最新事件消息 | operator 事件载荷 `message` | JSON 解析失败的 Event 会被忽略。 |
                | `data.items[].extraMessage` | string | 任意消息 | 额外消息 | server | 当前与 `message` 相同，空值时省略。 |
                | `data.items[].reason` | string | `ExecutionStarted`、`ExecutionProgress`、`ExecutionFinished` | 最新事件原因 | Kubernetes Event reason | 非这三类 reason 会被忽略。 |
                | `data.items[].code` | string | 进度编码 | 事件编码 | operator 事件载荷 `code` | operator 失败载荷中的 `errorCode` 当前未映射到返回字段。 |
                | `data.items[].startTime` | string | RFC3339 时间 | 开始时间 | Kubernetes Event 或 server 反推 | 未聚合到开始事件时可能为空。 |
                | `data.items[].endTime` | string | RFC3339 时间 | 结束时间 | Kubernetes Event | 只有结束事件才返回。 |
                | `data.items[].timeline[]` | array | 时间线节点 | 事件时间线 | server 聚合 | 节点字段包括 `time`、`code`、`status`、`message`、`reason`。 |
                | `meta.type` | string | `collection` | 元信息类型 | server | 集合响应固定值。 |
                | `meta.resourceType` | string | `TaskEvent` | 集合资源类型 | server | `BuildCollectionResponse` 入参固定传入。 |
                | `meta.pagination` | object | 分页信息 | 分页元信息 | server | 包含 `limit`、`total`、`partial`、`first`、`previous`、`next`、`last`。 |
                | `meta.sort` | object | 排序元信息 | 排序回显 | server | 回显 `sort` 和 `order`，不代表实际排序被应用。 |
                | `meta.filters` | object | query 过滤字段 | 过滤条件回显 | server | 回显除 `limit/page/sort/order/keyword` 外的 query 参数。 |
                | `trace_id` | string | trace id | 请求追踪 ID | trace middleware | dev 和非 dev 都会由中间件注入。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 资源类型不支持 | `resource` 为空或不在 `resolveEventResourceKind` 支持列表中。 |
                | 401 | `2001` | 认证失败 | 非 dev 环境下请求未携带有效 JWT，或 JWT 过期、签名无效。响应由 JWT 中间件返回统一信封，包含本地化 `message` 与稳定 `message_key`。 |
                | 500 | `5000` | Kubernetes Event 查询失败 | `CoreV1().Events(namespace).List` 调用 Kubernetes API 失败。 |

                operator 后续回写失败：无。该接口本身不创建任务，只读取已存在的 Kubernetes Event；operator 执行失败会作为 `ExecutionFinished` Warning Event 写入，再由本接口读取为 `status=Failed` 的历史事件。
            operationId: get_apis_v1_resource_name_history
            parameters:
                - description: 资源类型别名。server 会通过 `resolveEventResourceKind` 映射为 Kubernetes Event involvedObject.kind。
                  in: path
                  name: resource
                  required: true
                  schema:
                    enum:
                        - appbackup
                        - appbackups
                        - apprestore
                        - apprestores
                        - cluster
                        - clusters
                        - datasync
                        - datasyncs
                        - disasterbackup
                        - disasterbackups
                        - disasterconfig
                        - disasterconfigs
                        - disasterdrill
                        - disasterdrills
                        - disastergroup
                        - disastergroups
                        - disasterinstance
                        - disasterinstances
                        - disasterjob
                        - disasterjobs
                        - disasteroperation
                        - disasteroperations
                        - disasterpolicies
                        - disasterpolicy
                        - resourcesync
                        - resourcesyncs
                        - storagerepositories
                        - storagerepository
                    type: string
                - description: 资源对象名称。server 使用该值构造 Kubernetes fieldSelector，按 involvedObject.name 精确匹配。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: Event 所在 namespace。为空时默认 `disaster-system`。
                  in: query
                  name: namespace
                  required: false
                  schema:
                    default: disaster-system
                    type: string
                - description: 每页数量。默认 10，传 -1 返回全部。
                  in: query
                  name: limit
                  required: false
                  schema:
                    default: 10
                    type: integer
                - description: 页码。默认 1，小于等于 0 时按 1 处理。
                  in: query
                  name: page
                  required: false
                  schema:
                    default: 1
                    minimum: 1
                    type: integer
                - description: 开始时间过滤，RFC3339 格式。解析失败时当前代码忽略该过滤条件。
                  in: query
                  name: startTime
                  required: false
                  schema:
                    format: date-time
                    type: string
                - description: 结束时间过滤，RFC3339 格式。解析失败时当前代码忽略该过滤条件。
                  in: query
                  name: endTime
                  required: false
                  schema:
                    format: date-time
                    type: string
                - description: 排序字段回显。当前 handler 固定按事件时间倒序，不使用该字段改变排序。
                  in: query
                  name: sort
                  required: false
                  schema:
                    type: string
                - description: 排序方向回显。当前 handler 不使用该字段改变排序。
                  in: query
                  name: order
                  required: false
                  schema:
                    enum:
                        - asc
                        - desc
                    type: string
                - description: 关键字参数会被解析，但当前 handler 不使用该字段做过滤。
                  in: query
                  name: keyword
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/TaskEventCollectionEnvelope'
                    description: 成功返回指定资源的聚合历史事件集合。
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源类型为空或不支持。
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: Kubernetes Event 查询失败。
            security:
                - bearerAuth: []
            summary: 事件与历史 GET /apis/v1/:resource/:name/history
            tags:
                - 事件与历史
            x-controlled-resources:
                direct:
                    - Kubernetes Event
                downstream:
                    - 无
                scope: 指定 namespace 下匹配 involvedObject.kind 和 involvedObject.name 的结构化任务 Event
            x-operator-chain:
                controller: disaster-operator controllers call helper.ReportTaskStartedWithClient/ReportTaskProgressWithClient/ReportTaskFinishedWithClient to create Events
                crd: Kubernetes corev1.Event
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/helper/event_reporter.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/**/*
                serverFile: internal/apis/event/v1/router.go
                serverHandler: h.listResourceEvents
            x-runapi-target-id: example-runapi-target-id
    /apis/v1/deletion/check:
        post:
            description: |-
                ## 1. 接口用来干什么

                删除前检查目标资源是否还被其他资源依赖，并返回建议清理计划。该路径是 `/api/v1/deletion/check` 的 `/apis` 兼容注册，二者进入同一个 `DeletionCheckHandler.check`，行为完全一致。

                ## 2. 控制哪些资源

                直接操作资源：
                - disaster-operator CRD：按 `resource_kind/name/namespace` 读取目标对象，再扫描支持的 CRD 列表。
                - Kubernetes metadata：读取目标对象和候选对象的 labels、ownerReferences、UID。
                - AppBackup/AppRestore 场景读取远端 Velero `Schedule`、`Backup`、`Restore` 和 resource modifier `ConfigMap`，用于构造 cleanup plan。

                下层影响资源：
                - 无。该接口不创建、不更新、不删除任何资源，不触发 operator reconcile。

                资源作用范围：
                - 目标资源读取链路：`BindJSON` -> `normalizeKind` -> `getObjectByKind`。
                - 上游依赖链路：目标 `dependency-token` -> `dependency-to-<token>` label selector -> 扫描支持的 CRD -> 合并 ownerReferences。
                - 下游依赖链路：解析目标对象自身 `dependency-to-*` labels -> 按 token 反查目标资源。
                - cleanup plan 链路：按目标 kind 解析 finalizer cleanup 与 cascade cleanup；远端集群读取失败通常进入 `resolved=false/unresolved_reason`，不会使整个接口失败。
                - operator 侧基础能力来自 `disaster-operator/pkg/metadata/dependency_labels.go`、`dependency_query.go` 以及各 controller 的 dependency label sync。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `resource_kind` | string | 是 | `Cluster`、`StorageRepository`、`DisasterPolicy`、`DisasterConfig`、`DisasterInstance`、`DisasterGroup`、`AppBackup`、`AppRestore`、`DisasterDrill`、`DisasterBackup`、`DisasterOperation`、`DataSync`、`ResourceSync`、`DisasterJob` 及别名 | 目标资源类型 | 决定 `getObjectByKind` 读取哪个 CRD，并决定 cleanup plan 规则 | 会忽略大小写、斜杠前缀和非字母数字字符；不支持时返回 `400 code=1000`。 |
                | `name` | string | 是 | 资源名称 | 目标资源名称 | 读取目标对象 | 去除首尾空格后不能为空。 |
                | `namespace` | string | 否 | Kubernetes namespace | 目标资源 namespace | 读取 namespaced CRD 和扫描依赖资源 | 默认 `disaster-system`；Cluster、DisasterConfig 等集群级目标读取目标对象时不使用 namespace，但依赖扫描仍使用该 namespace。 |

                ## 4. 返回的详细说明

                成功响应使用统一 `Envelope`，`data` 为 `DeletionCheckResponse`。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | 成功固定为 `0`。 |
                | `data.target.kind` | string | 归一化 Kind | 目标资源类型 | server `normalizeKind` | 返回 canonical Kind。 |
                | `data.target.name` | string | 资源名称 | 目标资源名称 | Kubernetes metadata | 目标对象实际名称。 |
                | `data.target.namespace` | string | namespace、空字符串 | 目标 namespace | Kubernetes metadata | 集群级资源可能为空。 |
                | `data.target.uid` | string | Kubernetes UID | 目标 UID | Kubernetes metadata | 用于派生 dependency token。 |
                | `data.upstream[]` | array | 依赖引用列表 | 依赖目标的上游资源 | dependency labels、ownerReferences | 非空时 `can_delete=false`。被 cleanup plan 覆盖的子资源会从 upstream 中排除。 |
                | `data.downstream[]` | array | 依赖引用列表 | 目标依赖的下游资源 | 目标对象 `dependency-to-*` labels | token 无法反查时返回 `unresolved=true`。 |
                | `data.cleanup_plan.has_cleanup` | boolean | `true`、`false` | 是否存在清理计划 | server | finalizer 或 cascade 任一非空则为 true。 |
                | `data.cleanup_plan.finalizer_cleanup[]` | array | 清理引用列表 | finalizer 相关清理 | server、远端 Velero | AppBackup/AppRestore 场景可能包含 Velero 和 ConfigMap 资源。 |
                | `data.cleanup_plan.cascade_cleanup[]` | array | 清理引用列表 | 级联清理 | ownerReferences、spec 关系 | DisasterInstance、DisasterGroup、DisasterDrill 等场景可能包含子任务。 |
                | `data.cleanup_plan.*[].strategy` | string | `delete`、`delete_request`、`owner_reference` | 清理策略 | server | 用于前端提示，不代表本接口会执行清理。 |
                | `data.cleanup_plan.*[].resolved` | boolean | `true`、`false` | 是否解析到对象 | server | 远端失败、标签缺失或匹配过多时为 false。 |
                | `data.cleanup_plan.*[].unresolved_reason` | string | 错误或提示文本 | 未解析原因 | server | cleanup plan 的失败通常体现在这里，不使接口失败。 |
                | `data.can_delete` | boolean | `true`、`false` | 是否可删除 | server | 仅由 `len(upstream)==0` 决定。 |
                | `data.message` | string | `OK` | 结果消息 | server | 成功固定为 `OK`。 |
                | `trace_id` | string | trace id | 请求追踪 ID | trace middleware | dev 和非 dev 都会由中间件注入。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 请求体或参数非法 | JSON 绑定失败、`resource_kind` 不支持、`name` 为空。 |
                | 401 | `2001` | 认证失败 | 非 dev 环境下请求未携带有效 JWT，或 JWT 过期、签名无效。响应由 JWT 中间件返回统一信封，包含本地化 `message` 与稳定 `message_key`。 |
                | 404 | `3004` | 目标资源不存在 | `getObjectByKind` 返回 Kubernetes NotFound。 |
                | 500 | `5000` | 目标读取或 token 推导失败 | 目标 Get 返回非 NotFound 错误，或目标对象无法推导 dependency token。 |

                cleanup plan 中远端集群不可访问、远端 Velero List 失败、资源未匹配、匹配过多等情况通常返回在 `resolved=false` 与 `unresolved_reason` 中，不会使整个接口失败。
            operationId: post_apis_v1_deletion_check
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/DeletionCheckRequest'
                description: 删除检查请求体。
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DeletionCheckEnvelope'
                    description: 成功返回依赖检查结果和 cleanup plan。
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求体或参数非法。
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 目标资源不存在。
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 目标读取失败或 dependency token 推导失败。
            security:
                - bearerAuth: []
            summary: 删除检查 POST /apis/v1/deletion/check
            tags:
                - 删除检查
            x-controlled-resources:
                direct:
                    - disaster-operator CRDs
                    - Kubernetes metadata labels and ownerReferences
                    - Remote Velero resources for cleanup plan
                downstream:
                    - 无
                scope: 指定 namespace 与目标资源相关的依赖关系和清理计划
            x-operator-chain:
                controller: disaster-operator controllers sync dependency labels and cleanup labels; this endpoint only reads them
                crd: Cluster, StorageRepository, DisasterPolicy, DisasterConfig, DisasterInstance, DisasterGroup, AppBackup, AppRestore, DisasterDrill, DisasterBackup, DisasterOperation, DataSync, ResourceSync, DisasterJob
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/metadata/dependency_labels.go
                    - /home/chenxi/YS/disaster-operator/pkg/metadata/dependency_query.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/**/*
                serverFile: internal/apis/deletion_check/v1/router.go
                serverHandler: h.check
            x-runapi-target-id: example-runapi-target-id
    /apis/v1/events:
        get:
            description: |-
                ## 1. 接口用来干什么

                查询全局结构化任务历史事件列表。server 会读取指定 namespace 下所有 `testudo.softcdata.com/task-event=true` 的 Kubernetes `Event`，聚合为 `TaskEvent` 后支持任务类型、状态、对象名称、对象 UID、traceId、时间范围和关键字过滤。

                ## 2. 控制哪些资源

                直接操作资源：
                - Kubernetes `Event`：通过 `CoreV1().Events(namespace).List` 读取，不写入。

                下层影响资源：
                - 无。该接口是只读历史查询，不创建、不更新、不删除任何 CRD，也不触发 operator reconcile。

                资源作用范围：
                - 默认查询 `disaster-system` namespace，可通过 `namespace` query 参数改为其他 namespace。
                - 固定 Kubernetes LabelSelector：`testudo.softcdata.com/task-event=true`。
                - operator 侧事件来源为 `disaster-operator/pkg/helper/event_reporter.go` 的 `ReportTaskStartedWithClient`、`ReportTaskProgressWithClient`、`ReportTaskFinishedWithClient`，调用点分布在 cluster、storage、app backup、app restore、disaster operation、data sync、resource sync、group、drill、instance、policy 等 controller。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `namespace` | string | 否 | Kubernetes namespace | Event 所在 namespace | 选择读取哪个 namespace 下的 Event | 默认 `disaster-system`。 |
                | `limit` | integer | 否 | `-1` 或正整数 | 每页数量 | 控制内存分页返回数量 | 默认 `10`；`-1` 返回全部。 |
                | `page` | integer | 否 | 正整数 | 页码 | 控制内存分页起始位置 | 默认 `1`；小于等于 `0` 时按 `1` 处理。 |
                | `taskType` | string | 否 | 任意字符串 | 任务资源类型过滤 | 对 `TaskEvent.taskType` 做包含匹配 | 大小写敏感。 |
                | `status` | string | 否 | `InProgress`、`Success`、`Failed`、`Canceled`、`Unknown` | 任务状态过滤 | 对 `TaskEvent.status` 做精确匹配 | 大小写敏感。 |
                | `ownerName` | string | 否 | 任意字符串 | 任务名称过滤 | 对 `TaskEvent.taskName` 做包含匹配 | 大小写敏感。 |
                | `ownerUID` | string | 否 | Kubernetes UID | 资源 UID 过滤 | 对 `TaskEvent.id` 做精确匹配 | 用于定位同名对象的某一次资源实例。 |
                | `traceId` | string | 否 | trace id | 追踪 ID 过滤 | 对 `TaskEvent.traceId` 做精确匹配 | operator 未写入 traceId 时可能为 `-` 或空字符串。 |
                | `startTime` | string | 否 | RFC3339 时间，例如 `2026-05-08T10:00:00Z` | 开始时间 | 过滤 `TaskEvent.time` 早于该时间的事件 | 解析失败时当前代码会忽略该过滤条件。 |
                | `endTime` | string | 否 | RFC3339 时间，例如 `2026-05-08T12:00:00Z` | 结束时间 | 过滤 `TaskEvent.time` 晚于该时间的事件 | 解析失败时当前代码会忽略该过滤条件。 |
                | `keyword` | string | 否 | 任意字符串 | 关键字 | 在 `taskName`、`cluster`、`namespace`、`traceId` 中做包含匹配 | 大小写敏感。 |
                | `sort` | string | 否 | 任意字段名 | 排序字段 | 仅进入分页链接和 `meta.sort.name` | 当前 handler 固定按 `TaskEvent.time` 倒序，不使用该字段改变排序。 |
                | `order` | string | 否 | `asc`、`desc` 或任意字符串 | 排序方向 | 仅进入分页链接和 `meta.sort.order` | 当前 handler 不使用该字段改变排序。 |

                ## 4. 返回的详细说明

                成功响应使用统一 `Envelope`，`data.items` 为聚合后的 `TaskEvent` 列表，`meta` 为 `transport.BuildCollectionResponse` 生成的集合元信息。

                | 字段 | 类型 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | `code` | integer | `0` | 业务码 | server | 成功固定为 `0`。 |
                | `message` | string | `OK` | 响应消息 | server | `transport.Success` 写入。 |
                | `data.items[]` | array | `TaskEvent[]` | 历史事件列表 | server 聚合 | 先聚合结构化事件，再按 `time` 倒序排序。 |
                | `data.items[].id` | string | Kubernetes UID | 关联对象 UID | Kubernetes Event involvedObject | `ownerUID` 过滤使用该字段。 |
                | `data.items[].time` | string | RFC3339 时间 | 最新事件时间 | Kubernetes Event `firstTimestamp` | 排序使用该字段。 |
                | `data.items[].taskType` | string | `AppBackup`、`DisasterOperation` 等 | 任务资源类型 | Kubernetes Event involvedObject.kind | `taskType` 过滤对该字段做包含匹配。 |
                | `data.items[].taskName` | string | 任务名称 | 任务名称 | operator 事件载荷 `task` | `ownerName` 和 `keyword` 可匹配该字段。 |
                | `data.items[].namespace` | string | namespace | 资源 namespace | Kubernetes Event involvedObject.namespace | `keyword` 可匹配该字段。 |
                | `data.items[].cluster` | string | 集群名称、`-`、空字符串 | 关联集群 | operator 事件载荷 `cluster` | `keyword` 可匹配该字段。 |
                | `data.items[].status` | string | `InProgress`、`Success`、`Failed`、`Canceled`、`Unknown` | 任务状态 | operator 事件载荷 `status` | `status` 过滤对该字段做精确匹配。 |
                | `data.items[].duration` | string | `1m2s`、`-`、空字符串 | 耗时 | operator 事件载荷或 server 计算 | 结束事件优先使用 operator 载荷。 |
                | `data.items[].triggeredBy` | string | 用户名、`system`、空字符串 | 触发人 | operator 事件载荷 `user` | 聚合历史不会像 watch DTO 一样兜底 `system`。 |
                | `data.items[].traceId` | string | trace id、`-`、空字符串 | 追踪 ID | operator 事件载荷 `traceId` | `traceId` 过滤精确匹配，`keyword` 包含匹配。 |
                | `data.items[].message` | string | 任意消息 | 最新事件消息 | operator 事件载荷 `message` | JSON 解析失败的 Event 会被忽略。 |
                | `data.items[].extraMessage` | string | 任意消息 | 额外消息 | server | 当前与 `message` 相同，空值时省略。 |
                | `data.items[].reason` | string | `ExecutionStarted`、`ExecutionProgress`、`ExecutionFinished` | 最新事件原因 | Kubernetes Event reason | 非这三类 reason 会被忽略。 |
                | `data.items[].code` | string | 进度编码 | 事件编码 | operator 事件载荷 `code` | operator 失败载荷中的 `errorCode` 当前未映射到返回字段。 |
                | `data.items[].startTime` | string | RFC3339 时间 | 开始时间 | Kubernetes Event 或 server 反推 | 未聚合到开始事件时可能为空。 |
                | `data.items[].endTime` | string | RFC3339 时间 | 结束时间 | Kubernetes Event | 只有结束事件才返回。 |
                | `data.items[].timeline[]` | array | 时间线节点 | 事件时间线 | server 聚合 | 节点字段包括 `time`、`code`、`status`、`message`、`reason`。 |
                | `meta.type` | string | `collection` | 元信息类型 | server | 集合响应固定值。 |
                | `meta.resourceType` | string | `TaskEvent` | 集合资源类型 | server | `BuildCollectionResponse` 入参固定传入。 |
                | `meta.pagination` | object | 分页信息 | 分页元信息 | server | 包含 `limit`、`total`、`partial`、`first`、`previous`、`next`、`last`。 |
                | `meta.sort` | object | 排序元信息 | 排序回显 | server | 回显 `sort` 和 `order`，不代表实际排序被应用。 |
                | `meta.filters` | object | query 过滤字段 | 过滤条件回显 | server | 回显除 `limit/page/sort/order/keyword` 外的 query 参数。 |
                | `trace_id` | string | trace id | 请求追踪 ID | trace middleware | dev 和非 dev 都会由中间件注入。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 401 | `2001` | 认证失败 | 非 dev 环境下请求未携带有效 JWT，或 JWT 过期、签名无效。响应由 JWT 中间件返回统一信封，包含本地化 `message` 与稳定 `message_key`。 |
                | 500 | `5000` | Kubernetes Event 查询失败 | `CoreV1().Events(namespace).List` 调用 Kubernetes API 失败。 |

                当前代码不会因为未知过滤字段、非法 `startTime/endTime`、非法 `limit/page` 明确返回 `400`；这些情况会被忽略或按默认值处理。

                operator 后续回写失败：无。该接口本身不创建任务，只读取已存在的 Kubernetes Event；operator 执行失败会作为 `ExecutionFinished` Warning Event 写入，再由本接口读取为 `status=Failed` 的历史事件。
            operationId: get_apis_v1_events
            parameters:
                - description: Event 所在 namespace。为空时默认 `disaster-system`。
                  in: query
                  name: namespace
                  required: false
                  schema:
                    default: disaster-system
                    type: string
                - description: 每页数量。默认 10，传 -1 返回全部。
                  in: query
                  name: limit
                  required: false
                  schema:
                    default: 10
                    type: integer
                - description: 页码。默认 1，小于等于 0 时按 1 处理。
                  in: query
                  name: page
                  required: false
                  schema:
                    default: 1
                    minimum: 1
                    type: integer
                - description: 任务资源类型过滤，对 `TaskEvent.taskType` 做包含匹配。
                  in: query
                  name: taskType
                  required: false
                  schema:
                    type: string
                - description: 任务状态过滤，对 `TaskEvent.status` 做精确匹配。
                  in: query
                  name: status
                  required: false
                  schema:
                    enum:
                        - InProgress
                        - Success
                        - Failed
                        - Canceled
                        - Unknown
                    type: string
                - description: 任务名称过滤，对 `TaskEvent.taskName` 做包含匹配。
                  in: query
                  name: ownerName
                  required: false
                  schema:
                    type: string
                - description: 资源 UID 过滤，对 `TaskEvent.id` 做精确匹配。
                  in: query
                  name: ownerUID
                  required: false
                  schema:
                    type: string
                - description: 追踪 ID 过滤，对 `TaskEvent.traceId` 做精确匹配。
                  in: query
                  name: traceId
                  required: false
                  schema:
                    type: string
                - description: 开始时间过滤，RFC3339 格式。解析失败时当前代码忽略该过滤条件。
                  in: query
                  name: startTime
                  required: false
                  schema:
                    format: date-time
                    type: string
                - description: 结束时间过滤，RFC3339 格式。解析失败时当前代码忽略该过滤条件。
                  in: query
                  name: endTime
                  required: false
                  schema:
                    format: date-time
                    type: string
                - description: 关键字，在 taskName、cluster、namespace、traceId 中做包含匹配。
                  in: query
                  name: keyword
                  required: false
                  schema:
                    type: string
                - description: 排序字段回显。当前 handler 固定按事件时间倒序，不使用该字段改变排序。
                  in: query
                  name: sort
                  required: false
                  schema:
                    type: string
                - description: 排序方向回显。当前 handler 不使用该字段改变排序。
                  in: query
                  name: order
                  required: false
                  schema:
                    enum:
                        - asc
                        - desc
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/TaskEventCollectionEnvelope'
                    description: 成功返回聚合历史事件集合。
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: Kubernetes Event 查询失败。
            security:
                - bearerAuth: []
            summary: 事件与历史 GET /apis/v1/events
            tags:
                - 事件与历史
            x-controlled-resources:
                direct:
                    - Kubernetes Event
                downstream:
                    - 无
                scope: 指定 namespace 下的结构化任务 Event
            x-operator-chain:
                controller: disaster-operator controllers call helper.ReportTaskStartedWithClient/ReportTaskProgressWithClient/ReportTaskFinishedWithClient to create Events
                crd: Kubernetes corev1.Event
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/helper/event_reporter.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/**/*
                serverFile: internal/apis/event/v1/router.go
                serverHandler: h.listEvents
            x-runapi-target-id: example-runapi-target-id
    /apis/v1/platform-license/install:
        post:
            description: |-
                ## 1. 接口用来干什么

                安装平台 License。前端或管理员提交 License JSON 内容后，server 将内容写入固定 Secret，operator 与 server 后续都基于该 Secret 进行校验。安装接口写入 Secret 后会直接基于本次写入内容进行实时评价，避免读取旧状态缓存。

                ## 2. 控制哪些资源

                直接创建或更新资源：
                - `disaster-system/disaster-platform-license` Secret

                Secret 类型固定为 `testudo.softcdata.com/license`，数据键固定为 `license.lic`。安装后接口会实时读取 Cluster 列表、Namespace、install-id Secret 与 API Server CA 计算状态；生成当前指纹时会在 `disaster-platform-install-id` 不存在时创建该 Secret；该接口不创建 Cluster。

                ## 3. 入参详细说明

                请求体为 JSON。`license` 为 string，必传，完整 License JSON 文本。字段不能为空，必须是合法 JSON 文档。该字段会原样写入 Secret 的 `data.license.lic`。请求需要携带现有业务认证 Header；dev 环境按 server 配置跳过 JWT。

                ## 4. 返回详细说明

                返回统一 Envelope。`data` 为 `PlatformLicenseStatus`，表示写入 Secret 后基于当前 Secret 与当前平台指纹实时计算出来的状态。即使 License 格式错误、签名无效、未知 key、过期、指纹不匹配或产品不匹配，接口也会返回状态给前端展示；部署指纹或 API Server CA 计算失败时返回 `state=Unknown`、`reason=LicenseEnvironmentInvalid`，不会包装成 `LicenseInvalid`。只有请求体为空、非 JSON、Kubernetes 写入失败或 Cluster 计数失败才作为 HTTP 错误返回。

                安装响应与状态查询共用 `PlatformLicenseStatus`。当当前指纹生成成功时，响应会包含 `data.fingerprint`、`data.fingerprintVersion=k8s-v1` 和可复制给签发侧的 `data.fingerprintRequest`；当 License 内容已经被分类为 `Malformed`、`InvalidSignature`、`UnknownKey`、`UnsupportedVersion`、`ProductMismatch`、`NotYetValid` 或 `Expired` 且当前指纹生成失败时，接口保留 License 内容状态和 reason，`data.fingerprint` 为空字符串，`data.fingerprintRequest` 不返回。

                ## 5. 可能返回的错误

                `400 code=1000`：请求体不是 JSON、`license` 缺失、`license` 为空，或 `license` 字段不是合法 JSON 文档。
                `401 code=2001`：未通过 JWT 认证。
                `500 code=5000`：Kubernetes 客户端未初始化、创建或更新 Secret 失败，或读取 Cluster 失败。
            operationId: post_apis_v1_platform_license_install
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/PlatformLicenseInstallRequest'
                description: License 安装请求体。
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                allOf:
                                    - $ref: '#/components/schemas/Envelope'
                                    - properties:
                                          data:
                                              $ref: '#/components/schemas/PlatformLicenseStatus'
                                      type: object
                            examples:
                                malformedWithFingerprint:
                                    summary: 安装内容错误但返回当前指纹
                                    value:
                                        code: 0
                                        message: OK
                                        data:
                                            state: Malformed
                                            maxClusters: 2
                                            clusterCount: 1
                                            fingerprint: sha256:0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
                                            fingerprintVersion: k8s-v1
                                            fingerprintRequest:
                                                product: disaster-platform
                                                fingerprintVersion: k8s-v1
                                                fingerprint: sha256:0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
                                                namespace: disaster-system
                                                generatedAt: "2026-05-16T10:20:30Z"
                                            fingerprintMatched: false
                                            reason: LicenseInvalid
                                            message: license field "version" is required
                                            lastCheckedAt: "2026-05-16T10:20:30Z"
                                            source: liveEvaluation
                    description: 安装成功并返回实时 License 状态
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 平台许可 POST /apis/v1/platform-license/install
            tags:
                - 平台许可
            x-async-failure-status:
                readVia:
                    - GET /apis/v1/platform-license/status
                statusFields:
                    - data.state
                    - data.reason
                    - data.message
            x-controlled-resources:
                direct:
                    - License Secret
                downstream:
                    - operator 后续读取 License Secret 并刷新 License 状态 ConfigMap
                scope: 管理面 license namespace，默认 disaster-system
            x-operator-chain:
                controller: License status runnable reads Secret and writes status ConfigMap
                crd: corev1.Secret
                serverFile: internal/apis/platform_license/v1/router.go
                serverHandler: h.install
            x-runapi-target-id: example-runapi-target-id
    /apis/v1/platform-license/status:
        get:
            description: |-
                ## 1. 接口用来干什么

                查询当前平台 License 展示状态、当前未删除 Cluster 数量、集群额度和当前管理面集群指纹。前端用于展示免费版、企业版、过期、指纹不匹配等状态，并直接复制当前指纹或完整 `fingerprintRequest` 给签发侧生成 License。

                ## 2. 控制哪些资源

                直接读取资源：
                - `disaster-system/disaster-platform-license-status` ConfigMap
                - Cluster CR 列表

                兜底实时计算 License 状态时读取资源：
                - `disaster-platform-license` Secret

                每次返回响应前生成当前指纹时读取资源：
                - `kube-system` Namespace
                - 平台 Namespace
                - `disaster-platform-install-id` Secret
                - API Server CA

                每次返回响应前生成当前指纹时可能创建资源：
                - `disaster-platform-install-id` Secret

                该接口不创建业务 CR，也不授予企业权益；真正创建集群的判权由创建接口重新校验 Secret 与当前指纹。

                ## 3. 入参详细说明

                无请求体。无路径参数。无查询参数。请求需要携带现有业务认证 Header；dev 环境按 server 配置跳过 JWT。

                ## 4. 返回详细说明

                返回统一 Envelope。`data.state` 表示状态，可能为 `Free`、`Active`、`Expired`、`InvalidSignature`、`FingerprintMismatch`、`NotYetValid`、`Malformed`、`UnsupportedVersion`、`ProductMismatch`、`UnknownKey`、`Unknown`。`data.reason` 表示稳定原因；License 内容错误会优先返回 `LicenseInvalid`、`LicenseUnknownKey`、`LicenseUnsupportedVersion` 等真实原因，部署指纹或 API Server CA 计算失败会返回 `LicenseEnvironmentInvalid`。`data.maxClusters` 表示当前生效集群额度，免费版为 2，企业无限制为 -1。`data.clusterCount` 表示当前未删除 Cluster 数量。`data.source` 表示来源，`statusConfigMap` 表示读取展示缓存，`liveEvaluation` 表示实时校验 Secret 与指纹。

                `data.fingerprint` 表示当前管理面集群指纹，用于签发新的 License。`data.fingerprintVersion` 当前固定为 `k8s-v1`。`data.fingerprintRequest` 为可直接交给签发侧使用的完整请求对象，包含 `product=disaster-platform`、`fingerprintVersion`、`fingerprint`、`namespace` 和 `generatedAt`，字段与 `disasterctl license fingerprint` 输出保持一致。无 License、License 过期、License 指纹不匹配、License 内容错误时，只要当前指纹生成成功，都会返回这些字段。当前指纹生成失败时，`data.fingerprint` 为空字符串，`data.fingerprintVersion=k8s-v1`，不返回 `data.fingerprintRequest`；如果 License 内容已经被分类为 `Malformed`、`InvalidSignature`、`UnknownKey`、`UnsupportedVersion`、`ProductMismatch`、`NotYetValid` 或 `Expired`，接口保留该 License 内容状态和 reason，不用指纹生成错误掩盖内容错误。

                ## 5. 可能返回的错误

                `401 code=2001`：未通过 JWT 认证。
                `500 code=5000`：Kubernetes 客户端未初始化、读取 ConfigMap/Cluster 失败。实时评价中的 License 内容错误或部署环境错误通常以 `200 code=0` 返回在 `data.state`、`data.reason` 和 `data.message` 中。
                License 过期、产品不匹配、指纹不匹配等业务状态通常以 `200 code=0` 返回在 `data.state`、`data.reason` 和 `data.message` 中，不作为本接口 HTTP 错误。
            operationId: get_apis_v1_platform_license_status
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                allOf:
                                    - $ref: '#/components/schemas/Envelope'
                                    - properties:
                                          data:
                                              $ref: '#/components/schemas/PlatformLicenseStatus'
                                      type: object
                            examples:
                                freeWithFingerprint:
                                    summary: 免费版状态并返回当前指纹
                                    value:
                                        code: 0
                                        message: OK
                                        data:
                                            state: Free
                                            maxClusters: 2
                                            clusterCount: 1
                                            fingerprint: sha256:0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
                                            fingerprintVersion: k8s-v1
                                            fingerprintRequest:
                                                product: disaster-platform
                                                fingerprintVersion: k8s-v1
                                                fingerprint: sha256:0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
                                                namespace: disaster-system
                                                generatedAt: "2026-05-16T10:20:30Z"
                                            fingerprintMatched: false
                                            reason: NoLicense
                                            message: license secret is not installed
                                            lastCheckedAt: "2026-05-16T10:20:30Z"
                                            source: liveEvaluation
                                fingerprintEnvironmentInvalid:
                                    summary: 指纹生成失败
                                    value:
                                        code: 0
                                        message: OK
                                        data:
                                            state: Unknown
                                            maxClusters: 2
                                            clusterCount: 1
                                            fingerprint: ""
                                            fingerprintVersion: k8s-v1
                                            fingerprintMatched: false
                                            reason: LicenseEnvironmentInvalid
                                            message: "compute deployment fingerprint: read API server CA: open /var/run/secrets/kubernetes.io/serviceaccount/ca.crt: no such file or directory"
                                            lastCheckedAt: "2026-05-16T10:20:30Z"
                                            source: liveEvaluation
                    description: 查询成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 平台许可 GET /apis/v1/platform-license/status
            tags:
                - 平台许可
            x-async-failure-status:
                readVia:
                    - GET /apis/v1/platform-license/status
                statusFields:
                    - data.state
                    - data.reason
                    - data.message
            x-controlled-resources:
                direct:
                    - License 状态 ConfigMap
                    - Cluster
                    - License Secret
                downstream:
                    - 无
                scope: 管理面 license namespace，默认 disaster-system
            x-operator-chain:
                controller: License status runnable writes status ConfigMap; server may perform live verification fallback
                crd: corev1.ConfigMap and corev1.Secret
                serverFile: internal/apis/platform_license/v1/router.go
                serverHandler: h.status
            x-runapi-target-id: example-runapi-target-id
    /apis/v1/business-default-config:
        get:
            description: |-
                ## 1. 接口用来干什么

                `/apis/v1/business-default-config` 是 `/api/v1/business-default-config` 的兼容前缀，调用同一 server handler `h.getSnapshot`，用于返回业务默认配置单例快照。

                ## 2. 控制哪些资源

                直接读取资源：
                - ConfigMap：`disaster-system/disaster-business-default-config`。
                - server 内置业务默认配置字段目录。

                下层影响资源：
                - 无。GET 只读，不触发 operator reconcile。

                ## 3. 入参的详细说明

                不使用请求体和 query 参数。非 dev 环境需要有效 JWT。

                ## 4. 返回的详细说明

                响应结构与 `/api/v1/business-default-config` 完全一致，`data` 为 `BusinessDefaultConfigSnapshot`，字段含义、默认值、当前值、分组、可编辑性、生效方式和校验边界均由同一份字段目录生成。

                ## 5. 可能返回的错误

                与 `/api/v1/business-default-config` 一致：401 表示认证失败，500 表示 ConfigMap 读取失败、JSON 损坏或绕过接口写入的非法配置。
            operationId: get_apis_v1_business_default_config
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                allOf:
                                    - $ref: '#/components/schemas/Envelope'
                                    - type: object
                                      properties:
                                          data:
                                              $ref: '#/components/schemas/BusinessDefaultConfigSnapshot'
                    description: 返回当前业务默认配置快照
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 业务默认配置 GET /apis/v1/business-default-config
            tags:
                - 业务默认配置
            x-controlled-resources:
                direct:
                    - ConfigMap disaster-business-default-config
                    - server 业务默认配置字段目录
                downstream:
                    - 无
                scope: disaster-system namespace 单例配置
            x-operator-chain:
                controller: 无直接 controller 调用，字段语义与 disaster-operator OperatorRuntimeConfig 保持一致
                crd: corev1.ConfigMap
                serverFile: internal/apis/business_default_config/v1/router.go
                serverHandler: h.getSnapshot
            x-runapi-target-id: 1cc44c0ccd401001
        patch:
            description: |-
                ## 1. 接口用来干什么

                `/apis/v1/business-default-config` 是 `/api/v1/business-default-config` 的兼容前缀，调用同一 server handler `h.patchConfig`，用于部分更新业务默认配置单例。

                ## 2. 控制哪些资源

                直接更新资源：
                - ConfigMap：`disaster-system/disaster-business-default-config`。

                下层影响资源：
                - 不直接写 operator runtime config；更新值会被前端后续读取，并写入对应业务请求体。

                ## 3. 入参的详细说明

                请求体与 `/api/v1/business-default-config` 一致，支持分组结构和扁平 key。duration 必须传带单位字符串；int 必须传 JSON number；未知字段、只读字段、类型错误、范围错误和跨字段关系错误返回 400。

                ## 4. 返回的详细说明

                成功后返回更新后的 `BusinessDefaultConfigSnapshot`，并刷新 `updatedAt`、`updatedBy`。

                ## 5. 可能返回的错误

                与 `/api/v1/business-default-config` 一致：400 表示请求体或字段值非法，401 表示认证失败，403 表示非 admin 用户修改，409 表示 Kubernetes 更新冲突，500 表示服务端错误。
            operationId: patch_apis_v1_business_default_config
            parameters: []
            requestBody:
                required: true
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/BusinessDefaultConfigPatchRequest'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                allOf:
                                    - $ref: '#/components/schemas/Envelope'
                                    - type: object
                                      properties:
                                          data:
                                              $ref: '#/components/schemas/BusinessDefaultConfigSnapshot'
                    description: 更新成功并返回最新快照
                "400":
                    content:
                        application/json:
                            schema:
                                allOf:
                                    - $ref: '#/components/schemas/ErrorEnvelope'
                                    - type: object
                                      properties:
                                          meta:
                                              $ref: '#/components/schemas/BusinessDefaultConfigFieldErrorMeta'
                    description: 请求体或字段值非法
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 业务默认配置 PATCH /apis/v1/business-default-config
            tags:
                - 业务默认配置
            x-controlled-resources:
                direct:
                    - ConfigMap disaster-business-default-config
                    - server 业务默认配置字段目录
                downstream:
                    - 后续由前端写入业务请求体
                scope: disaster-system namespace 单例配置
            x-operator-chain:
                controller: 无直接 controller 调用，字段语义与 disaster-operator OperatorRuntimeConfig 保持一致
                crd: corev1.ConfigMap
                serverFile: internal/apis/business_default_config/v1/router.go
                serverHandler: h.patchConfig
            x-runapi-target-id: 1cc44c0d04401001
    /apis/v1/business-default-config/fields:
        get:
            description: |-
                ## 1. 接口用来干什么

                `/apis/v1/business-default-config/fields` 是 `/api/v1/business-default-config/fields` 的兼容前缀，调用同一 server handler `h.listFields`，用于按字段维度搜索、筛选、排序和分页展示业务默认配置。

                ## 2. 控制哪些资源

                直接读取 ConfigMap `disaster-business-default-config` 和 server 内置字段目录。该接口只读，不触发 operator reconcile。

                ## 3. 入参的详细说明

                参数与 `/api/v1/business-default-config/fields` 一致：`keyword`、`q`、`groupKey`、`editable`、`effectMode`、`page`、`limit`、`sort`、`order`。
                `keyword` 对 `key`、`name`、`description`、`value`、`groupKey`、`groupName` 执行大小写不敏感包含匹配；`groupKey`、`editable`、`effectMode` 为筛选条件。

                ## 4. 返回的详细说明

                成功响应为集合信封，`data.items[]` 为 `BusinessDefaultConfigField`，`meta.pagination` 返回分页信息，`meta.links.self` 保留本次查询参数。

                ## 5. 可能返回的错误

                与 `/api/v1/business-default-config/fields` 一致：400 表示 `editable` 查询值非法，401 表示认证失败，500 表示配置读取失败。
            operationId: get_apis_v1_business_default_config_fields
            parameters:
                - name: keyword
                  in: query
                  required: false
                  schema:
                      type: string
                  description: 搜索关键字，对字段 key、名称、说明、当前值、分组 key、分组名称执行包含匹配。
                - name: q
                  in: query
                  required: false
                  schema:
                      type: string
                  description: 搜索关键字兼容别名。
                - name: groupKey
                  in: query
                  required: false
                  schema:
                      type: string
                  description: 分组精确筛选。
                - name: editable
                  in: query
                  required: false
                  schema:
                      type: boolean
                  description: 按是否可编辑筛选。
                - name: effectMode
                  in: query
                  required: false
                  schema:
                      type: string
                      enum:
                          - hot
                          - restart
                          - startup
                  description: 按生效方式筛选。
                - name: page
                  in: query
                  required: false
                  schema:
                      type: integer
                      default: 1
                  description: 页码。
                - name: limit
                  in: query
                  required: false
                  schema:
                      type: integer
                      default: 10
                  description: 每页数量，传 -1 返回全部。
                - name: sort
                  in: query
                  required: false
                  schema:
                      type: string
                  description: 排序字段。
                - name: order
                  in: query
                  required: false
                  schema:
                      type: string
                      enum:
                          - asc
                          - desc
                  description: 排序方向。
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                allOf:
                                    - $ref: '#/components/schemas/Envelope'
                                    - type: object
                                      properties:
                                          data:
                                              $ref: '#/components/schemas/BusinessDefaultConfigFieldCollection'
                                          meta:
                                              $ref: '#/components/schemas/CollectionMeta'
                    description: 返回字段集合
                "400":
                    content:
                        application/json:
                            schema:
                                allOf:
                                    - $ref: '#/components/schemas/ErrorEnvelope'
                                    - type: object
                                      properties:
                                          meta:
                                              $ref: '#/components/schemas/BusinessDefaultConfigFieldErrorMeta'
                    description: 查询参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 业务默认配置 GET /apis/v1/business-default-config/fields
            tags:
                - 业务默认配置
            x-controlled-resources:
                direct:
                    - ConfigMap disaster-business-default-config
                    - server 业务默认配置字段目录
                downstream:
                    - 无
                scope: disaster-system namespace 单例配置
            x-operator-chain:
                controller: 无直接 controller 调用，字段语义与 disaster-operator OperatorRuntimeConfig 保持一致
                crd: corev1.ConfigMap
                serverFile: internal/apis/business_default_config/v1/router.go
                serverHandler: h.listFields
            x-runapi-target-id: 1cc44c0ce8c01000
    /apis/v1/business-default-config/frontend-fields:
        get:
            description: |-
                ## 1. 接口用来干什么

                `/apis/v1/business-default-config/frontend-fields` 是 `/api/v1/business-default-config/frontend-fields` 的兼容前缀，调用同一 server handler `h.listFrontendSpecFields`，用于返回前端传参字段目录。
                前端通过该接口拿到默认值、字段层级和接口用途，再写入创建备份、恢复、实例、演练或容灾操作的请求体。

                ## 2. 控制哪些资源

                直接读取 server 内置前端传参 key/value 目录。该接口只读，不读取或修改 ConfigMap，不触发 operator reconcile。

                ## 3. 入参的详细说明

                参数与 `/api/v1/business-default-config/frontend-fields` 一致：`keyword`、`q`、`page`、`limit`、`sort`、`order`。
                `keyword` 对 `key` 与 `value` 执行大小写不敏感包含匹配；查全部传 `limit=-1`；`sort` 支持 `key` 和 `value`。

                ## 4. 返回的详细说明

                成功响应为集合信封，`data.items[]` 为 `BusinessDefaultConfigFrontendField`，`data.fieldMap` 为按 key 索引的字段 map，`meta.pagination` 返回分页信息，`meta.links.self` 保留本次查询参数。
                每个条目包含 `key/value`、`requestPathSegments`、`specPathSegments` 和 `apiUsages`，前端可直接通过 `fieldMap["operation.timeoutMinutes"]` 获取字段使用方式。

                ## 5. 可能返回的错误

                与 `/api/v1/business-default-config/frontend-fields` 一致：401 表示认证失败，500 表示服务端内部错误。
            operationId: get_apis_v1_business_default_config_frontend_fields
            parameters:
                - name: keyword
                  in: query
                  required: false
                  schema:
                      type: string
                  description: 搜索关键字，对前端传参字段 key 和 value 执行包含匹配。
                - name: q
                  in: query
                  required: false
                  schema:
                      type: string
                  description: 搜索关键字兼容别名，仅当 `keyword` 为空时生效。
                - name: page
                  in: query
                  required: false
                  schema:
                      type: integer
                      default: 1
                  description: 页码，默认 1。
                - name: limit
                  in: query
                  required: false
                  schema:
                      type: integer
                      default: 10
                  description: 每页数量，默认 10，传 -1 返回全部。
                - name: sort
                  in: query
                  required: false
                  schema:
                      type: string
                      enum:
                          - key
                          - value
                  description: 排序字段，支持 `key` 或 `value`。
                - name: order
                  in: query
                  required: false
                  schema:
                      type: string
                      enum:
                          - asc
                          - desc
                  description: 排序方向。
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                allOf:
                                    - $ref: '#/components/schemas/Envelope'
                                    - type: object
                                      properties:
                                          data:
                                              $ref: '#/components/schemas/BusinessDefaultConfigFrontendFieldCollection'
                                          meta:
                                              $ref: '#/components/schemas/CollectionMeta'
                    description: 返回前端传参字段集合
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 业务默认配置 GET /apis/v1/business-default-config/frontend-fields
            tags:
                - 业务默认配置
            x-controlled-resources:
                direct:
                    - server 前端传参 key/value 目录
                downstream:
                    - 前端后续写入 AppBackup/AppRestore/DisasterInstance/DisasterOperation/DisasterDrill 请求体
                scope: 只读目录，无 Kubernetes 资源写入
            x-operator-chain:
                controller: 无直接 controller 调用；字段最终由前端通过业务接口写入 CRD spec 后被 operator 消费
                crd: AppBackup, AppRestore, DisasterInstance, DisasterOperation, DisasterDrill
                serverFile: internal/apis/business_default_config/v1/router.go
                serverHandler: h.listFrontendSpecFields
            x-runapi-target-id: 1cc4b4011d801001
    /apis/v1/system-settings:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.listSettings`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - SystemSettings ConfigMap/Secret 及资产

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: get_apis_v1_system_settings
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 系统设置 GET /apis/v1/system-settings
            tags:
                - 系统设置
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - SystemSettings ConfigMap/Secret 及资产
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/system_settings/v1/router.go
                serverHandler: h.listSettings
            x-runapi-target-id: example-runapi-target-id
        post:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.createSetting`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - SystemSettings ConfigMap/Secret 及资产

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: post_apis_v1_system_settings
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 请求体字段待逐接口按 server request struct 与 operator 调用链补齐。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 系统设置 POST /apis/v1/system-settings
            tags:
                - 系统设置
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - SystemSettings ConfigMap/Secret 及资产
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/system_settings/v1/router.go
                serverHandler: h.createSetting
            x-runapi-target-id: example-runapi-target-id
    /apis/v1/system-settings/{config_key}:
        delete:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.deleteSetting`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - SystemSettings ConfigMap/Secret 及资产

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: delete_apis_v1_system_settings_config_key
            parameters:
                - description: 路径参数 `config_key`，中文含义待逐接口调用链确认。
                  in: path
                  name: config_key
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 系统设置 DELETE /apis/v1/system-settings/:config_key
            tags:
                - 系统设置
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - SystemSettings ConfigMap/Secret 及资产
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/system_settings/v1/router.go
                serverHandler: h.deleteSetting
            x-runapi-target-id: example-runapi-target-id
        put:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.updateSetting`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - SystemSettings ConfigMap/Secret 及资产

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: put_apis_v1_system_settings_config_key
            parameters:
                - description: 路径参数 `config_key`，中文含义待逐接口调用链确认。
                  in: path
                  name: config_key
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 请求体字段待逐接口按 server request struct 与 operator 调用链补齐。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 系统设置 PUT /apis/v1/system-settings/:config_key
            tags:
                - 系统设置
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - SystemSettings ConfigMap/Secret 及资产
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/system_settings/v1/router.go
                serverHandler: h.updateSetting
            x-runapi-target-id: example-runapi-target-id
    /apis/v1/system-settings/assets/{config_key}:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.getAsset`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - SystemSettings ConfigMap/Secret 及资产

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: get_apis_v1_system_settings_assets_config_key
            parameters:
                - description: 路径参数 `config_key`，中文含义待逐接口调用链确认。
                  in: path
                  name: config_key
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 系统设置 GET /apis/v1/system-settings/assets/:config_key
            tags:
                - 系统设置
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - SystemSettings ConfigMap/Secret 及资产
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/system_settings/v1/router.go
                serverHandler: h.getAsset
            x-runapi-target-id: example-runapi-target-id
        post:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.uploadAsset`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - SystemSettings ConfigMap/Secret 及资产

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: post_apis_v1_system_settings_assets_config_key
            parameters:
                - description: 路径参数 `config_key`，中文含义待逐接口调用链确认。
                  in: path
                  name: config_key
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 请求体字段待逐接口按 server request struct 与 operator 调用链补齐。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 系统设置 POST /apis/v1/system-settings/assets/:config_key
            tags:
                - 系统设置
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - SystemSettings ConfigMap/Secret 及资产
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/system_settings/v1/router.go
                serverHandler: h.uploadAsset
            x-runapi-target-id: example-runapi-target-id
    /apis/v1/system-settings/public:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.listPublicSettings`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - SystemSettings ConfigMap/Secret 及资产

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: get_apis_v1_system_settings_public
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 系统设置 GET /apis/v1/system-settings/public
            tags:
                - 系统设置
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - SystemSettings ConfigMap/Secret 及资产
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/system_settings/v1/router.go
                serverHandler: h.listPublicSettings
            x-runapi-target-id: example-runapi-target-id
    /apis/v1/users:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.listUsers`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - disaster-server 用户 Secret

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: get_apis_v1_users
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 用户管理 GET /apis/v1/users
            tags:
                - 用户管理
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - disaster-server 用户 Secret
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/user/v1/router.go
                serverHandler: h.listUsers
            x-runapi-target-id: example-runapi-target-id
        post:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.createUser`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - disaster-server 用户 Secret

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: post_apis_v1_users
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 请求体字段待逐接口按 server request struct 与 operator 调用链补齐。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 用户管理 POST /apis/v1/users
            tags:
                - 用户管理
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - disaster-server 用户 Secret
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/user/v1/router.go
                serverHandler: h.createUser
            x-runapi-target-id: example-runapi-target-id
    /apis/v1/users/{username}:
        delete:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.deleteUser`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - disaster-server 用户 Secret

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: delete_apis_v1_users_username
            parameters:
                - description: 路径参数 `username`，中文含义待逐接口调用链确认。
                  in: path
                  name: username
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 用户管理 DELETE /apis/v1/users/:username
            tags:
                - 用户管理
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - disaster-server 用户 Secret
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/user/v1/router.go
                serverHandler: h.deleteUser
            x-runapi-target-id: example-runapi-target-id
    /apis/v1/users/{username}/password:
        patch:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.patchUserPassword`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - disaster-server 用户 Secret

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: patch_apis_v1_users_username_password
            parameters:
                - description: 路径参数 `username`，中文含义待逐接口调用链确认。
                  in: path
                  name: username
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 请求体字段待逐接口按 server request struct 与 operator 调用链补齐。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 用户管理 PATCH /apis/v1/users/:username/password
            tags:
                - 用户管理
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - disaster-server 用户 Secret
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/user/v1/router.go
                serverHandler: h.patchUserPassword
            x-runapi-target-id: example-runapi-target-id
    /apis/v1/users/{username}/status:
        patch:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `h.patchUserStatus`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - disaster-server 用户 Secret

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。 接口成功后，operator 后续执行失败需要通过详情、列表以及事件流接口读取状态回写。
            operationId: patch_apis_v1_users_username_status
            parameters:
                - description: 路径参数 `username`，中文含义待逐接口调用链确认。
                  in: path
                  name: username
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 请求体字段待逐接口按 server request struct 与 operator 调用链补齐。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security:
                - bearerAuth: []
            summary: 用户管理 PATCH /apis/v1/users/:username/status
            tags:
                - 用户管理
            x-async-failure-status:
                readVia:
                    - GET 详情接口
                    - GET 列表接口
                    - WebSocket 事件流接口
                statusFields:
                    - status.phase
                    - status.reason
                    - status.message
            x-controlled-resources:
                direct:
                    - disaster-server 用户 Secret
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/apis/user/v1/router.go
                serverHandler: h.patchUserStatus
            x-runapi-target-id: example-runapi-target-id
    /apis/v1/watch/{resource}/{name}/events:
        get:
            description: |-
                ## 1. 接口用来干什么

                建立 WebSocket 连接，实时监听指定资源对象的结构化任务事件。server 先按 `resource/name/namespace/origin` 初始化 Kubernetes Event watch 的起始 `resourceVersion`，再把后续匹配的 Kubernetes watch 事件转换为 `TaskEvent` 并通过 WebSocket 推送给前端。

                ## 2. 控制哪些资源

                直接操作资源：
                - Kubernetes `Event`：先执行一次 `List` 获取最新 `resourceVersion`，再执行 `Watch` 实时读取，不写入。

                下层影响资源：
                - 无。该接口是只读事件流，不创建、不更新、不删除任何 CRD，也不触发 operator reconcile。

                资源作用范围：
                - 默认监听 `disaster-system` namespace，可通过 `namespace` query 参数改为其他 namespace。
                - `resource` 会先通过 `resolveEventResourceKind` 映射为 Kubernetes involvedObject.kind。
                - `name` 会写入 Kubernetes fieldSelector，按 involvedObject.name 精确匹配。
                - `origin` 会拼入 LabelSelector：默认 `user`，排除 `disaster-instance` 来源；`instance` 只监听灾备实例派生任务；`all` 不按来源过滤。
                - operator 侧事件来源为 `disaster-operator/pkg/helper/event_reporter.go` 的 `ReportTaskStartedWithClient`、`ReportTaskProgressWithClient`、`ReportTaskFinishedWithClient`。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `resource` | string | 是 | 见 schema enum | 资源类型别名 | 映射为 Kubernetes Event 的 `involvedObject.kind` | 不支持的值返回 `400 code=1000`。 |
                | `name` | string | 是 | 资源对象名称 | 资源名称 | 组成 Kubernetes fieldSelector，按 involvedObject.name 精确匹配 | 路径为空时返回 `400 code=1000`。 |
                | `namespace` | string | 否 | Kubernetes namespace | Event 所在 namespace | 选择监听哪个 namespace 下的 Event | 默认 `disaster-system`。 |
                | `origin` | string | 否 | `user`、`instance`、`all` | 事件来源 | 控制 `testudo.softcdata.com/task-origin` 标签过滤 | 空值等同 `user`；其他值返回 `400 code=1000`。 |
                | `token` | string | 否 | JWT | WebSocket query token | 在非 dev 环境由 `WebSocketTokenAdapter` 写入 `Authorization: Bearer <token>` | 与 `Authorization` header、`Sec-WebSocket-Protocol` 二选一即可。 |
                | `Authorization` | header string | 否 | `Bearer <JWT>` | 标准认证头 | 非 dev 环境通过 JWT 中间件认证 | OpenAPI security 使用 `bearerAuth` 表示。 |
                | `Sec-WebSocket-Protocol` | header string | 否 | JWT 原文 | WebSocket 子协议 token | `WebSocketTokenAdapter` 会取第一个 protocol 值作为 JWT，并回写同名子协议 | token 中含协议头非法字符时应使用 query `token`。 |

                ## 4. 返回的详细说明

                HTTP 层成功时返回 `101 Switching Protocols`。升级后服务端通过 WebSocket 推送 JSON Envelope。

                | 帧类型 | Envelope 字段 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | 连接成功 | `code` | `0` | 业务码 | server | 首帧成功固定为 `0`。 |
                | 连接成功 | `data.status` | `connected` | 连接状态 | server | WebSocket 建立并成功创建 watcher 后发送。 |
                | 连接成功 | `meta.type` | `connected` | 帧类型 | server | 标识连接成功。 |
                | 心跳 | `meta.type` | `heartbeat` | 心跳帧 | server | 默认每 30 秒发送一次，`data` 为空并因 `omitempty` 可能不出现。 |
                | 事件 | `data.type` | `ADDED`、`MODIFIED`、`DELETED`、`ERROR` | Kubernetes watch 事件类型 | Kubernetes watch | 来自 `watch.Event.Type`。 |
                | 事件 | `data.object` | `TaskEvent` 或 `null` | 任务事件 DTO | `ConvertToTaskEventDTO` | 只转换 `ExecutionStarted`、`ExecutionProgress`、`ExecutionFinished` 且 message 是 JSON 的 Event；其他 Event 转换为 `null`。 |
                | 事件 | `data.object.status` | `InProgress`、`Success`、`Failed`、`Canceled`、`Unknown` | 任务状态 | operator 事件载荷 `status` | watch DTO 对空状态兜底为 `Unknown`。 |
                | 事件 | `data.object.triggeredBy` | 用户名、`system` | 触发人 | operator 事件载荷 `user` | watch DTO 对空用户兜底为 `system`。 |
                | 关闭 | `meta.type` | `closed` | watcher 关闭 | server | Kubernetes watcher channel 关闭时发送。 |
                | 超时 | `meta.type` | `timeout` | 连接超时 | server | 默认 30 分钟超时，`meta.reason=connection timeout`。 |
                | 错误帧 | `code` | `5000` | watcher 创建失败 | server | WebSocket 已升级后创建 watcher 失败时推送错误 Envelope。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 入参非法 | `origin` 不是 `user/instance/all`，`resource` 为空，`name` 为空，或 `resource` 不在 `resolveEventResourceKind` 支持列表中。 |
                | 401 | `2001` | 认证失败 | 非 dev 环境下请求未携带有效 JWT，或 JWT 过期、签名无效。响应由 JWT 中间件返回统一信封，包含本地化 `message` 与稳定 `message_key`。 |
                | 500 | `5000` | 初始化 watcher 失败 | 升级前的 `Events(namespace).List` 失败，返回 `failed to init watcher`。 |
                | 500 | 无 | WebSocket 升级失败 | `upgrader.Upgrade` 失败时返回 `{"message":"WebSocket 升级失败: ..."}`。 |

                WebSocket 已升级后的错误不会再走 HTTP 状态码：`watcherFunc` 创建失败会通过 WebSocket 推送 `code=5000` 的错误 Envelope。
            operationId: get_apis_v1_watch_resource_name_events
            parameters:
                - description: 资源类型别名。server 会通过 `resolveEventResourceKind` 映射为 Kubernetes Event involvedObject.kind。
                  in: path
                  name: resource
                  required: true
                  schema:
                    enum:
                        - appbackup
                        - appbackups
                        - apprestore
                        - apprestores
                        - cluster
                        - clusters
                        - datasync
                        - datasyncs
                        - disasterbackup
                        - disasterbackups
                        - disasterconfig
                        - disasterconfigs
                        - disasterdrill
                        - disasterdrills
                        - disastergroup
                        - disastergroups
                        - disasterinstance
                        - disasterinstances
                        - disasterjob
                        - disasterjobs
                        - disasteroperation
                        - disasteroperations
                        - disasterpolicies
                        - disasterpolicy
                        - resourcesync
                        - resourcesyncs
                        - storagerepositories
                        - storagerepository
                    type: string
                - description: 资源对象名称。server 使用该值构造 Kubernetes fieldSelector，按 involvedObject.name 精确匹配。
                  in: path
                  name: name
                  required: true
                  schema:
                    type: string
                - description: Event 所在 namespace。为空时默认 `disaster-system`。
                  in: query
                  name: namespace
                  required: false
                  schema:
                    default: disaster-system
                    type: string
                - description: 事件来源过滤。空值等同 `user`。
                  in: query
                  name: origin
                  required: false
                  schema:
                    default: user
                    enum:
                        - user
                        - instance
                        - all
                    type: string
                - description: WebSocket query token。非 dev 环境可用该字段携带 JWT。
                  in: query
                  name: token
                  required: false
                  schema:
                    type: string
                - description: WebSocket 子协议 token。server 会将第一个 protocol 值作为 JWT。
                  in: header
                  name: Sec-WebSocket-Protocol
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "101":
                    description: WebSocket 连接升级成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: origin 非法、resource/name 缺失或 resource 不支持。
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
                "500":
                    content:
                        application/json:
                            schema:
                                oneOf:
                                    - $ref: '#/components/schemas/ErrorEnvelope'
                                    - $ref: '#/components/schemas/MessageResponse'
                    description: 初始化 Kubernetes watcher 失败或 WebSocket 升级失败。
            security:
                - bearerAuth: []
            summary: '[WebSocket] 事件与历史 GET /apis/v1/watch/:resource/:name/events'
            tags:
                - 事件与历史
            x-controlled-resources:
                direct:
                    - Kubernetes Event
                downstream:
                    - 无
                scope: 指定 namespace 下匹配 involvedObject.kind、involvedObject.name 与 origin 标签的结构化任务 Event
            x-disaster-protocol: websocket
            x-message-schema:
                $ref: '#/components/schemas/TaskEventWatchEnvelope'
            x-operator-chain:
                controller: disaster-operator controllers call helper.ReportTaskStartedWithClient/ReportTaskProgressWithClient/ReportTaskFinishedWithClient to create Events
                crd: Kubernetes corev1.Event
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/helper/event_reporter.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/**/*
                serverFile: internal/apis/event/v1/router.go
                serverHandler: h.watchResourceEvents
            x-runapi-target-id: example-runapi-target-id
    /apis/v1/watch/events:
        get:
            description: |-
                ## 1. 接口用来干什么

                建立 WebSocket 连接，实时监听全局结构化任务事件。server 先按 `namespace/origin` 初始化 Kubernetes Event watch 的起始 `resourceVersion`，再把后续匹配的 Kubernetes watch 事件转换为 `TaskEvent` 并通过 WebSocket 推送给前端。

                ## 2. 控制哪些资源

                直接操作资源：
                - Kubernetes `Event`：先执行一次 `List` 获取最新 `resourceVersion`，再执行 `Watch` 实时读取，不写入。

                下层影响资源：
                - 无。该接口是只读事件流，不创建、不更新、不删除任何 CRD，也不触发 operator reconcile。

                资源作用范围：
                - 默认监听 `disaster-system` namespace，可通过 `namespace` query 参数改为其他 namespace。
                - 固定包含 `testudo.softcdata.com/task-event=true` 的结构化任务事件。
                - `origin` 会拼入 LabelSelector：默认 `user`，排除 `disaster-instance` 来源；`instance` 只监听灾备实例派生任务；`all` 不按来源过滤。
                - operator 侧事件来源为 `disaster-operator/pkg/helper/event_reporter.go` 的 `ReportTaskStartedWithClient`、`ReportTaskProgressWithClient`、`ReportTaskFinishedWithClient`。

                ## 3. 入参的详细说明

                | 字段 | 类型 | 是否必传 | 可传入值 | 中文含义 | 传入目的 | 约束与默认值 |
                |---|---|---|---|---|---|---|
                | `namespace` | string | 否 | Kubernetes namespace | Event 所在 namespace | 选择监听哪个 namespace 下的 Event | 默认 `disaster-system`。 |
                | `origin` | string | 否 | `user`、`instance`、`all` | 事件来源 | 控制 `testudo.softcdata.com/task-origin` 标签过滤 | 空值等同 `user`；其他值返回 `400 code=1000`。 |
                | `token` | string | 否 | JWT | WebSocket query token | 在非 dev 环境由 `WebSocketTokenAdapter` 写入 `Authorization: Bearer <token>` | 与 `Authorization` header、`Sec-WebSocket-Protocol` 二选一即可。 |
                | `Authorization` | header string | 否 | `Bearer <JWT>` | 标准认证头 | 非 dev 环境通过 JWT 中间件认证 | OpenAPI security 使用 `bearerAuth` 表示。 |
                | `Sec-WebSocket-Protocol` | header string | 否 | JWT 原文 | WebSocket 子协议 token | `WebSocketTokenAdapter` 会取第一个 protocol 值作为 JWT，并回写同名子协议 | token 中含协议头非法字符时应使用 query `token`。 |

                ## 4. 返回的详细说明

                HTTP 层成功时返回 `101 Switching Protocols`。升级后服务端通过 WebSocket 推送 JSON Envelope。

                | 帧类型 | Envelope 字段 | 可能取值 | 中文含义 | 字段来源 | 说明 |
                |---|---|---|---|---|---|
                | 连接成功 | `code` | `0` | 业务码 | server | 首帧成功固定为 `0`。 |
                | 连接成功 | `data.status` | `connected` | 连接状态 | server | WebSocket 建立并成功创建 watcher 后发送。 |
                | 连接成功 | `meta.type` | `connected` | 帧类型 | server | 标识连接成功。 |
                | 心跳 | `meta.type` | `heartbeat` | 心跳帧 | server | 默认每 30 秒发送一次，`data` 为空并因 `omitempty` 可能不出现。 |
                | 事件 | `data.type` | `ADDED`、`MODIFIED`、`DELETED`、`ERROR` | Kubernetes watch 事件类型 | Kubernetes watch | 来自 `watch.Event.Type`。 |
                | 事件 | `data.object` | `TaskEvent` 或 `null` | 任务事件 DTO | `ConvertToTaskEventDTO` | 只转换 `ExecutionStarted`、`ExecutionProgress`、`ExecutionFinished` 且 message 是 JSON 的 Event；其他 Event 转换为 `null`。 |
                | 事件 | `data.object.taskType` | `AppBackup`、`DisasterOperation` 等 | 任务资源类型 | Kubernetes Event involvedObject.kind | 全局事件流不按 kind 过滤。 |
                | 事件 | `data.object.status` | `InProgress`、`Success`、`Failed`、`Canceled`、`Unknown` | 任务状态 | operator 事件载荷 `status` | watch DTO 对空状态兜底为 `Unknown`。 |
                | 事件 | `data.object.triggeredBy` | 用户名、`system` | 触发人 | operator 事件载荷 `user` | watch DTO 对空用户兜底为 `system`。 |
                | 关闭 | `meta.type` | `closed` | watcher 关闭 | server | Kubernetes watcher channel 关闭时发送。 |
                | 超时 | `meta.type` | `timeout` | 连接超时 | server | 默认 30 分钟超时，`meta.reason=connection timeout`。 |
                | 错误帧 | `code` | `5000` | watcher 创建失败 | server | WebSocket 已升级后创建 watcher 失败时推送错误 Envelope。 |

                ## 5. 可能返回的错误

                | HTTP 状态码 | 业务码 | 错误原因 | 触发条件 |
                |---|---|---|---|
                | 400 | `1000` | 入参非法 | `origin` 不是 `user/instance/all`。 |
                | 401 | `2001` | 认证失败 | 非 dev 环境下请求未携带有效 JWT，或 JWT 过期、签名无效。响应由 JWT 中间件返回统一信封，包含本地化 `message` 与稳定 `message_key`。 |
                | 500 | `5000` | 初始化 watcher 失败 | 升级前的 `Events(namespace).List` 失败，返回 `failed to init watcher`。 |
                | 500 | 无 | WebSocket 升级失败 | `upgrader.Upgrade` 失败时返回 `{"message":"WebSocket 升级失败: ..."}`。 |

                WebSocket 已升级后的错误不会再走 HTTP 状态码：`watcherFunc` 创建失败会通过 WebSocket 推送 `code=5000` 的错误 Envelope。
            operationId: get_apis_v1_watch_events
            parameters:
                - description: Event 所在 namespace。为空时默认 `disaster-system`。
                  in: query
                  name: namespace
                  required: false
                  schema:
                    default: disaster-system
                    type: string
                - description: 事件来源过滤。空值等同 `user`。
                  in: query
                  name: origin
                  required: false
                  schema:
                    default: user
                    enum:
                        - user
                        - instance
                        - all
                    type: string
                - description: WebSocket query token。非 dev 环境可用该字段携带 JWT。
                  in: query
                  name: token
                  required: false
                  schema:
                    type: string
                - description: WebSocket 子协议 token。server 会将第一个 protocol 值作为 JWT。
                  in: header
                  name: Sec-WebSocket-Protocol
                  required: false
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "101":
                    description: WebSocket 连接升级成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: origin 非法。
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MiddlewareAuthError'
                    description: 认证失败
                "500":
                    content:
                        application/json:
                            schema:
                                oneOf:
                                    - $ref: '#/components/schemas/ErrorEnvelope'
                                    - $ref: '#/components/schemas/MessageResponse'
                    description: 初始化 Kubernetes watcher 失败或 WebSocket 升级失败。
            security:
                - bearerAuth: []
            summary: '[WebSocket] 事件与历史 GET /apis/v1/watch/events'
            tags:
                - 事件与历史
            x-controlled-resources:
                direct:
                    - Kubernetes Event
                downstream:
                    - 无
                scope: 指定 namespace 下按 origin 标签过滤的结构化任务 Event
            x-disaster-protocol: websocket
            x-message-schema:
                $ref: '#/components/schemas/TaskEventWatchEnvelope'
            x-operator-chain:
                controller: disaster-operator controllers call helper.ReportTaskStartedWithClient/ReportTaskProgressWithClient/ReportTaskFinishedWithClient to create Events
                crd: Kubernetes corev1.Event
                operatorFiles:
                    - /home/chenxi/YS/disaster-operator/pkg/helper/event_reporter.go
                    - /home/chenxi/YS/disaster-operator/internal/controller/**/*
                serverFile: internal/apis/event/v1/router.go
                serverHandler: h.watchEvents
            x-runapi-target-id: example-runapi-target-id
    /healthz:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `func(ctx context.Context, c *app.RequestContext`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - 服务健康与认证 token

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。
            operationId: get_healthz
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security: []
            summary: 根接口 GET /healthz
            tags:
                - 根接口
            x-controlled-resources:
                direct:
                    - 服务健康与认证 token
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/router/router.go
                serverHandler: func(ctx context.Context, c *app.RequestContext
            x-runapi-target-id: example-runapi-target-id
    /login:
        post:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `jwtMiddleware.LoginHandler`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - 服务健康与认证 token

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。
            operationId: post_login
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 请求体字段待逐接口按 server request struct 与 operator 调用链补齐。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security: []
            summary: 根接口 POST /login
            tags:
                - 根接口
            x-controlled-resources:
                direct:
                    - 服务健康与认证 token
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/router/router.go
                serverHandler: jwtMiddleware.LoginHandler
            x-runapi-target-id: example-runapi-target-id
    /readyz:
        get:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `func(ctx context.Context, c *app.RequestContext`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - 服务健康与认证 token

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。
            operationId: get_readyz
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 该接口不使用请求体。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security: []
            summary: 根接口 GET /readyz
            tags:
                - 根接口
            x-controlled-resources:
                direct:
                    - 服务健康与认证 token
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/router/router.go
                serverHandler: func(ctx context.Context, c *app.RequestContext
            x-runapi-target-id: example-runapi-target-id
    /refresh_token:
        post:
            description: |-
                ## 1. 接口用来干什么

                该接口对应 server handler `middleware.RefreshTokenHandler`，用途需要在逐接口阶段结合 handler、request struct、response DTO 与 operator 调用链补齐。

                ## 2. 控制哪些资源

                直接操作资源：
                - 服务健康与认证 token

                下层影响资源：
                - 待逐接口查询 operator controller 后补齐。

                资源作用范围：
                - 待逐接口查询 server 与 operator 调用链后补齐。

                ## 3. 入参的详细说明

                请求字段 schema 已先生成骨架，字段中文含义、可传入值、传入目的、是否必传、约束与默认值需要在逐接口阶段补齐。

                ## 4. 返回的详细说明

                返回字段 schema 已先生成统一信封骨架，具体 data 字段、字段来源、可能取值、为空条件需要在逐接口阶段补齐。

                ## 5. 可能返回的错误

                server 当场返回错误已先列出通用响应码，具体触发条件需要在逐接口阶段补齐。
            operationId: post_refresh_token
            parameters: []
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GenericObject'
                description: 请求体字段待逐接口按 server request struct 与 operator 调用链补齐。
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Envelope'
                    description: 处理成功
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 请求参数错误
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 认证失败
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 权限不足
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源不存在
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 资源冲突
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ErrorEnvelope'
                    description: 服务端内部错误
            security: []
            summary: 根接口 POST /refresh_token
            tags:
                - 根接口
            x-controlled-resources:
                direct:
                    - 服务健康与认证 token
                downstream:
                    - 待逐接口查询 operator controller 后补齐
                scope: 待逐接口查询 server 与 operator 调用链后补齐
            x-operator-chain:
                controller: 待逐接口确认
                crd: 待逐接口确认
                serverFile: internal/router/router.go
                serverHandler: middleware.RefreshTokenHandler
            x-runapi-target-id: example-runapi-target-id
servers:
    - description: 当前部署地址
      url: /
tags:
    - name: Kubernetes 资源
    - name: 事件与历史
    - name: 删除检查
    - name: 存储仓库
    - name: 容灾任务
    - name: 容灾备份
    - name: 容灾实例
    - name: 容灾演练
    - name: 容灾策略
    - name: 容灾组
    - name: 容灾配置
    - name: 应用备份
    - name: 应用恢复
    - name: 根接口
    - name: 用户管理
    - name: 系统设置
    - name: 业务默认配置
    - name: 平台许可
    - name: 统计
    - name: 集群管理
