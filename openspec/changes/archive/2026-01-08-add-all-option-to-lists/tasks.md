# 任务：实现资源名称列表 API

## 1. 实现 (Implementation)
- [x] 1.1 **集群名称 API**: 在 `internal/apis/disaster_cluster/v1` 中实现 `GET /clusters/names`，并在路由中注册。
- [x] 1.2 **存储名称 API**: 在 `internal/apis/disaster_storage/v1` 中实现 `GET /storages/names`，并在路由中注册。
- [x] 1.3 **策略名称 API**: 在 `internal/apis/disaster_policy/v1` 中实现 `GET /policies/names`，并在路由中注册。
- [x] 1.4 **OpenSpec 更新**: 为以上三个模块添加新的 API 规范。
