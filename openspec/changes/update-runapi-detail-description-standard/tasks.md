## 1. 提案与规范
- [x] 1.1 创建 RunAPI 详细说明治理提案
- [x] 1.2 固化五段详细说明结构
- [x] 1.3 固化入参字段表与返回字段表
- [x] 1.4 固化 server 与 operator 调用链取证规则
- [x] 1.5 固化已有 RunAPI 说明保留规则
- [x] 1.6 通过 `openspec validate update-runapi-detail-description-standard --strict`

## 2. Server 全量接口清单
- [x] 2.1 读取 `internal/router/router.go`，确认所有注册模块
- [x] 2.2 逐模块读取 `router.go`，整理方法、路径、handler、是否 WebSocket
- [x] 2.3 标记 `/apis` 与 `/api` 前缀下的真实注册路径
- [x] 2.4 标记公开接口、鉴权接口、WebSocket token 适配接口
- [x] 2.5 输出 server 接口清单，字段包含模块、方法、路径、handler、直接操作资源、备注

## 3. RunAPI 全量接口清单
- [x] 3.1 确认 RunAPI/ApiPost 项目 ID 与目标项目名称
- [x] 3.2 拉取 RunAPI 全量目录与接口列表
- [x] 3.3 逐接口记录接口 ID、目录 ID、目录名称、方法、路径、已有详细说明状态
- [x] 3.4 标准化 RunAPI 路径参数写法
- [x] 3.5 输出 RunAPI 接口清单

## 4. 差异对账
- [x] 4.1 使用 `METHOD + 标准化路径` 对账 server 清单与 RunAPI 清单
- [x] 4.2 输出 RunAPI 缺失接口清单
- [x] 4.3 输出 RunAPI 已存在接口清单
- [x] 4.4 输出模块疑似错位接口清单
- [x] 4.5 输出路径相似但无法自动确认的人工复核清单
- [x] 4.6 按模块生成逐接口 Todo
- [x] 4.7 输出按模块组织的逐接口勾选清单，清单项包含方法、路径、RunAPI 状态、目标动作、处理状态

## 5. 逐接口查询 operator 调用链
- [ ] 5.1 按差异对账后的逐接口 Todo 选择当前接口
- [ ] 5.2 查询该接口对应的 server handler、request struct、校验、默认值、转换逻辑
- [ ] 5.3 查询该接口对应的 server DTO、响应转换函数、错误返回点
- [ ] 5.4 查询该接口对应的 operator CRD Spec 与 Status 类型
- [ ] 5.5 查询该接口对应的 operator controller 逻辑
- [ ] 5.6 查询该接口影响的 Velero 与 Kubernetes 原生资源
- [ ] 5.7 记录该接口的 status 字段、reason、message 与下层失败来源

## 6. 单接口详细说明编写
- [ ] 6.1 逐接口确认接口用来干什么
- [ ] 6.2 逐接口确认直接操作资源、下层影响资源、资源作用范围
- [ ] 6.3 逐接口确认入参字段中文含义、可传入值、传入目的、是否必传、约束与默认值
- [ ] 6.4 逐接口确认返回字段中文含义、可能取值、字段来源、字段用途、为空条件
- [ ] 6.5 逐接口确认 server 当场返回错误
- [ ] 6.6 逐接口确认 operator 后续回写失败字段
- [ ] 6.7 逐接口生成五段详细说明草稿

## 7. RunAPI 新增与更新
- [ ] 7.1 对 RunAPI 缺失接口执行新增，保持中文目录与接口命名规范
- [ ] 7.2 对 RunAPI 已存在接口执行详细说明更新
- [ ] 7.3 对已有详细说明的接口，将新说明放在最前面，将原说明保留到 `## 原有说明`
- [ ] 7.4 对模块疑似错位接口执行目录修正
- [ ] 7.5 每次更新前读取接口详情，确保保留现有请求参数、Header、Body、响应示例
- [ ] 7.6 每次更新后读取接口详情，确认说明、方法、路径、目录符合预期

## 8. 验收与交付
- [ ] 8.1 汇总已新增接口、已更新接口、已移动接口
- [ ] 8.2 汇总无法确认字段与需要业务确认的问题
- [ ] 8.3 抽样核对高风险接口，覆盖创建、更新、删除、动作、WebSocket、校验接口
- [ ] 8.4 确认所有接口均满足五段详细说明结构
- [ ] 8.5 确认逐接口勾选清单只勾选已完成调用链取证、详细说明写入、RunAPI 回读验证的接口
- [ ] 8.6 输出发版前 RunAPI 文档完善报告
