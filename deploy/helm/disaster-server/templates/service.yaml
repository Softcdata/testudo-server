apiVersion: v1
kind: Service
metadata:
  name: {{ include "disaster-server.fullname" . }}
  labels: {{ include "disaster-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{ include "disaster-server.selectorLabels" . | nindent 4 }}
